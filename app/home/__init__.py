"""Hava — new home page surface (BUILD.md step 1).

Built at ``/home`` first; cuts over to ``/`` after dogfooding. Mocked data in
this step; replaced with live catalog reads in step 2 (``/api/home/*``
endpoints + ``featured: bool`` columns on events/programs/providers).
"""
