"""Provider profile surface (directory pivot V1).

Renders ``/provider/<slug>`` — the per-record front door for the directory.
Sibling to ``app.home`` (homepage surface); the category landing pages will
land in ``app.categories`` as a follow-up. Source of truth for copy/layout
is ``outputs/chatgpt_response_provider_profile_ux_spec.md``.
"""
