"""Phase B7 — pay-per-lead plumbing (DORMANT, flag-gated).

This package is lead-capture + attribution scaffolding ONLY. It is dormant
behind the ``LEADS_ENABLED`` env flag (default OFF) and contains NO billing,
payment, pricing, or charge logic. See :mod:`app.leads.capture`.
"""
