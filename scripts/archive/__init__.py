"""Archived one-off scripts.

Completed backfills, superseded audits, and one-off operator probes kept
runnable for reference: ``python -m scripts.archive.<name>``. Nothing in
app/, tests/, or CI references this package. See docs/CLEANUP_AUDIT.md.
"""
