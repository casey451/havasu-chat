# Development-only dependencies. Not installed in production.
# Install with: pip install -r dev-requirements.txt
ruff==0.15.12

# Static type checking (T1.1). The CI `typecheck` job installs requirements.txt
# too, since mypy needs the real deps to resolve types. Pinned to keep the gate
# reproducible. Stub-only packages for libraries that ship no inline types.
mypy==1.18.2
types-requests==2.33.0.20260518
types-PyYAML==6.0.12.20260518
types-python-dateutil==2.9.0.20260518

# Test runner + its transitive pins, moved out of prod requirements.txt (OPS-7).
# All three transitives are pytest-only — nothing under app/, scripts/, or
# tests/ imports pygments/pluggy/iniconfig directly. CI's test job installs
# this file alongside requirements.txt; locally: pip install -r dev-requirements.txt
pytest==9.0.3
pytest-xdist==3.8.0
iniconfig==2.3.0
pluggy==1.6.0
Pygments==2.20.0

# Contrib scraper verify scripts only (scripts/azcc_towing_verify.py,
# scripts/az_roc_verify.py — manual local runs; no workflow references these and
# nixpacks never runs `playwright install`, so prod never could run them). Demoted
# from prod requirements per the open question on #261 / audit :213. Browsers
# still need a one-time `playwright install chromium` locally.
playwright==1.59.0
pyee==13.0.1
pytesseract==0.3.13
