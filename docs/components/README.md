# Component docs (`docs/components/`)

Per-component reference docs: purpose, public surface, internal structure, conventions, and known limitations. **Start here when working on a specific subsystem.** Each file is named after the component (for example `unified_router.md` alongside `app/chat/unified_router.py`).

**Policy:** See [Working agreement — Component documentation](../WORKING_AGREEMENT.md#component-documentation).
