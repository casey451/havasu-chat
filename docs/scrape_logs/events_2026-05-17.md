# Scrape log — `events` — 2026-05-17

Per `docs/operations/scrape_logs_template.md`. First per-category
scrape run for Phase 5.8 (eighth sub-phase of the Phase 5 restructure,
post-5.7 SHIPPED 2026-05-17 at `e60b051` + SHA-cleanup at `4b20e37` +
5.8 kickoff at `8dfa2a2`). **Single-layer scrape** (Google only — OSM
scope locked to on-the-water per brief §3.2.e; events has no OSM
surface). **No Layer-4 verifier** for 5.8 — kickoff §3 resolved Option
C (defer AZ event aggregators + LHC Tourism Board paths to V1.5).

**Narrow scope** — 7 of the 10 entertainment_attractions labels are
in-scope; 3 cat-7 labels (parks, golf courses, mini golf) deferred
because 5.7 already absorbed them.

---

## §0 Pre-flight (closed)

| Check | Result |
|---|---|
| `git log -1 --oneline` | `0b426e1` (post §1 sustainability) over `8dfa2a2` kickoff over `4b20e37` SHA-cleanup over `e60b051` 5.7 SHIP |
| `python -m alembic current` | `0a1b2c3d4e5f` ✅ (unchanged across all 5.x phases) |
| `python -m pytest --collect-only` | **1964 collected** ✅ (5.7 baseline 1946 + 18 from `0b426e1`'s `test_phase5_8_places_load_resolver.py`) |
| `python outputs/diagnose_category_id_gap.py` | `events` slug present at id=2 ✅; all 5.1-5.7 cats intact |
| `gh run list --branch main --limit 3` | Top 3 runs ✓ green on main (`0b426e1` / `8dfa2a2` / `4b20e37`); `parks-rec-scrapes` cron continues ❌ per 5.7 §4.5 root-cause (out of 5.8 scope) |
| Google Places key + spend cap | In `.env` ✅; spend cap active. Operator declined rotation per "all keys will be changed at conclusion of this project" |
| Playwright | Not needed for 5.8 (Option C — no Layer-4 verifier built) |
| Widened four-file shape check (5.7 §0 carry) | `git diff --stat` empty across `places_categories.json` + `places_load.py` + `models.py` + `google_types_mapping.py` ✅; fifth-recurrence forecast did NOT materialize |
| Working tree clean | ✅ (untracked carry-over from 5.7: `hava_api_catalog.docx`, `~$va_api_catalog.docx` Word lock, 2 historical `outputs/ci_*_log_failed.txt`, `outputs/_deltest`) |
| DB baseline | `outdoors-parks-trails`: 27 entries ✅; `events`: 2 entries ✅ (Buses By The Bridge + Desert Storm HQ, the 5.7 §2 FLIPs) |

**No pre-flight surprises** — the §0 cadence cleared without surfacing
any `places_categories.json` corruption (5.7-session-2 forecast of a
fifth recurrence did not materialize again this session).

---

## §1 Layer 1 — Google Places (only scrape layer for 5.8)

### Sustainability commit (sustainability-first pattern, lands BEFORE Layer 1)

`0b426e1` — `fix(scripts): _PRIMARY_TYPE_MAP extend for Phase 5.8
sustainability layer`. Added 7 direct `_PRIMARY_TYPE_MAP` entries for
the events (cat-2) primary types in
`app/contrib/google_types_mapping.py`:

```python
"event_venue": ("events", "commercial"),
"art_gallery": ("events", "place"),
"museum": ("events", "place"),
"live_music_venue": ("events", "commercial"),
"movie_theater": ("events", "commercial"),
"bowling_alley": ("events", "commercial"),
"amusement_arcade": ("events", "commercial"),
```

Direct mappings beat the 5.7
`(None, "entertainment_attractions") -> "outdoors-parks-trails"`
catch-all per the resolver order in
`scripts/places_load._resolve_category_id`, so cat-7 routing remains
correct for wildlife_refuge / tourist_attraction while these 7 route
to cat-2.

Regression tests at `tests/test_phase5_8_places_load_resolver.py` —
18 collection items (7 parametrized event-type mappings + 1
place-type-default gate + 10 preservation tests for 5.7 +
5.6 / 5.5 / 5.4 / 5.3 / 5.2 fallbacks). Pytest 1946 → 1964. CI green
on commit before §1 Layer 1 dispatch.

### Narrow-scope wrapper

`outputs/phase5_8_narrow_label_filter.py` — short-circuits the
discovery loop to only the 7 in-scope entertainment_attractions
labels: event venues, live music venues, art galleries, museums,
movie theaters, bowling alleys, arcades. The 3 deferred labels
(mini golf, golf courses, parks) live in the same domain but are
already absorbed by 5.7's lane. Mirrors
`outputs/phase5_7_narrow_label_filter.py` exactly (Path A.2 — same
standalone outputs/ wrapper pattern).

### Discovery — dry-run

```
python outputs/phase5_8_narrow_label_filter.py --dry-run
```

| Field | Value |
|---|---|
| Mode | dry-run (event venues only) |
| Categories run | 1 |
| Requests | 3 |
| Unique places | 60 |
| Cost (actual) | ~$0.10-0.20 (within projection) |
| Smoke notes | ✅ sys.path bootstrap worked first-try (5.7 boot bug did NOT re-surface); event venues alone produced 60 unique across 3 pages |

### Discovery — full 7-label sweep

```
python outputs/phase5_8_narrow_label_filter.py
```

| Field | Value |
|---|---|
| Mode | full |
| Categories run | 7 |
| Requests | 9 |
| Unique places | **89** |
| Cost (actual) | ~$0.50-0.70 (slightly under projection) |

Per-label split (7 labels, all `entertainment_attractions` domain):

| Label | Pages | New unique |
|---|---|---|
| movie theaters | 1 | 2 |
| bowling alleys | 1 | 1 |
| arcades | 1 | 10 |
| museums | 1 | 1 |
| art galleries | 1 | 7 |
| live music venues | 1 | 18 |
| event venues | 3 | 50 |

Notes: 6 of 7 labels capped at 1 page (LHC's events density is lower
than retail or auto). event venues alone took 3 pages and contributed
56% of unique hits. The dry-run pulled 60 event-venue uniques; the
full run pulled 50 (slight variance is normal — Google ranks shift
between calls).

### Enrichment

```
python -m scripts.places_enrichment --limit 200
```

| Field | Value |
|---|---|
| Input | 89 |
| Cache-hits (resume skip) | 87 |
| New enrichments | 2 |
| 404 errors | 0 |
| Other errors | 0 |

**97.8% cache reuse** — the highest cache-hit rate seen across the
5.x phases (vs 5.6's 94.2%). Explanation: events overlaps heavily
with prior phase discoveries — bars/restaurants (cat-1 from 5.1) often
tag as live music venues; lakeside venues (cat-6 from 5.2) tag as
event venues; parks/golf (cat-7 from 5.7) tag as entertainment
attractions; etc. The cumulative cache at 2,642 enriched rows
pre-load + 2 new = 2,644 post-load.

### Load — dry-run

```
python -m scripts.places_load --category events --dry-run
```

| Field | Value |
|---|---|
| Enriched rows in cache | 2,644 |
| After `--category events` filter | 70 |
| After ZIP filter | 52 kept, 18 dropped |
| ZIP-drop top buckets | 85210 (4), 85250 (2), 85205 / 86442 / 85305 / 85142 / 92363 / 86338 / 85383 / 85260 / 85365 / 85254 / 85226 / 85344 (1 each) |
| Dry-run notes | No DB writes; load identical shape to full run below |

ZIP drops are mostly metro-Phoenix / Mesa / Yuma outliers from Google's
multi-city aggregation — same pattern as prior phases.

### Load — full

```
python -m scripts.places_load --category events
```

| Field | Value |
|---|---|
| Input rows | 52 |
| Skipped (no name) | 0 |
| **Inserted (new entities)** | **1** |
| **Updated (existing place_id)** | **29** |
| **Reconcile ambiguous (geo+name conflict)** | **22** |
| Reconcile merged (geo) | 0 |
| `category_id` resolved (Tier 1) | **52** ✅ |
| **`category_id` unmapped (operator queue)** | **0** ✅ |
| EntityCategory rows inserted | 1 |

**🎯 Sustainability commit validation:** 0 unmapped of 52 — the 7 new
`_PRIMARY_TYPE_MAP` entries shipped at `0b426e1` caught every event
primary_type. No surgical fix-forward needed (unlike 5.5 / 5.6 which
fixed up _DISCOVERY_DOMAIN_FALLBACK reactively post-load).

**Cross-cat overlap is the §2 surface:** 29 "updates" means the
Provider row data refreshed but EntityCategory was NOT touched (per
the 5.7 close-out §5 sustainability matrix — re-pulls don't perturb
operator-curated category assignments). Those 29 entities exist in
their original categories (likely cat-7 / cat-6 / cat-1 from prior
phase loads); §2 audit decides which to FLIP to cat-2.

**Post-load cat-2 state:**
- Pre-load: 2 (Buses By The Bridge + Desert Storm HQ — 5.7 §2 FLIPs)
- Post-load §1: 3 (the 1 new insert from this run)
- Net to reach gate-1 (≥20): need +17 from §2 FLIPs

### Total §1 cost

| Step | Cost |
|---|---|
| Discovery (dry-run) | ~$0.10-0.20 |
| Discovery (full) | ~$0.50-0.70 |
| Enrichment | ~$0.02 (2 new × ~$0.01) |
| **Total Layer 1** | **~$0.62-0.92** |

Well under the kickoff §1 projection ($0.50-1.20) — high cache reuse +
low new-enrichment count + Narrow scope kept the burn small.

---

## §2-§4 outcomes (summarized; details in `outputs/phase5_8_events_audit.md`)

_TBD — populated post-§2 audit._

---

## Open questions / carry-forward

- _TBD — populated during the dispatch._

---

*Live scrape log — populated incrementally as the §1 dispatch
proceeds. Final values + §2-§4 sections land at SHIP time alongside
the audit doc + apply-scripts + gate verification.*
