> **Fresh Claude or Cursor session?** Read this file first, then `docs/maintainability/master_build_plan.md` (the canonical 13-phase execution plan; supersedes earlier strategy + roadmap docs for sequencing), then `docs/maintainability/dispatch_protocol.md` + `docs/maintainability/dispatch_channels.md` (the 12 working-agreement rules + channel-pick playbook). Older entry points (`docs/WORKING_AGREEMENT.md`, `docs/BACKLOG.md`, `docs/PROJECT.md`, `HAVA_CONCIERGE_HANDOFF.md`) carry pre-pivot framing and are still useful as code-level reference but not strategic state-of-record.

# Current state

This document is updated at the end of each session that ships work. It is the canonical answer to "where is the project right now."

## Production

- **Production URL:** https://havasu-chat-production.up.railway.app
- **Current main HEAD (origin):** `2ab5f07` (session-23, 2026-05-13, `chore(docs+outputs): master plan filename fix + Phase 4.3 dispatch prompt artifact`) on origin/main; pushed. **Phase 4.1 + Phase 4.2 of the master build plan SHIPPED on origin (NOT yet deployed to production)** — Phase 4.1: new `app/core/background.py` retry-wrapper + Sentry breadcrumb integration; new must-not-lose `Outbox(Base)` table + migration `0a1b2c3d4e5f_phase4_outbox` chaining off `e1f2a3b4c5d6`; new `scripts/outbox_redrive.py`; magic-link send refactored from sync try/except to Outbox row + BackgroundTasks delivery; +28 net-new tests in `tests/test_phase4_background.py`. Phase 4.2: new `app/contrib/ingest_base.py` abstract `BaseIngestClient` (4 methods) + `RawHit`/`EnrichedHit`/`EntityPayload` dataclasses; new `app/contrib/google_places_scraper.py` library module + `GooglePlacesClient` Layer-1 subclass; new `app/contrib/google_types_mapping.py` operator-maintainable types table; anchored edits on `scripts/places_discovery.py` (now thin wrapper + `--category` flag) + `scripts/places_enrichment.py` (now thin wrapper); +16 net-new tests in `tests/test_phase4_ingest_client_interface.py`. Session-23 substantive chain on origin: `91cd37b` (feat(phase4.1) — 12 files, +1770/-14) → `f5b3953` (chore(phase4.1) ruff autofix — 3 files, +7/-9) → `a75cfe8` (docs: Phase 4.1 ship-line on master plan + STATE.md session-23 refresh) → `aaac4db` (chore(outputs): Phase 4.2 dispatch prompt artifact) → `86eeaf8` (feat(phase4.2) — 6 files, +1094/-460) → `2ab5f07` (chore(docs+outputs): master plan filename fix + Phase 4.3 dispatch prompt artifact). Phase 4.3 + 4.4 dispatch prompts at `outputs/cursor_dispatch_prompt_phase_4_3.md` (SHA-patched post-4.2 with `86eeaf8` + pytest baseline `1749`) + not yet authored respectively. Session-22 substantive chain (now historical): `d5e9b71` (session-21 SHA-patch chore) → `18a4100` (chore(ci): re-enable parks-rec-scrapes cron after production deploy lands) → `5faa37c` (fix(db): break Phase 1D dual-write hook registration cycle — centralize in app/db/__init__.py; resolves the parks-rec-scrapes failure session-21 misdiagnosed as ORM-vs-prod-DB schema mismatch; +1 regression test net-new). Session-21 substantive chain (now historical): `5dbde39` (Phase 3.2 substantive) → `bd9b00f` (chore: Phase 3.2 dispatch prompt artifact) → `294567b` (Phase 3 SHIPPED master plan + STATE.md session-21 refresh) → `43b5f8f` (session-21 close-out) → `59521dd` (chore: pause parks-rec-scrapes cron pre-deploy) → `d5e9b71` (SHA-patch chore at session-21 close).
- **Currently deployed in production:** `d5e9b71` (session-21 close-out SHA-patch chore — Railway auto-deployed when 9 staged env-var changes were applied 2026-05-13; deploy timestamp 07:06:34 UTC). The full Phase 1C + 1D + Phase 2A (account-lite + magic-link + claim flow + favorites + admin role + viewer_is_owner) + Phase 2B (FTS + pg_trgm + /api/search + photos + R2 + sweep + three-tier hero/gallery) + Phase 3 (v1.1 schema + category taxonomy rewrite + 4-pass audited backfill + 10-district seed + entity backfills) stack landed in one push. Six alembic migrations applied: `f8e9d0c1b2a3` Phase 1D NOT NULL flip → `92ce4899dc08` Phase 2A.1 account-lite (users + magic_link_tokens + sessions + user_favorites + claims) → `c8d9e0f1a2b3` Phase 2B.2 FTS (`CREATE EXTENSION pg_trgm` + `search_vector tsvector GENERATED ALWAYS AS STORED` + 4 GIN/trgm/partial-JSON indexes) → `f9e8d7c6b5a4` Phase 2B.1 photos table (19 columns + 2 CHECK + 2 FK CASCADE + 5 indexes) → `d0e1f2a3b4c5` Phase 3.1 v1.1 schema additions (5 new tables + 7 entity columns + users.preferred_mode) → `e1f2a3b4c5d6` Phase 3.2 data pass (rename 7 slugs + delete family/community + insert 2 new + 4-pass backfill + 10-district seed + entities.district_id + entities.featured). Pre-flight verifications session-22 (operator-run): `SELECT * FROM pg_available_extensions WHERE name='pg_trgm'` returned `1.6 / 1.6` (extension already installed on Railway Postgres); all 8 required env vars (R2_* × 5 + RESEND_* × 2 + AUTH_MAGIC_LINK_BASE_URL × 1) confirmed present. Post-deploy smoke: `/home` rendered (events visible, search box active); `/api/search?q=marina` returned ranked JSON 200 with multi-result match (River Scene marinas surfaced cleanly — confirms FTS index built + queryable + ranking pipeline works); alembic head queried directly on prod Postgres returned `e1f2a3b4c5d6`. Magic-link login + photo upload smokes deferred (functional surface, not migration correctness — exercisable any time post-deploy). UI is mid-rebuild per session-22 operator note; rendered home page categories show a mix of canonical 12 + legacy free-text labels (`Professional`, `Beauty & care`) which is browser/CDN-cached vintage of the home page rather than a data problem — confirmed via the post-`/home` direct alembic query.
- **Build phase:** **Phase 1 + Phase 2 + Phase 3 LIVE in production (deployed 2026-05-13 session-22); Phase 4.1 SHIPPED on origin in session-23 (2026-05-13) but not yet deployed.** Phase 1: 1A schema-additive (`ff9832d`) → 1B data backfill (`d475b06`) → production hotfix (`5132162`, Postgres boolean-default coercion) → 1C application read pivot (`e0417c8`) → 1D dual-write + entity_id NOT NULL flip + close-out (`3f3628e`). Phase 2A: account-lite schema + ORM + Resend scaffold (`6000138`) → magic-link auth routes + SessionMiddleware + 4 login templates (`714ca52`) → claim flow + favorites + admin role parallel-path + viewer_is_owner + Lane 2A close-out (`5fea2ce`). Phase 2B.2: Postgres FTS + pg_trgm + tier 2 LIKE→FTS dispatch + new `app/search/` package + new `app/chat/tier2_synonyms.py` (`d631c77`). Phase 2B.3: public `GET /api/search` endpoint + home + provider profile search bar + Lane 2B consumer wiring (`8338505`). Phase 2B.1: photos schema + R2 client + Pillow pipeline + 4 upload/manage/delete routes + stuck-upload sweep + three-tier hero/gallery extension on derive_hero_photo + derive_gallery (`1c57c73`). Phase 3.1: v1.1 schema additions (`7925a14`). Phase 3.2: data pass (`5dbde39`). **Phase 4.1 at `91cd37b` (cleaned up at `f5b3953`)** ships the background-jobs scaffold: new `app/core/background.py` with `with_retry` sync + `with_retry_async` helpers + Sentry breadcrumb integration; new Outbox(Base) table + migration `0a1b2c3d4e5f_phase4_outbox` chaining off `e1f2a3b4c5d6`; new `scripts/outbox_redrive.py` for the must-not-lose redrive sweep (operator-runnable; Railway-cron-service wiring lives in Phase 4.4 close-out); magic-link send refactored from inline sync try/except to Outbox row + `BackgroundTasks.add_task(deliver_outbox_row, ...)` (email body / recipient / Resend API call semantics unchanged — the handler flows through `_magic_link_outbox_handler` which lazy-imports `send_magic_link` from `app.auth.email_sender`). **Phase 1D session-22 fix at `5faa37c`** centralized dual-write hook registration in `app/db/__init__.py`; Phase 4.1 builds on that import-discipline lesson by lazy-importing `Outbox` from `app.db.models` inside functions (the `app/core/background.py` module top deliberately does NOT pull `app.db.models` — preserves the gotcha #17 cure). See master plan §4 Phase 1 + §4 Phase 2 + §4 Phase 3 + §4 Phase 4 "Shipped (incremental)" lists for the per-sub-phase ship-lines, and `docs/maintainability/dispatch_channels.md` gotcha #17 for the circular-import postmortem. **Next dispatchable sub-phase:** Phase 4.2 (layered-scrape client interface + Google Places refactor — master plan §4 Phase 4 below + brief `outputs/cursor_brief_phase_4_background_jobs_scrape.md` §5). Phase 4.2 dispatch prompt not yet authored; the brief body is positioned. Phase 4.1 → 4.2 → 4.3 → 4.4 close-out is the remaining work to close Phase 4.
- **Pytest:** **1749 collected (1749 passed + 2 skipped + 30 subtests)** (1733 entering Phase 4.2 per session-23 post-4.1 baseline; **+16 net-new** in `tests/test_phase4_ingest_client_interface.py` covering BaseIngestClient abstract-method enforcement, GooglePlacesClient is-a-subclass + source_name + dedupe_key shape, google_types_mapping lookups for known/unknown types, GooglePlacesClient.to_entity_payload produces correct EntityPayload, GOOGLE_PLACES_LIMITER attribute introspection, and a subprocess gotcha-#17 regression that asserts `from app.contrib.google_places_scraper import GooglePlacesClient` does NOT trigger an import-chain cycle). The 2 skipped are now (a) the Postgres-only FTS execution path from `tests/test_search_fts.py` (unchanged since session-18) + (b) the Phase 4.2 refactor-regression smoke deferred-to-operator (`python -m scripts.places_discovery --category eat-drink --dry-run` should produce same log output as pre-refactor; operator runs on demand). Full-suite run completed in 7:18 from session-23 Windows-side venv; zero regressions.
- **Alembic head (origin):** `0a1b2c3d4e5f` (Phase 4.1 outbox table; on origin, NOT yet deployed; Phase 4.2 has no migration so head is unchanged from 4.1). **Alembic head (deployed prod):** still `e1f2a3b4c5d6` (Phase 3.2 data pass — last production deploy was session-22's). Chain: ... → `f8e9d0c1b2a3` Phase 1D NOT NULL flip → `92ce4899dc08` Phase 2A.1 account-lite schema → `c8d9e0f1a2b3` Phase 2B.2 FTS + pg_trgm → `f9e8d7c6b5a4` Phase 2B.1 photos table → `d0e1f2a3b4c5` Phase 3.1 v1.1 schema additions → `e1f2a3b4c5d6` Phase 3.2 data pass → `0a1b2c3d4e5f` Phase 4.1 outbox table. Phase 4.1's migration is portable (sa.JSON, sa.String + CHECK constraints, sa.func.now() defaults — no Postgres-only types, no `op.execute` raw SQL, no `sa.text("1")` / `sa.text("0")` Boolean defaults — mirrors Phase 2B.1 photos precedent). Migration upgrade + downgrade-1 + upgrade cycle verified clean on fresh SQLite in `tests/test_phase4_background.py::test_outbox_migration_upgrade_downgrade_cycle_clean` (uses `monkeypatch.setenv("DATABASE_URL", ...)` because `alembic/env.py:24` clobbers `cfg.set_main_option` — documented inside the test docstring as a discovery from this session). Will walk cleanly on Railway Postgres at next deploy (no production runtime gate on Phase 4.1; the new Outbox table starts empty + the magic-link route auto-creates rows on each request).
- **CI lint status:** Green; ruff clean on every Phase 1 sub-phase ship.
- **Feature flags:** unchanged from session-13 — `FEATURE_FLAG_DISCLOSURE_RENDERER` = `false` (HOLD until enrichment populates Sponsor + Provider rows); `FEATURE_FLAG_CONFIDENCE_TIER` = `true` (verified live); audience-signal persistence = AUTOMATIC (column-gated).
- **Health:** `GET /health` expected to return **200** with `db_connected`. Phase 1A + 1B schema/backfill is LIVE in production Postgres; Phase 1C application read pivot + Phase 1D dual-write helpers + NOT NULL flip migration ship on next deploy. Chat-route response shape pinned unchanged across the entire Phase 1 lane via regression coverage.
- **Catalog posture:** live `events` and `contributions` rows from River Scene import only (71 / 71 at last verify per `docs/maintainability/non_river_scene_cleanup.md`); `providers` / `programs` / `field_history` / `llm_mentioned_entities` empty. Phase 1B's backfill created `entities` rows + extension records for the 71 River Scene Events (`entity_type='event'`); legacy `events` table rows now carry non-null `entity_id` FK. Re-verify before relying on numbers.
- **Prior production state (pre-session-15):** `b22aa86` (session-13 ship; pytest 1442; alembic head `f1a2b3c4d5e6`). Substantive deployed runtime was `54a56b1` (Phase 2 follow-up cluster + sponsor outreach surface + forward-looking artifacts). See "Recently shipped" entries below for the per-commit narrative.

## Tests

- **Test command:** `python -m pytest -q`
- **Note:** Legacy seed / master-file tests were removed with the River Scene–only ingestion cleanup. Expectations and failure modes changed vs older baselines; run the suite after major pulls.

## Recent commits (newest first)

> Session-23 (2026-05-13) shipped **Phase 4.1 of the master build plan to COMPLETION on origin** — the first sub-phase of Phase 4 (background-jobs scaffold + Outbox must-not-lose surface; layered-scrape work is 4.2 + 4.3 + 4.4 close-out). Two commits this session: `91cd37b` (feat substantive — 12 files, 1770 insertions / 14 deletions) → `f5b3953` (chore: ruff autofix follow-up — 3 files, 7/-9; corrects a false "Ruff clean" claim on 91cd37b's commit body, the author had not actually run ruff before declaring it). Phase 4.1 dispatched against the brief at `outputs/cursor_brief_phase_4_background_jobs_scrape.md` with the SHIP-OUTBOX-NOW operator decision-lock from §2 — magic-link is the canonical must-not-lose job, deferring the Outbox would accept silent V1 user-signup failures. The brief was authored at session-23 boot alongside the dispatch prompt; pre-positioned earlier in the same session at `809ae69`. New `app/core/background.py` ships `with_retry` (sync) + `with_retry_async` retry helpers with Sentry breadcrumb integration + the Outbox kind/state constants + handler registry + `enqueue_outbox` + `deliver_outbox_row` (state machine: pending → in_flight → delivered on success / pending+attempts+=1 on transient / failed at attempts≥5); the module deliberately does NOT import `app.db.models` at module top (gotcha-#17 cure carried forward from session-22's circular-import fix at `5faa37c`). New migration `0a1b2c3d4e5f_phase4_outbox` chains off `e1f2a3b4c5d6`; new `scripts/outbox_redrive.py` is the operator-runnable redrive sweep (`--idle-seconds`, `--max-rows`, `--dry-run`, `--verbose` flags). Magic-link send refactored from sync `try: send_magic_link(...) except: logger.exception(...)` at `app/auth/routes.py:148` to `enqueue_outbox(db, kind=OUTBOX_KIND_MAGIC_LINK, payload={"email", "token", "next_path"})` → `db.commit()` → `background_tasks.add_task(deliver_outbox_row, outbox_id)` — same email body, same recipient, same Resend API call (the handler flows through `_magic_link_outbox_handler` which lazy-imports `send_magic_link` from `app.auth.email_sender`). 4 existing test files patched (`test_auth_flow.py`, `test_claims.py`, `test_favorites.py`, `test_photos_routes.py`) to move the `_capture_send` monkeypatch target from `app.auth.routes.send_magic_link` to `app.auth.email_sender.send_magic_link` (the canonical definition the new Outbox handler reaches via function-scope import). 2 pre-existing Phase 3 tests (`test_phase3_data_pass.py` + `test_phase3_schema_additions.py`) had `test_migration_upgrade_downgrade_upgrade_cycle` assertions hardcoding `e1f2a3b4c5d6` as the alembic head; Phase 4.1's migration moving the head was the first thing to trip them. Replaced with `ScriptDirectory.from_config(cfg).get_current_head()` so future phases don't trip the same wire. Pytest **1703 → 1733** (+28 net-new in `tests/test_phase4_background.py` + 2 pre-existing tests now passing again, no count delta from those). Alembic head **`e1f2a3b4c5d6 → 0a1b2c3d4e5f`** on origin (production still at `e1f2a3b4c5d6` until next Railway deploy). Ruff clean after `--fix` (5 issues: 4× I001 import sort + 1× F401 unused `OUTBOX_STATE_IN_FLIGHT`). HALT at brief §3 Phase 4.1 boundary per dispatch instructions; Phase 4.2 (layered-scrape client interface + Google Places refactor) dispatches in a fresh Cursor session. Session-23 lesson: **the Linux bash mount can serve a stale view of an Edit-modified file for the duration of the session, even after retries + `sync` + file copies** — extends gotcha #4/#7 with the observation that brand-new files written via the Write tool propagate fine, but Edit-modified files can stick at their pre-edit byte count (session-23 saw `app/db/models.py` stuck at 1475 lines / 67448 bytes the entire session even though Read tool showed the correct 1536-line post-Edit content). Practical effect: sandbox pytest verification of new tests is blocked when the test depends on imports from an Edit-modified file. Cure: trust Windows-side Read for file content; defer the canonical pytest run to the operator's Windows venv (which is what session-23 did + what produced the green 1733 result). Session-22 (2026-05-13) shipped Phase 1+2+3 to Railway production + restored parks-rec-scrapes cron + fixed the Phase 1D circular-import bug at `5faa37c` (latent regression since session-16 ship at `3f3628e`). Session-20 (2026-05-12) shipped **Phase 3.1 of the master build plan to COMPLETION on origin** — the first sub-phase of Phase 3 (v1.1 schema additions only; Phase 3.2 data backfill + close-out pending). Five-plus commits this session (1 gotcha-#16 docs, 1 Phase 3.2 district UX operator reality-check docs, 1 substantive Phase 3.1 ship, 1 chore for the session-19 dispatch-prompt patches that had been sitting uncommitted, plus this Phase 3.1 docs close-out + a session-20 close-out commit to follow). Phase 3.1 dispatched at session boot to a single fresh Cursor chat with the pre-positioned + SHA-patched prompt at `outputs/cursor_dispatch_prompt_phase_3_1.md`; Cursor returned a clean §13 with 5 sensible deviations all within the brief §9 invitations, one of which bypassed an explicit §4.1 HALT (seasonal_hours JSON column + Phase 1A extension table coexist via `seasonal_hour_rows` relationship rename) — operator-confirmed during commit as the right call since brief §4.1 explicitly listed the column as additive (which IS operator intent). Working tree carried two session-19 SHA-patched dispatch prompts that had never been committed (2B.1 + 2B.3 prompts patched in-place at session-19 paste-time per the SHA-patch-and-inline-present rhythm); landed as a small chore at `38abbcb` mirroring the `c9ab794` pattern. Mid-session: surfaced the **Phase 3.2 district paragraph UX** as an operator-reality-check decision — Lake Havasu (~57k pop, ~46 sq mi) is too small for a 10-district paragraph-landing-page UX (McCulloch is the main commercial strip; English Village is the only bounded-character district; the other 8 in the draft are directional/landmark/geographic). Three candidate paths captured in brief §7 (pare to 2-3 real districts + default, defer paragraphs to V1.5, or re-think to streets/landmarks); decision deferred to 3.2 dispatch authoring time. 5 Bucket C category-backfill decisions all locked at recommendation: `beauty_personal_care` → NULL queue / V1.5 defer, `tourism` → NULL queue for triage, `barbershop` test fixture → NULL, K-12/charter/public schools → `classes-sports-recreation`, bowling/arcades/mini golf → `classes-sports-recreation`. Gotcha #16 landed at `3bf9f66` — embedded double-quotes inside `-m '...'` bodies on PowerShell get re-tokenized as quote boundaries and break the commit even inside an outer single-quoted body (session-19 lost a docs commit to this; rule for future agents: avoid embedded double-quotes entirely in `-m '...'` bodies, use plain text or Unicode curly quotes for emphasis). Pytest 1664 (Cursor §0 baseline) → 1681 collected (+17 net-new from Phase 3.1). Alembic head `f9e8d7c6b5a4 → d0e1f2a3b4c5` (Phase 3.1 schema additions). New session-20 lesson: **descriptive notes vs change notes in Cursor §13 are not always distinguishable** — Cursor's §11 mentioned an alembic/env.py change but the §4 file list only showed `app/db/models.py` modified; turned out env.py was already in the shape Cursor described (had `get_database_url()` at runtime since some prior session), so the §11 note was descriptive of existing state, not a change. Rule from session-20 forward: always cross-check Cursor's claimed file list against actual `git status` Windows-side before staging; never trust §11 prose alone to determine file scope. Session-19 (2026-05-12) had shipped **Phase 2 of the master build plan to COMPLETION on origin**. Seven commits in session-19: 8338505 2B.3 feat → c464007 2B.3 docs → 1c57c73 2B.1 feat (Lane 2B close-out + Phase 2 COMPLETE) → 3d89e58 chore (Phase 3 brief + 3.1 dispatch prompt artifacts; bundled docs via gotcha #16 candidate surface) → c9ab794 (3.1 dispatch prompt SHA patch) → 26e6eb4 (close-out). Final session-19 pytest 1607 → 1663 (+56 = +9 from 2B.3 + +47 from 2B.1). Session-18 (2026-05-12) had shipped Phase 2A.3 (claim flow + favorites + admin role parallel-path + viewer_is_owner — closing out Lane 2A of Phase 2) and Phase 2B.2 (Postgres FTS + pg_trgm + tier 2 LIKE→FTS dispatch + new app/search/ package) IN PARALLEL via two Cursor chats per dispatch_protocol Rule 3 (file-disjoint). For deeper narrative + pragmatic deviations per commit, see "Recently shipped (high signal)" below + master plan §4 Phase 1 / §4 Phase 2 / §4 Phase 3 incremental ship-line lists + `docs/SESSION_HANDOFF_2026-05-12_session19.md`.

```
2ab5f07 chore(docs+outputs): master plan filename fix + Phase 4.3 dispatch prompt artifact
86eeaf8 feat(phase4.2): layered-scrape client interface + Google Places refactor
aaac4db chore(outputs): Phase 4.2 dispatch prompt artifact (pre-positioned post-4.1-ship; no operator decision-lock)
a75cfe8 docs: Phase 4.1 ship-line on master plan + STATE.md session-23 refresh
f5b3953 chore(phase4.1): ruff autofix -- import sort + drop unused OUTBOX_STATE_IN_FLIGHT
91cd37b feat(phase4.1): background-jobs scaffold + Outbox must-not-lose surface
809ae69 chore(outputs): Phase 4 brief + 4.1 dispatch prompt artifacts (pre-positioned; Outbox locked SHIP-NOW)
50f7d3e chore: patch session-22 docs commit SHA (d506b5a) + close-out SHA (81b6f55) into session-23 boot prompt + session-22 handoff doc + STATE.md Recent commits
81b6f55 docs: session-22 handoff + session-23 boot prompt + STATE recent-commits refresh
d506b5a docs: Phase 3 DEPLOYED 2026-05-13 + dispatch_channels gotcha #17 + STATE session-22 production block refresh
5faa37c fix(db): break Phase 1D dual-write hook registration cycle -- centralize in app/db/__init__.py
18a4100 chore(ci): re-enable parks-rec-scrapes cron after production deploy lands -- 6 migrations f8e9d0c1b2a3 -> e1f2a3b4c5d6 applied on prod Postgres; ORM-vs-DB mismatch resolved
d5e9b71 chore: patch session-21 close-out SHA (43b5f8f) + cron-pause SHA (59521dd) into session-22 boot prompt + session-21 handoff doc + STATE.md Recent commits
59521dd chore(ci): pause parks-rec-scrapes cron until production deploy lands (ORM-vs-prod-DB schema mismatch since Phase 3.1)
43b5f8f docs: session-21 handoff + session-22 boot prompt + STATE recent-commits refresh
294567b docs: Phase 3 SHIPPED on master plan + Phase 3.2 ship-line + STATE.md session-21 refresh + district paragraphs draft top-matter path (b) note
bd9b00f chore(outputs): Phase 3.2 dispatch prompt artifact (post-ship)
5dbde39 feat(db+app): Phase 3.2 -- category taxonomy rewrite + audited backfill + district seed as backend tag + entity backfills
c4fdc69 chore: patch session-20 close-out SHA (37c1bd9) into session-21 boot prompt + session-20 handoff doc + STATE.md Recent commits
37c1bd9 docs: session-20 handoff + session-21 boot prompt + STATE recent-commits refresh
81a83a1 docs: Phase 3.1 shipped line on master plan + STATE.md session-20 refresh
38abbcb chore(outputs): patch Phase 2B.1 + 2B.3 dispatch prompt TBD placeholders to session-19 ship SHAs (5fea2ce + d631c77 + c8d9e0f1a2b3 + 1607)
7925a14 feat(db): Phase 3.1 -- v1.1 schema additions (5 new tables + 7 entity columns + users.preferred_mode + ORM + 17 tests)
540efbd docs(outputs): Phase 3.2 district UX operator reality check (Havasu too small for 10-district paragraph plan)
3bf9f66 docs(maintainability): dispatch_channels gotcha #16 -- embedded double-quotes inside -m bodies on PowerShell
26e6eb4 docs: session-19 handoff + session-20 boot prompt + STATE recent-commits refresh
c9ab794 chore(outputs): patch Phase 3.1 dispatch prompt TBD placeholders to session-19 ship SHAs (1c57c73 + f9e8d7c6b5a4 + 1663)
3d89e58 chore(outputs): Phase 3 brief + 3.1 dispatch prompt artifacts (pre-positioned; also bundled master_build_plan + STATE.md docs updates due to PowerShell double-quote parsing failure on intended separate docs commit attempt)
1c57c73 feat(db+photos+providers): Phase 2B.1 -- photos + R2 + Pillow + upload routes + sweep + Lane 2B close-out + Phase 2 COMPLETE
c464007 docs: Phase 2B.3 shipped line + STATE.md session-19 refresh + R2 prereq lock note
8338505 feat(search+ui): Phase 2B.3 -- public /api/search + home + provider profile search bar + Lane 2B consumer wiring
aed79ac docs: session-18 handoff + session-19 boot prompt + STATE recent-commits refresh
4cb329d docs(maintainability): dispatch_channels gotcha #15 -- bash mount index.lock corruption in mixed-OS sessions
740223a chore(outputs): Phase 2B.1 + 2B.3 dispatch prompt artifacts (pre-positioned)
bc9cebc docs: Phase 2B.2 shipped line + STATE.md session-18 refresh
d631c77 feat(db+search+chat): Phase 2B.2 -- Postgres FTS + pg_trgm + tier 2 LIKE-to-FTS dispatch
6f7f1e9 docs: Phase 2A.3 shipped line + Lane 2A SHIPPED header + dispatch prompt SHA patch
5fea2ce feat(auth+admin+providers): Phase 2A.3 -- claim flow + favorites + admin role + viewer_is_owner + Lane 2A close-out
0d73b0f docs: session-17 handoff + session-18 boot prompt + STATE recent-commits refresh
7f5b1f7 chore(outputs): Phase 3 district paragraphs ChatGPT draft (prompt + response)
95d9f79 chore(outputs): Phase 2A.3 dispatch prompt artifact
9e672b5 docs: Phase 2A.2 shipped line + STATE.md session-17 refresh (round 2)
714ca52 feat(auth): Phase 2A.2 -- magic-link auth flow + session middleware + login UI
0e8e9e3 chore(outputs): Lane 2B brief + 2B.2 dispatch prompt artifacts
37e7770 docs(maintainability): dispatch_channels gotcha #14 -- reflog vs ancestry forensics
2423d4f chore(outputs): Phase 2A.2 dispatch prompt artifact
9150be5 docs: Phase 2A.1 shipped line + STATE.md session-17 refresh
5bf4c14 chore(outputs): Phase 2A dispatch artifacts (brief + 2A.1 dispatch prompt)
6000138 feat(db+auth): Phase 2A.1 -- account-lite v0.1 schema + ORM + Resend scaffold
4a5ee24 docs(outputs): patch session-17 boot prompt SHAs to current origin head
dcf2f7a docs(outputs): operator-prereq guide for Phase 2 (Resend + R2)
4bb74bc docs(maintainability): dispatch_channels gotcha #13 — PowerShell ; vs &&
03f7160 docs: session-16 handoff + session-17 boot prompt
1c98365 docs(STATE): session-16 close-out + Phase 1 complete on origin
cd079fc docs(maintainability): Phase 1D shipped line + Phase 1 SHIPPED close-out
3f3628e feat(app): Phase 1D — dual-write to entities + extensions across ingest paths
a27058d chore(outputs): Phase 1D dispatch prompt artifact
e2e66ad docs(maintainability): Phase 1C shipped line on master plan
e0417c8 refactor(app): Phase 1C — read pivot to ENTITY across providers/chat/scripts
a7984c6 docs(brief): fold Postgres boolean-default lesson into Phase 1 brief §10
2817951 docs(STATE): post-hotfix production refresh + session-16 entry for 5132162
5132162 hotfix(db): Postgres boolean default coercion in Phase 1A migration
9dee044 docs: session-15 handoff + session-16 boot prompt
24fa935 docs(maintainability): Phase 1B shipped line + brief amendment for deferred NOT NULL flip
6513798 chore(lint): ruff cleanup on app/providers/view_models.py (I001 + F401)
d475b06 feat(db): Phase 1B — backfill Provider/Event/Program into entities + extensions
ec84eb4 docs(maintainability): district paragraphs phased V1/V1.5/V2 (hybrid lock)
bf7383b chore(outputs): session-15 dispatch artifacts
7e13c3a docs(sponsor_outreach): land session-13 cold-pitch surface (5 files)
287e503 docs(maintainability): Phase 1A shipped line on master plan
ff9832d feat(db): Phase 1A — unified ENTITY schema + 11 extension tables (additive)
4211553 docs(maintainability): category backfill audit + Phase 3 amendment
32ef46a docs(maintainability): master build plan (13 phases, 6-9 months, ENTITY schema + Tier 1/2/3 sequencing + Opus design)
```

Older commits (pre-pivot Slice 60-71 + Phase 2 follow-up cluster + earlier slice narrative) are preserved historically below + in `git log --oneline`. Master plan §10 decision log holds the durable strategic-decision chain.

<details>
<summary>Pre-session-15 historical commits (folded for readability)</summary>

```
531dd13 refactor(core): drop orphaned open_ended_search_message + restore Outcome (Slice 71b)
a7c637f docs(STATE): close-out Slice 71 intent + search deletion (Campaign #36 closed)
f8a5126 docs(BACKLOG): close #36 — intent + search dormant surface deleted (Slice 71)
cbe1779 docs(decision): fix Slice 71 §10 substantive SHA after amend
c8cc2db refactor(core): delete dormant intent + search surface (Slice 71, #36 close)
2256ad4 docs(BACKLOG): tick #18 Phase C app/ entry + api routes Slice 69
24285ce docs(STATE): close-out Slice 70 search.md / dedupe peer-review fixes
de2df9b docs(components): search.md peer-review fixes + dedupe.md cross-ref (Slice 70)
45d2e2e docs(components): app/ entry + api routes batch (Slice 69)
6e89fd0 docs(STATE): close-out Slice 68 intent disposition decision
f7ed055 docs(decision): app/core/intent.py disposition (Slice 68, #36)
dd8c175 docs(BACKLOG): tick #18 Phase C app/core heavyweights Slice 67b
5251ffa docs(components): app/core/ heavyweights batch (Slice 67b)
e1bb7d8 docs(STATE): close-out Slice 67a app/core docs
17a4767 docs(BACKLOG): tick #18 Phase C app/core Slice 67a
e8a6313 docs(components): app/core/ small+mid batch (Slice 67a)
6cb24c2 docs(BACKLOG): close #31 campaign + decision-doc Outcome (Slice 65 #35)
b4b83f9 refactor(static): bridge refactor + IIFE drop (Slice 65, #31 close)
c449c27 docs(STATE): close-out Slice 66 API schema docs
738383c docs(BACKLOG): tick #18 Phase C API schemas Slice 66
5ec29e1 docs(components): API surface docs Slice 66
189c3f8 docs(STATE): close-out for Slice 63 CSS extraction (#31 step 2/3)
5264c16 docs(BACKLOG): file + close #34 (Slice 63 CSS); tick #31 progression
17b679e refactor(static): extract CSS to /static/styles (Slice 63, #31 step 2/3)
1f6573e docs(STATE): close-out Slice 64 app/db docs
7bfb3b9 docs(BACKLOG): tick #18 Phase C app/db batch (Slice 64)
cd36d0b docs(components): app/db doc gap-fill (Slice 64)
d662639 docs(STATE): close-out for Slice 61 JS extraction (campaign #31 step 1/3)
```

</details>

## Recently shipped (high signal)

- **Session-23-extension (2026-05-13, same calendar day as Phase 4.1 ship) — Phase 4.2 of the master build plan SHIPPED on origin (NOT yet deployed) — layered-scrape client interface + Google Places refactor; new `app/contrib/ingest_base.py` abstract `BaseIngestClient` with 4 methods + 3 dataclasses (`RawHit`, `EnrichedHit`, `EntityPayload`); new `app/contrib/google_places_scraper.py` library extracted from the existing 264-line `scripts/places_discovery.py` + 349-line `scripts/places_enrichment.py`; new `app/contrib/google_types_mapping.py` operator-maintainable Google `types[]` → `(category_slug, place_type)` table with ~28 initial entries; anchored edits on the two existing scripts (now thin wrappers + `--category` flag on `places_discovery.py` per design memo §6.1 step 1).** Two substantive commits + two docs commits this lane: `86eeaf8` (feat — 6 files, +1094/-460) → `2ab5f07` (chore docs+outputs — master plan filename fix surfaced by hygiene-scan Explore agent during Cursor session + Phase 4.3 dispatch prompt artifact pre-positioned and SHA-patched after the feat landed). Cursor dispatched against the pre-positioned prompt at `outputs/cursor_dispatch_prompt_phase_4_2.md` (authored at session-23 close alongside Phase 4.1 ship); chains off Phase 4.1's `f5b3953` autofix tip + alembic head `0a1b2c3d4e5f` (4.2 has no migration so head stays). Texture rule preserved — same DB rows out from `places_discovery.py` + `places_enrichment.py`, same rate-limit semantics (reuses `GOOGLE_PLACES_LIMITER` verbatim), same idempotency posture (resume-safe `load_processed_ids` pattern intact). Six brief §9 deviation invitations all landed at recommendation (4-method abstract interface as specified, split `run_discovery`/`run_enrichment` shape, free-form `EntityPayload.extension_payloads` dict, ~28-entry `google_types_mapping` initial coverage, `--category` flag baked in, `places_client.py` untouched). Pytest **1733 → 1749** (+16 net-new; refactor-regression smoke deferred-to-operator as 1 skipped). Ruff clean (operator verified after-the-fact via explicit `ruff check` on the 6 changed paths — `86eeaf8`'s commit body did NOT falsely claim Ruff clean, which means session-23's lesson about that bad pattern was absorbed durably from the Phase 4.1 ship). **Parallel work done while Cursor was in-flight:** (a) Explore-agent hygiene scan surfaced one factual error at `docs/maintainability/master_build_plan.md:210` (`app/core/background_tasks.py` should be `app/core/background.py` — the actual Phase 4.1 ship at `91cd37b` chose the shorter name); fix applied + bundled into `2ab5f07`. (b) general-purpose-agent Phase 5 prereq audit returned 7 substantive surprises (master plan's 60-100h Phase 5 operator estimate is bottom-up 80-190h; 3 operator decisions implicitly assumed but actually open — `beauty_personal_care` final home, RV-park vs lodging boundary, Eat & Drink Google `types[]` coverage list; 4 external sources named in strategy memo but never URL-verified — AZ ROC, LHC P&R, LHC business licenses, Mohave County GIS; `manual_recovery_checklist.md` structurally ready but content-empty; `heat_exposure` priority-30 list unauthored; `parks-rec-scrapes.yml` health status not verified post-deploy) but the agent's file write to `outputs/phase5_prereq_checklist.md` did not actually propagate to Windows-authoritative disk — agent sandbox quirk; doc deferred + the agent's surprises captured in chat history as forward-looking research notes. (c) Phase 4.3 dispatch prompt direct-authored at `outputs/cursor_dispatch_prompt_phase_4_3.md` (~280 lines mirroring 4.1/4.2 shape; operator decision-lock baked in: DEFER `entities.sources` JSON-array migration per brief §6.5).
  - **`86eeaf8`** — feat(phase4.2): layered-scrape client interface + Google Places refactor. 6 files in the commit. New `app/contrib/ingest_base.py` defines `BaseIngestClient(ABC)` with 4 abstract methods (`discover` / `enrich` / `dedupe_key` / `to_entity_payload`) + 3 `@dataclass` carriers (`RawHit` with source/source_stable_id/name/lat/lng/raw; `EnrichedHit` wrapping raw_hit + enriched dict; `EntityPayload` source-agnostic shape with name/entity_type/lat/lng/address/phone/website/description/category_slug/google_place_id/source/extension_payloads). Module top is pure-Python dataclasses + ABC — NO ORM imports at module top (gotcha-#17 cure carried forward). New `app/contrib/google_places_scraper.py`: `GooglePlacesClient(BaseIngestClient)` with `source_name = "google_places"`; reuses `GOOGLE_PLACES_LIMITER` for QPS pacing + retry on 429/5xx (rate_limiter.py untouched per brief §5.6); preserves resume-safe `load_processed_ids` pattern verbatim. Methods: `discover(query)` paginated Google Places Text Search; `enrich(hit)` Place Details fetch via place_id; `dedupe_key(hit)` returns `google_place_id`; `to_entity_payload(hit)` maps Google response → source-agnostic `EntityPayload` using `google_types_mapping` for category_slug + place_type. Orchestration methods `run_discovery(category, dry_run)` + `run_enrichment(dry_run)` match the existing script entry-point shapes so the script wrappers are thin. New `app/contrib/google_types_mapping.py` operator-maintainable mapping table per strategy memo §3.1 — `_PRIMARY_TYPE_MAP: dict[str, tuple[str, str | None]]` with ~28 entries; `map_google_types_to_slug_and_place_type(types: list[str]) -> tuple[str | None, str | None]` tries primary type first then fallbacks; returns `(None, None)` if no match (operator-queue surface for review per Phase 5). Anchored edits on `scripts/places_discovery.py` (now thin wrapper with `--category` flag) + `scripts/places_enrichment.py` (now thin wrapper). 16 net-new tests covering `BaseIngestClient` abstract-method enforcement, `GooglePlacesClient` subclass conformance + correct `source_name` + correct `dedupe_key`, `google_types_mapping` lookups for `["restaurant"]` / `["dog_park", "park"]` / unknown types, `GooglePlacesClient.to_entity_payload` produces correct payload, GOOGLE_PLACES_LIMITER attribute introspection, subprocess gotcha-#17 import-chain regression. 1 skipped (refactor-regression smoke deferred-to-operator).
  - **`2ab5f07`** — chore(docs+outputs): master plan filename fix + Phase 4.3 dispatch prompt artifact. Bundles two related touchups: (a) `docs/maintainability/master_build_plan.md:210` was changed from `app/core/background_tasks.py` to `app/core/background.py` to match the actual Phase 4.1 ship at `91cd37b` (surfaced by hygiene-scan Explore agent during Cursor's 4.2 session). (b) `outputs/cursor_dispatch_prompt_phase_4_3.md` is the pre-positioned Phase 4.3 dispatch prompt (~280 lines mirroring 4.1/4.2 shape) — chains off Phase 4.2's `86eeaf8` + pytest baseline 1749 (both SHA-patched in this same commit per session-19+20+21+22+23 SHA-patch-at-close rhythm); operator decision-lock baked in (DEFER `entities.sources` JSON-array migration per brief §6.5).
  - **This docs close-out commit (next chore)** — master plan §4 Phase 4 "Shipped (incremental)" extended with Phase 4.2 ship-line + STATE.md Production block refresh (HEAD SHA `f5b3953 → 2ab5f07`, pytest `1733 → 1749`, alembic head unchanged at `0a1b2c3d4e5f`) + Recent commits prepend (`a75cfe8 + aaac4db + 86eeaf8 + 2ab5f07` were also absent from the previous code block) + this Recently shipped §1 prepend.

**Session-23-extension lessons absorbed (3):**

1. **Sub-agent file writes don't always propagate to the parent's Windows-authoritative filesystem.** The Phase 5 prereq audit general-purpose agent reported writing `outputs/phase5_prereq_checklist.md` (~225 lines, structured per brief-§10 shape) successfully, but the file did not exist on Windows when Cowork primary re-checked. The agent's *findings* (7 substantive surprises) were captured in its summary report and are durable in chat history, but the file artifact is not. Cure: when delegating file-creation work to a sub-agent, verify the file landed on Windows-authoritative disk via Read/Glob before assuming success; treat the agent's "wrote N lines" as advisory not authoritative. Same lesson scope as session-23's gotcha-#4/#7 bash-mount staleness — the agent's *intent* to write isn't the same as the file's *presence* on the target filesystem.

2. **Lesson #1 from Phase 4.1 (ruff-clean honesty) was absorbed cleanly.** `86eeaf8`'s commit body did NOT claim "Ruff clean" — Cursor wisely left that to the operator's after-the-fact `ruff check` invocation, which then confirmed clean. The Phase 4.1 walk-back-commit (`f5b3953`) was the durable cure: future ship-line commits leave verification claims to the operator's actual runs.

3. **Parallel-work scope discipline held cleanly.** While Cursor worked on Phase 4.2's `app/contrib/*` + `scripts/places_*` + `tests/test_phase4_ingest_client_interface.py` scope, Cowork primary did file-disjoint parallel work — hygiene scan (read-only across `docs/` + `outputs/`), Phase 5 prereq audit (new file in `outputs/`, didn't land but didn't conflict either), Phase 4.3 dispatch prompt authoring (new file in `outputs/`). Zero merge conflicts at commit time. Rule 3 (file-disjoint sequential lanes) generalizes cleanly to Cursor-vs-Cowork parallel work, not just two Cursor sessions.

- **Session-23 (2026-05-13) — Phase 4.1 of the master build plan SHIPPED on origin (NOT yet deployed) — background-jobs scaffold + Outbox must-not-lose surface; magic-link send refactored from inline sync try/except to Outbox row + BackgroundTasks delivery; +28 net-new tests in `tests/test_phase4_background.py`; 4 existing test files repatched to track the moved monkeypatch target; 2 pre-existing Phase 3 hardcoded-head assertions future-proofed via `ScriptDirectory.get_current_head()`.** Two substantive commits this session: `91cd37b` (feat: 12 files, +1770/-14) → `f5b3953` (chore: ruff autofix follow-up; 3 files, +7/-9). Session boot at HEAD `809ae69` (the Phase 4 brief + 4.1 dispatch-prompt artifacts pre-positioned at session-23 authoring with the SHIP-OUTBOX-NOW operator decision-lock baked into §2 of the brief — magic-link is the canonical must-not-lose job, deferring would accept silent V1 user-signup failures). Baseline: pytest 1703 + alembic head `e1f2a3b4c5d6` confirmed via repo inspection. **Substantive lanes executed:**
  - **`91cd37b`** — feat(phase4.1): background-jobs scaffold + Outbox must-not-lose surface. New `app/core/background.py` ships `with_retry` (sync) + `with_retry_async` retry helpers with Sentry breadcrumb integration (`category="background-jobs"` + level=`warning` per retry + level=`error` on exhaustion / fatal); 4 Outbox kind constants (`magic_link` / `sponsor_notification` / `image_processing` / `other`) + 4 state constants (`pending` / `in_flight` / `delivered` / `failed`) + `OUTBOX_MAX_ATTEMPTS=5`; `register_outbox_handler` / `get_outbox_handler` registry; `enqueue_outbox(db, kind, payload)` writes a pending row in the caller's open session via `db.flush()` (no commit — caller's responsibility); `deliver_outbox_row(row_id, *, session_factory=None)` claims the row via `pending → in_flight` transition, invokes the registered handler once, transitions to `delivered` on success or back to `pending` (incrementing `attempts`) on failure / `failed` at attempts≥5. Default magic-link handler does function-scope `from app.auth.email_sender import send_magic_link` so importing this module never pulls the auth stack. **Critical:** module top deliberately does NOT import `app.db.models` — the `Outbox` ORM class is lazy-imported inside `deliver_outbox_row` + `enqueue_outbox` + `_record_outbox_failure` to preserve the gotcha-#17 cure documented at the top of `app/db/__init__.py` (Phase 1D circular-import fix from session-22's `5faa37c`). New migration `0a1b2c3d4e5f_phase4_outbox` chains off `e1f2a3b4c5d6` (Phase 3.2 data pass — the production-deployed tip). Creates `outbox` table with portable schema: `id VARCHAR(36) PK`, `kind VARCHAR(32) NOT NULL` + CHECK constraint, `payload JSON NOT NULL`, `state VARCHAR(20) NOT NULL DEFAULT 'pending'` + CHECK constraint, `attempts INTEGER NOT NULL DEFAULT 0`, `last_attempt_at DATETIME NULL`, `last_error VARCHAR(500) NULL`, `created_at + updated_at DATETIME NOT NULL DEFAULT sa.func.now()`, `delivered_at DATETIME NULL`; 2 indexes (`(state, created_at)` for redrive-poll selectivity + `(kind)` for operator inspection). Mirrors Phase 2B.1 photos precedent — sa.JSON / sa.String + CHECK / sa.func.now() defaults / no Postgres-only types / no raw `op.execute()`. `Outbox(Base)` ORM class appended to `app/db/models.py` BEFORE the trailing `_register_provider_slug_listeners()` call (with a comment block clarifying the distinction between the provider-slug listener pattern and the gotcha-#17-governed Phase 1D dual-write hook in `app/db/__init__.py`). New `scripts/outbox_redrive.py` is the operator-runnable redrive sweep with `--idle-seconds` (default 30 — avoids racing the hot-path BackgroundTasks invocation), `--max-rows` (default 100), `--dry-run`, `--verbose` flags; function-scope imports keep the script lightweight. **Magic-link send refactored** at `app/auth/routes.py:108-160`: removed the direct `from app.auth.email_sender import send_magic_link` import + the inline `try: send_magic_link(...) except: logger.exception(...)` block at the end of `request_link`; replaced with `enqueue_outbox(db, kind=OUTBOX_KIND_MAGIC_LINK, payload={"email": normalized, "token": plaintext, "next_path": safe_next})` → `db.commit()` → `background_tasks.add_task(deliver_outbox_row, outbox_id)`. Email body / recipient / Resend API call semantics unchanged — the handler flows through `_magic_link_outbox_handler` which lazy-imports `send_magic_link`. `BackgroundTasks` injected as a function param. **28 net-new tests** in `tests/test_phase4_background.py`: `with_retry` happy / exhaustion / retry-then-success / backoff escalation / fatal_on bypass / no re-raise / Sentry breadcrumb on retry+exhaustion / invalid max_attempts; `with_retry_async` happy / exhaustion / uses-asyncio-sleep-not-time-sleep; Outbox model importable with expected columns / default state=pending+attempts=0 / state CHECK rejects invalid / kind CHECK rejects invalid / migration upgrade+downgrade-1+upgrade cycle on tmp_path SQLite (uses `monkeypatch.setenv("DATABASE_URL", ...)` because `alembic/env.py:24` clobbers `cfg.set_main_option` — discovered + documented inside the test docstring); `deliver_outbox_row` success / transient / exhaustion / idempotent on delivered / missing row; `outbox_redrive` idle filter / max-rows cap / dry-run skip; magic-link integration via live `TestClient` — delivery + send-failure-leaves-pending-with-attempts-bumped; subprocess gotcha-#17 regression that asserts `import app.core.background` does NOT pull `app.db.models` into `sys.modules`; `enqueue_outbox` flush-no-commit / rejects unknown kind / `register_outbox_handler` rejects unknown kind. **4 existing test files patched** to monkeypatch the new send-magic-link location (`app.auth.email_sender.send_magic_link` instead of `app.auth.routes.send_magic_link`): `test_auth_flow.py` (_capture_send helper + 2 inline patches), `test_claims.py`, `test_favorites.py`, `test_photos_routes.py`. Texture rule preserved: same captured-token semantics, just the patch target moves to where the actual definition lives. Pytest 1703 → 1731 (+28) on the feat commit. **One critique walked back, one false claim corrected:** the feat commit body claimed "Ruff clean" but ruff had not actually been run; 5 issues surfaced when the operator ran ruff before the chore commit (4× I001 import-block sort across `app/auth/routes.py`, `scripts/outbox_redrive.py`, `tests/test_phase4_background.py` module-top + function-scope alembic imports inside the migration cycle test; 1× F401 unused `OUTBOX_STATE_IN_FLIGHT` in the new test file). Cleaned up in `f5b3953` follow-up.
  - **2 pre-existing Phase 3 tests patched in the same feat commit** — `test_phase3_data_pass.py::test_migration_upgrade_downgrade_upgrade_cycle` + `test_phase3_schema_additions.py::test_migration_upgrade_downgrade_upgrade_cycle` had hardcoded `assert ver == "e1f2a3b4c5d6"` after a full upgrade-head/downgrade-1/upgrade-head cycle. Worked when `e1f2a3b4c5d6` was the head; broke the moment Phase 4.1's migration moved the head to `0a1b2c3d4e5f`. Replaced with `ScriptDirectory.from_config(cfg).get_current_head()` so future phases don't trip the same wire. Net pytest delta unchanged (2 tests stayed at 1 each; just the assertion shape changed). Pytest after these patches: **1733 passed + 1 skipped + 30 subtests**.
  - **`f5b3953`** — chore(phase4.1): ruff autofix. Follow-up to `91cd37b` after the operator ran ruff and surfaced 5 issues that the prior commit body falsely claimed clean. All 5 auto-fixed via `ruff check --fix`: import-block reorders in `app/auth/routes.py` (moves the new `from app.core.background import (...)` block into proper alphabetical sort with the `from app.auth.*` and `from app.core.*` neighbors), `scripts/outbox_redrive.py` (trailing blank line after the import block), `tests/test_phase4_background.py` module-top (sort) + function-scope inside `test_outbox_migration_upgrade_downgrade_cycle_clean` (sort); drop `OUTBOX_STATE_IN_FLIGHT` from the test module's import list (referenced in docstring only, never used in code). Targeted pytest re-run after autofix: `tests/test_phase4_background.py + tests/test_auth_flow.py` 50/50 passed. Commit body explicitly walks back the prior false-clean claim ("My Phase 4.1 ship-line commit (91cd37b) claimed Ruff clean and that was inaccurate — I had not actually run ruff before declaring it.") so the operator-facing audit trail is honest about the recovery.

**Session-23 lessons absorbed (3):**

1. **Don't claim "Ruff clean" without actually running it.** Session-23's `91cd37b` feat commit body declared "Ruff clean" as part of its acceptance gate summary, but the assertion was inferred from "I wrote the code to match codebase style" not from a real `ruff check` invocation. Five real issues surfaced when the operator ran it (4× I001 import sort + 1× F401). Cost: one follow-up chore commit (`f5b3953`) + a public walk-back inside that commit body. Cure: when a brief's §13 acceptance gate lists "Ruff clean," run `ruff check` explicitly before declaring it — even in a sandbox where the venv install is broken, surface the inability to verify rather than asserting the positive. Same lesson scope as session-22's lesson #1 (confirm via log/repro before locking a diagnosis): the assumption + the visible signal aren't the same as direct confirmation.

2. **Bash mount staleness can persist for the entire session on Edit-modified files** (extends gotcha #4/#7 / session-22 lesson #4). Session-23 saw `app/db/models.py` stuck at its pre-edit 1475-line / 67448-byte view on the Linux bash mount for the entire session, even though the Edit tool's Windows-side write landed at 1536 lines (verified via Read). `sync`, `cp` to /tmp, multiple-minute waits, and a second Edit didn't refresh the mount. Brand-new files written via the Write tool propagated fine. Practical effect: in-sandbox pytest of new tests that import from the Edit-modified file fails with confusing `ImportError`; the canonical pytest run has to happen on Windows side. Cure: trust Windows-side Read for file content + defer pytest / ruff / full-suite verification to the operator's Windows venv when bash-mount-stale-Edit is observed.

3. **Pre-existing tests that hardcode the alembic head are a forward-incompatibility trap** every new migration trips. Session-23's `91cd37b` shipped a clean migration that left `0a1b2c3d4e5f` as the new head; 2 Phase 3 reversibility tests still asserted `assert ver == "e1f2a3b4c5d6"` (the Phase 3.2 head, frozen as a literal in session-21's Phase 3.2 ship). Cure: phase-N reversibility tests should query the current head dynamically via `ScriptDirectory.from_config(cfg).get_current_head()` — same shape Phase 4.1's `test_outbox_migration_upgrade_downgrade_cycle_clean` uses for its own check. Lesson durable forward: any future migration that lands without breaking these tests confirms the pattern; any future migration that *does* break them means another phase forgot to migrate to the dynamic-head shape and should be patched as part of its own ship-line.

- **Session-22 (2026-05-13) — Production deploy of Phase 1C + 1D + 2A + 2B + 3 SHIPPED to Railway (6 migrations walked cleanly; alembic head `b2c3d4e5f6a7 → e1f2a3b4c5d6`); parks-rec-scrapes cron restored; latent Phase 1D circular-import bug fixed at `5faa37c` with regression test (resolves the parks-rec-scrapes failure that session-21 handoff §2 misdiagnosed as ORM-vs-prod-DB schema mismatch); workflow_dispatch run #26 verified green end-to-end at 1m 5s.** Three substantive commits + two docs commits + this session-22 close-out commit. Session boot at HEAD `d5e9b71` (session-21 SHA-patch chore on top of `59521dd`); baseline verified via Read on `.git/refs/heads/main` + parent-walk decompression via `python3 + zlib.decompress` on `.git/objects/d5/...` (per gotcha #14 cure pattern; gotcha #15 discipline held — zero bash `git` operations against working tree all session; four-session streak now); alembic head `e1f2a3b4c5d6_phase3_data_pass` confirmed via Glob on `alembic/versions/`; pytest 1702 baseline per STATE.md (full Windows-side run completed 6:30 mid-session; matches handoff). Casey chose production deploy lane (b) over Phase 4 brief-authoring (a) per AskUserQuestion to unblock the paused parks-rec-scrapes cron. **Three substantive lanes executed in order:**
  - **Production deploy via Railway auto-deploy** — Operator applied 9 staged env-var changes in Railway UI (RESEND_* + R2_* keys that had been staged since session-19's R2 lock + session-17's Resend lock but never applied because the last deploy `5132162` predated both Phase 2A.2 + 2B.1 code that needs them). Railway auto-redeployed on env-var change; deploy timestamp 07:06:34 UTC. Six alembic migrations walked cleanly: `b2c3d4e5f6a7 → f8e9d0c1b2a3` Phase 1D NOT NULL flip → `92ce4899dc08` Phase 2A.1 account-lite (5 new tables) → `c8d9e0f1a2b3` Phase 2B.2 FTS (`CREATE EXTENSION pg_trgm` was a no-op — pre-flight `SELECT * FROM pg_available_extensions WHERE name='pg_trgm'` returned `1.6 / 1.6` showing extension already installed, so first-deploy risk was zero) + `search_vector tsvector GENERATED ALWAYS AS STORED` + 4 GIN/trgm/partial-JSON indexes → `f9e8d7c6b5a4` Phase 2B.1 photos table → `d0e1f2a3b4c5` Phase 3.1 v1.1 schema additions → `e1f2a3b4c5d6` Phase 3.2 data pass. Post-deploy smoke: `/home` rendered cleanly with events surfaced; `/api/search?q=marina` returned multi-result ranked JSON 200 (River Scene marinas — "Marina Fuel Station", "Havasu Riviera Marina", "Lake Havasu Marina Fuel Dock" — surfaced with non-trivial ranking scores); direct Postgres query confirmed `SELECT version_num FROM alembic_version` returned `e1f2a3b4c5d6`. Magic-link + photo-upload functional smokes deferred (not migration-correctness gates).
  - **`18a4100`** — chore(ci): re-enable parks-rec-scrapes cron after production deploy lands. One-line patch to `.github/workflows/parks-rec-scrapes.yml` uncommenting the `schedule:` block (`cron: "15 */6 * * *"`) + replacing the PAUSED comment block with a RESTORED block citing the deploy SHA `d5e9b71` + the alembic head `e1f2a3b4c5d6`. Workflow_dispatch retained for operator manual runs.
  - **`5faa37c`** — fix(db): break Phase 1D dual-write hook registration cycle — centralize in app/db/__init__.py. Manual workflow_dispatch run after the cron restore failed with `ImportError: cannot import name 'ContactPoint' from partially initialized module 'app.db.models'` — exposing that session-21's "ORM-vs-prod-DB schema mismatch" diagnosis (handoff §2) was wrong. The parks-rec-scrapes workflow had been failing every 6h since Phase 1D shipped at `3f3628e` (session-16, ~5 months earlier) for an unrelated module-import-order bug; the failure timing aligned with Phase 3.1's ship by coincidence (the workflow's exit-code-1 pattern + the visible schema gap made the misdiagnosis plausible without log inspection). Real cause: `app/db/database.py` ran `_register_orm_listeners()` at module-top, which force-loaded `app.db.models` via `importlib.import_module` then imported `app.db.entity_dual_write` (which itself imports `ContactPoint` and other ORM classes from models at its module top). Worked when uvicorn was the entry point (models loaded fully first); broke any entry point that reached `contribution_store → models → database` before models finished initializing (database.py module-top register call ran while models was still mid-load → entity_dual_write tried to import ContactPoint from the partially-initialized models → ImportError). **First attempted fix** moved registration to end-of-file in `models.py` (mirror of `_register_provider_slug_listeners()` end-of-file pattern); that shifted the cycle to `entity_dual_write → models → entity_dual_write` whenever a test imported entity_dual_write directly (`test_phase1d_dual_write.py:10` was the surface). **Final fix:** moved registration to `app/db/__init__.py` (was empty; now contains `from app.db.entity_dual_write import register_catalog_dual_write_hooks` + the registration call + a comprehensive docstring explaining the cycle history). The package `__init__.py` runs once when any `app.db.*` submodule is first imported, BEFORE any submodule attribute lookup, so it serializes the load order (`__init__.py → entity_dual_write → models → database → back up the chain → __init__.py calls hook`). No matter which submodule is the entry point, `__init__.py` drives the full load before any leaf submodule attribute lookup — structurally cycle-free. Both leaf modules (database.py + entity_dual_write.py) are now free of cycle-creating imports. Three files modified + 1 new file: `app/db/__init__.py` NEW (37 lines: registration + docstring history of two attempted-and-rejected leaf-module locations to deter future regressions); `app/db/database.py` MODIFIED (-11 / +11: `_register_orm_listeners()` function + module-top call removed; replaced with explanatory comment pointing at `__init__.py`); `tests/test_phase1d_dual_write.py` MODIFIED (+57 lines: regression test `test_scraper_entry_point_import_chain_does_not_cycle` runs the failing import path in a subprocess to bypass sys.modules cache masking from earlier tests; asserts both `load_latest_snapshots` imports + `_CATALOG_DUAL_WRITE_HOOKS_REGISTERED` flag True). Pytest **1702 → 1703** (+1 net-new). Zero regressions. Fix application-code-only — no migration impact, production runtime unaffected because uvicorn's entry-point order has always worked (the bug only manifested in the GitHub Actions scraper workflow). Companion lesson durably captured in dispatch_channels.md gotcha #17: confirm via log/repro before locking a diagnosis — session-21's misdiagnosis was made from workflow exit code + known origin-vs-prod schema gap without ever reading the actual stack trace; both context pieces were true but neither was the actual cause.
  - **Workflow_dispatch verification (run #26):** manually triggered on GitHub Actions after fix push; completed in 1m 5s with green ✓ status; pull-and-load step ran 4s vs the previous 48s+exit-code-1 failure. Cron is durably restored for the next scheduled fire (15 minutes past every 6h boundary).
  - **Side note (operator):** UI is mid-rebuild per session-22 operator note; the rendered `/home` page during smoke checks showed a mix of canonical 12 category slugs + legacy free-text labels (`Professional`, `Beauty & care`) which is browser/CDN-cached vintage of the home page rather than a data correctness problem (alembic head verified at `e1f2a3b4c5d6` post-deploy). Whatever the next session does on UI will land against fresh data shape.

**Session-22 lessons absorbed (4):**

1. **Confirm via log/repro before locking a diagnosis.** Session-21's "ORM-vs-prod-DB schema mismatch" diagnosis was made from workflow exit code 1 + the known origin-vs-prod schema gap, without ever inspecting the actual GitHub Actions stack trace. Both context pieces were true; neither was the actual cause. The first-failure surface in the trace (the ImportError) told us exactly what was broken. Cure (now durable in dispatch_channels.md gotcha #17): when diagnosing a workflow failure, always read the actual stack trace from the GitHub Actions log before locking a root-cause story — even when the "obvious" diagnosis matches the timeline and the known state-of-the-world. Same lesson scope as gotcha #11 ("Done" with no chat output may still have written the file) — assumption + visible signal aren't the same as direct confirmation.

2. **Module-import-time hook registration creates cycles when alternate entry points exist.** Phase 1D dual-write hook registration at `app/db/database.py` module-top worked under uvicorn (canonical entry point loads `app.main → models` first) but cycled under any other entry point (`scripts/parks_rec_load.py → contribution_store → models → database` reached database before models finished). Latent regression since session-16's Phase 1D ship at `3f3628e` (~5 months). Cure: cross-module registration belongs in the package `__init__.py`, not a leaf module — `__init__.py` runs before any submodule attribute lookup, so it can drive load order without itself being part of any sub-module cycle. Both leaf modules (database.py + entity_dual_write.py) must be free of cycle-creating imports for ALL entry points to work; only a third location (the package init) satisfies this. First-attempt fix in this session moved registration to end-of-file in `models.py` (mirror of `_register_provider_slug_listeners()`); that shifted the cycle rather than breaking it. Fully durable now in dispatch_channels.md gotcha #17 + the `app/db/__init__.py` docstring history.

3. **Gotcha #15 discipline held throughout** (continuation; four-session streak now session-19 + 20 + 21 + 22) — zero bash `git` operations against the working tree all session; HEAD verification via Read on `.git/refs/heads/main` + parent-walk decompression via `python3 + zlib.decompress` on `.git/objects/d5/...` per gotcha #14's cure pattern; alembic head via Glob on `alembic/versions/`; file-presence via Glob/Grep. Used the bash sandbox extensively for grep/wc/cat reads but never for git plumbing against the working tree.

4. **Linux mount view of the working tree can lag Windows edits — Windows-side Read is authoritative for file content too** (extending gotcha #7 from `.git` views to working-tree files). Mid-session the Edit tool modified `app/db/models.py` (verified via Read at line 1491 showing new dual-write block) but the Linux bash mount continued to serve the pre-edit 1475-line version even after `sync` + `O_DIRECT` + `dd`. Practical impact: in-sandbox pytest runs against the mount would have tested stale code; the verification was deferred to Casey's Windows-side venv (which sees the current file content). Cure: trust Windows-side Read for file content; defer pytest verification to Windows-side when Linux mount lag is observed.

- **Session-21 (2026-05-12) — Phase 3.2 of the master build plan SHIPPED on origin; Phase 3 of the master build plan COMPLETE on origin (3.1 + 3.2); district paragraph UX direction locked as path (b) (defer paragraphs to V1.5, seed districts as backend tag with `paragraph = NULL`); 5 Bucket C category-backfill decisions encoded in migration (3 as explicit NULL UPDATEs + 2 as documented-only locks per mid-session clarification).** Four-plus commits this session: `5dbde39` (Phase 3.2 substantive), `bd9b00f` (chore: Phase 3.2 dispatch prompt artifact, post-ship), this Phase 3.2 docs close-out (Phase 3 SHIPPED master plan header + 3.2 ship-line + STATE.md session-21 refresh + district draft top-matter path (b) note), and a session-21 close-out commit to follow. Session boot at HEAD `c4fdc69` (session-20 SHA-patch chore on top of `37c1bd9`); baseline check via Read on `.git/refs/heads/main` + parent-walk decompression via `python3 + zlib.decompress` on `.git/objects/c4/...` (per gotcha #14 cure pattern) confirmed `c4fdc69` is the predicted session-20 SHA-patch chore on top of `37c1bd9`; alembic head `d0e1f2a3b4c5` confirmed via Glob on `alembic/versions/`; pytest 1681 baseline per STATE.md (linux-mount has no pytest installed; gotcha #15 discipline held throughout — zero bash `git` operations against working tree all session). Three lanes pursued: (a) **resolved Phase 3.2 district UX direction** via AskUserQuestion — operator picked **path (b)**: defer paragraph landing pages to V1.5, seed districts with `slug + name + display_order` only (paragraph NULL); the simplest 3.2 scope; clears `[CASEY: ...]` placeholder polish work entirely; honors session-20 reality check that Havasu's geography doesn't justify 10 paragraph landing pages. (b) **Authored Phase 3.2 dispatch prompt** at `outputs/cursor_dispatch_prompt_phase_3_2.md` (~500 lines) chaining off `d0e1f2a3b4c5` with the 5 Bucket C decisions locked + path (b) UX direction locked + Postgres portability rules + HALT at Phase 3 close-out boundary; surfaced body inline for Casey to paste into fresh Cursor chat per session-19 SHA-patch-and-inline-present rhythm. (c) **Dispatched Cursor + verified ship + committed**. During Cursor in-flight time, **sub-agent surfaced a critical gap** in the dispatch prompt's LOCKED OPERATOR DECISIONS block: items A.4 (K-12/charter/public schools) and A.5 (bowling/arcades/mini golf) don't have standalone legacy `Provider.category` strings in audit memo §2. Sub-agent verified via reading the audit memo + DRAFT predecessor end-to-end + extracting Bucket A/B/C source strings + cross-checking against the dispatch prompt; Bucket A (24 strings) + Bucket B improved-homes (5) + Bucket B professional-services (5) + Bucket C items A.1-A.3 all match exactly; items A.4 + A.5 are sub-questions of existing Bucket B mappings (A.4 subsumed by `education` mapping; A.5 subsumed by `entertainment_attractions` NULL deferral with Phase 5 `google_primary_category` triage rule). Sent Cursor a mid-session clarification via paste-into-Cursor message reframing items A.4 + A.5 as documented-only locks; Cursor absorbed cleanly + reframed Pass 4 to 3 explicit NULL UPDATEs only + Test #23/#24 became combined education + entertainment_attractions deferral tests. Cursor §13 returned **22 net-new tests passing + ruff clean + alembic upgrade-downgrade-upgrade cycle verified on fresh SQLite**. Pytest 1681 → 1702 (+21 collected per Cursor §13). Alembic head `d0e1f2a3b4c5 → e1f2a3b4c5d6`. Highlights:
  - **`5dbde39`** — Phase 3.2 substantive: single data-pass migration `e1f2a3b4c5d6_phase3_data_pass` chains off `d0e1f2a3b4c5`. Renames 7 surviving slugs to new-taxonomy shape (`eat-and-drink → eat-drink`, `home-services → home-property-services`, `health → health-wellness-care`, `outdoors-and-parks → outdoors-parks-trails`, `shopping → shopping-essentials`, `auto-and-gas → auto-rv-fuel`, `lodging → lodging-vacation-rentals`); backs up `family` + `community` UUIDs to ad-hoc `_phase32_category_bak` table; NULLs providers/programs FK references + DELETEs `entity_categories` junction rows pointing at the soon-to-be-deleted rows (junction `category_id` is NOT NULL so brief §9 NULL-or-DELETE either-or collapsed to DELETE-only); deletes `family` + `community`; inserts `classes-sports-recreation` + `public-civic-resources`; resets sort_order 1-12 per synthesis §1 Tier 1/2/3 ordering. Four-pass Provider/Program backfill driven by audit memo §2 (Pass 1 Bucket A + Bucket E renames; Pass 2 Bucket B improved homes — `childcare_education` / `education` / `edu` → `classes-sports-recreation`, `religion_community` → `public-civic-resources`, `fitness_sports` → `health-wellness-care`, `entertainment_attractions` NULL queue per Phase 5 defer; Pass 3 Bucket B professional-services NULL queue — `insurance` / `financial` / `legal` / `real_estate` / `professional_services`; Pass 4 Bucket C three explicit NULL UPDATEs only — `beauty_personal_care` / `tourism` / `barbershop`). Deterministic 10-district `op.bulk_insert` with `paragraph = NULL` per **path (b) operator lock** (paragraph landing pages deferred to V1.5; draft preserved at `outputs/chatgpt_response_district_paragraphs_v1.md` for V1.5 re-engagement). `entities.district_id` backfill from `locations.district` name match — the `entities.district` String column never existed (Phase 1A semantics caught at dispatch time by Cursor; brief §5.4's drop-column instruction was moot). `entities.featured` backfill from `Provider.featured`. `users.preferred_mode` SQL no-op (3.1 direct-NOT-NULL). Anchored Edits on `app/home/queries.py` (CATEGORY_LABELS replaced with canonical 12; NEW `LEGACY_PROVIDER_CATEGORY_LABELS` constant preserves free-text `Provider.category` display until Phase 13 — prevents blank labels for operator-queue rows on profile/admin views; `_category_label` dual-lookup checks canonical first then legacy), `app/providers/queries.py` (`category_label_for` legacy fallback), `app/db/models.py` (`District.paragraph` altered to `Mapped[str | None]` per path b), `scripts/ingest/validate_enrichment_csv.py` (`_REJECTED_ENRICHMENT_CATEGORY_SLUGS` frozenset rejects 7 renamed + 2 deleted legacy slugs with clear error; allowlist sourced from `CATEGORY_LABELS.keys()`). 5 test files updated for new-taxonomy slug ripples + new `tests/test_phase3_data_pass.py` with 22 tests covering migration cycle + 12-slug shape + Tier sort_order + Buckets A/B/C/E + districts NULL paragraph + `locations.district → district_id` backfill + entities.district nonexistence assertion + featured backfill + CATEGORY_LABELS + validator vocab + idempotency + entity_categories junction cleanup + /home 200 smoke. **Five accepted deviations per brief §9:** (a) `entities.district` String column nonexistent — Phase 1A semantics catch at dispatch time; source for `district_id` backfill is `locations.district` instead; ORM relationship stays `district → District` (no rename, no drop_column). (b) `entity_categories` junction DELETE not NULL since junction `category_id` is NOT NULL; brief §9 either-or collapsed to DELETE-only. (c) Bucket C items A.4 + A.5 absorbed as **documented-only locks** per session-21 mid-session clarification — A.4 subsumed by Bucket B Pass 2 `education` mapping; A.5 subsumed by `entertainment_attractions` NULL deferral with Phase 5 `google_primary_category` triage rule recorded inline at Pass 2; Pass 4 final shape is three explicit NULL UPDATEs only. (d) NEW `LEGACY_PROVIDER_CATEGORY_LABELS` constant in `app/home/queries.py` preserves free-text `Provider.category` display until Phase 13 — interim plumbing for the operator-queue period; arguably "beyond what audit memo + synthesis specify" per brief §11 but rationale sound. (e) Test helpers mirror migration UPDATE/ORM semantics since migration does not re-run per test. Cursor session, single dispatch.
  - **`bd9b00f`** — chore: Phase 3.2 dispatch prompt artifact (post-ship). Lands `outputs/cursor_dispatch_prompt_phase_3_2.md` as historical artifact; preserves pre-clarification framing for items A.4 + A.5 (post-clarification reframe lives in `5dbde39` migration + tests + commit body, mirroring session-20 brief §4.1 narration precedent). Mirrors session-19 `3d89e58` + session-20 `38abbcb` dispatch-prompt-as-chore precedent.
  - **This docs close-out commit** — master plan §4 Phase 3 SHIPPED header + Phase 3.2 ship-line under "Shipped (incremental)" + STATE.md Production block refresh + Recent commits prepend (with `c4fdc69` brought into the block — session-20 SHA-patch chore wasn't included by `37c1bd9` close-out commit's Recent commits update) + Recently shipped §1 prepend + district paragraphs draft top-matter path (b) note (status flipped from "illustrative not canonical pending UX direction" to "V1 deferred to V1.5; preserved for V1.5 re-engagement").

**Session-21 lessons absorbed (6):**

1. **Sub-agent pre-flight verification catches dispatch-prompt gaps before Cursor halts.** The audit memo §2 cross-check sub-agent ran during Cursor in-flight time surfaced that A.4 + A.5 didn't have standalone legacy strings; the mid-session clarification message landed in <1 min of Cursor time and Cursor absorbed cleanly. Pattern: use Explore sub-agent for pre-flight dispatch-prompt verification during long Cursor sessions when there's audit/memo cross-referencing to do; the cost is one sub-agent context burn but the savings is one or more Cursor round-trips.

2. **Mid-session clarification messages work** — paste-into-Cursor reframe of items A.4 + A.5 was absorbed in the same session without HALT. Future sessions: if Cursor is mid-flight + a gap surfaces, send a focused clarification message; don't always wait for §13 HALT and re-dispatch. Caveat: the clarification must be small + targeted (one section of the prompt), not a full re-author.

3. **The dispatch prompt as-shipped historical artifact** is the right framing — preserves pre-clarification context as a record; post-clarification reframe lives in commit message + STATE.md narrative + tests. Mirrors session-20 brief §4.1 narration precedent (didn't patch the brief; narrated in commit). Decision-rule for future agents: brief / dispatch-prompt content stays as-authored-pre-Cursor; mid-flight deviations get narrated in the post-ship commit + STATE.md.

4. **Phase 1A semantics still surfacing in Phase 3** — Cursor caught that `entities.district` String column never existed (Phase 1A unified district into `locations` extension); brief §5.4's drop-column instruction was authored assuming entities.district existed. Future Cursor inheritance: when a brief instruction touches a column, always verify the column exists in models.py before authoring SQL. Same lesson scope as Phase 3.1's seasonal_hours JSON column vs extension table coexistence (session-20 brief §4.1 narration).

5. **Gotcha #15 discipline held throughout** (continuation; carrying forward session-19 + session-20 streak) — zero bash `git` operations against working tree all session; HEAD verification via Read on `.git/refs/heads/main` + parent-walk decompression via python3+zlib on `.git/objects/` per gotcha #14 cure pattern; alembic head via Glob on `alembic/versions/`; file-presence via Glob/Grep. Three-session streak now.

6. **Gotcha #16 discipline held throughout** — all session-21 commit recipes used PowerShell-safe single-quoted `-m` bodies with em-dashes / `->` / plain text for emphasis; no embedded double-quote pairs; all commits landed clean. Two-session streak.

- **Session-20 (2026-05-12) — Phase 3.1 of the master build plan SHIPPED on origin; Phase 3.2 district UX operator reality check; dispatch_channels gotcha #16; session-19 dispatch-prompt patches landed as durable historical state.** Five-plus commits this session: `3bf9f66` (gotcha #16 docs), `540efbd` (Phase 3.2 district UX operator reality check), `7925a14` (Phase 3.1 substantive — v1.1 schema additions), `38abbcb` (chore: session-19 SHA-patched 2B.1 + 2B.3 dispatch prompts), this Phase 3.1 docs close-out, and a session-20 close-out commit to follow. Session boot at HEAD `26e6eb4` (session-19 close-out); baseline check via Read on `.git/refs/heads/main` + Glob on `alembic/versions/` confirmed `f9e8d7c6b5a4` photos head + STATE.md authoritative pytest 1663 + 1 skipped. Three lanes pursued in this session: (a) **Phase 3.1 dispatched at boot** to a fresh Cursor chat using the pre-positioned + SHA-patched prompt at `outputs/cursor_dispatch_prompt_phase_3_1.md` (patched in `c9ab794` with `1c57c73` + `f9e8d7c6b5a4` + `1663` baseline values); Cursor returned §13 with 5 sensible deviations all within brief §9 invitations, one of which bypassed brief §4.1's explicit HALT for the seasonal_hours JSON column vs extension table conflict (operator-confirmed during commit as the right call since §4.1 explicitly listed the column as additive). (b) **Bucket C operator decisions walked + locked** during Cursor in-flight time: all 5 locked at recommendation (`beauty_personal_care` NULL queue / V1.5 defer; `tourism` NULL queue for triage; `barbershop` test fixture NULL; K-12/charter/public schools `classes-sports-recreation`; bowling/arcades/mini golf `classes-sports-recreation`). (c) **Phase 3.2 district paragraph UX reality check** surfaced: Lake Havasu (~57k pop, ~46 sq mi) is too small for a 10-district paragraph-landing-page UX; McCulloch is the main commercial strip; English Village is the only bounded-character district. Three candidate paths captured in brief §7 + district draft top now flags itself as illustrative-not-canonical. Decision deferred to 3.2 dispatch authoring time. Highlights:
  - **`3bf9f66`** — dispatch_channels gotcha #16: embedded `"..."` inside `-m '...'` bodies on PowerShell break the commit. Session-19 surface — a docs commit attempt failed when the `-m` body contained an embedded double-quote pair around `SHIPPED 2026-05-12`. PowerShell's native-command argument re-tokenizer (the layer translating PowerShell args to Win32 `argv[]` for non-PowerShell executables like `git.exe`) treats embedded `"..."` as quote-state changes EVEN inside an outer single-quoted body. Single quotes correctly suppress `$variable` interpolation and most metacharacter expansion, but the embedded double-quote pair causes the tokenizer to split the argument; what was supposed to be one `-m` argument becomes three positional args; git takes the first as the `-m` body and treats the rest as pathspecs, which then don't match. Consequence in session-19: docs files stayed staged from the prior `git add`, the next `git add` for outputs picked them up, and the chore commit at `3d89e58` bundled all four files with a subject mislabel. Rule 12 (no amend after push) meant we lived with it. Cure: avoid embedded `"..."` entirely in `-m '...'` bodies on PowerShell; use plain text (no quotes needed for emphasis), Unicode curly quotes (`"..."` U+201C/U+201D — re-tokenizer treats as regular chars), backtick-escape (`` `" ``), or stdin-piped `-F -` for long messages. Same lesson scope as gotchas #3 + #8 + #13 — PowerShell quoting/parsing surprises. Bash + zsh do NOT have this problem because they respect single-quote literalness for the entire argument body. Also extended gotcha #15: session-19 surfaced that even read-only git plumbing like `git ls-tree` should be replaced with the parent-walk pattern; rule from session-20 forward is zero `git ...` from bash sandbox against the working tree.
  - **`540efbd`** — Phase 3.2 district UX operator reality check. `outputs/cursor_brief_phase_3_v11_schema_pass.md` §7 + `outputs/chatgpt_response_district_paragraphs_v1.md` top edited to capture: Havasu's actual scale (~57k pop, ~46 sq mi) is too small for a 10-district paragraph-landing-page UX. McCulloch is the main commercial strip (street-based search would match user mental models better than district filters); English Village is the only bounded-character district; the other 8 in the draft are directional ("North End", "South side"), landmark-based ("Site Six", "Mesquite Bay"), or geographic ("Castle Rock area", "Highway 95 Corridor") — not user-mental-model districts. Phase 3.1 schema (districts table + `entities.district_id` FK) is forward-compatible and ships unaffected. Phase 3.2 district UX direction is OPEN with three candidate paths: (a) pare to 2-3 real districts (English Village + Downtown/McCulloch + maybe Lakefront commercial) plus a "Greater Lake Havasu" default, drop the 10-paragraph plan; (b) ship 3.2 with `district_id` backfill from String but defer paragraph landing pages to V1.5, use district as backend tag; (c) re-think the primitive entirely (districts demoted to backend tag, surface streets and landmarks as user-facing filter dimension; reopens 3.1 scope, highest cost). Decision deferred to Phase 3.2 dispatch authoring time. District paragraphs draft at `outputs/chatgpt_response_district_paragraphs_v1.md` now flags itself as illustrative not canonical and instructs operator/future Cursor not to polish until UX direction resolves.
  - **`7925a14`** — Phase 3.1 substantive: v1.1 schema additions. New alembic migration `d0e1f2a3b4c5_phase3_schema_pass` chains off `f9e8d7c6b5a4` (Phase 2B.1 photos). Creates 5 new tables: `districts` (id, slug UNIQUE, name, paragraph, display_order, timestamps, index); `alert_subscriptions` (user_id FK, alert_type, delivery_channel, UNIQUE on triple, CHECK constraints); `alerts_dispatched` (audit log, denormalized alert_type per brief, delivery_status CHECK); `external_conditions_cache` (source string PK, upsert pattern, fetched_at index); `peer_recommendations` (recommender_user_id FK, entity_id FK UNIQUE per recommender, recommendation_text column-named `text`, status CHECK, entity_id_status composite index — ships disabled-by-flag for V1.5 pilot). Extends `entities` via `batch_alter_table` with 7 nullable-or-defaulted columns: `heat_exposure` VARCHAR(20) CHECK in (`indoor`/`shaded`/`outdoor`/`water_adjacent`); `crowd_notes` JSON; `is_mobile_service` BOOL NOT NULL default `sa.false()`; `boat_access` JSON; `seasonal_hours` JSON (coexists with Phase 1A `seasonal_hours` extension table via brief §9 intentional-column path; ORM relationship renamed `seasonal_hours → seasonal_hour_rows`); `district_id` VARCHAR(36) FK `districts.id` ON DELETE SET NULL, indexed; `featured` BOOL NOT NULL default `sa.false()`, indexed (non-partial). Extends `users` with `preferred_mode` VARCHAR(16) NOT NULL default `'default'` CHECK in (`default`/`boat`). ORM in `app/db/models.py`: 5 new model classes appended + Entity extended with 7 Mapped properties + District relationship + `seasonal_hour_rows` rename + User extended with `preferred_mode`. 17 net-new tests in `tests/test_phase3_schema_additions.py` covering migration upgrade-downgrade-upgrade cycle on fresh SQLite (DATABASE_URL monkeypatch), table shapes, 5 CHECK constraint violation rejections, UNIQUE duplicate rejections, FK CASCADE on user delete via raw DELETE + PRAGMA foreign_keys=ON, default values, ORM relationships, index presence via inspect. Pytest **1664 collected → 1681 collected** (+17; 1680 passed + 1 skipped + 30 subtests). Alembic head `f9e8d7c6b5a4 → d0e1f2a3b4c5`. Ruff clean. **Five pragmatic deviations Cursor flagged transparently + accepted:** (a) seasonal_hours JSON column + extension table coexist (brief §4.1 explicit-HALT bypassed; Cursor applied the brief §9 "intentional column" deviation path with `seasonal_hour_rows` relationship rename; operator confirmed during commit since §4.1 listed the column as additive which IS operator intent); (b) `users.preferred_mode` direct NOT NULL with server_default (brief §4.7 allowed either; 3.2 backfill no-op); (c) `PeerRecommendation.recommendation_text` Python attribute maps to DB column `text` (minor); (d) `alerts_dispatched.alert_type` left denormalized per brief; (e) `entities.featured` non-partial index on both dialects (Postgres-only partial index skipped per brief §9). Cursor §11 mentioned an `alembic/env.py` change but the §4 file list only showed `app/db/models.py` modified — `git status` Windows-side confirmed env.py was NOT modified; §11 was descriptive of existing state (env.py already uses `get_database_url()` at runtime in 3 sites since some prior session). **No production runtime impact** until next deploy; new columns and tables start empty, ORM dormant until Phase 3.2 wires writers + Phase 5/6/8 wire readers. Cursor session, single dispatch.
  - **`38abbcb`** — chore: patch Phase 2B.1 + 2B.3 dispatch prompt TBD placeholders to session-19 ship SHAs. Session-19 SHA-patched these two prompts in-place at paste-time per the SHA-patch-and-inline-present rhythm (lesson 6 in session-19 handoff), but the patched versions never got committed cleanly during session-19 close-out. They sat uncommitted in the working tree across all of session-20. Cursor §0 noted them as pre-existing outputs/ edits. Landed now as a small chore lane to preserve the historical state — the patched prompts show what was actually pasted to Cursor in session-19 for the 2B.1 + 2B.3 Lane 2B ships. Diffs surgical: `<TBD-FILL-AFTER-2A.3-LANDS>` → `5fea2ce`, `<TBD-FILL-AFTER-2B.2-LANDS>` → `d631c77`, `<TBD-FTS-migration-rev>` → `c8d9e0f1a2b3`, `<TBD-pytest-count>` → `1607`. No semantic content change beyond placeholder fills. Same shape as `c9ab794` which patched the Phase 3.1 dispatch prompt.
  - **Lessons absorbed (session-20):** (1) **Gotcha #15 (bash mount index.lock) discipline held throughout** — zero bash `git` operations against the working tree across the entire session; HEAD verification via Read on `.git/refs/heads/main`, recent commits via STATE.md cross-reference, alembic head via Glob, file-presence via Glob/Grep. (2) **Gotcha #16 landed durably** — embedded `"..."` inside `-m '...'` bodies on PowerShell get re-tokenized as quote boundaries; session-20 wrote its own gotcha #16 commit using gotcha #16's own medicine (`-m` body with em-dashes and plain text, no embedded double-quote pairs) and it parsed clean. Future sessions inherit the rule. (3) **Operator reality-check feedback loop matters** — Casey pushed back on the 10-district paragraph plan with grounded local knowledge (Havasu too small, McCulloch is the strip, English Village is the only real district); reality check captured durably in brief + district draft so Phase 3.2 dispatch authoring re-engages the question rather than executing the original plan. Strategic-direction updates from operator local knowledge always beat polished-from-research drafts; bake "ask the operator what's actually true about their geography/users" into early-phase brief authoring. (4) **Cursor §11 prose may be descriptive not change-revealing** — Cursor's §13 §11 mentioned env.py modification but §4 file list didn't include env.py; `git status` Windows-side was the truth (env.py NOT modified; §11 described existing state). Rule from session-20 forward: always cross-check Cursor's claimed file list against actual `git status` Windows-side before staging; never trust §11 prose alone to determine commit scope. (5) **Dispatch prompt SHA-patch pattern continues durable** — session-20's chore commit at `38abbcb` mirrored the `c9ab794` pattern, preserving session-19's in-place SHA fills as durable historical state. Future agents: when a dispatch prompt gets SHA-patched in-place pre-paste, land it as a chore commit at session close-out — don't let patched prompts sit in working tree across session boundaries. (6) **Primary-side parallel-work cadence during Cursor in-flight time scales** — session-20 walked 5 Bucket C operator decisions + surfaced the Phase 3.2 district UX reality check + authored gotcha #16 docs + queued the session-19-dispatch-prompt chore all during Cursor 3.1's ~hour of in-flight time. The operator-decision walkthrough was efficient via structured AskUserQuestion calls with recommendations attached.

- **Session-19 (2026-05-12) — Phase 2 of the master build plan SHIPPED to COMPLETION on origin.** Three substantive commits + two docs commits + one queued chore commit (Phase 3 dispatch artifacts pre-positioned). Phase 2B.3 shipped first at `8338505` (public `GET /api/search` + home + provider profile search bar + Lane 2B consumer wiring) followed by docs at `c464007`. R2 operator prereq locked mid-session. Phase 2B.1 shipped second at `1c57c73` (photos schema + R2 client + Pillow pipeline + 4 upload/manage/delete routes + stuck-upload sweep + three-tier hero/gallery extension + **Lane 2B close-out + Phase 2 SHIPPED header**); docs commit follows. Total session-19 pytest delta: 1607 → 1663 (+56 = +9 from 2B.3 + +47 from 2B.1). Final alembic head: `c8d9e0f1a2b3 → f9e8d7c6b5a4` (2B.1 photos migration). Session boot at HEAD `aed79ac` (session-18 close-out). Three-track session: (a) dispatched Phase 2B.3 (search bar UI + public `/api/search` + Lane 2B consumer wiring) to a single fresh Cursor chat using the pre-positioned + SHA-patched prompt at `outputs/cursor_dispatch_prompt_phase_2b_3.md` (5fea2ce + d631c77 + 1607 + c8d9e0f1a2b3 placeholders patched in-place before paste); (b) walked operator through Cloudflare R2 setup per `outputs/operator_prereqs_phase_2.md` §2 Path A in parallel (~30-45 min); (c) dispatched Phase 2B.1 to a second fresh Cursor chat once R2 was locked + 2B.3's prompt was already mid-flight (parallel-dispatch posture per Rule 3 file-disjoint — 2B.1 on `app/photos/*` + Photo model + new migration + `derive_hero_photo` extension; 2B.3 on `app/search/routes.py` + templates + `search.js`; only shared file `app/main.py` which both lanes anchor `include_router` to in the same router-include block). The R2 walkthrough produced: Cloudflare account + R2 activation + bucket `havasuchat-photos-prod` (Western North America `WNAM` auto-region) + Account API Token (Object Read & Write, bucket-scoped, no TTL) + Public Development URL enabled (`pub-<hash>.r2.dev`) + 5 R2_* env vars dropped into Railway. Path B custom domain `cdn.askhava.com` deferred to launch-polish per design memo §8. 2B.1 dispatch prompt patched to known session-19 ship SHAs (5fea2ce + d631c77 + 1607 baseline + c8d9e0f1a2b3 alembic head) in same session, ready to paste into a fresh Cursor chat any time. Boot-time gotcha #15 discipline observed cleanly: zero bash `git` operations against the working tree across the entire session; HEAD verified via Read on `.git/refs/heads/main`, recent commits verified via STATE.md Recent commits block read + cross-referenced against the `aed79ac` boot-prompt expectation, alembic head verified via Glob on `alembic/versions/`. Sandbox pytest collect cannot import project deps (FastAPI/SQLAlchemy/Pillow/boto3/etc.) so test-count confirmation deferred to Windows-side; static grep on the six session-18-shipped test files + the four `viewer_is_owner` cases in `tests/test_provider_profile_page.py` cross-checked the 1607 baseline at static-function-count level. Highlights:
  - **`8338505`** — Phase 2B.3 public `GET /api/search` + home + provider profile search bar + Lane 2B consumer wiring (7 files, +769/-1). Cursor session, single dispatch. New `app/search/routes.py` (~320 lines): no-auth `GET /api/search` (q required → 400 if missing; category/district/entity_type filters; limit default 20 max 50; cursor pagination via opaque base64 `{"o":<offset>}`); response shape `{results: [...], next_cursor: str | None}` per row `{entity_id, entity_type, slug, name, description, district, hero_url, profile_url}`. Postgres dispatch via 2B.2's `search_fts.build_tsquery_string(filters)` + `search_fts.entities_search_vector_match(tsq)`; SQLite test path via in-route `_sqlite_entity_text_and` mirroring `sqlite_fallback` shape (Entity.name + description ILIKE AND `_category_needle_set` synonym needles); ranking via 2B.2's `build_rank_score_expr_for_filters` on Postgres, `_verification_bonus_sql` + featured-case `cast(case((feat.is_(True), 25.0), else_=0.0), Float)` on SQLite. Additive OR-branch `_provider_synonym_exists_predicate` for commercial recall: EXISTS on Provider aliased + `_category_needle_set(q_raw)` ILIKE needles on `Provider.category` + normalized `Provider.google_primary_category` (replace underscores with spaces) so `q=barbershop` reaches `google_primary_category=barber_shop` rows without bypassing FTS for primary name/description match. Search bar in `app/templates/home.html` (line 31) as `<section class="directory-search-wrap">` placed BEFORE existing hero chat composer (line 60) — existing chrome untouched; compact `directory-search--compact` variant in `app/templates/provider_profile.html` topbar (line 128). New `app/static/js/search.js` (`/static/js/search.js` path matches existing `js/chat-new.js` convention, not `app/static/search.js` at repo root): vanilla JS bound via `[data-directory-search]` selector, fetches `/api/search?q=…`, renders top 8 results in `[data-directory-search-results]` dropdown, click-through navigates to `profile_url` (commercial `/provider/<slug>`, events `/events/{id}`, programs `/programs/{id}`, places `/home` placeholder). `app/main.py` +1 import (line 43) + 1 `include_router(search_router)` (line 286). `app/static/styles/home.css` extended with `.directory-search*` styles. 9 net-new tests in `tests/test_search_route.py` covering brief §7.3's 8 acceptance scenarios (q=plumber JSON ranked, anonymous 200, missing-q 400, filter combinations, cursor round-trip, hero_url via derive_hero_photo, q=barbershop synonym, home + provider profile HTML smoke) + cursor encode/decode unit. Pytest **1607 → 1616** (+9 net-new). Alembic head unchanged at `c8d9e0f1a2b3` (no migration in 2B.3). Ruff clean. **Seven pragmatic deviations Cursor flagged transparently + accepted:** (a) **brief symbol-name adaptation** — brief assumed `build_fts_query` symbol on `app/search/fts.py` but 2B.2 actually shipped `build_tsquery_string` / `entities_search_vector_match` / `build_entity_select_for_filters`; Cursor's §0 re-grep caught this and adapted (exactly the brief-staleness recognition the §0 read protocol exists for); (b) UI placement between topbar and hero on home.html (above the chat-style composer in the hero — brief §10 menu option); (c) JS path `/static/js/search.js` followed existing convention not the brief's `app/static/search.js`; (d) pagination cursor shape = opaque base64 JSON `{"o":<offset>}` offset (brief §10 explicitly allows); (e) **additive `profile_url` row field** beyond brief §7.1's row spec so events route to `/events/{id}` and programs to `/programs/{id}` (events/programs not slug-routable, places fall back to `/home`); (f) **scope expansion** — additive OR `_provider_synonym_exists_predicate` reuses 2B.2's `_category_needle_set` to add Provider-table EXISTS+ILIKE for commercial recall (FTS only indexes Entity text, so `q=barbershop` wouldn't hit `barber_shop`-tagged providers without this; **parallel taxonomy matching in additive OR, NOT a raw-LIKE FTS bypass** — reuses 2B.2 helpers, doesn't reinvent); (g) template smoke = string containment on rendered HTML, brief §10 explicitly accepts. **Operator notes:** manual smoke not executed in Cursor session (no browser; operator runs `/home` → type "plumber" → click result → land on `/provider/<slug>`; repeat from profile page). `alembic upgrade head` on Cursor's ad-hoc DATABASE_URL hit the benign pre-Phase-1A SQLite drift (`duplicate column name: slot` on `sponsors` at `1a2b3c4d5e6f → 2a3b4c5d6e7f` — same mid-chain drift surfaced since session-15; not a 2B.3 issue; cure is drop + recreate local dev DB Windows-side). Cursor noted `derive_hero_photo` in this 2B.3 ship still uses the two-tier `attributes.hero_pin_photo_url → google_photo_refs[0]` chain (no Photo tier yet) since 2B.1 hadn't shipped at the time of the §13 report — when 2B.1 ships, the three-tier extension will automatically flow through `/api/search` via the existing derive_hero_photo consumer chain (no 2B.3 follow-up needed). **No production runtime impact** until next deploy; once deployed, anonymous viewers see a directory search bar on home + provider profile pages and can search the catalog via FTS-backed `/api/search` on Postgres (SQLite ILIKE-fallback in tests).
  - **R2 operator prereq locked (this session, walkthrough output):** Cloudflare account activated + R2 enabled + bucket `havasuchat-photos-prod` created in WNAM auto-region with Standard storage class and Public Development URL enabled (Path A — default `pub-<hash>.r2.dev` URL; Path B custom domain `cdn.askhava.com` deferred per design memo §8). Account API Token created with `Object Read & Write` permission scoped to `havasuchat-photos-prod` (no TTL); three one-time values copied immediately (Access Key ID + Secret Access Key + Endpoint URL `https://896c71271092f0d9a77f4560a038f3e4.r2.cloudflarestorage.com`). Five R2_* env vars dropped into Railway: `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_ENDPOINT_URL` (account-level, not bucket-scoped — bucket name supplied separately), `R2_BUCKET_NAME=havasuchat-photos-prod`, `R2_PUBLIC_URL_BASE=https://pub-<hash>.r2.dev`. Railway auto-redeploys on env-var change but the redeploy is a no-op until Phase 2B.1 ships and code starts reading the vars. **Phase 2B.1 is now fully unblocked** — dispatch prompt at `outputs/cursor_dispatch_prompt_phase_2b_1.md` has been patched with known session-19 SHAs (5fea2ce + d631c77 + 1607 + c8d9e0f1a2b3) in same session and is ready to paste into a fresh Cursor chat any time.
  - **`1c57c73`** — Phase 2B.1 photos schema + R2 client + Pillow pipeline + 4 upload/manage/delete routes + stuck-upload sweep + three-tier hero/gallery extension + **Lane 2B close-out + Phase 2 SHIPPED** (19 files, +2296/-16). Cursor session, single dispatch; ran in parallel with the 2B.3 lane. New alembic migration `f9e8d7c6b5a4_photos_table` chains off `c8d9e0f1a2b3` (Phase 2B.2 FTS); single `op.create_table("photos")` with FKs to `entities.id` + `users.id` (both ON DELETE CASCADE), CHECK constraints on `status` ('uploading', 'processing', 'live', 'flagged', 'deleted') + `mime_type` ('image/jpeg', 'image/png', 'image/webp'), 5 indexes (entity_id, uploaded_by_user_id, status, image_hash, entity_hash_status composite). Defaults via `sa.true()` / `sa.false()` / `sa.func.now()` per Postgres portability rule. New `app/photos/` package: `r2_client.py` (lazy-singleton `get_r2_client`, region_name='auto', `upload_bytes` with `Cache-Control: public, max-age=31536000, immutable`, `delete_object` best-effort, `build_public_url`); `processor.py` (6-stage pure-function pipeline: decode_and_validate → strip_exif MANDATORY → compute_hash SHA-256 dedup on post-strip pixel bytes → generate_variants 256×256/512×512/1280×720 × WebP-q82+JPEG-q85 via ImageOps.fit center-crop → upload_all_variants → finalize_photo_row; plus `process_uploaded_photo` BackgroundTask orchestrator with broad try/except → flagged/decode_failed defensive shape, idempotent on re-run); `routes.py` (4 endpoints: `POST /api/entities/{entity_id}/photos` multipart upload with `auth=require_user` + `authz=admin OR verified Claim via find_existing_claim` + `entity_type IN ('commercial', 'place')` + MIME whitelist + 10 MB cap + rate-limit checks; `DELETE /api/photos/{id}` soft-delete; `POST /api/photos/{id}/set-hero` atomic clear-siblings + flip; `POST /api/photos/{id}/reorder`); `schemas.py` (PhotoUploadResponse + PhotoListItem Pydantic); `limits.py` (check_uploader_daily_cap 20/day + check_entity_photo_cap 50 place / 100 commercial, entity_cap BEFORE daily_cap for clearer UX); `sweep.py` (run_stuck_photo_sweep flips status=uploading older than 24h to status=flagged with processing_error=decode_failed). Photo model tail-appended to `app/db/models.py`. Entity.photos viewonly relationship uses string primaryjoin `and_(Photo.entity_id == Entity.id, Photo.status == "live")` without `foreign()` to dodge forward-ref/circular-import at class body time (same shape as Phase 2B.2 `sqlite_fallback.py` lazy-import discipline). `app/main.py`: photos_router include after search_router; run_stuck_photo_sweep folded into `_hourly_cleanup_loop` alongside run_expired_review_cleanup. `app/providers/queries.py`: `derive_hero_photo` + `derive_gallery` extended to three-tier (Tier 1 owner Photo is_hero+status=live → hero_url; Tier 2 EXISTING attributes.hero_pin_photo_url; Tier 3 EXISTING google_photo_refs[0]); `selectinload(Entity.photos)` on profile load; `/api/search`'s `hero_url` consumer flows through this chain automatically (no 2B.3 follow-up needed). `requirements.txt`: `Pillow==12.0.0` + `boto3==1.42.17`. `tests/conftest.py`: 5 R2_* env-var setdefault stubs (cite AUTH_DEV_MODE pattern; production R2 vars are LIVE in Railway since session-19 R2 lockdown; conftest stubs prevent tests from hitting real R2). 47 net-new tests across 6 files. Pytest **1616 → 1663** (+47). Alembic head `c8d9e0f1a2b3 → f9e8d7c6b5a4`. Ruff clean. Migration upgrade + downgrade-1 + upgrade cycle green on fresh SQLite. **Five pragmatic deviations Cursor flagged + accepted:** (a) Entity.photos primaryjoin string + no foreign() (circular-import dodge); (b) upload rate-limit order: entity_cap BEFORE daily_cap so full-entity returns clearer error than masked-by-daily-cap; (c) set_hero response typing `dict[str, Any]` for FastAPI flexibility; (d) orchestrator broad try/except → flagged/decode_failed (brief §10 explicitly invited); (e) R2 failure path: row stays uploading on exception, sweep handles long stalls (risk #7 mitigation per brief). **Operator notes:** Postgres upgrade+downgrade cycle NOT exercised by Cursor — SQLite cycle verified; operator should smoke on Railway/staging Postgres before deploy. Manual smoke not run (no live R2 from Cursor); operator runs curl POST a real JPEG with verified-claimant cookie + verify R2 bucket has 6 variant objects (thumbnail/medium/hero × WebP/JPEG) + Photo row transitions to status=live + cdn_url/hero_url/thumbnail_url/medium_url populated. **No production runtime impact** until next deploy; once deployed, verified-claimant merchants can upload owner photos to provider profiles, three-tier hero/gallery surfaces them on provider profile + /api/search results. **Phase 2 closes out with this commit:** Lane 2A chain `6000138 → 714ca52 → 5fea2ce` + Lane 2B chain `d631c77 → 8338505 → 1c57c73`. Total Phase 2 pytest delta: 1518 → 1663 (+145 net-new). Final Phase 2 alembic head: `f9e8d7c6b5a4`.
  - **Phase 3 brief + 3.1 dispatch prompt pre-positioned in-flight:** `outputs/cursor_brief_phase_3_v11_schema_pass.md` (~580 lines, two-sub-phase decomposition: 3.1 schema additions only — 7 new entity columns + 5 new tables + users.preferred_mode + ORM models + ~15-20 new tests; 3.2 category taxonomy rewrite + audited backfill per `category_backfill_mapping_audit_2026-05-14.md` + district seed from operator-polished paragraphs + Phase 3 close-out) and `outputs/cursor_dispatch_prompt_phase_3_1.md` (~230 lines, mirrors 2B.3 dispatch prompt shape with `<TBD-FILL-AFTER-2B.1-LANDS>` + `<TBD-PHOTOS-MIGRATION-REV>` + `<TBD-pytest-count>` placeholders patched at session close to `1c57c73` + `f9e8d7c6b5a4` + `1663`). Phase 3.2 dispatch prompt deferred to 3.1 close-out (chains off whatever 3.1's alembic revision is); 3.2 also gated on operator locking 5 Bucket C decisions per brief §7 (`beauty_personal_care`, `tourism`, `barbershop` fixture, K-12/charter schools, recreational entertainment) + polishing the district paragraphs draft (5 `[CASEY: ...]` placeholders + 5 verify items in `outputs/chatgpt_response_district_paragraphs_v1.md`). Authored primary-side during Phase 2B.1 in-flight time (session-18 pre-author-while-Cursor-works pattern; this round was primary-side not sub-agent because the brief is strategic + the primary had fresh context). Worth absorbing as new lesson #5 (below).
  - **Lessons absorbed (session-19):** (1) **Gotcha #15 (bash mount index.lock) discipline held throughout** — zero bash git operations attempted against the working tree across the entire session; HEAD verification via Read on `.git/refs/heads/main`, recent commits via STATE.md cross-reference, alembic head via Glob on `alembic/versions/`, file-presence via Glob/Grep. Sandbox pytest can't collect (project deps not installed in the Linux sandbox) — Casey runs pytest Windows-side as the authoritative source of truth. (2) **Per-step operator walkthrough scales well for one-shot consequences.** Bulk-mode dump of all R2 steps was acceptable as a first pass but Casey opted into per-step mode for the actual execution — token-creation in particular benefits from step-by-step ("the secret is shown ONCE; have a temp note ready") since irrecoverable. (3) **Cloudflare onboarding-funnel UX detour matters** — first attempt at the "Create API Token" path landed on the account-level API token UI with 200+ permissions instead of the streamlined R2-specific token UI; back-out + direct URL `<account-id>/r2/api-tokens` reached the right screen. Worth a one-liner in `outputs/operator_prereqs_phase_2.md` §2 step 4 if the doc ever gets a refresh. (4) **Dispatch prompt SHA-patch pattern continues to work** — patched 2B.3 prompt in-place via Edit before paste (file's durable, future reads see real SHAs not `<TBD-...>`); same for 2B.1 prompt at R2-lock time. Patched copy presented inline in chat for paste so Casey doesn't have to navigate to the file in his editor. (5) **Primary-side brief authoring during Cursor in-flight time scales for strategic docs.** Session-18 sub-agented dispatch-prompt authoring (docs-only, structured), but the Phase 3 brief is heavy strategic work (~580 lines + locked decisions + risk register + scope discipline) — primary-side authoring is more efficient when the primary already has fresh context from reading audit memo + master plan + district draft moments earlier. Sub-agent pattern still right for narrow tightly-scoped prompts; primary-side right for synthesis-heavy work. Bake into rhythm: primary-author when you have context cached + parallel Cursor lane is active; sub-agent when scope is small + structured. (6) **Parallel-dispatch validation continues at production scale.** Session-18 first validated Rule 3 file-disjoint parallel dispatch at production scale (2A.3 + 2B.2). Session-19 confirmed the pattern works for THREE-lane scenarios: 2B.3 lane in Cursor + R2 walkthrough operator-side + 2B.1 lane in Cursor (started while 2B.3 was still running but after R2 lockdown produced the env-var setdefault values). Both Cursor lanes saw each other's commits land on disk during their work (2B.3 committed mid-2B.1; 2B.1 saw the search_router include + `/api/search` route during its `app/main.py` anchored Edit + correctly slotted `photos_router` next to it). Working tree converges; commits stay file-disjoint per Rule 8 via per-file `git add` staging.

- **Session-18 (2026-05-12) — Phase 2A.3 SHIPPED + Phase 2B.2 SHIPPED on origin (Lane 2A close-out + first Lane 2B sub-phase); 4-5 commits this session via PARALLEL dispatch of two Cursor lanes per dispatch_protocol Rule 3.** First time the parallel-dispatch posture (set up across sessions 15 + 17) actually ran at production scale — two Cursor chats took separate paste-ready prompts (`outputs/cursor_dispatch_prompt_phase_2a_3.md` + `outputs/cursor_dispatch_prompt_phase_2b_2.md`) at session boot, worked independently on file-disjoint domains (Lane 2A: `app/auth/*` + `app/admin/router.py::_guard` + `app/providers/router.py::viewer_is_owner` + claim/favorites templates + `app/chat/tier2_catalog_render.py`; Lane 2B: `app/search/*` + `alembic/versions/c8d9e0f1a2b3_*.py` + `app/chat/tier2_db_query.py` + new `app/chat/tier2_synonyms.py`), and returned coordinated §13 reports — both reports showed final pytest 1607 because the working tree had both lanes' code present simultaneously when each Cursor ran its final pytest. Lane 2A is now feature-complete on origin (commit chain `6000138 → 714ca52 → 5fea2ce`; pytest delta across Lane 2A: 1518 → 1586 +68 net-new; final Lane 2A alembic head `92ce4899dc08` unchanged after 2A.1 since 2A.2 + 2A.3 ship no migrations). Lane 2B is 1 of 3 shipped (`d631c77` = 2B.2 FTS infra); 2B.1 photos + 2B.3 search bar UI remain, both with dispatch prompts pre-positioned in this same session by parallel sub-agents. Highlights:
  - **`5fea2ce`** — Phase 2A.3 claim flow + favorites + admin role parallel-path + viewer_is_owner + Lane 2A close-out (20 files, +1725/-17). Cursor session, single dispatch. Claim flow ships `GET/POST /claim/<slug>` (anonymous-redirect to `/login?next=/claim/<slug>`, Entity-slug lookup via Phase 1D's `derive_provider_slug` reservation, find-or-render-status logic) + admin queue (`GET /admin/claims`, `POST /admin/claims/<id>/verify` with claimant `User.role end_user → merchant` auto-promote + `verification_method` + `verified_by` + `verified_at`, `POST /admin/claims/<id>/reject` with `rejection_reason`). Favorites ship `POST /api/favorites/toggle` (entity_is_favoritable validation against `commercial / place / event`; returns `{action, favorite_count}`), `GET /api/favorites` (JSON list), `GET /account/favorites` (HTML), plus heart-icon JS on Provider profile + Tier 2 catalog render helper. Admin role parallel-path on `app/admin/router.py::_guard`: admin-cookie path stays primary, additive role check on `request.state.current_user.role == "admin"`, authenticated non-admin returns 403, anonymous still redirects to `/admin/login`. `viewer_is_owner` wired via new `_viewer_owns_provider(db, *, current_user, provider) -> bool` helper on `app/providers/router.py` (anonymous False, admin role short-circuits True, otherwise Claim row query with `current_user.id` + `provider.entity_id` + `status == "verified"`). New `app/auth/claims.py` (4 helpers: `entity_is_claimable`, `find_existing_claim`, `create_pending_claim`, `get_entity_by_slug`) + `app/auth/favorites.py` (4 helpers: `entity_is_favoritable`, `toggle_favorite`, `list_user_favorites`, `favorite_count_for_user`) + `app/static/favorites.js` (heart-icon click handler reading `data-current-user` / `data-favorite-entity-id`; POST + flip + toast) + 5 new templates (flat path). 3 new test files (`test_claims.py` 8 + `test_favorites.py` 8 + `test_admin_role_parallel.py` 3). 9 modified files. Pytest **1563 → 1586** (+23 net-new). Alembic head unchanged at `92ce4899dc08`. Ruff clean. **Six pragmatic deviations flagged + accepted:** (a) admin claim routes in `app/admin/router.py` vs `app/auth/routes.py` (reuses `_guard`, stays next to admin HTML); (b) `_guard` returns 403 for authenticated non-admin instead of password-login redirect loop (clearer; anonymous unchanged); (c) `render_tier2_provider_cards_html` helper not wired into chat formatter (preserves anonymous chat shape; ready for future catalog surfaces per brief §11 deferring merchant-edit-form UI); (d) claim notes UI-only not persisted (V1 simplicity); (e) flat `account_favorites.html` (matches existing template tree); (f) `test_auth_flow.py` import-order fix (`app.main` first to avoid models ↔ database listener circular-import edge during collection). Also: `create_pending_claim` uses `db.get(Entity, id)` + explicit `entity_is_claimable()` check instead of brief §7.1 recommended JOIN-at-insert shape (simpler, atomic enough). **No production runtime impact** until next deploy; once deployed, signed-in users can claim providers + favorite providers + see Saved listings at `/account/favorites`; admin claim queue at `/admin/claims`.
  - **`d631c77`** — Phase 2B.2 Postgres FTS + pg_trgm + tier 2 LIKE→FTS dispatch (10 files, +761/-99). Cursor session, single dispatch. New alembic migration `c8d9e0f1a2b3_entities_fts_pgtrgm` chains off `92ce4899dc08`; dialect-gated upgrade + downgrade with early `bind.dialect.name != "postgresql"` return so fresh-DB SQLite upgrade stays green. Postgres-only DDL: `CREATE EXTENSION IF NOT EXISTS pg_trgm`, `ALTER TABLE entities ADD COLUMN search_vector tsvector GENERATED ALWAYS AS (setweight A name || setweight B description) STORED`, `CREATE INDEX ix_entities_search_vector USING gin (search_vector)`, `CREATE INDEX ix_entities_name_trgm USING gin (name gin_trgm_ops)`, plus two partial JSON indexes `ix_providers_emergency_service` + `ix_providers_dog_friendly` on `providers.attributes ->> 'X' = 'true'` (scope expansion over brief §6.2's entities-only FTS scope — additive Postgres-only performance indexes for Tier 2 attribute filters, gated to Postgres via same dialect check so SQLite path unaffected). New `app/search/` package: `fts.py` 140 lines (FTS query builders, `to_tsquery` safety via `[a-z0-9]+` token allowlist + reserved-keyword rejection of `or` / `and` / `not` + `bindparams` to prevent injection + AND across dimensions / OR within synonym needles per brief §6.3); `sqlite_fallback.py` 52 lines (ILIKE chain fallback, lazy-imports `Entity` for circular-import dodge); `ranking.py` 95 lines (composite rank-float: featured +25, verification bands +30, recency +15, base FTS rank via `ts_rank_cd` on Postgres). New `app/chat/tier2_synonyms.py` 95 lines (synonym helpers extracted from `tier2_db_query.py` to break a `sqlite_fallback ↔ models` circular import; `tier2_db_query.py` re-exports `_category_needle_set` / `_category_synonyms` / `_singularize_category` so existing `tests/test_tier2_db_query.py` callers stay unchanged). Modified `app/chat/tier2_db_query.py`: `_is_postgres(session)` runtime check; Tier 2 `OR + entities.search_vector @@ to_tsquery` branch added on Postgres for `_query_providers_orm` / `_query_events` / `_query_programs` alongside legacy ILIKE predicates (recall superset on Postgres; legacy retained for SQLite test parity); composite ORDER BY on Postgres via `search_ranking.build_rank_score_expr_for_filters`. 3 new test files (`test_search_fts.py` 92 + `test_search_ranking.py` 72 + `test_search_parity.py` 62). Pytest **1586 → 1607** (+21 net-new; 1 skipped is `test_search_fts.py`'s Postgres-only execution path under `skip-unless-postgres` since dev environment has no Postgres bind). Alembic head `92ce4899dc08 → c8d9e0f1a2b3`. Ruff clean. **Operator note:** Postgres FTS path NOT smoke-tested locally; operator should verify on Railway/staging with a Tier 2 query, confirm `EXPLAIN` uses GIN, run `downgrade -1` / `upgrade head` there. Flaky early background pytest run showed two `F` before a fresh full run completed all green; treated as transient unless reproduced. **No production runtime impact** until next deploy; once deployed, Tier 2 queries use FTS + composite ranking on Postgres; SQLite test path keeps ILIKE alive permanently for parity (do NOT drop `sqlite_fallback.py` once FTS lands in prod — SQLite test fixtures depend on it).
  - **Parallel work landed during the same session (forward-looking artifacts + lessons):** Two general-purpose sub-agents dispatched in parallel (file-disjoint output paths) authored pre-positioned dispatch prompts for the remaining Lane 2B sub-phases: `outputs/cursor_dispatch_prompt_phase_2b_1.md` (302 lines, photos + R2 + Pillow + upload route, gated on Cloudflare R2 operator prereq) + `outputs/cursor_dispatch_prompt_phase_2b_3.md` (281 lines, search bar UI + `GET /api/search` + Lane 2B close-out, gated on 2B.2 ship which is now done). Both pre-positioned per session-17's "pre-author-while-Cursor-works" pattern (reduces next-dispatch latency from ~30 min brief-authoring to ~0 min paste). Sub-agents reported clean: 2B.1 prompt covers 13 ORDER MATTERS steps mapped to brief §5.1-§5.10 with `<TBD-FILL-AFTER-2A.3-LANDS>` placeholders; 2B.3 prompt covers 8 ORDER MATTERS steps mapped to brief §7.1-§7.4 with placeholders for 2A.3 ship SHA + 2B.2 ship SHA + FTS migration rev + pytest count. **New lesson absorbed (gotcha #15, queued for `dispatch_channels.md` in the session-18 close-out commit):** bash mount git is NOT safe for working-tree-reading operations in mixed-OS sessions (Windows + Linux bash sandbox) — running `git status -s` / `git diff --stat HEAD` from bash creates a `.git/index.lock` that Linux can't unlink (`Operation not permitted`), and Windows git then refuses to commit until the lock is manually removed. From here forward in mixed-OS sessions: use Read + Grep tools (Windows-authoritative) for working-tree inspection, never bash git. The fix: `Remove-Item .git\index.lock` from PowerShell. Resend operator prereq from session-17 holds (Lane 2A magic-link auth wired but unused until deploy + signed-in user clicks); R2 operator prereq remains pending and only gates Lane 2B.1.

- **Session-17 (2026-05-11) — Phase 2A.1 + 2A.2 SHIPPED on origin (first two sub-phases of Phase 2 Lane 2A account-lite v0.1); ten+ commits across the session, all pushed.** Phase 2A.2 brought magic-link auth fully live for signed-in users; merchant-facing affordances (claim flow + viewer_is_owner wiring) ship in 2A.3 (dispatch prompt pre-positioned). Phase 2A.2 highlights:
  - **`714ca52`** — Phase 2A.2 magic-link auth flow + session middleware + login UI (12 files, +1000/-4). Cursor session, single dispatch. Four routes: `GET /login` (form), `POST /api/auth/request-link` (slowapi 10/hr IP rate-limit + DB `count(*) WHERE email = ? AND created_at > now-1h ≥ 5` silent path → same check-email template; no email-existence probing), `GET /auth/callback` (consumes token, find-or-creates User, creates AuthSession, signs cookie, redirects to safe-next or `/account`), `POST /logout` (deletes session row + clears cookie). Plus `GET /account` landing (pragmatic deviation — required for callback default-redirect target). New `app/auth/session.py` (136 lines) with `SessionMiddleware` (Starlette `BaseHTTPMiddleware`) reading `hava_session` cookie (itsdangerous-signed with salt `havasu-session`, secret `HAVA_SESSION_SECRET → ADMIN_PASSWORD` fallback, 30-day max age, `RAILWAY_ENVIRONMENT`-keyed Secure flag), looking up `AuthSession` + checking `expires_at` + `user.is_active`, attaching `request.state.current_user` + `current_session`, debounced `last_seen_at` update via module-level `_LAST_SEEN_MONO: dict[str, float]` keyed by session id + `time.monotonic()` 60s window (tests pin behavior with `monotonic` patched to `lambda: 1.0`), `db.refresh` + `db.expunge` after commit so Jinja can read `current_user.email` post-session-close, cookie-clear-on-response when session invalid/expired. New `app/auth/dependencies.py` (`get_current_user`, `require_user`, `require_admin`). New `app/auth/email_helpers.py` (`normalize_email`, `is_valid_email`, `generate_magic_link_token`, `hash_token`, `hash_request_ip`, `hash_optional_fingerprint`). New `app/auth/routes.py` (229 lines) with `_safe_next` open-redirect guard (leading `/`, rejects `//`, `://`, `..` after `unquote`). 4 new login templates (`login.html`, `login_check_email.html`, `login_expired.html`, `account.html`). `app/auth/email_sender.py` extended with optional `next_path` param via `urllib.parse.urlencode` so emailed links carry safe `next` query through verify. `tests/conftest.py` adds `AUTH_DEV_MODE=1` + `AUTH_MAGIC_LINK_BASE_URL=http://testserver` via `setdefault`. `app/main.py` extended per brief §6.6 (only middleware + router include — strict anchored Edit). Pytest **1543 → 1563** (+20 net-new in `tests/test_auth_flow.py`; plus bonus 21st test pinning send_magic_link exception → check-email HTML). Alembic head unchanged at `92ce4899dc08` (no migration in 2A.2). Ruff clean. **Six pragmatic deviations flagged by Cursor + accepted:** (a) `GET /account` route + `account.html` template — brief implied via the §6.4 callback redirect target but didn't list as explicit deliverable; (b) `send_magic_link(..., next_path=...)` optional param via `urlencode`; (c) detached User on `request.state` via `db.refresh + db.expunge` after last_seen commit, logout uses `db.get(AuthSession, sess.id)` for detached-row delete (with the operator note that future code must not lazy-load unloaded relations on `current_user`); (d) `last_seen_at` debounce = module-level dict + monotonic (per brief §10 menu of options); (e) `_hourly_cleanup_loop` fold for expired-token/session GC NOT added — would exceed anchored "only middleware + router" main.py edit scope; recommended as 2A.3 or follow-up; (f) tests import `app.main` before other `app.auth` imports to avoid models ↔ database listener circular-import edge during collection. **No production runtime impact** until next deploy; once deployed, signed-in users can log in via magic link but no merchant-facing affordances render yet (claim flow + `viewer_is_owner` plumbing is 2A.3 scope).
  - **Parallel work landed during the same session (forward-looking artifacts + lessons):** `0e8e9e3` shipped the Lane 2B brief (`outputs/cursor_brief_phase_2b_image_storage_search.md`, 928 lines, sub-agent authored) + 2B.2 dispatch prompt (`outputs/cursor_dispatch_prompt_phase_2b_2.md`, ~175 lines) covering image storage + Postgres FTS + pg_trgm + chat tier 2 LIKE→FTS swap. `37e7770` added `dispatch_channels.md` gotcha #14 (reflog vs ancestry forensics — when bash mount git is broken per Rule 7, walk parent links from HEAD via `python3 + zlib.decompress` on `.git/objects`; don't grep `.git/logs/HEAD` which records HEAD movements through abandoned branches + dead experiments not the current ancestry). Phase 2A.3 dispatch prompt pre-positioned at `outputs/cursor_dispatch_prompt_phase_2a_3.md`. ChatGPT district paragraphs draft (Phase 3 V1 deliverable) authored + saved to `outputs/chatgpt_response_district_paragraphs_v1.md` — 10 paragraphs, 5 `[CASEY: ...]` placeholders + 5 "Casey to verify" items pending operator polish; no rush, Phase 3 isn't dispatching anytime soon. **Resend operator prereq locked** mid-session: `askhava.com` sender domain verified at GoDaddy DNS (SPF + DKIM + MX records, verified within ~5 min of submission); API key generated with sending-only scope; 4 env vars in Railway production (`RESEND_API_KEY`, `RESEND_FROM_ADDRESS=Hava <noreply@askhava.com>`, `AUTH_MAGIC_LINK_BASE_URL`, `AUTH_DEV_MODE=false`). R2 operator prereq remains pending; only gates Lane 2B.1 (photos).

- **Session-17 (2026-05-11) — Phase 2A.1 SHIPPED on origin (first sub-phase of Phase 2 Lane 2A account-lite v0.1); two commits, both pushed.** Booted into Phase 2 dispatch posture per `outputs/session_17_boot_prompt.md`. Boot-time forensic note: `.git/logs/HEAD` (the reflog) contains old abandoned-branch entries (tier2 / cache v2 / editorial home refactor / aquatic event pruner / redesign branch merge) that DON'T appear in the current `main` ancestry — walking commit objects via parent links (`python3 -c "..." + zlib.decompress` on `.git/objects/...`) is the decisive verification when bash mount `git log` is broken per Rule 7. Cowork primary initially false-alarmed on a state divergence reading the reflog; corrected immediately on the parent-walk. **Lesson absorbed:** when verifying repo state from the sandbox, walk parent links from HEAD, don't grep the reflog. Highlights:
  - **`6000138`** — Phase 2A.1 account-lite v0.1 schema + ORM + Resend scaffold (7 files, +1114/-2). Cursor session, single dispatch. Five additive tables (`users`, `magic_link_tokens`, `sessions`, `user_favorites`, `claims`) per brief §4. New `app/auth/__init__.py` + `app/auth/email_sender.py` (`send_magic_link` with `AUTH_DEV_MODE` truthy-set `{1,true,yes,on}` mirroring `RATE_LIMIT_DISABLED` convention). Migration `92ce4899dc08_account_lite_v01` chains off `f8e9d0c1b2a3`; Postgres-portable (`sa.true()` / `sa.func.now()` defaults; no raw SQL); FK `ondelete` `CASCADE` for parent-deletes + `SET NULL` for `claims.verified_by`; CHECK constraints on `users.role` / `claims.status` / `claims.verification_method`; UniqueConstraints + indexes per §4 spec. **`user_favorites` + `claims` FK to `entities.id`** (master plan amendment over the design memo's polymorphic shape — Phase 1 already unified everything via `entities.entity_type` discriminator). Pytest **1518 → 1543** (+25 net-new across `tests/test_account_lite_schema.py` 21 runs + `tests/test_email_sender.py` 4). Migration upgrade+downgrade+upgrade cycle passes on fresh SQLite. Ruff clean. **Three pragmatic deviations Cursor flagged + accepted:** (a) `Session` ORM class renamed to `AuthSession` (table still `sessions`) to avoid `sqlalchemy.orm.Session` namespace clash on the models.py module during import — explicit allowance in brief §4.3; (b) `alembic/env.py` import-order reorder (`from app.db.database import …` before `import app.db.models`, with `noqa: I001`) to prevent partial-import deadlock when Alembic loads `Base.metadata` cold — surfaced a real circular-import edge `database._register_orm_listeners` ↔ `models` ↔ `entity_dual_write`; app path already imports `database` first so no app-side change needed; (c) dev-mode test asserts `logger.info` mock instead of `caplog` capture — equivalent coverage, just a different test mechanism. **Operator prereq locked this session:** Resend account + sender domain `askhava.com` verified (DKIM `resend._domainkey` + SPF `send` TXT + MX `send` to `feedback-smtp.us-east-1.amazonses.com` — all three DNS records added at GoDaddy + verified by Resend within ~5 min of submission) + API key generated with sending-only scope + 4 env vars dropped into Railway (`RESEND_API_KEY`, `RESEND_FROM_ADDRESS=Hava <noreply@askhava.com>`, `AUTH_MAGIC_LINK_BASE_URL=https://havasu-chat-production.up.railway.app`, `AUTH_DEV_MODE=false`). Resend env vars unused until 2A.2 wires the `/api/auth/request-link` route. **No production runtime impact** until next deploy; schema is dormant on the next deploy as well (no app code reads the new tables until 2A.2 + 2A.3 ship).
  - **`5bf4c14`** — Phase 2A dispatch artifacts (2 files, +1227). Cowork primary authored `outputs/cursor_brief_phase_2a_account_lite.md` (~720 lines, mirrors Phase 1 brief §0/§3/§10/§11/§12/§13 shape; 3 sub-phases scoped; Postgres portability checklist carried forward from session-15 lesson; `before_flush` safety-net pattern invited per session-16 lesson; 12-row risk register) and `outputs/cursor_dispatch_prompt_phase_2a.md` (~80 lines, mirrors 1C/1D dispatch prompt shape). Per dispatch_channels gotcha #12 — durable workspace `outputs/` artifacts that future agents can fresh-clone reference. **Phase 2A.2 + 2A.3 are next:** 2A.2 wires the auth flow routes + session middleware + login UI per brief §6 (~3-4 days); 2A.3 ships claim flow + favorites + admin role parallel-path + close-out per brief §7 (~2-3 days). 2A.2 dispatch prompt to be authored after 2A.1 ship-line lands.

- **Session-16 (2026-05-14) — Phase 1 of the master build plan COMPLETE on origin; six commits (four substantive + two docs); all pushed to origin/main.** Booted into Phase 1C dispatch posture per `outputs/session_16_boot_prompt.md`; baseline confirmed (HEAD `5132162`, pytest 1503, alembic head `b2c3d4e5f6a7`); cleared the bash-mount git gotcha #7 by reading `.git/refs/heads/main` directly (Linux mount served broken `git status`/log views; Windows-side state was clean). Highlights:
  - **`2817951`** — STATE.md Production block refresh + session-16 entry capturing the `5132162` hotfix narrative (Postgres rejecting `sa.text("1")` / `sa.text("0")` as Boolean `server_default` values that SQLite happily accepted; swapped to `sa.true()` / `sa.false()`; deploy succeeded; production alembic head moved straight from `f1a2b3c4d5e6` through `a1b2c3d4e5f6` to `b2c3d4e5f6a7` in one Railway upgrade).
  - **`a7984c6`** — folded the Postgres boolean-default lesson into `outputs/cursor_brief_phase_1_entity_schema.md` §10 (What NOT to do) with three sub-rules: use `sa.true()` / `sa.false()` not `sa.text("1")` / `sa.text("0")` for Boolean server_defaults; use `sa.func.now()` not `sa.text("CURRENT_TIMESTAMP")` for timestamp defaults; verify any raw SQL in `op.execute()` works on Postgres (cross-referenced §12 risk #5).
  - **`e0417c8`** — Phase 1C application read pivot (8 files, +827/-38). Cursor session, single dispatch. Pattern A (ENTITY-aware joins on legacy drivers) for `app/chat/tier2_db_query.py` (+176 — heaviest single file); Pattern B (legacy + alias via `Provider.entity` relationship) for `app/providers/queries.py` (+135) + `app/providers/view_models.py` (+12); light touches on `scripts/places_load.py` (+20 — locations.google_place_id join) + `app/contrib/places_client.py` (+7 — docstring only); `app/contrib/enrichment.py` no-op verified (Contributions store Places results on Contribution not Provider.entity). Pytest **1503 → 1512** (+9 net-new across `tests/test_tier2_db_query_entity_pivot.py` + `tests/test_provider_profile_page.py` + `tests/test_chat_route_integration.py` extension `ChatRoutePhase1CEntityPivotTests`). **One substantive deviation Cursor flagged + accepted:** hybrid read pattern (legacy-driver outerjoin Entity + `entity_id IS NULL` orphan fallback) instead of brief §7.1's strict Pattern A `select(Entity)` — correct call given Phase 1B's deferred NOT NULL flip; test fixtures still constructed Provider/Event/Program rows directly without entity_id and a pure ENTITY-first SELECT would silently drop them. **No production runtime impact** until next deploy; chat-route response shape pinned unchanged.
  - **`e2e66ad`** — Phase 1C shipped line on master plan §4 Phase 1 "Shipped (incremental)" list (one substantial line replacing the "Phase 1C — pending." placeholder).
  - **`a27058d`** — Phase 1D dispatch prompt artifact (`outputs/cursor_dispatch_prompt_phase_1d.md`, 125 lines). Pre-positioned during standby so dispatch was instant when Casey said go. Includes explicit 5-step order-matters sequence (helpers → ingest-path routing → sponsor query routing → NOT NULL flip migration → invert/delete Phase 1B's nullable-pin test), Postgres compatibility reminder, and Option X vs Y decision point on whether to re-tighten the 1C orphan-fallback hybrid pattern after dual-writes guarantee entity_id at write time.
  - **`3f3628e`** — Phase 1D dual-write + entity_id NOT NULL flip + close-out (14 files, +814/-14). Cursor session, single dispatch. New `app/db/entity_dual_write.py` factors `create_provider_and_entity` / `create_event_and_entity` / `create_program_and_entity` helpers (atomic legacy + Entity + extension write, idempotent). New `app/db/sponsor_resolve.py` implements §8.2 discriminator-branching sponsor lookup. Ingest paths routed through helpers across `app/admin/router.py`, `app/contrib/approval_service.py`, `scripts/places_load.py`, `scripts/ingest/ingest_enrichment_csv.py`, `app/programs/router.py`. New alembic migration `f8e9d0c1b2a3_legacy_entity_id_not_null` chains off `b2c3d4e5f6a7` (`op.batch_alter_table` × 3 with portable `nullable=False` only — Postgres-clean per the absorbed §10 lesson; no boolean defaults, no raw SQL). `app/db/models.py` updated: entity_id columns flipped from `Mapped[str | None]` to `Mapped[str]`. Pytest **1512 → 1518** (+6 net-new in `tests/test_phase1d_dual_write.py`); Phase 1B nullable-pin test inverted to `test_entity_id_fk_columns_are_not_null_after_phase_1d_closeout`. **Three pragmatic deviations Cursor flagged + accepted:** (a) `before_flush` Session listener safety net via `register_catalog_dual_write_hooks` in `app/db/database.py` — catches raw `db.add(...)` paths that bypass explicit helpers; same precedent as the slug-listener pattern from session-13's `d967568`; double-call harmless via idempotency guards. (b) `derive_provider_slug` in `app/db/seed_helpers.py` now reserves slugs used by commercial Entity rows to avoid `UNIQUE entities.slug` failures when dual-writing. (c) `Sponsor.business_id` stays Integer; resolution uses `str(business_id) → Provider.id` lookup — brief §8.2 explicitly allowed this; Phase 11 monetization will unify Sponsor.business_id → Entity.id String UUID. Brief §8.1's speculative `app/api/routes/events.py` doesn't exist; Cursor correctly identified Event creates flow through `approve_contribution_as_event`. **Decision-point resolution:** Cursor chose **Option X** (left Phase 1C's orphan-fallback hybrid pattern unchanged in `tier2_db_query` + `queries` + `places_load`) — defensive, no churn, dead-branch cleanup deferred to Phase 13 if ever desired.
  - **Phase 1 close-out summary:** Total commit chain since `32ef46a` master plan ship = 15 commits across sessions 14/15/16 (eleven from session-15 through `5132162` hotfix; four from session-16). Total pytest delta: **1476 → 1518** (+42 net-new tests across `test_entity_schema.py` +21, `test_entity_backfill.py` +6 → +5 after 1D inversion, `test_tier2_db_query_entity_pivot.py` +5, `test_provider_profile_page.py` +1, `test_chat_route_integration.py` +3, `test_phase1d_dual_write.py` +6, plus +1 net adjustment). Final origin alembic head: `f8e9d0c1b2a3`. Production Postgres alembic head still at `b2c3d4e5f6a7` — the Phase 1C application read pivot + Phase 1D dual-write + NOT NULL flip migration deploy at operator cadence; chat-route response shape pinned unchanged via regression coverage so deploy can land at any natural cadence. **Next dispatchable lane:** Phase 2 (account-lite v0.1 + image storage + search index) — Lane 2A and Lane 2B file-disjoint per dispatch_protocol Rule 3, parallelizable. Both need fresh dispatch briefs authored, neither queued yet. Lane 2A also needs operator action on Resend API key registration (~30 min); Lane 2B needs Cloudflare R2 bucket + CDN domain (~2 hours per `image_storage_design.md` §8). **Lessons absorbed during session-16:** (a) the bash-mount git gotcha #7 hit again on session boot — `.git/refs/heads/main` direct read is the reliable fallback when `git status` / `git log` are broken Linux-side; (b) Cursor's hybrid-read-pattern deviation in 1C was the right call and the correct kind of brief-staleness recognition we want from the dispatched agent (the brief was authored before Phase 1B's NOT NULL deferral landed); (c) the `before_flush` safety-net pattern from session-13's slug listener generalizes well to dual-write, and Cursor reached for it independently — worth folding into future write-path briefs as a "consider safety-net listener for fixture coverage" prompt.

- **Session-15 close-out hotfix (2026-05-14, commit `5132162`) — `hotfix(db): Postgres boolean default coercion in Phase 1A migration`.** Production deploy of the Phase 1A migration (`a1b2c3d4e5f6`) crashloop'd against Railway Postgres because the migration used `sa.text("1")` and `sa.text("0")` as `server_default` values for the `is_active` Boolean columns on the new `entities` core + several extension tables. SQLite happily coerces those text literals to 1/0 booleans at write time; Postgres rejects them outright (`column "is_active" is of type boolean but default expression is of type text`). Hotfix swaps to `sa.true()` / `sa.false()` (Alembic's portable boolean literal helpers — emit `TRUE`/`FALSE` on Postgres and `1`/`0` on SQLite). Single-file change to `alembic/versions/a1b2c3d4e5f6_*.py`; no app-layer touch; pytest unchanged at 1503 (the SQLite test path was already accepting the previous text("1")/text("0") so no regression-test gap was hidden). Deploy succeeded on this commit, and **Phase 1A + Phase 1B migrations applied to production Postgres for the first time on this deploy** — production alembic head moved from `f1a2b3c4d5e6` (pre-session-15) straight through `a1b2c3d4e5f6` to `b2c3d4e5f6a7` in a single Railway upgrade. **Lesson absorbed for Phase 1C and 1D dispatch briefs:** the bash sandbox runs SQLite; production runs Postgres; raw SQL or `server_default` constructs that work in one don't necessarily work in the other. Use Alembic's portable helpers (`sa.true()` / `sa.false()` for booleans; `sa.text("...")` only when the SQL is genuinely portable). Folded into `outputs/cursor_brief_phase_1_entity_schema.md` §10 (What NOT to do) so future sub-phases land Postgres-clean on first deploy. Also session-15 closed cleanly with `9dee044` (session-15 handoff doc + session-16 boot prompt) prior to the hotfix being authored.

- **Session-15 (2026-05-14) — Phase 1A + 1B of the master build plan SHIPPED; nine commits, all pushed to origin.** Booted into master-plan execution mode (the 13-phase plan landed at `32ef46a` in session-14 superseded earlier sequencing). Highlights:
  - **`4211553`** — category backfill audit + Phase 3 amendment. Sub-agent audit of `category_backfill_mapping_DRAFT.md` (authored 2026-05-13 against ORIGINAL 12-slug taxonomy) against the locked NEW 12-slug taxonomy from session-14 ChatGPT research (`outputs/chatgpt_taxonomy_research_synthesis.md` §1). Surfaced a master-plan hole at §4 Phase 3 (silent on category seed rewrite implied by the taxonomy shift). Phase 3 amended: 3 new deliverables (rename 7 slugs, delete family + community, insert classes-sports-recreation + public-civic-resources, run audited backfill); effort bumped 3-5d/~1hr → 5-8d/~2-3hr operator. Pro-services strings (`insurance`, `financial`, `legal`, `real_estate`, `professional_services`) deferred to V1.5+ per synthesis §7 Q5 (NULL operator-queue at backfill). Audit memo at `docs/maintainability/category_backfill_mapping_audit_2026-05-14.md`. 2 of 5 §4 lock-now items resolved (rest queued for Phase 3 start).
  - **`ff9832d`** — Phase 1A unified ENTITY schema (additive). New `entities` core table with `entity_type` discriminator (commercial / place / event / program) + 11 extension tables: `entity_categories`, `locations`, `hours`, `seasonal_hours`, `contact_points`, `features`, `offerings`, `service_areas`, `schedules`, `source_evidence`, `sponsorship_slots`. New `app/db/entity_types.py` constants module. Sponsor.entity_type discriminator added (nullable). Migration `a1b2c3d4e5f6` chains off `f1a2b3c4d5e6`. **Zero app-layer code changes** — schema dormant in production. +21 net-new tests (1476 → 1497). **Four Cursor deviations transparently flagged + accepted:** `passive_deletes=True` on Entity→extension relationships for SQLite ON DELETE CASCADE; `engine.dispose()` in cascade test (caught PRAGMA pollution into unrelated DELETE tests); `SourceEvidence.verified_at` + `SeasonalHours.hours_overlay` nullable for backfill compatibility. Dispatched via Cursor per `outputs/cursor_brief_phase_1_entity_schema.md`.
  - **`287e503`** — Phase 1A Shipped line on master plan §4 Phase 1 (structured as "Shipped (incremental)" list — each sub-phase appends its own ship-line as it lands).
  - **`7e13c3a`** — landed session-13 cold-pitch sponsor outreach surface (5 files: `verified_presence_pitch.md` modified + 4 new — followup_emails, leavebehind, objection_faq, referral_script). Master plan §2 referenced these as shipped but they were never committed. Per build-first sequencing these are shelf artifacts; default-plan cold-pitch path stays shelved until Phase 11 monetization.
  - **`bf7383b`** — session-15 dispatch artifacts (`outputs/cursor_brief_phase_1_entity_schema.md` + `outputs/session_15_boot_prompt.md`). Per dispatch_channels.md gotcha #12 — durable workspace `outputs/` artifacts that future agents can fresh-clone reference.
  - **`ec84eb4`** — district paragraphs phased V1 / V1.5 / V2 lock (hybrid pattern). Operator decision 2026-05-14: V1 (Phase 3) keeps operator-authored 8-12 paragraphs (~1 hour, preserves day-1 launch moat); V1.5 (Phase 13) adds UGC "describe this district" alongside peer recommendations (shared moderation surface); V2 adds AI-summarized enrichment with operator-approval gate + explicit provenance label. Resolves the operator-only-forever framing implicit in Opus design §2.4 (now scoped to V1).
  - **`d475b06`** — Phase 1B data backfill (Provider / Event / Program → entities + extensions). Migration `b2c3d4e5f6a7` chains off `a1b2c3d4e5f6`. Backfill logic factored into `app/db/entity_backfill.py` (579 lines) for unit-testability + idempotent re-run. Sponsor.entity_type backfilled to `commercial` for legacy rows. +6 net-new tests (1497 → 1503). **Two Cursor deviations flagged + accepted:** (a) `providers.entity_id` / `events.entity_id` / `programs.entity_id` stay **nullable through 1B AND 1C** (NOT NULL flip moved to Phase 1D once dual-write helpers populate entity_id at write time — existing test fixtures construct rows directly and would fail integrity if non-null landed before 1D). Deviation pinned in `test_entity_id_fk_columns_remain_nullable_for_dual_write_gap`. (b) SQLite time binds normalized via `_sql_time` helper.
  - **`6513798`** — ruff cleanup on `app/providers/view_models.py` (I001 import sort + F401 unused `Any`). Pre-existing failures since pre-session-15; Cursor flagged in Phase 1A baseline. CI lint job had been failing for multiple sessions; this commit returned it to green.
  - **`24fa935`** — Phase 1B Shipped line on master plan §4 Phase 1 + brief amendment (`outputs/cursor_brief_phase_1_entity_schema.md` §3 + §6.1 + §8 patched to reflect the deferred NOT NULL flip — stage 3 of the 1B migration struck through; Phase 1D gains §8.0 with the NOT NULL flip as a Phase 1D deliverable with order-matters safety constraint).
  - **Strategic decisions locked this session (master plan §10 decision log):** (1) category taxonomy rewrite + audited backfill land in Phase 3 (effort bumped); (2) professional-services strings deferred to V1.5+; (3) district paragraphs phased V1/V1.5/V2 hybrid. The audit memo + brief amendment + master plan amendments are durable artifacts; the deviations are pinned in tests/code. **Next dispatch:** Phase 1C application read pivot (biggest sub-phase, 7-10 day brief estimate). Operator action between sub-phases: drop + recreate local SQLite dev DB to clear pre-1A drift surfaced by Cursor's Path A baseline self-check (`duplicate column name: slot` at `1a2b3c4d5e6f → 2a3b4c5d6e7f`; benign for production).

- **Provider.slug field + backfill migration + ingest-path wiring SHIPPED (commit `d967568`, 2026-05-13)** — Directory pivot V1 follow-up; gates the Provider profile page implementation lane (Verified Presence sponsor sales). New `Provider.slug: String(120), unique, indexed` column on `app/db/models.py` immediately after `district` (post-pivot §8.2 addition). Slug values derived from `provider_name` via new shared utility module `app/utils/slug.py` exposing `slugify` (re-export of the historical `parks_rec_loader._slug` regex pattern) + `make_unique_slug` (lowest-integer suffix for collision handling). Pre-existing `parks_rec_loader._slug` becomes a re-export to preserve the existing call site. Backfill via 3-stage migration `f1a2b3c4d5e6_provider_slug.py` (chains off `e7f8a9b0c1d2`): add nullable → SELECT-and-UPDATE every existing row with deterministic-ordered slug derivation + collision suffixes → flip NOT NULL. New rows get a slug at insert time via four explicit `derive_provider_slug` call sites (`app/admin/router.py`, `app/contrib/approval_service.py`, `scripts/places_load.py`, `scripts/ingest/ingest_enrichment_csv.py`) PLUS a defensive ORM `before_insert` listener (`app/db/seed_helpers.register_provider_slug_hooks`) registered at `models.py` import time via `_register_provider_slug_listeners()` — covers test fixtures and any future Provider-construction path that forgets the explicit call. Annotation stays `Mapped[str | None]` because the migration's NOT NULL flip is DB-layer; touching the annotation comes later with the `category_id` backfill ticket. Implementation channel: Cursor (heavy multi-file lane, brief at `outputs/cursor_brief_provider_slug.md`). 13 new tests (`tests/test_slug_util.py` 7 + `tests/test_provider_slug_migration.py` 6). Pytest **1429 → 1442 passed**. Alembic head `e7f8a9b0c1d2 → f1a2b3c4d5e6`. Ruff clean after auto-fix. **Two pragmatic deviations Cursor flagged transparently:** (1) §0 baseline mismatch — HEAD was `1580acd` (one docs commit ahead of brief's `11b248f` expectation), git status had untracked rate-limiter memo + outputs/ session artifacts, alembic current showed the benign `1a2b3c4d5e6f (mergepoint)` local-DB-drift label. Cursor proceeded judgment-call and shipped clean — correct call. (2) Added the `before_insert` listener as a safety net beyond the brief's explicit-call-site approach. Justified — covers test fixtures and missed call sites. **Lesson absorbed (folded into rate-limiter Option A brief):** §0 baseline language in dispatch briefs should hard-pin on prerequisite conditions ("does column X exist") not on commit-position language ("most recent commit should be X") — the latter is fragile to unrelated commits landing between brief authoring and dispatch. **No chat-route runtime impact** — column is dormant until application code reads it (next: Provider profile page).

- **Phase 2.5 rate-limiter §8 decisions memo + design-doc status block SHIPPED (commit `0a0644d`, 2026-05-13)** — Unblocks the rate-limiter Option A implementation lane (#65). All 7 open §8 questions + 1 design-doc §3 extra LOCKED on the recommended path. New `docs/maintainability/phase2_5_rate_limiter_decisions_memo.md` (505 lines, §1 summary recommendations table + §2 per-question detail with evidence/options/recommendations + §3 design-doc discrepancies surfaced during investigation + §4 open meta-questions). Design-doc §8 amended with a "Status (2026-05-13)" block listing all 8 locked decisions + 2 implementation-side carry-overs. **Locked decisions:** (Q1) third-party-source framing confirmed; (Q2) **4 QPS default + per-instance `qps=` override** preserves discovery's 4 QPS lookup-path AND enrichment's 6.5 QPS sweep (design doc's "same constants" claim was wrong — discovery uses 0.25s sleep, enrichment uses 0.15s); (Q3) keep `PlacesLookupResult(status="error", error_message="http_429")` envelope, defer retry queue + alerts to P2; (Q4) **structured logs to existing destination in P1**, reuse Option B's `provider_api_quota` table for telemetry when P2 lands (avoids two migrations); (Q5) **single-process P1 only** — Option A ships, Option B follow-up gated on first concrete multi-process scenario (post-pivot V1 directory is single-process); (Q6) defer `url_fetcher.py` per-host throttling (different shape: LRU+TTL host map vs per-source fixed budget); (Q7) eventually wrap OpenAI via same `SourceLimiter` interface, NOT in P1; do not rename `call_anthropic_messages` (header retains the name per provider swap 2026-05-07); (memo §3 extra) `river_scene.py::_sleep_polite` out of scope per design §9. **Implementation-side carry-overs from memo §3:** (a) HTTP library mismatch — runtime is `httpx`, scripts are `requests`; decision is to **standardize scripts on `httpx`** (cleanest path); (b) `PAGINATION_SLEEP_S` is not retry logic — keep inline in `places_discovery.py`. **Investigation channel:** general-purpose sub-agent (Cowork primary dispatched after Claude Code paste-channel mismatch — see BACKLOG entry §3 for the lesson). Decision round driven by Cowork primary via two AskUserQuestion rounds (4 + 4 questions); operator took all 8 locked answers on recommended path. Cursor Option A implementation brief authored to `outputs/cursor_brief_rate_limiter_option_a.md` later in session. **No production runtime impact** — doc-only ship.

- **Session-13 dispatch artifacts shipped — briefs + prompts + UX spec (commit `b22aa86`, 2026-05-13)** — Five durable dispatch artifacts saved under workspace `outputs/` path (not the session sandbox, which doesn't persist): `chatgpt_prompt_provider_profile_ux.md` (ChatGPT prompt grounded against Provider schema as of `6f6ef79` + voice-anchored against `verified_presence_pitch.md`); `chatgpt_response_provider_profile_ux_spec.md` (ChatGPT-shipped UX spec verbatim; 11 sections, copy bank, tier-delta table, 8 questions resolved this session); `cursor_brief_provider_slug.md` (the brief that shipped `d967568`); `cc_prompt_rate_limiter_decisions_memo.md` (original CC channel — channel mixed up, sub-agent recovery path used); `cursor_brief_provider_profile_page.md` (Cursor brief for Provider profile page; dispatched to CC mid-session-13). **New convention this session** (worth folding into `dispatch_channels.md`): anything the next session needs to reference must be saved under the workspace `outputs/` path, not the session-scratch path — the prior session's `outputs/` artifacts didn't persist into session-13, which forced a re-author of two prompts. Two more artifacts authored autonomously after operator stepped away land in a follow-up commit (rate-limiter Option A brief + Home Services category page UX prompt). **No production runtime impact** — durable artifacts, not on the chat-route runtime path.

- **Directory pivot V1 schema SHIPPED — `Category` model + `category_id` FKs + `attributes`/`district` on Provider (commits `6f6ef79` schema + `aea87b8` lint cleanup, 2026-05-13)** — First bounded engineering work under the directory-first product shape (strategic pivot landed 2026-05-12 in `b97942d`; §8 decisions 1–4 locked 2026-05-13 in `a6e9f6c`). New `Category(Base)` class with 12 seeded canonical categories per pivot §8.1 (`eat-and-drink`, `events`, `family`, `home-services`, `health`, `on-the-water`, `outdoors-and-parks`, `shopping`, `auto-and-gas`, `lodging`, `pets`, `community`). Additive `category_id: Mapped[int | None]` FK on `Provider` (around `models.py:91`) and `Program` (around `:305`), parallel to the existing string `category` / `activity_category` columns — backfill and deprecation are deferred to a future ticket. Additive `attributes: Mapped[dict | None]` JSON column on Provider (operator-curated structured fields like service area, sub-trade, emergency-service flag — distinct from `raw_enrichment_json` which is the raw scraper dump). Additive `district: Mapped[str | None]` String(64) column on Provider (Eat & Drink district tagging without first-class `Place` rows; pivot §8.2 deferred Place to Phase 2). New `category_ref` relationship attribute on both Provider and Program (named with `_ref` suffix to avoid collision with existing string `category` attribute on Provider). Alembic migration `e7f8a9b0c1d2_directory_v1_schema.py` chains on top of `d6e7f8a9b0c1`; uses `op.batch_alter_table` for SQLite-friendliness; seeds 12 categories via `op.bulk_insert(sa.table(...), ...)` (Cursor's pragmatic adaptation — `op.create_table` doesn't return a usable table object in this Alembic setup). `tests/test_directory_schema.py` adds 7 net-new tests covering seed verification, additive coexistence with legacy string columns, JSON round-trip, district round-trip, both relationship navigations, and Category.slug uniqueness. Implementation channel: Cursor (heavy multi-file lane; same heavily-prescriptive-brief pattern that worked for #64). Brief authored to `outputs/cursor_brief_directory_v1_schema.md`. **Pre-commit forensics worth preserving:** false-alarm cycle on local DB drift — `python -m alembic current` showed `1a2b3c4d5e6f (mergepoint)` which Cowork primary initially read as a multi-head conflict signal; chain-walking down_revision values across all migration files revealed `1a2b3c4d5e6f` is a long-resolved merge that lives 6 revisions earlier in the linear chain (Casey's local SQLite dev DB is just stale; production chain at `d6e7f8a9b0c1` was unaffected; Casey's local-DB issue is dev hygiene only). **Lesson absorbed:** `(mergepoint)` label on an unexpected `alembic current` output is a chain-walking diagnostic (`Grep ^down_revision alembic/versions/`), not a multi-head alarm. **Lint follow-up `aea87b8`:** schema commit's three files were ruff-clean, but pushing surfaced 4 pre-existing ruff failures in three unrelated test files (`tests/test_chat_route_integration.py` F401 unused `MagicMock` from #63 + I001 import-group blank line; `tests/test_entity_matcher_category_guard.py` I001 same; `tests/test_entity_matcher_trade_superlative.py` I001 alphabetical reorder). Local ruff was an older version than CI's pinned `ruff==0.15.12` in `dev-requirements.txt` — newer ruff has stricter I001. **Lesson absorbed:** local ruff installs should match `dev-requirements.txt` pin (`python -m pip install ruff==0.15.12`) before pre-commit lint checks. Worth folding into `dispatch_channels.md` gotchas alongside Linux-bash-mount staleness and PowerShell encoding rules. Net pytest **1422 → 1429 passed** (full suite; +7 from schema lane; lint commit changed only import lines so no test-count change). Alembic head **`d6e7f8a9b0c1` → `e7f8a9b0c1d2`**. **No chat-route runtime impact** yet — schema gained columns but no application code reads them. Production deploys to Railway will apply `e7f8a9b0c1d2` cleanly (single migration on top of the deployed `d6e7f8a9b0c1` head).

- **#64 SHIPPED — Confabulation harness v2: emit `category` / `activity_category` in `per_row.csv` + config-driven anchor allowlist (commit `aa3abd4`, 2026-05-12)** — Closes the spec-vs-toolchain gap surfaced by yesterday's HALT 3 close-out template ambiguity-resolution lane. `app/eval/confabulation_report.py::write_per_row_csv` now emits 9 columns with `category` (Provider) and `activity_category` (Program) inserted after `row_name`; empty CSV cells when the field is absent. The 2-name regression-anchor allowlist moved from inline at the previous `:167-173` to a module-top constant `_DEFAULT_REGRESSION_ANCHORS = ("Aqua Beginnings", "Grace Arts Live")` with governance comments referencing `halt3_definition.md` §6; `write_summary_md(..., *, anchors=...)` accepts an optional tuple override (default anchors unchanged). `scripts/confabulation_eval.py::_probe_name_map()` now returns `dict[str, dict[str, str | None]]` loading `Provider.category` and `Program.activity_category` into a per-id metadata map; `_enrich_for_reports` fills `category` / `activity_category` by row type; new `--anchor-set-file PATH` CLI arg reads newline-delimited names (blank/`#` lines skipped) and passes as `anchors=`. **Column-name correction (forensic note for the next handoff):** the original BACKLOG #64 description and the v1 dispatch brief both cited `Program.track_kind` as the Program-side category column. That column does not exist on the model; actual column is `Program.activity_category` (`app/db/models.py:248`). The mistake traces to yesterday's halt3 ambiguity-resolution sub-agent — it surface-read `track_kind` from doc/code text without cross-checking against the model file. Caught by the v1 brief's "trust the source" clause when CC ran step-0 verification; v2 brief shipped under the corrected name. **Lesson absorbed:** sub-agent investigations on schema-adjacent surfaces should ground column names against `app/db/models.py` directly, not just the consuming code paths. **Pre-dispatch Glob false negative:** primary's `**/test_confabulation*.py` Glob returned "No files found"; the file existed with 6 pre-existing tests. Cursor caught and extended in place rather than authoring fresh. Lesson: don't trust a single Glob for "does this file exist" — pair with a `Read` of a likely path or `Grep` for distinctive content. Ship channel: Cursor (after CC v1 halted on the column-name issue). **Two minor brief deviations** Cursor reported transparently — `tests/test_confabulation_eval_script.py` got fixture updates (out-of-brief but in-scope after the `name_map` shape change broke its existing tests) and `_anchor_names_from_file` was factored as a module-level helper with its own unit test (brief's "factor out if useful" path). **Deferred:** optional per-category breakdown section in `summary.md` — close-out actor can derive from `per_row.csv`. **Did not ship:** `relay/halt3-anchor-set.txt` — operator-authored at close-out if using `--anchor-set-file`. **No production runtime impact** — eval surface only. Pytest **1417 → 1422 passed** (+5 net-new test methods, +1 in-place 7→9 column header pin extension). `docs/maintainability/halt3_closeout.md` updated in lockstep — §2 `per_row.csv` schema (9 columns), §3.2 anchor-set composition (module-top constant location), §3.2 anchor-set governance note, §3.2 per-category methodology (read columns directly, no DB join), §3.3 per-row offender ranking source (9-column header pin).

- **HALT 3 pre-investigation + Phase 2.5 forward-looking design (commits `da27f9d` / `d3ba173`, 2026-05-11)** — Two doc-only lanes shipped after the #63 close-out, both forward-looking against the multi-week HALT 3 / Phase 2.5 sequence. **`da27f9d`:** HALT 3 close-out template (`docs/maintainability/halt3_closeout.md`) hardened against the 5 spec ambiguities its draft author flagged at template authoring time — output-dir path mismatch (HIGH confidence: harness writes only to `--output-dir`, no relay mirroring), per-row offender column names (HIGH: header is hard-coded in `app/eval/confabulation_report.py:42-52`), anchor-set canonical source (HIGH: 2-name allowlist `Aqua Beginnings` / `Grace Arts Live` at `app/eval/confabulation_report.py:167-173`, not a separate file), per-category band granularity (MEDIUM: harness emits no category dimension; manual join at close-out OR ship #64 first), catalog-flagging cutoff (MEDIUM: `gating_runs_with_hit >= 2` per "repeated hits" textual reading). Pre-investigation done via general-purpose sub-agent dispatch (read-only forensics across `halt3_closeout.md` + `halt3_definition.md` + `confabulation-eval-runbook.md` + `phase1_deploy_runbook.md` + `scripts/confabulation_eval.py` + `app/eval/confabulation_report.py` + `app/eval/confabulation_query_gen.py`; ~84s investigation, 8 tool uses). Filed **#64** (harness v2: emit `category` in `per_row.csv` + expose anchor allowlist as config) for the spec-vs-toolchain gap surfaced during ambiguity 3/4 investigation — strategy doc requires per-category bands but the v1 harness emits no category dimension and hard-codes the anchor allowlist inside `write_summary_md`. **`d3ba173`:** Phase 2.5 third-party-source rate-limiter design at `docs/maintainability/phase2_5_rate_limiter_design.md` (206 lines, 9 sections + scope-correction §0). CC's investigation found the dispatch brief's framing ("enrichment-ingest rate-limiter") was wrong — the CSV ingest script (`scripts/ingest/ingest_enrichment_csv.py`) makes zero external API calls; the actual rate-limit surface is upstream at `app/contrib/places_client.py::lookup_provider` (Gap A: runtime path has no backoff, documented at `docs/components/places_client.md:55`) and `scripts/places_{discovery,enrichment}.py` (Gap B: duplicated DIY backoff constants). Recommended approach (Option A): new `app/contrib/rate_limiter.py` exposing `SourceLimiter` with QPS pacing + exp backoff; route both Places paths through `GOOGLE_PLACES_LIMITER`; design the interface so Option B (DB-backed token bucket mirroring `_rate_limited` IP-hash precedent) is a drop-in P2 replacement when concurrent workers ship. Skipped Option C (Redis — disproportionate infra) and Option D (quota pre-check — Places (New) doesn't expose). 7 open questions for the implementation lane in §8 (per-source default QPS, failure-mode policy, observability surface, Phase 2.5 launch concurrency, `url_fetcher.py` inclusion, OpenAI-as-source). Filed as **#65** (DESIGN SHIPPED 2026-05-11; implementation OPEN, MEDIUM, gated on §8 open questions). **No production code touched in either lane** — runtime behavior unchanged since `54a56b1`. Pytest **1417 passed** (no test changes). Brief-authoring lesson absorbed for the rate-limiter lane: read the call-graph from third-party SDK boundaries upward before pointing the brief at a specific script — assumptions about "where the rate-limiter belongs" should be verified against the actual outbound-call sites, not inferred from the toolchain's user-facing entry point.

- **#63 SHIPPED — Project-wide LLM-mock policy + chat-route LLM-coupled integration coverage (commits `a6abf35` / `1ecce24`, 2026-05-11)** — Closes the deferred LLM-coupled coverage from CC's chat-route integration ship halt (`109c7ac`, 2026-05-10 evening). **Part 1 (`a6abf35`):** authored `docs/maintainability/llm_mock_pattern.md` (87 lines, 6 sections) codifying the existing `with patch.object(llm_messages, "OpenAI", return_value=fake)` pattern as the project standard. Doc covers the canonical pattern, the shared helpers, when to deviate (e.g. `test_llm_cache.py`'s `llm_cache.OpenAI` patch target — different module-local import means a single fixture can't cover all callers), and rejected alternatives (autouse fixture in `conftest.py` — implicit-coupling risk; env-var stubbing in production — leaks test concerns into prod). **Part 2 (`1ecce24`):** extracted `_resp` / `_patched_openai` helpers (previously redefined in 4+ test files) into `tests/_llm_mocks.py` as `build_chat_completion_response` + `patched_openai_client`; added `ChatRouteIntegrationLLMCoupledTests` (5 test methods) in `tests/test_chat_route_integration.py` covering (1) Tier 2 listing shortcut at the HTTP boundary (zero-token deterministic path; LLM patched with `side_effect=AssertionError` to prove no LLM call), (2) Tier 3 fallthrough on explicit-rec phrasing, (3) #49 cache contract (raw text in `LlmResponseCache`, `_enforce_low_tier_phone` re-runs on cache hit, second LLM call patched to AssertionError), (4) #52 alias-resolution at the HTTP boundary (CANONICAL_EXTRAS resolves "what is the best plumber in lake havasu" to All Seasons Plumbing), (5) #55 confidence-tier integration via `mock.call_args` inspection of the Tier 3 user_text (operator-vocab Provider produces canonical MEDIUM hedge fragment in LLM context). **Two brief deviations** discovered during implementation and documented in test docstrings: area #1 doesn't actually need the LLM mock (Tier 2 listing shortcut is zero-token by Slice D design), and area #4 expects `tier_used == "3"` not "1" (`_is_explicit_rec` matches `\bbest\b` and routes to Tier 3 before Tier 1 is consulted, even with entity resolved by the matcher). No production code changed — integration test surface only; runtime behavior unchanged since `54a56b1`. Pytest **1412 → 1417 passed** (+5 net-new test methods).

- **Phase 2 follow-up cluster — #52 + #56 + #57 + #58 + #59 + #60 + #61 SHIPPED (commits `d060240` / `6c6ca02` / `f990488` / `9fff5c2` / `9460f69`, 2026-05-10 afternoon and evening)** — Seven tickets across the matcher and confidence-tier surfaces. **#52** (Cursor `d060240`): trade-aligned bypass in `_category_guard_skips_row` + new `gymnastics_cheer` trade cluster + two `CANONICAL_EXTRAS` aliases for smoke-aligned coverage. 8 net-new tests in `tests/test_entity_matcher_trade_superlative.py`. Followup #62 (P3) filed for the alias-resolution-vs-disambiguation product question. **#56** (Cursor `6c6ca02`): chat-route UTF-8 regression test — `tests/test_chat_route_utf8.py` with 4-case TestClient matrix pinning Starlette body-parse behavior at the wire layer (RFC 8259 means JSON is UTF-8 octets regardless of charset declaration). Closes the test gap from #51's doc-only ship. **#58** (CC `f990488`): direct floor coverage for delegating entry points — 2 new test methods × 4 parametrized subtests on `match_entity_with_ambiguity` and `query_has_ambiguous_entities`. Investigation confirmed clean delegation across all entry points; no #50 regression to surface. **#57 + #59** (Cursor `9fff5c2`): bidirectional lock-step `_OPERATOR_VOCAB_METHODS ↔ _OPERATOR_VOCAB` test fixture symmetry (replaces one-way ⊇ check) + 90-day MEDIUM boundary parametrization across 7 trusted methods at `age=90` (MEDIUM) and `age=91` (LOW). Scraper intentionally excluded — different MEDIUM rule. **#60 + #61** (CC `9460f69`): index-side floor non-application invariant (`_needles_for_canonical("Lake Havasu Mountain Bike Club")` contains `"mtb"`) + smoke catalog Class E3 layer clarification (post-#51, the wire-level concern is precondition-met by `; charset=utf-8`; row exercises matcher-side accent-folding only). Pytest **1377 → 1409 passed** across the cluster.

- **Forward-looking artifacts — 2026-05-11 handoff + HALT 3 close-out template + post-enrichment smoke catalog + end-user FAQ + enrichment sprint runbook + sponsor quick-reference (2026-05-10 evening, multi-commit)** — Shipped via sub-agents and ChatGPT in parallel with the engineering tickets. **`docs/SESSION_HANDOFF_2026-05-11.md`** is the canonical entry point for the next session. **`docs/maintainability/halt3_closeout.md`** is a fill-in-the-blank template (~145 lines, 85 `[FILL]` placeholders) for the eventual HALT 3 close-out — surfaced 5 spec ambiguities the operator will need to resolve at close-out time (output-dir path mismatch, per-row offender column names, anchor-set canonical source, per-category band granularity, catalog-flagging cutoff). **`docs/maintainability/post_enrichment_smoke_catalog.md`** (~210 lines, 42 queries across 8 classes) is the validation instrument for the eventual `FEATURE_FLAG_DISCLOSURE_RENDERER` flag flip — surfaced 6 open spec questions worth resolving before the flip (regime-naming spec mismatch, category alignment of candidate sponsors, regime B suppression on zero-organic, cache key composition, multi-sponsor rotation determinism, CT2.B ship status). **`docs/user_facing/hava_user_faq.md`** is the first end-user-facing doc on-tree (operators have 5 docs, end-users had zero before). **`docs/sponsor_outreach/enrichment_sprint_runbook.md`** is the operator-facing soup-to-nuts playbook for running the enrichment sprint. **`docs/sponsor_outreach/sponsor_quick_reference.md`** is the launch-day reference card for sponsors. **`docs/maintainability/2026-05-10_absorption_forensics.md`** documents the parallel-commit-absorption pattern observed today + drafts a Rule 13 candidate for `dispatch_protocol.md` — verdict was DON'T ADD (1 novel case, low severity, cure already implicit in Rule 2; keep candidate text paste-ready if a second instance recurs).

- **#54 close — dangling `relay/halt1-closure-final-lexicons.md` references scrubbed (2026-05-10, in flight)** — Parallel sub-agent A is closing the doc-hygiene ticket tonight; commit expected to land before the next session opens. Stripped lingering cross-references to the never-shipped lexicons doc across `docs/components/`, `docs/confabulation-eval-runbook.md`, and `docs/maintainability/halt3_definition.md`; lexicons that were inferred-from-context are now documented inline at point of use. `docs/BACKLOG.md` flips #54 OPEN → RESOLVED. **Status: in flight, expected to commit before next session.**

- **Dispatch protocol Rule 12 — amend-while-parallel-lanes-in-flight rule added (commit `10b2f5d`, 2026-05-10)** — Codified the lesson from today's #50/#51 git wrinkle: `git commit --amend` is unsafe when parallel sub-agent / Cursor / Claude Code lanes still hold uncommitted edits in the working tree, because the amend reshuffles the index in ways that can swallow or split unrelated work. New Rule 12 in `docs/maintainability/dispatch_protocol.md`: **never amend when any other agent might still be writing files; commit a follow-up fix instead.** Sits alongside Rule 2 (wait-for-text-report) and Rule 8 (sequential commits per lane) as the "git operations during parallel dispatch" cluster.

- **#50 ship — ≥3-char minimum-length floor at entity matcher entry (commit `f9e9b06`, 2026-05-10)** — Closes the long-standing low-priority bug where 1-char and 2-char queries (`a`, `bo`, `mu`) matched arbitrary short entity prefixes via `partial_ratio` token padding. New entry-point guard in `app/chat/entity_matcher.py` returns `(None, 0.0)` when the cleaned query has fewer than 3 substantive characters before any scoring runs. Pytest **1369 → 1377 passed** (+8 net-new tests in `tests/test_entity_matcher_min_length_floor.py` covering 1-char, 2-char, exactly-3-char, padding-token edge cases, and Tier 1 preservation for legitimate 3-char canonicals like `BMX`). **#50 RESOLVED.**

- **#51 close-out — accent-bearing query 400s diagnosed as smoke-harness encoding artifact, NOT-A-BUG (commit `79f7396` content / `b361526` original, 2026-05-10)** — The reported "accent queries return HTTP 400" reproduction turned out to be PowerShell's default code page mangling UTF-8 in the 39-query smoke catalog before bytes ever hit `/api/chat`; production accepts accent-bearing queries correctly when the request is encoded UTF-8 end-to-end. Closed as not-a-bug with a short post-mortem in `docs/BACKLOG.md`. UTF-8 charset patches landed across 4 docs: `docs/maintainability/backlog_46_smoke_check_queries.md`, `docs/maintainability/phase1_deploy_runbook.md`, `docs/maintainability/dispatch_protocol.md`, and `docs/SESSION_HANDOFF_2026-05-09_evening.md` — each gained an explicit `chcp 65001` / `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8` snippet for the smoke-harness sections. **#51 CLOSED (not-a-bug).**

- **#55 ship — `confidence_tier._KNOWN_METHODS` extended for Lane 3 operator vocab (commit `847f79a`, 2026-05-10)** — Closes the hygiene gap flagged after Lane 3 expanded `verification_method` CHECK to include the operator-friendly vocab (`phone_call`, `in_person`, `web_form_submission`, `email_confirmation`). The `confidence_tier` classifier's `_KNOWN_METHODS` set was still on the legacy two-value enum (`manual`, `owner_confirmed`), which would have under-ranked operator-vocab rows once enrichment lands them. Set extended to the unified seven-value vocab; `classify_confidence` updated to map operator vocab through the same tier weighting as the legacy values. Pytest **1348 → 1369 passed** (+21 net-new tests across `tests/test_confidence_tier_operator_vocab.py` and parametrized expansions of the existing `test_confidence_tier.py`). **#55 RESOLVED.**

- **Sponsor outreach surface complete — handoff addendum + dispatch protocol cheat sheet + 3 cold-email variants + reply handlers + post-launch comms (commits `cbcf713` / `63d257f` / `5730f8a` / `df612d4`, 2026-05-10)** — Operator-facing artifacts for the enrichment sprint. **`cbcf713`:** `SESSION_HANDOFF_2026-05-09_evening.md` addendum + `docs/maintainability/dispatch_protocol.md` cheat sheet (the 12-rule reference card the session-start doc points at). **`63d257f`:** three new cold-email variants in `docs/sponsor_outreach/cold_email_variants_2026-05-09.md` for restaurants, boat repair, and auto repair (joins the existing plumber / HVAC / pool / urgent-care variants). **`5730f8a`:** `docs/sponsor_outreach/reply_handlers.md` covering yes / no / questions / follow-up branches with copy-paste templates. **`df612d4`:** `docs/sponsor_outreach/post_launch_comms.md` for launch / day-25 check-in / retention sequencing. Casey-driven workflow now has end-to-end paper for cold outreach → reply → onboarding → launch → retention.

- **Lane 4 ship — P2.OBS.1 disclosure-renderer observability columns (commit `24abe82`, 2026-05-09 evening)** — Added four nullable telemetry columns to `chat_logs` (`disclosure_regime`, `disclosure_sponsor_id`, `disclosure_tone_allowlist_passed`, `disclosure_eligible`) via Alembic revision `d6e7f8a9b0c1`. Columns are dormant until `FEATURE_FLAG_DISCLOSURE_RENDERER` flips to `true` post-enrichment; renderer wiring writes to them defensively (`hasattr` gate) so the migration can ship ahead of the flag flip without breaking existing chat path. Closes the last Phase 2 first-week lane.

- **Lane 3 ship — P2.BL.45 `verification_method` CHECK constraint expanded for operator vocab (commit `1749675`, 2026-05-09 evening)** — Backlog #45 closed. Alembic migration widens the `Provider.verification_method` CHECK constraint from the legacy two-value enum (`manual`, `owner_confirmed`) to a seven-value union that adds the four operator-friendly vocab values (`phone_call`, `in_person`, `web_form_submission`, `email_confirmation`) plus `unverified`. The enrichment ingest script (`scripts/ingest/ingest_enrichment_csv.py`) was already mapping operator vocab → DB enum lossily; this migration removes the lossy mapping and lets the operator-vocab strings round-trip cleanly. Setup for #55 (`confidence_tier._KNOWN_METHODS` extension) which shipped 2026-05-10.

- **Lane 2 ship — P2.HOME.1 `DISCLOSURE_WORD` consistency on `/home` (commit `8c5d008`, 2026-05-09)** — Routed the `disclosure_render.DISCLOSURE_WORD` module constant through `app/home/router.py` and the home template so the word "Sponsored" renders identically on the home page placement surface and the chat tier-3 placement surface. Removes a class of drift bugs where home and chat could disagree on the disclosure label string after a future copy change.

- **Phase 2 first-week Lane 1 close — #47/#48/#49 fix bundle shipped (2026-05-09 afternoon)** — Single Cursor lane (commit `dd484a0`) closed all three production behavior bugs surfaced yesterday from the §6.2 CT flag verification chain. **#47 (entity matcher cross-category guard):** new `category_blob` field on `_EntityRow` populated from Provider/Google/Program category fields during `refresh_entity_matcher`; `_trade_cluster_tags` taxonomy + `_row_supports_trade_intent` + `_distinctive_entity_tokens` + `_query_explicitly_names_row` helpers; guard wired through `extract_catalog_entities_from_text`, `match_entity_with_ambiguity`, `find_near_match`, `match_entity_with_rows`. Architectural deviation from dispatch's recommended Option 2 (Tier 3 retrieval filter): Cursor built the guard at matcher level with its own trade-cluster taxonomy rather than reusing the intent classifier's category extraction — broader blast radius but defense-in-depth across all four matcher call sites. **#48 (negation-aware post-process skip):** `_LLM_NEGATION_VOICE_PATTERNS` regex list + `_llm_voice_denies_catalog_hit` helper in `tier2_formatter.py`; `_enforce_low_tier_phone` returns early when LLM voice contains "no result" / "not in catalog" / "I don't have" / "I don't see" / "no [word] listed" patterns. Single fix covers both Tier 2 and Tier 3 paths since they share `_enforce_low_tier_phone`. **#49 (raw cache storage, post-process at serve):** Tier 3 cache write stores `text_for_cache = post-strip_soft_suggest only` (template-free cleanup runs once); `_enforce_low_tier_phone` runs at serve time on both miss path (post-LLM) and cache hit path (post-lookup), gated on `FEATURE_FLAG_CONFIDENCE_TIER`. Cache hit ordering: lookup → `_enforce_low_tier_phone` → sponsored injection → return; mirrors miss path. Minor deviation from dispatch's strict-raw recommendation — `strip_soft_suggest` runs once at write rather than every hit; acceptable as long as that helper stays stable. **Tests:** 13 net new across 3 files — `tests/test_entity_matcher_category_guard.py` (5: original BMX/plumber bug + sibling cases including BMX alias preservation and electrician/BMX cross-category), `tests/test_tier3_postprocess_negation_skip.py` (6: 5 parametrized over distinct negation phrasings + 1 sanity), `tests/test_llm_cache_raw_storage.py` (2: raw storage + cache-hit re-post-process). Full suite **1340 passed**. **Adversarial checks against live catalog (after `refresh_entity_matcher`):** `phone for addrss` → None/None; `sloane number` → None/None; `what is the best plumber in lake havasu` → None (no BMX false positive); `hours for All Seasons Plumbing` → ('All Seasons Plumbing', 100.0) — Tier 1 path preserved. **Operational state:** pre-deploy production `LlmResponseCache` was confirmed empty via Railway web SQL (`DELETE FROM llm_response_cache;` returned 0 rows on 2026-05-09 ~18:00 UTC); post-deploy purge still recommended to flush any entries written between Cursor's commit and Railway redeploy. Production smoke + 30-query catalog (`docs/maintainability/backlog_46_smoke_check_queries.md`) is the substantive verification per protocol rule 5; once clean, `FEATURE_FLAG_CONFIDENCE_TIER` is safe to re-enable in production. **Phase 2 follow-up worth filing:** the 3rd adversarial check returned None instead of surfacing a real plumber from the catalog — trade-tag guard appears over-conservative for trade superlatives. Acceptable per dispatch criteria; small ticket if ideal-case matcher behavior is desired. **Filed by:** Cursor (Lane 1 implementation), Cowork primary (review + integration). Three new ship-log entries appended to `docs/BACKLOG.md`; #47/#48/#49 status flipped from OPEN → RESOLVED.

- **Phase 1 close — fully closed including Backlog #46 (2026-05-09 morning session)** — Three parallel lanes converged this morning to close the last open item from yesterday's evening work. **Cursor shipped Backlog #46** (the entity-matcher #44 connector-word bypass) — replaced `_best_partial_ratio_per_needle_token` with `_typo_guard_query_token_matches_needle(tok, needle) -> bool` in `app/chat/entity_matcher.py`; uses substantive needle tokens with `len >= 5`, applies `_TYPO_PER_TOKEN_THRESHOLD = 80` for needle tokens >5 chars, and applies `_TYPO_FIVE_CHAR_NEEDLE_TOKEN_THRESHOLD = 89` for needle tokens of exactly 5 chars (the `addrss` vs `dress` edge case). All four required adversarial outcomes confirmed: `phone for addrss` → None, `sloane number` → None, `phone for mdshrkbrwry` → NEAR Mudshark 65.45, `phone for mudsharks brewry` → MATCH Mudshark 84.375. Full suite **1314 passed**. **CC shipped the parallel adversarial regression suite** at `tests/test_entity_matcher_adversarial.py` (308 lines, 13 tests across Class A connector-bypass / Class B #44 preservation / Class C boundary cases) — landed GREEN-on-arrival because Cursor's fix was already in the working tree when CC opened the file, but serves as permanent CI coverage that pins the bypass class for good. **ChatGPT (non-file-connected) generated 30 adversarial query patterns** — Cowork primary triaged and saved as `docs/maintainability/backlog_46_smoke_check_queries.md` for post-deploy manual smoke against production `/api/chat`. **One subtle behavior worth knowing for future readers:** bare-form severe typos like `mdshrkbrwry` and `mudsharks brewry` return None — the existing `_best_score_padded` F6 early-return path only fires the WRatio scorer when intent-stripping changes the query. Realistic chat shape (`phone for X`) works correctly. Real users always include intent prefixes. Smoke-check doc updated to use `phone for X` for Class D NEAR-band cases. **Backlog #46 RESOLVED.** Phase 1 code work fully closed; ready for commit + push + Railway deploy + flag flip when operator decides. Three new ship-log entries appended to `docs/BACKLOG.md` (Cursor #46 fix, CC adversarial suite, smoke-check doc); #46 status flipped from OPEN to RESOLVED.

- **Phase 1 close — full suite green, runbook ready for flag flip (2026-05-08 evening session)** — Multi-agent orchestration session that closed Phase 1 cleanup. Eight ship-logs landed across the post-keystone surface: **Lane #41a-followup** (Cursor) switched `app/db/types.py::TZAwareDateTime.process_result_value` to return aware Lake-Havasu datetimes on read; dropped the `.replace(tzinfo=None)` workaround at both call sites in `app/chat/context_builder.py`; updated `tests/test_tz_aware_datetime.py` helpers and assertions to match aware contract (8 tests still green); incidental syntax fixes in `app/db/models.py` and `app/chat/disclosure_render.py` removed mid-write garbage (`ble=False,` and stray `regime, )` after `SponsoredBlock(...)`) that was blocking imports — Linux-mount-staleness pattern per handoff §9. **Test-suite triage** (Claude Code) audited the broader suite, ship-fixed 6 of 15 failures across `tests/test_phase2_integration.py` (4 fixes — OOS mode revert, dining bucket drop, tier-name parametrize per mode), `tests/test_unified_router.py` (1 fix — pin deterministic intake-voice contract instead of legacy debug placeholder), and `tests/test_phase8_10_river_scene.py` (1 fix — bumped multi-day fixture dates from May 7-9 to Dec 1-3 because parser rejects past `start_d`). **Cache-pollution autouse fixture** (Cursor) added `delete()` against `LlmResponseCache` rows in `tests/test_phase2_integration.py` to clear `app.chat.llm_cache` between tests so the shared canonical query `"What is fun to do this weekend?"` (warmed by `test_voice_trailing_question_guard` / sid `p2-v-17`) doesn't pollute downstream graceful-fallback tests; corrected one assertion (`tier_used == "3"` was the bug — the cache-hit shortcut had been masking the real exception path which leaves `tier_used == "placeholder"`). **Lane #44** (Claude Code) entity-matcher near-match severe-typo regression — diagnosed root cause as commit `10d0ecb` introducing a `_TYPO_PER_TOKEN_THRESHOLD = 80.0` precondition that dilutes severe typos against long needle phrases; shipped `_best_partial_ratio_per_needle_token` helper at `app/chat/entity_matcher.py:193` taking max `partial_ratio` against each ≥3-char needle word with full-phrase fallback for short canonicals; both guard call sites at `_best_score:248` and `_typo_path_passes_guard:305` use the helper. **Independent code review + voice-battery investigation** flagged a real connector-word bypass — `_best_partial_ratio_per_needle_token` materially weakens the guard for any needle containing a 3-char connector word (`"and"`, `"the"`, `"for"`, `"jiu"`, `"bmx"`); two confirmed production wrong-entity matches against the live 2,232-provider catalog (`"sloane number"` → `Number One Nails` 75.9; `"phone for addrss"` → `Ross Dress for Less` 72.7). Decision: keep #44 as-shipped (full suite is 1314/1314 green; affected query patterns are bounded edge cases) and address via **Backlog #46** in a focused follow-up lane. **Lane #41b** (Cursor) verified the working tree already matched the lane intent — `tier3_handler.py::date_context` already aware, no `try/except TypeError` shields existing in `confidence_tier.py`, `disclosure_render.py::_temporal_overlap` correctly normalizes naive `date_context` for unit tests (kept). No diff applied; Phase 1 surface 163 passed; Phase 1 close. **Operator enrichment tooling** (general-purpose agent) shipped six new files — `templates/enrichment/business_enrichment_template.csv` + `README.md`, `scripts/ingest/__init__.py`, `validate_enrichment_csv.py` (~290 lines, NANP/category/ISO-8601/email/length validation; refuses to write), `ingest_enrichment_csv.py` (~270 lines, idempotent upsert keyed on case-insensitive `(provider_name, category)`, `--dry-run` flag, transaction-wrapped, INSERT/UPDATE/SKIP-NOOP logging), `tests/test_enrichment_ingestion.py` (16 passing). Five sensible-default decisions baked in; verification_method vocab mapping (operator-friendly `phone_call`/`in_person`/`web_form_submission`/`email_confirmation` → DB enum `manual`/`owner_confirmed`) is lossy; tracked as **Backlog #45** for proper schema fix via new migration. **Phase 1 deploy runbook audit** (Claude Code) found 1 BLOCKER + 3 Important + 2 Nits; full-suite re-run during audit confirmed `1314 passed, 0 failed`. **Cowork primary corrections** edited `docs/maintainability/phase1_deploy_runbook.md` to fix all six issues — §6.3 smoke #1 reclassified as expected-suppress with Phase 1 scope note explaining `GENERIC_CATEGORY` regime is reserved for Lane X3 / Phase 2; §7 replaced "12 pre-existing failures" framing with full-suite-green status and prominent #46 caveat; §8 added §8.2 documenting the operator enrichment toolchain workflow; §10 collapsed three #41 OPEN bullets into single RESOLVED line and added #45 + #46; §2.2 + §3 commit-grouping nits resolved. **Code fix in same audit lane:** `app/db/chat_logging.py:82` anchored Edit replaced stale `FEATURE_FLAG_AUDIENCE_SIGNAL_PERSIST=true` WARN (the env var was never read anywhere) with correct guidance pointing at `alembic upgrade head`. **Final state:** Phase 1 surface 163/163, full suite 1314/1314, runbook audited and corrected, two new backlog items filed (#45, #46) with reproduction recipes and recommended fixes. Phase 1 code work fully closed; ready for flag flip when operator decides. **Multi-agent topology that worked:** Cowork primary as single source of truth for cross-lane state and BACKLOG/STATE integration; Cursor for focused-file edits; Claude Code for heavy investigation lanes (triage, audit, root-cause analysis); general-purpose agents (spawned via Cowork's Agent tool) for independent code or review lanes returning text reports for primary integration. Anchored `Edit` over full-file `Write` on shared files held throughout — no truncation collisions this session despite ~10 lanes in flight. Eight ship-log entries appended to `docs/BACKLOG.md`; two new backlog items appended (#45, #46).

- **UI Data Correctness pass + visitor-mode prep (2026-05-08)** — Phase 1 of the *Ask Hava — Detailed Plan* (`ask-hava-detailed-plan.docx` at repo root). Four parallel lanes shipped against `app/home/queries.py` and adjacent: **Lane A** added `_category_label()` helper + widened `CATEGORY_LABELS` (twelve common slugs explicit) + hardened `_card_blurb` sanitizer (`_LABEL_LINE_RE` strips labelled-field dumps; `_URL_RE` widened for schemeless `www.*` and bare-domain fragments; trailing-fragment trim; venue+date fallback for empty Event blurbs). **Lane B** rewrote `tonight()` with time-of-day filter (`or_(start_time IS NULL, start_time >= effective_floor)`), added `tonight_or_today_label(now)` (≥16:00 → "Tonight" else "Today"), soft venue de-dup (3× fetch → one-per-`location_name` walk → backfill); wired the heading via `app/home/router.py` and `app/templates/home.html`. **Lane C** added `_PLACEHOLDER_PHONE_RE` NANP-reserved guard in `_format_phone`, conditional `tel:` rendering with `.phone-missing` fallback, plus `scripts/cleanup/null_placeholder_phones.py` operator script (`--dry-run`/`--apply` with timestamped logs). **Spec marked RESOLVED** at `docs/maintainability/ui_data_correctness_spec.md` with a "lessons learned" coordination note: parallel agents on shared files must use anchored Edit, never Write — full-file Write truncations broke `queries.py`, `tests/test_home_queries.py`, `BACKLOG.md`, and migration `2a3b4c5d6e7f` mid-flight during this session. **Sponsor Phase 2B migration restored (`2a3b4c5d6e7f_evolve_sponsors_for_four_tier_inventory.py`, 110 lines)** from commit `3c55cf9` — the source had been deleted while compiled `.pyc` artifacts remained, causing 30+ `duplicate column name: slot` failures across `test_programs.py` and adjacent. **Lane S3 audience-signal logging slice** shipped ahead of schedule (pulled forward from Phase 2): new `app/chat/audience_signal.py` (306 lines) computes `visitor`/`local`/`ambiguous` from CDN geo headers + Lake Havasu local time + Lake Havasu seasonal calendar + visitor/local keyword scan; `unified_router.route()` widened with optional `request_headers`/`client_ip`/`accept_language` kwargs; `chat_logging.py` writes `chat_logs.audience_signal` defensively (`hasattr` gate + one-shot WARN until Lane S1's column lands). **Two specs landed:** `docs/maintainability/ui_data_correctness_spec.md` (RESOLVED) and `docs/maintainability/disclosure_renderer_spec.md` (1,002 lines, status OPEN — Phase 1 keystone for monetization defensibility). Phase 1 schema + renderer keystone closed in the same session: **Lane S1** (revision `f7e8d9c0b1a2`) added `Provider.last_verified_at` + `Provider.verification_method` (CHECK), `Event.last_verified_at`, `Sponsor.verified_fields_present` (NOT NULL, server default false), `chat_logs.audience_signal` (CHECK), plus partial index `ix_chat_logs_audience_signal`; SQLAlchemy models updated with matching nullability/lengths/defaults; `alembic upgrade head` / `downgrade -1` / re-`upgrade head` round-trip clean. **Lane S2** shipped `app/chat/disclosure_render.py` (376 lines, deterministic renderer with `PlacementRegime` enum, frozen `SponsoredBlock`, `DISCLOSURE_WORD = "Sponsored"` module constant, `select_placement_regime`, `render_sponsored_block`, tone-allowlist enforcement, `_pick_sponsor` deterministic ordering, `_eligible` regime gating, `is_renderer_enabled()` reading `FEATURE_FLAG_DISCLOSURE_RENDERER`) + `tests/test_disclosure_render.py` (32 tests across 8 categories per spec §6) + `tests/fixtures/disclosure_regression_golden.json`. The renderer reads `Sponsor.verified_fields_present` defensively via `getattr(default=False)` so it compiles regardless of S1 timing; with S1 in production, the field returns its actual value. **Lane S3.1** (API-route header forwarding) shipped — anchored Edit on `app/api/routes/chat.py::post_concierge_chat` forwards `request_headers`, `client_ip`, `accept_language` into `route()` so `audience_signal.classify_audience()` sees real CDN headers (`CF-IPCountry`, `X-Vercel-IP-Country`, `X-Forwarded-For`, `X-Zip`, `Accept-Language`) and `geo_bucket` reflects actual visitor geography rather than defaulting to `unknown`. New `tests/test_chat_route_audience_forwarding.py` integration test (Backlog #37 RESOLVED). **Lane CT1** (confidence-tier classifier module) shipped as well — pure deterministic `app/chat/confidence_tier.py` (304 lines) with `ConfidenceTier` enum, `ConfidenceAssessment` dataclass, `classify_confidence()`, `is_stale()`, `hedge_phrase()`. 22 tests. Companion spec `docs/maintainability/confidence_tier_integration_spec.md` (~410 lines) authored for the formatter integration (Lane CT2 — split into CT2.A Tier 2 + CT2.B Tier 3, gated on the eventual `FEATURE_FLAG_CONFIDENCE_TIER` env var). **Lane X2** (disclosure renderer Tier 3 wiring) shipped — `app/chat/tier3_handler.py` 206 → 331 lines (+125), three new private helpers (`_format_sponsored_block`, `_inject_sponsored_block`, `_maybe_render_sponsored_block`), new optional `organic_context` kwarg on `answer_with_tier3`. Renderer fires once per request when `FEATURE_FLAG_DISCLOSURE_RENDERER=true`; injection happens at both cache-hit return and post-`cache_store` LLM return so cached LLM text stays free of sponsor-injected output. Regime-aware injection: `EMERGENCY_URGENT` prepends, `GENERIC_CATEGORY` injects after the first sentence. Renderer exceptions caught at the helper boundary and logged at WARN — chat path can never crash on sponsored work. New `tests/test_disclosure_render_integration.py` (377 lines, 8 tests) covers: flag-default-off (renderer never invoked, asserted via spy), GENERIC_CATEGORY injection, EMERGENCY_URGENT prepend, SPECIFIC_QUALITY suppression, tone-violation suppression, no-eligible-sponsors passthrough, renderer-raises → WARN-only, disclosure word verbatim. **All Phase 1 keystone work complete: 113 passed across the seven Phase 1 test files** (`test_home_queries.py`, `test_home_queries_lane_a.py`, `test_audience_signal.py`, `test_disclosure_render.py`, `test_disclosure_render_integration.py`, `test_confidence_tier.py`, `test_tier3_handler.py`). Five new backlog items filed today: **#37 RESOLVED** (Lane S3.1 API route forwarding); **#38 OPEN low-priority** (`request_time_utc` parameter rename in `audience_signal.py`); **#39 DEFERRED to Phase 2** (thread audience signal into disclosure-renderer placement-regime selection); **#40 OPEN** (Lane X2.1 — wire `organic_context` from `app/chat/unified_router.py` into `answer_with_tier3` so `EMERGENCY_URGENT` regime can render organic alternatives in production); **#41 OPEN** (Lane S1.1 — single migration adding `timezone=True` to `Provider.last_verified_at`, `Event.last_verified_at`, `Sponsor.starts_at`, `Sponsor.ends_at` to remove the naive/aware-datetime workarounds in CT1, X1, and X2). Three lanes shipped behind feature flags (default off): `FEATURE_FLAG_DISCLOSURE_RENDERER` (X1 + X2 + X2.1), `FEATURE_FLAG_CONFIDENCE_TIER` (CT1 + CT2.A + CT2.B), and the audience-signal logging lane (Lane S3 + S3.1, persistence resolved by Lane S1). **Phase 1 keystone work complete on the code side.** Three additional lanes landed in the same session after the previous STATE update: **Lane S1.1** (revision `b4c5d6e7f8a9`) altered `Provider.last_verified_at`, `Event.last_verified_at`, `Sponsor.starts_at`, `Sponsor.ends_at` to `DateTime(timezone=True)` so Postgres stores TIMESTAMPTZ and the temporal-overlap workarounds in `disclosure_render._temporal_overlap`, `confidence_tier.classify_confidence`, and `tier3_handler._maybe_render_sponsored_block` can be removed in a follow-up cleanup (Backlog #41a). **Lane CT2.A** wired the confidence-tier classifier into `app/chat/tier2_formatter.py` (159 → 244 lines) — `is_confidence_tier_enabled()`, `_annotate_rows_with_confidence_hint()`, deterministic `_enforce_low_tier_phone()` post-processor, plus EXCEPTION clause in `prompts/tier2_formatter.txt` and 10 integration tests. **Lane CT2.B** wired the same classifier into `app/chat/context_builder.py` for Tier 3 (200 lines) — `_hedge_suffix_for()` per-record helper, parenthetical hedge suffixes on Provider and Event context lines, plus instruction block in `prompts/system_prompt.txt` and 11 integration tests. **Lane X2.1** added `_organic_context_for_tier3()` to `app/chat/unified_router.py` (+148 lines) and threaded `organic_context` through all four `answer_with_tier3` call sites in `_handle_ask`, unblocking the `EMERGENCY_URGENT` regime in production. **Combined Phase 1 test surface: 130 passed across ten test files.** Backlog deltas this session: **#40 RESOLVED** (X2.1 closed the organic_context wiring); **#41 RESOLVED** (S1.1 closed the timezone migration); **#41a OPEN** (SQLite tz workaround cleanup — TypeDecorator strategy recommended); **#42 OPEN** (CT2.B.1 — phone post-process on Tier 3 path; row-threading strategy needed); **#43 OPEN** (URGENT_NOW sub_intent classifier extension so "right now" / "urgent" / "ASAP" phrasing routes to EMERGENCY_URGENT instead of OPEN_ENDED → SPECIFIC_QUALITY). Twelve pre-existing failures in the broader suite (entity matcher, phase2 integration, river_scene, tier2 catalog render, unified router) confirmed unrelated to this session's lanes. Five ship-log entries in `BACKLOG.md`: Lane C, Lane A, Lane B, Sponsor Phase 2B migration restored, Lane S3 audience signal.

- **Parks-rec ingestion (WebTrac + Aquatic Center) + Tier 2 surfacing fixes (2026-05-07)** — End-to-end pipeline that pulls every registerable activity from Lake Havasu City's Vermont Systems WebTrac registration system at `register.lhcaz.gov` and the Aquatic Center weekly class schedule at `lhcaz.gov/parks-recreation/open-swim-schedule`, snapshots them as timestamped JSON, and loads through the existing **`Contribution → approve_contribution_as_event/_program`** flow. Single-day sections route to **`Event`**, recurring sections (date-range + multi-day pattern) route to **`Program`**. Filters Unavailable / Full / Pool-Closed / private-practice rows out of the catalog at load time; the chat layer reads the snapshot for direct-ask carve-outs. Each event/program is tagged with content category (**`arts`**, **`sports`**, **`aquatics`**, **`food`**, **`fitness`**, **`recreation`**) and audience (**`adult`** / **`youth`** based on program-name keyword match). New modules: **`app/contrib/webtrac.py`**, **`app/contrib/lhcaz_aquatic.py`**, **`app/contrib/scrape_runner.py`**, **`app/contrib/parks_rec_loader.py`**. New CLIs: **`scripts/run_scrapes.py`**, **`scripts/parks_rec_load.py`**, **`scripts/parks_rec_reset_and_reload.py`**, **`scripts/webtrac_pull.py`**. New tests: **`tests/test_webtrac_parser.py`** (9 + 5 state-coverage) and **`tests/test_lhcaz_aquatic_parser.py`** (10) covering the real Vermont Systems `itemstatus--available / --unavailable / --full` markup. Scheduled refresh every 6h via **`.github/workflows/parks-rec-scrapes.yml`** (GH Actions cron, hits production Postgres via **`DATABASE_URL`** repo secret; idempotent — re-running a snapshot writes 0 rows because dedup short-circuits on **`source_url`**). Initial real load wrote **14 events + 7 programs** (WebTrac) + **107 events** (Aquatic) into production. Companion chat-layer fixes (Cursor) in **`app/chat/tier2_db_query.py`** + **`app/chat/tier2_handler.py`**: SQL prefilter now checks tags in addition to title/description (was excluding tag-only matches like Adult-tagged WebTrac rows); exact-date listings no longer chronologically cap at 8 in a way that hid afternoon WebTrac classes behind morning aquatic rows; **`this_weekend`** window now includes Friday; deterministic month/day correction prevents the LLM/router from rewriting explicit dates like "Friday May 8" to a different week; **`art classes`** free-text normalizes to **`category='arts'`**; temporal queries return dated events only (undated programs/providers no longer crowd through the LLM formatter). Production smoke against the live Railway URL confirmed Hava surfaces WebTrac events for content, audience, and date queries. **Authoritative companion doc:** **`docs/scrapes.md`**.

- **`531dd13`** — **Slice 71b: orphan-helper cleanup + decision-doc §10 completion** — Drops **`open_ended_search_message`** from **`app/core/intent.py`** (orphaned after Slice **71** removed the **`search.py`** pipeline). **`intent_module_disposition_decision.md`** §10 lists full shipping-SHA chain; §8 Verification posture unchanged (was already present). **`docs/components/intent.md`** trimmed to a single public entry point. **`docs/BACKLOG.md`** #36 resolution paragraph extended. Pytest **921 passed**, **5 deselected**; **ruff clean**. Doc-and-delete only; no new runtime paths vs Slice **71**.

- **Backlog #36 close — dormant `intent.py` + `search.py` surface deleted (Slice 71)** — Substantive refactor **`c8cc2db`** implements Option A approved 2026-05-06 at §7 of **`intent_module_disposition_decision.md`** (scope covers both modules per Slice 70 peer review). **`app/core/intent.py`** retains only Tier 1 pre-screen **`detect_out_of_scope_category`** (+ commercial-services helpers/rescue tuples) and disposition-parity **`open_ended_search_message`** (no remaining `app/` importer after **`search.py`** pipeline removal). **`app/core/search.py`** retains only **`_deterministic_embedding_1536`** (admin synthetic embeddings). Test fallout beyond the four bootstrap-named files was load-bearing: entire **`test_phase5.py`** removed; **`test_phase8_9_event_ranking.py`** reduced to recurrence-only coverage; **`test_phase87_privacy.py`** drops tests that imported **`emit_search_diag_embedding_block`**. Local verification: **`python -m pytest -q -m "not integration"`** → **921 passed**, **5 deselected**; **`python -m ruff check`** clean; API smoke on **`POST /api/chat`** (out-of-scope weather query → **`sub_intent`** **`OUT_OF_SCOPE`**; Tier 2 weekend listing; Tier 3 wiring probed with Tier 2 patched to miss); **`_deterministic_embedding_1536`** unit-normal smoke. **`cbe1779`** corrects decision-doc §10 substantive SHA bookkeeping. BACKLOG #36 closed **`f8a5126`**. STATE close-out revision is the **`docs(STATE)`** row at top of **`Recent commits`** below.

- **`45d2e2e`..`2256ad4`** — **Phase C component-docs growth: `app/` entry + `app/api/routes/` batch (Slice 69)** — Six new component docs covering the FastAPI app entry point and the four JSON API route modules — closes the doc gap on **`app/`**'s actual HTTP entry surface (the surface readers hit when the chat UI calls **`POST /api/chat`** or admin pages submit JSON). Suggested write order ran small-to-big to calibrate voice on the smaller files first. **Group A (small):** **`bootstrap_env.md`** (~59 lines doc / 21-line module) documents `ensure_dotenv_loaded()` — the idempotent, `override=False` `.env` loader that runs before any `app.*` import in `main.py` (load-bearing for the per-file E402 ignore in `pyproject.toml`). **Group B (route modules):** **`chat_route.md`** (~98 / 119) documents `POST /api/chat` (120/min slowapi limit — the inline comment + module docstring explain the deliberate divergence from Phase 2.3's stated 5/min), the dispatch to `unified_router.route`, and the Tier-3-only `BackgroundTasks` hook to `scan_and_save_mentions`. **`contribute_route.md`** (~129 / 285) flags the public intake's actual shape — HTML form (despite living under `app/api/routes/`), custom DB-tracked IP-hash limiter (1 submission/hour via `count_submissions_since_by_ip_hash`, NOT slowapi), and the form-state-preservation pattern through `_render_contribute_page`. **`admin_contributions_route.md`** (~114 / 133) documents the five-endpoint `/admin/api/contributions*` JSON API with the local `require_admin` cookie dependency, the 202 Accepted return on the enrich endpoint, and pairs explicitly with the HTML surface at **`admin_contributions_html.md`**. **`admin_mentions_route.md`** (~119 / 152) documents the four-endpoint `/admin/api/mentioned-entities*` JSON API including the promote handler's `Contribution` creation with `source="llm_inferred"`, the unreviewed-only state guard, and the `_parse_day_bounds` 422 path. **Group C (heavyweight):** **`main.md`** at **159 lines** (well under the 300-line halt threshold; structure held cleanly without needing a multi-doc split) documents the post-Slice-51 FastAPI app shell — six app-level routes (`/`, `/privacy`, `/terms`, `/health`, `/events`, `/events/{event_id}`), two exception handlers (`RateLimitExceeded` + `RequestValidationError` via `friendly_errors`), six included routers, the `/static` mount, the lifespan-driven `init_db()` + hourly `run_expired_review_cleanup` background task, the Sentry init with `scrub_sentry_event` / `scrub_sentry_breadcrumb` chat-body PII hooks, and the constrained Markdown-subset parser (`_render_doc_markdown_to_html`) feeding the three Jinja2 templates (`privacy_doc.html`, `event_not_found.html`, `event_permalink.html`). Each route doc cross-references upstream dependencies; the two admin route docs explicitly cross-reference their HTML pair docs from Slice 60. **Indexed in `docs/maintainability/project_index.md`** (6 new rows in the component-docs section, alphabetized within the new batch). **Total docs: 64 → 70.** Standard close-out adds **`docs/STATE.md`** after substantive **`45d2e2e`** and Backlog tick **`2256ad4`**. **Bootstrap-vs-reality corrections** captured in the docs (rather than fighting the bootstrap): `chat.py`'s rate limit is 120/min not 30/min (with the deliberate divergence rationale); `contribute.py` is HTML not JSON; `main.py` carries 6 app-level routes not the bootstrap's 8 (no `/calendar`, `/programs`, `/event-not-found` direct mounts — those are router-included or template-rendered) and uses 3 Jinja templates (`/privacy` and `/terms` share `privacy_doc.html`); `require_admin` cookie helper is duplicated across `admin_contributions_route.py` and `admin_mentions_route.py` rather than imported from `app/admin/auth.py`. Shipped in parallel with Slice 70 (peer-review quality pass) — both slices' deltas preserved cleanly via rebase-and-append on the three shared close-out files. Doc-only ship; pytest **965 passed**, **5 deselected** (unchanged vs Slice 65 baseline); ruff clean. CI **success** on substantive **`45d2e2e`**.

- **`de2df9b`** — **Component doc quality pass: `search.md` peer review + `dedupe.md` cross-ref (Slice 70)** — Addresses Cursor's review of the Slice **67b** `search.md` heavyweight: factual fixes (General group order, four slot booleans, OpenAI-only vs early-exit outcome nuance, §Related test scope), lead copy for four `SearchOutcome` flags, and a load-bearing dormancy note that **`app/core/search.py`**'s retrieval pipeline is not exercised by chat runtime (Tier 2 SQL + Tier 3 in `app/chat/` replaced it); only **`_deterministic_embedding_1536`** is imported for admin synthetic embeddings. **`dedupe.md`** stops claiming **`find_duplicate`** is called from **`search.py`** (both Purpose ~line 9 and Related ~line 77). Feeds **Backlog #36** disposition context alongside Slice **68**'s `intent.py` decision doc — Casey's §7 still gates implementation. Doc-only; pytest **965 passed**, **5 deselected**; **ruff clean**. CI **success** for pushed SHA ([run](https://github.com/casey451/havasu-chat/actions/runs/25460294357)).

- **`f7ed055`** — **Decision doc: `app/core/intent.py` disposition (Slice 68, Backlog #36)** — Surveys the dormant deterministic cascade (`detect_intent` + helpers + twelve labels) versus the two production imports (`detect_out_of_scope_category`, `open_ended_search_message`). Four options (A delete dead surface, B LLM-down deterministic fallback, C document dormant, D hybrid trim). §7 remains **placeholder** until Casey signs; `.cursorrules` stale Phase **8.5** intent line called out but **not edited** in this slice (implementation-side change). Indexed in **`docs/maintainability/project_index.md`**. Doc-only; pytest **965 passed**, **5 deselected**; **ruff clean**.

- **`5251ffa`..`dd8c175`** — **Phase C component-docs growth: `app/core/` heavyweights batch (Slice 67b)** — Four heavyweight `app/core/` modules covered in extraction → slots → intent → search write order (calibrating voice on the smaller heavyweights before tackling `search.py`'s 1,025 lines). **`extraction.md`** (~125 lines) documents the OpenAI-driven event-detail extraction with the LLM-or-fallback two-path merge (heuristic floor for required fields; LLM ceiling for description/event_url/contacts) and the load-bearing 1536-dim vs 32-dim embedding asymmetry between the OpenAI and deterministic-fallback paths. **`slots.md`** (~134 lines) documents the four `extract_*` slot extractors, the `FAMILY_ALIASES` priority order, the `_term_matches_in_text` length-based word-boundary switch (prevents "gym" matching inside "gymnastics"), last-write-wins merge semantics, and the `FAMILY_ALIASES` ↔ `app.core.search.ACTIVITY_TYPES` key coupling. **`intent.md`** (~162 lines) documents all twelve intent labels, the fifteen-step `detect_intent` cascade with order-load-bearing notes, and the relationship paragraph distinguishing this module (regex/heuristic, deterministic) from `app/chat/intent_classifier.py` (LLM-driven Tier 1 classifier) — they share `detect_out_of_scope_category` but answer different questions. Notes that `detect_intent` itself has no live runtime caller in `app/` post the legacy `/chat` removal (only tests + the intent_classifier helper reuse). **`search.md`** (~202 lines, the heavyweight) documents the ten-stage `search_events` pipeline (embedding gen, candidate gather, activity filter, literal-match filter, embedding split + scoring with +0.5 specific-phrase bonus, threshold filter with separate OpenAI vs deterministic logic, keyword path, merge with id-dedup, time-scoped sort with recurring deprioritization, three outcome flags), the `SearchStrategy` decision dispatch, the layered-by-event-count `format_search_results` formatter, and the deliberate 1536-dim choice on the query side that mirrors `extraction.py`'s asymmetry. Stayed at 202 lines — well under the bootstrap's 400-line halt threshold; section boundaries held cleanly without needing a multi-doc split. **Indexed in `docs/maintainability/project_index.md`** (4 new rows appended after Slice 57's confabulation_* rows). **`app/core/` component-doc coverage closes to 17/17** (4 pre-existing + 9 from Slice 67a + 4 from this slice). **`docs/components/` holds 64 notes.** Standard close-out adds **`docs/STATE.md`** after substantive **`5251ffa`** and Backlog inventory **`dd8c175`**. Doc-only ship; pytest **965 passed**, **5 deselected** (unchanged vs Slice 65 baseline); ruff clean.

- **`e8a6313`..`17a4767`** — **Phase C component-docs growth: `app/core/` small+mid batch (Slice 67a)** — Nine docs cover **`llm_http`** (shared SDK read timeout), **`timezone`** (`America/Phoenix`), **`field_tracking`** (tracked-field tuples), **`provider_name`** (Unicode + legacy suffix normalization), **`event_recurrence`** (substring + whole-word recurrence heuristics), **`search_log`** (**`SEARCH_DIAG_VERBOSE`** file logging), **`conversation_copy`** (casual user-visible strings; constants listed by name in-doc), **`program_search`** (program scoring/cards + **`slots.expand_query_synonyms`** — see forthcoming **`slots.md`** in Slice **67b**), **`session`** (in-memory TTL/session dict caveats). **`docs/components/` ~60** after substantive; **`Slice 67b`** closes four heavyweight **`app/core/`** modules without overlapping filenames. Standard close-out after substantive **`e8a6313`** and BACKLOG **`17a4767`**. Doc-only; pytest **965 passed**, **5 deselected**; **ruff clean**.

- **`b4b83f9`..`6cb24c2`** — **Static-html extraction campaign step 3/3 (CLOSE): bridge refactor + IIFE drop (Slice 65, Backlog #35; campaign #31 closed)** — Third and closing implementation slice of the static-html extraction campaign (Option A approved 2026-05-06 at §7). Two changes in two files: (1) `app/static/js/calendar.js` drops the IIFE wrapper that Slice 61 preserved for the top-level early-return guard; the guarded body now lives inside `function initCalendar()` returning the calendar API object `{ open, close, selectDay }` or `null` when the DOM check fails, and the module-level export is `export const havasuChatCalendar = initCalendar()`. (2) `app/static/js/chat.js` adds a top-level `import { havasuChatCalendar } from "./calendar.js"` and drops the `window.` prefix on both occurrences in the `data.open_calendar` branch (the defensive null-check stays — the export can be `null` when the calendar DOM isn't present at module-load time, an extreme edge case the check costs nothing to handle). Header comments on both files rewritten to reflect the new shape. **`app/static/index.html` NOT touched** — the two `<script type="module">` tags from Slice 61 already supported the new export/import contract; Step 5 byte-equivalence verified hashes byte-identical (`8d1bc572...da65` both pre and post, 1981 bytes). **Behavior parity verification (load-bearing — runtime-behavior refactor):** local UI smoke covered chat send/receive, onboarding chips, Tier-3 thumb feedback, calendar manual open + select + event injection + Escape close, **and the bridge path** (chat response with `data.open_calendar=true` triggering the calendar overlay) — all worked identically pre and post. DevTools console: `window.havasuChatCalendar` returns `undefined` post-refactor; the global is cleanly gone. `/static/js/calendar.js` HTTP 200 9318 bytes (was 9137; +181 from comment rewrite + IIFE→function/export structure); `/static/js/chat.js` HTTP 200 13691 bytes (was 13424; +267 from comment rewrite + import line). **Pytest 965 passed, 5 deselected** (unchanged from Slice 63 baseline). **Ruff clean.** Pre-flight: Slice 63 production-verified by Casey on 2026-05-06 (page renders identical, `/static/styles/index.css` 200, console clean). **Backwards-compatibility note:** any external code depending on the `window.havasuChatCalendar` global (devtools snippets, browser extensions, unknown user JS) breaks post-refactor; internal-app surface, acceptable per Casey's call at campaign approval. **Campaign #31 close package** (`6cb24c2`): Backlog #35 filed/resolved with full Slice 65 detail; Backlog #31 Campaign progression block flips Step 3/3 from PENDING to shipped with a CLOSED 2026-05-06 marker and end-state summary; Backlog #34 cross-reference updated to point at #35; Backlog #33's resolution gains a "Production verified 2026-05-06 (Casey)" line; `docs/maintainability/static_html_extraction_decision.md` Status moves from "Decided" to "Implemented (campaign closed 2026-05-06)" with a new §10 Outcome section listing all four shipping SHAs (precursor `7d57876` + Slice 61 `65f71e8` + Slice 63 `17b679e` + Slice 65 `b4b83f9`) and four campaign-vs-sketch surprises (the unanticipated `StaticFiles` mount addition in Slice 61, calendar.js IIFE preservation through Slice 61, the explicit-import bridge mechanism in Slice 65, and no `shared.js` materializing). Matches Campaign #30's close pattern at `a8fcbf5`. **End state:** chat UI is a clean three-file vanilla structure (`index.html` 45-line markup shell + `js/chat.js` + `js/calendar.js` ES modules with explicit imports + `styles/index.css`); no inline anything; no global-namespace bridge. Step 8 production verification is Casey-driven post-deploy: manual UI smoke against Railway's live URL with explicit attention to (a) the bridge path (chat response triggering calendar open) and (b) confirming `window.havasuChatCalendar` is `undefined` in the live console; rollback is `git revert b4b83f9` if any feature regresses.

- **`5ec29e1`..`738383c`** — **Phase C component-docs growth: API surface batch (Slice 66)** — Six notes document **`app/programs/router.py`** (`/programs` JSON + `/programs/submit` HTML, slowapi limits) and five Pydantic modules under **`app/schemas/`** (`chat`, `contribution`, `event`, `llm_mention`, `program`), including post–campaign **#30** **`Program`** **`time`** + **`HH:MM`** parse/serialize behavior (**`docs/components/schema_program.md`** ↔ **`docs/maintainability/schema_time_harmonization_decision.md`**). **`app/programs/` component-doc coverage 1/1; `app/schemas/` 5/5.** **`docs/components/` holds 51 notes.** Standard close-out adds **`docs/STATE.md`** after substantive **`5ec29e1`** and inventory **`738383c`**. Doc-only ship; pytest **965 passed**, **5 deselected** (unchanged vs Slice 61 baseline).

- **`17b679e`..`5264c16`** — **Static-html extraction campaign step 2/3: CSS to /static/styles (Slice 63, Backlog #34; campaign parent #31)** — Second implementation slice of the static-html extraction campaign (Option A approved 2026-05-06 at §7). Lifts the inline `<style>` block from `app/static/index.html` (lines 8–400, 393 body lines, ~9.5 KB of CSS) into a new `app/static/styles/index.css` (9767 bytes total — 393 body lines copied verbatim, plus a 6-line traceability header). `index.html` collapses 439 → 45 lines (-9493 bytes) with the inline `<style>...</style>` block replaced by a single `<link rel="stylesheet" href="/static/styles/index.css" />` in the same head position between `<title>` and `</head>`; absolute href so the path holds even if the chat UI is later mounted under a subroute. **`app/main.py` NOT touched** — Slice 61's `app.mount("/static", StaticFiles(directory=_STATIC_DIR), name="static")` already serves `/static/styles/*` via natural subdirectory traversal (curl-probed at Step 1 to confirm 200 + `text/css; charset=utf-8`). **Behavior parity verification (load-bearing):** GET `/` response diff shows common prefix 192 bytes (HTML head through the leading `<` of the swap), removed 9548 bytes (the inline `<style>...</style>` block content + closing tag), added 55 bytes (the `<link rel="stylesheet" ...>` tag), common suffix 1734 bytes (everything from the tag-closing `>` onward — `</head>`, `<body>`, the entire body content, the two module script tags, `</body>`, `</html>`); zero accidental drift; only the style→link delta. `/static/styles/index.css` returns HTTP 200 with content-type `text/css; charset=utf-8`. **Local UI smoke (Step 4, Casey-driven):** chat shell, onboarding chips, message bubbles, calendar grid + day-detail cards, mobile responsive layout all visually identical pre and post; browser console clean, no JS errors; Network tab shows the stylesheet 200 with initiator `index.html`. **Pytest 965 passed, 5 deselected** — unchanged from Slice 61 baseline. **Ruff clean.** Pre-flight gate: Slice 61 production-verified by Casey on 2026-05-06 (both `/static/js/*.js` returned 200 from Railway, console clean, end-to-end smoke green) — that confirmation cleared the precondition because this slice leans on the same `StaticFiles` mount for the new `/static/styles/` subdirectory. Step 8 production verification is Casey-driven post-deploy: manual UI smoke against Railway's live URL covering chat shell visual fidelity + Network tab confirms `/static/styles/index.css` 200; rollback is `git revert 17b679e` if any visual regression appears (the inline `<style>` block returns intact). Campaign status: step 1/3 (JS) shipped at `65f71e8` and prod-verified; step 2/3 (CSS) is this slice; step 3/3 (shell cleanup + `window.havasuChatCalendar` bridge refactor + naming) remains PENDING.

- **`cd36d0b`..`7bfb3b9`** — **Phase C component-docs growth: `app/db/` batch (Slice 64)** — Five component docs cover unified-router chat logging (`log_unified_route`), Contribution queue helpers, SQLAlchemy bootstrap (`engine`, `SessionLocal`, `Base`, `get_db`, `init_db`), LLM mention persistence, and a **`models.md`** deep reference for all seven ORM mapped tables (relationships, indexes, Alembic pointers, alignment with `HAVA_CONCIERGE_HANDOFF.md` §4). **`app/db/` component-doc coverage is 5/5** (was 0/5). **`docs/components/` holds 45 notes.** Standard close-out adds **`docs/STATE.md`** (this revision) after substantive **`cd36d0b`** and Backlog inventory **`7bfb3b9`**. Doc-only ship; pytest **965 passed**, **5 deselected** (unchanged vs Slice 61 baseline).

- **`7d57876`..`90ae1a9`** — **Static-html extraction campaign step 1/3: JS to ES modules (Slice 61, Backlog #33; campaign parent #31)** — First implementation slice of the static-html extraction campaign approved at §7 of `docs/maintainability/static_html_extraction_decision.md` (Option A — vanilla split assets — recorded in precursor `7d57876` after Casey's 2026-05-06 approval). Lifts `app/static/index.html`'s ~696 lines of inline chat + calendar IIFEs into `app/static/js/chat.js` (443 lines, 13424 bytes; bare module — IIFE wrapper dropped because ES modules have their own scope) and `app/static/js/calendar.js` (262 lines, 9137 bytes; **IIFE wrapper preserved** because the body has a top-level `if (!overlay || !btn) return;` guard at original line 887 that cannot live at module top level). `index.html` collapses 1133 → 439 lines (-25085 bytes), with the inline `<script>...</script>` block replaced by two `<script type="module" src=...>` tags (calendar listed first to signal expected initialization order — calendar writes `window.havasuChatCalendar`, chat reads it defensively). `app/main.py` gains `from fastapi.staticfiles import StaticFiles` and `app.mount("/static", StaticFiles(directory=_STATIC_DIR), name="static")` after `_STATIC_DIR` is defined; the mount was not previously configured. **No CSS touched** (the inline `<style>` block stays — CSS extraction is step 2/3). **No `shared.js`** — zero shared helpers between the two IIFEs at Step 0 audit. **Behavior parity verification (load-bearing):** GET `/` response diff shows common prefix 11345 bytes, removed 25224 bytes (the inline script block), added 103 bytes (two module tags), common suffix 26 bytes — zero accidental drift; only the script-block delta. `/static/js/calendar.js` returns HTTP 200 with 9137 bytes; `/static/js/chat.js` returns HTTP 200 with 13424 bytes. **Local UI smoke (Casey-driven, Step 4):** chat send/receive, onboarding chips (Visiting/Local → Yes/No → example prompts), Tier-3 thumb feedback, calendar open + select + event injection + Escape/click-outside close all worked identically pre and post; browser console clean, no JS errors. **Pytest 965 passed, 5 deselected** — unchanged from Slice 59 baseline. **Ruff clean.** The `window.havasuChatCalendar` cross-module bridge is preserved as a window global; refactor to explicit imports is deferred to step 3/3 of the campaign so each step has bounded blast radius. Step 8 production verification is Casey-driven post-deploy: manual UI smoke against Railway's live URL covering the same six checks, watching specifically for `/static/js/*.js` returning 200 and a clean browser console; rollback is `git revert 65f71e8` if any feature regresses.

- **`4224433`** — **Phase C component-docs growth: `app/contrib/` helper batch (Slice 62)** — Six component docs cover enrichment orchestration, Places client + URL fetcher, hours structuring, River Scene pull orchestration, and Date-line parsing. With existing `river_scene`, `approval_service`, and `mention_scanner` notes, **`app/contrib/` component-doc coverage is 9/9.** **`docs/components/` holds ~40 notes.** Backlog inventory tick **`eadb35f`**. Doc-only ship.

- **`99d3ecb`** — **Phase C component-docs growth: `app/admin/` HTML-helper batch (Slice 60)** — Five new component docs (`admin_categories_html`, `admin_contributions_html`, `admin_feedback_html`, `admin_mentions_html`, `admin_nav_html`) describe the cookie-gated operator HTML surfaces that pair with Slice 40 `admin_auth` / `admin_router` documentation. `docs/maintainability/project_index.md` gains five index rows; **`docs/BACKLOG.md`** inventory updated in the same Slice 60 bookkeeping push as this STATE refresh (Backlog #18 Phase C). **`docs/components/` now holds 34 component notes.** Doc-only ship; pytest and application code unchanged.

- **`7f30faf`..`bab4619`** — **CI hardening: concurrency group + `gh` CLI verify docs (Slice 59, Backlog #32, #18 Phase D extension)** — Two friction reductions for the CI workflow recurring across recent close-out reports. (a) `.github/workflows/ci.yml` gains a top-level `concurrency` block (sibling of `permissions:` / `jobs:`) keyed on `${{ github.workflow }}-${{ github.ref }}` with `cancel-in-progress: true`. New pushes cancel in-progress prior runs on the same ref; saves CI minutes when a slice ships its substantive → BACKLOG-tick → STATE-close-out sequence in quick succession (only the final run matters for `main`'s green-state). (b) `docs/WORKING_AGREEMENT.md` gains a new `## CI verification` section under the existing `## Verification` block, documenting `gh run list --branch main --limit 1 --json conclusion,headSha,databaseId` as the canonical post-push verification command, with install instructions for Windows (`winget install --id GitHub.cli`), macOS (`brew install gh`), and Linux. Section explicitly forbids declaring a slice shipped without verifying CI status. **Step 0 audit punted on inference** — `gh` is not installed locally so the gh-driven cache-hit audit wasn't runnable; `actions/setup-python@v5` + `cache: 'pip'` is canonical and `requirements.txt` has been stable since Slice 51 (`35cd6ac`), so cache hits should be the default on recent runs; first post-merge CI run is the empirical check. **No production touch; no code change.** Pytest unchanged at 965; ruff clean. Backlog #32 filed and resolved same-slice; #18 Phase D paragraph annotated with the Slice 59 extension reference. **Smoke-verify of the concurrency group is forward-looking** — the next slice's close-out push sequence is the first real test; if older runs show as cancelled in the dashboard, the config is working as intended. **Self-dogfood note:** this slice's own close-out CI verification falls back to the browser dashboard because `gh` isn't yet installed locally; the new docs cover the install for next time.

- **`13c5633`..`f36bb8e`** — **Static index extraction decision filed/closed (Slice 58, Backlog #31)** — Added `docs/maintainability/static_html_extraction_decision.md` as a decision-doc-only slice for `app/static/index.html` extraction planning. Doc characterizes the current monolith from source (1133-line single file, inline CSS + two IIFEs with `window.havasuChatCalendar` bridge and `/api/chat`, `/api/chat/onboarding`, `/api/chat/feedback`, `/events` call surfaces), surveys five options (vanilla split assets, SPA, lightweight reactive, JS-only split, status quo), and recommends **Option A** as the lowest-blast-radius path. Recommendation rationale explicitly preserves the static deploy model (`FileResponse`), distinguishing this from Slice 51's Jinja2 extraction precedent in `main.py`. `project_index.md` gained the maintainability row; Backlog #31 filed and marked RESOLVED in the same slice. Doc-only ship; no runtime behavior changes.

- **`133d83e`..`60c60eb`** — **Phase C component-docs growth: `app/eval/` confabulation harness batch (Slice 57)** — Added five component docs: `docs/components/confabulation_query_gen.md`, `docs/components/confabulation_invoker.md`, `docs/components/confabulation_evidence.md`, `docs/components/confabulation_detector.md`, and `docs/components/confabulation_report.md`. Coverage captures the full harness path (probe generation → invocation modes → Tier 2 evidence capture seam → Layer 1/2/3 detection → JSONL/CSV/summary report writers), includes the `InvocationResult` name-collision adapter note between invoker and detector shapes, and documents direct callers/tests per module. `docs/maintainability/project_index.md` gained five rows in the component index. Follow-up bookkeeping in `docs/BACKLOG.md` ticks Backlog #18 Phase C component-doc growth inventory with Slice 57 commit `133d83e`. Doc-only ship; no runtime code path changes.

- **`632215d`..`a8fcbf5`** — **#30 Slice 56 (campaign CLOSE): drop strings, rename typed → canonical** — Terminal slice of the schema time-type harmonization campaign (Backlog #30, Option B). The dual-write architecture from Slice 53 is reversed; the canonical end state lands in one commit because the steps don't compose safely if separated (intermediate state where ORM expects `Time` but DB stores strings, or vice versa, would be broken). **Schema:** `Program.schedule_start_time` / `schedule_end_time` are now `Time` columns at the canonical names; `_typed` shadow columns dropped; `String(5)` source columns dropped; `@validates` dual-write removed; conversion logic moved to `ProgramCreate.parse_hhmm` (`mode='before'`) at the Pydantic schema boundary. **Migration:** `batch_alter_table` for cross-dialect (SQLite + Postgres) safety; pre-flight `RuntimeError` aborts if any typed column is NULL (Slice 53's backfill should have left zero — defensive gate); `downgrade()` reverses by re-creating strings + backfilling from typed via `strftime`, so `alembic downgrade -1` is the canonical rollback path. **Wire format preserved:** `ProgramRead.serialize_hhmm` field_serializer keeps the `HH:MM` zero-second-suffix on API responses (Pydantic 2 default would have shipped `HH:MM:SS`, which `tests/test_programs.py:134-135` and any external client relying on the same shape would not have accepted). **Bundled fixture sweep:** the substantive commit also includes 9 test files migrated from string-shaped `Program(schedule_start_time="HH:MM", ...)` factories to `time(H, M)` (the factories bypass `ProgramCreate` and would otherwise fail SQLite's "Time type only accepts Python time objects" bind constraint); the bootstrap's >10-hit halt threshold was waived per implementer/Casey discussion (mechanical sweep, low review hazard, CI greenness preferred over two-commit ergonomics). Test suite delta: `test_program_typed_time_dual_write.py` deleted (covered `@validates`); `test_program_schema_time_parsing.py` added (5 tests covering `parse_hhmm` string conversion, time pass-through, and three rejection cases). **Verification (load-bearing):** byte-equivalence diff across 4 reader surfaces (program_dict / tier3_context / admin_renders / tier1_time_window) — every hash byte-identical pre vs post (`f8d245a5...`, `96185cef...`, `a6d643044e7...`, `fe4f343c...`). The Tier 3 capture is the load-bearing one — that block feeds Anthropic Haiku as part of every Tier 3 query. **Pytest 963 → 965** (-3 deleted dual-write tests + 5 new schema tests); ruff clean. **Step 5 of the bootstrap intentionally skipped** — the bootstrap proposed a `time.fromisoformat()` wrap at `app/admin/contributions_html.py:661-662`, but the actual code at that location builds `ProgramApprovalFields` (a separate Pydantic schema with `str` fields), not `Program(...)` directly; the Program write happens downstream in `approval_service.py:140` via `ProgramCreate` which now handles conversion at its boundary. **Production verification (Step 17) is Casey-driven and multi-axis** (the only campaign slice that drops production data columns): Railway deploy + `information_schema` schema check + `COUNT(*) FILTER (WHERE schedule_*_time IS NULL) == 0` data check + admin/Tier-2 UI roundtrip. Rollback: `alembic downgrade -1` then `git revert 632215d`. **Backlog #30 closed:** decision-doc Status changed from "Decided" to "Implemented (campaign closed 2026-05-06)"; new §10 "Outcome" section captures the four shipping SHAs and four campaign-vs-sketch surprises (Slice 55's smaller-than-predicted footprint; Slice 54's +1 Tier 1 reader; Slice 53's SQLite bindparam fix; Slice 56's contributions_html misclassification).

- **`b3ca35d`..`f691aa5`** — **#30 Slice 55 (campaign Phase 3): `app/admin/` display reads migrate to typed columns** — Step 3 of 4 in the schema time-type harmonization campaign (Backlog #30, Option B). Migrates the two admin display-read sites — `_program_card_admin_html` (line 896) and `_queue_program_item_html` (line 1842) in `app/admin/router.py` — from `schedule_*_time` strings to `schedule_*_time_typed` columns. Adds a small file-local `_hhmm_or_empty(t: time | None) -> str` helper. **Slice meaningfully smaller than the §5 sketch suggested:** the decision doc predicted "~6 files; admin-template updates"; reality at `5cc6556` was 2 sites in 1 file because Slice 53's `@validates` dual-write contract handles all admin writers transparently and three other admin categories (form chrome, FastAPI Form parameters, form prefill) legitimately stay string-shaped until Slice 56's canonical rename. **Byte-equivalence verification:** pre-migration hash `a6d643044e749ea...` == post-migration hash `a6d643044e749ea...` over 98 programs × {card, queue} renderings; admin HTML output is byte-identical to pre-Slice-55. **No schema changes; no test changes; no helper escape-posture changes** (each call site keeps its existing escape function — stdlib `html.escape` at site 1, file-local `_escape` at site 2; helper returns bare strings to keep escape responsibility at the call site). Pytest 963 unchanged; ruff clean. **Production verification (Step 11) is Casey-driven and the lightest of the campaign so far** — admin UI only; visual spot-check of the program list page in a browser, no SQL, no chat-tier or LLM concerns. Rollback is `git revert b3ca35d`. **Backlog #30:** Slice 55 ticked at `b3ca35d` (BACKLOG bullet + decision-doc §5 row 55). **Campaign now 3/4 complete; only Slice 56 remains** — the structurally larger cleanup that drops the source string columns, renames `schedule_*_time_typed` → `schedule_*_time` with `Time` type, and tightens all the deferred admin sites (forms, writers, prefill, schema) in one breath. Worth drafting carefully when scheduled; not a quick-batch candidate.

- **`13883da`..`9e43edf`** — **#30 Slice 54 (campaign Phase 2): `app/chat/` + `app/core/` readers migrate to typed columns** — Step 2 of 4 in the schema time-type harmonization campaign (Backlog #30, Option B). Migrates readers from `schedule_*_time` strings to `schedule_*_time_typed` columns added in Slice 53. **5 reader sites in 4 files:** `app/core/program_search.py` (`_format_hhmm` signature `str` → `time | None`; two call sites in `_program_card`), `app/chat/tier2_db_query.py:330` (`schedule_hours` field in `_program_dict`), `app/chat/context_builder.py:122` (Tier 3 context block program segment), and `app/chat/tier1_handler.py:257-269` (TIME_LOOKUP sub-intent schedule-window rendering). The Tier 1 site was outside the bootstrap's stated scope — Step 0 grep + read of the function body found it is a real production reader (regex pattern at `tier1_templates.py:48`); folded into Slice 54 to keep the campaign contract clean (every chat-and-core reader migrates uniformly) and prevent Slice 56 from absorbing reader work alongside the schema rename. **Byte-equivalence verification (load-bearing safety property):** 4 captures hashed pre-migration, hashed post-migration, diffed; all 4 byte-identical (sha256 prefixes: `f8d245a5` for `_program_dict` Tier 2 dict, `96185cef` for `build_context_for_tier3` Tier 3 LLM prompt, `73dd53cd` for `_program_card`, `fe4f343c` for Tier 1 TIME_LOOKUP window). The Tier 3 capture is the load-bearing one — that block goes into the Anthropic Haiku prompt; whitespace drift would shift LLM responses and remove the ability to attribute output drift to "the migration" vs "the model." **No schema changes; no test changes; no `_format_hhmm` output-format change.** Pytest 963 unchanged; ruff clean. **Production verification (Step 11) is Casey-driven and lighter than Slice 53** — code-only ship, no SQL needed; spot-check a Tier 2 program query and a Tier 1 TIME_LOOKUP query against pre-deploy responses. Rollback is `git revert 13883da`. **Backlog #30:** Slice 54 ticked at `13883da`; decision-doc §5 row 53 catch-up tick (`83d41f7`) and row 54 tick (`13883da`) bundled into the BACKLOG commit so the campaign-sketch table reflects current state. Slices 55–56 remain OPEN.

- **`83d41f7`..`d8de5e3`** — **#30 Slice 53 (campaign Phase 1): typed time columns + `@validates` dual-write on `Program`** — First implementation slice of the schema time-type harmonization campaign (Backlog #30, Option B). Adds nullable shadow columns `schedule_start_time_typed` / `schedule_end_time_typed` on the `programs` table alongside the existing `String(5)` HH:MM columns; an ORM `@validates` decorator on `Program` intercepts every write to `schedule_start_time` / `schedule_end_time` and dual-writes the typed column. The string columns remain the source of truth through Slices 53–55; Slice 56 will drop the strings and rename the typed columns to the canonical names so reader code keeps working unchanged (it just receives a `time` object instead of a `str`). **Design choice — `@validates` over explicit per-call-site dual-write:** writer surface is small (5 ORM call sites: `app/admin/router.py` ×2, `app/programs/router.py` ×2, `app/contrib/approval_service.py` ×1; 0 raw `db.execute(update(...))` writers verified at Step 0); model-level validator eliminates the per-site forgot-to-dual-write failure mode and reduces Slice 56 cleanup to a single decorator removal. **Migration is Python-driven row-iteration backfill** rather than dialect-branched SQL — `datetime.time.fromisoformat()` parses `HH:MM` identically across SQLite and Postgres. **Two notes from local-first verification:** (a) `bindparam("st", type_=sa.Time())` typing was required on the UPDATE — SQLite's stdlib `sqlite3` driver doesn't bind `datetime.time` natively (Postgres' `psycopg2` does); without bindparam typing the migration raises `ProgrammingError` on every dev's first `alembic upgrade head`. (b) SQLite's non-transactional DDL means `op.add_column` auto-commits before the backfill runs; if backfill errors mid-migration, recovery is `ALTER TABLE programs DROP COLUMN schedule_{start,end}_time_typed` then `alembic upgrade head` again. Postgres deploys in a transaction; this recovery is local-dev only. **Verification (local, ./data/events.db @ 98 programs):** alembic head `7d8c9e0f1a2b → f4a5b6c7d8e9`; 98 / 98 typed start populated; 98 / 98 typed end populated; **0 string-vs-typed mismatches**; pytest **960 → 963** (+3 wiring tests in `tests/test_program_typed_time_dual_write.py`: constructor, attribute-assignment, commit + fresh-session reload); ruff clean. **Step 11 production verification is Casey-driven and pending post-deploy** — confirm Railway picks up `83d41f7` (or `d8de5e3`), surfaces `upgrade head f4a5b6c7d8e9` in the deploy log, then run read-only SQL spot-check (`COUNT(*) typed populated == COUNT(*) total` and string-vs-typed mismatches == 0) via the Railway dashboard. Backlog #30: Slice 53 ticked at `83d41f7`; Slices 54–56 stay OPEN. Decision-doc and BACKLOG #30 description (with stale `start_time_typed`/`end_time_typed` names from the pre-rename draft) intentionally left as-shipped from Slice 52; drift captured in commit body, reconciled at Slice 56 cleanup.

- **`277ce05`** — **#30 file (decision portion close): schema time-type harmonization decision (Slice 52)** — Decision-doc-only slice surveying the inconsistency between `Event.start_time` (`sqlalchemy.Time`) and `Program.schedule_start_time` (`sqlalchemy.String(5)` HH:MM). New `docs/maintainability/schema_time_harmonization_decision.md` (~85 lines) follows the h1 / h2 / river_scene_event_output decision-doc pattern: §1 inconsistency, §2 why-it-matters (134 occurrences across 29 files at `d188517` was the trigger — too many for a single big-bang slice), §3 four options (A big-bang, B phased dual-write, C application-layer `@property`, D do nothing), §4 recommendation Option B, §5 four-slice campaign sketch (53 add typed cols + dual-write → 54 migrate chat/core readers → 55 migrate admin readers → 56 drop strings + rename), §6 alternatives rejected, §7 decision, §8 verification posture. **Casey's §7 decision (2026-05-05): Option B — phased migration.** Indexed in `docs/maintainability/project_index.md`. Filed Backlog #30 same-slice; the decision portion is RESOLVED, the implementation campaign (Slices 53–56) stays OPEN under #30. Per-slice bootstraps written just before each slice executes (i.e., Slice 53's bootstrap is drafted before Slice 53 starts; Slice 56's bootstrap is not pre-written until 53 has shipped). No code changes in this slice; pytest unchanged at 960 (959 non-integration + 1 from Slice 51's smoke test); ruff clean. Halt expectation per the auto-pilot prompt: §7 was preserved as a placeholder until Casey's sign-off, then edited in place; only one substantive commit, body reflects the actual decision rather than the bootstrap's "expected outcome" framing.

- **`35cd6ac`..`1c28846`** — **#29 close: Jinja2 templating extraction — privacy/terms/permalink/not_found out of app/main.py (Slice 51)** — Behavior-preserving refactor of the four user-facing HTML pages. `app/main.py` was 540 lines and roughly half was inline HTML in f-strings; this slice moves the four pages into `app/templates/` Jinja2 templates and trims `app/main.py` to 401 lines (-26%). Adds `Jinja2==3.1.4` to `requirements.txt` (MarkupSafe was already a transitive). Three standalone templates: `privacy_doc.html` (shared by `/privacy` and `/terms`), `event_permalink.html`, `event_not_found.html`. Replaces `_load_*_html` / `_render_*_page` with three template-driven response builders (`_render_static_doc`, `_render_not_found_response`, `_render_permalink_response`); the custom markdown→HTML parser (`_render_doc_markdown_to_html`) stays in code (specific to the constrained markdown subset of `docs/privacy.md` / `docs/tos.md`). **Verification: per-page TestClient baseline-vs-after diff across all four pages produced whitespace-only diffs** — Jinja2's default `keep_trailing_newline=False` strips the trailing newline after `</html>` (1-byte diff per page); otherwise byte-identical for the test event. Pytest 959 → 960 (+1 templating-wiring smoke test in `tests/test_phase87_privacy.py`); ruff clean. **Implementation note:** the bootstrap (`relay/slice_51_jinja2_extraction.md`) proposed a shared `_base.html` with `style_extra` blocks, but the existing inline HTML had distinct per-page CSS (different `:root` vars, different `.wrap` padding, generic vs page-scoped `a`-rules); implementing the bootstrap literally would have produced non-whitespace CSS diffs and failed Step 5. Switched to three standalone templates (no `_base.html`), each a literal port of its source HTML — the shared boilerplate would have been five lines and not worth the block complexity given the per-page CSS divergence. Filed and resolved Backlog #29 same-slice; cross-references Backlog #18 Phase A.

- **`7212f5b`..`4db64d3`** — **Phase C component-docs growth: app/chat/ batch close — entity_matcher + local_voice_matcher + context_builder (Slice 50)** — Three small `app/chat/` modules in one slice, matching the Slice 44 / Slice 46 batch pattern. (1) `docs/components/entity_matcher.md` covers rapidfuzz `token_set_ratio > 75` against `Program.provider_name`, the `CANONICAL_EXTRAS` hand-curated alias map (15 keys), the lazy in-memory `_rows` cache, the alphabetically-earlier-canonical tiebreaker used by both `match_entity` and `extract_catalog_entities_from_text`, and provider-only matching (Phase 6.4.1). (2) `docs/components/local_voice_matcher.md` covers Phase 6.5-lite curated-blurb matching: whole-word `\b...\b` keyword scoring with case-insensitive regex, season + session-hint filters, stable ordering on ties via `(-score, idx)`. Doc explicitly clarifies `\b` semantics: the keyword `"hike"` does NOT fire on "hiked"/"hiking"/"hikes" — the trailing letters break the right-side boundary; add inflected forms to `keywords` rather than loosening the regex. (3) `docs/components/context_builder.md` covers Tier 3 catalog context assembly: provider-first formatting, `MAX_PROVIDERS=10`, word-budget cap (`MAX_CONTEXT_WORDS=1500`), entity-first ordering when classifier supplied an `entity`, conservative-fallback message when catalog empty, per-provider event cap of 8, hours truncation at 200 chars. Indexed in `project_index.md` (3 new rows). **`app/chat/` component-doc coverage now 17/17 (was 14/17 at session start).** Total docs across the codebase: 26 (was 23). Pytest unchanged at 964 (959 non-integration + 5 integration deselected); ruff clean. Phase C component-docs growth (Backlog #18) sub-bullet ticks forward; remains open as ongoing growth — next clusters per the audit are `app/admin/` (3 substantive files), `app/contrib/`, and `app/eval/`. **Implementation note:** the bootstrap-drafted commit subject was 110 chars; trimmed to 62 chars to meet WORKING_AGREEMENT's 73-char rule (body kept verbatim). Three small wording corrections vs the bootstrap drafts: `\b` regex semantics in local_voice_matcher.md (two paragraphs) and dropping the bootstrap's incorrect "Haiku 4096-token cache window" rationale in context_builder.md's word-budget paragraph.

- **`8ac6373`..`991bff8`** — **Phase D follow-up: ruff E402 triage + ruleset extension (Slice 49)** — Closes the Phase D follow-up family (I001 + E402). Step 0 found 52 E402 findings (was 49 in Slice 39's Step 0; +3 from Slice 47's import sort moving things around). Triage outcome: **Bucket A (intentional bootstrap-ordering): 46 findings across 6 files** — `app/main.py` runs `ensure_dotenv_loaded()` before module-level imports that read env at import time; 4 `scripts/*` files do `sys.path.insert(_ROOT)` before importing `app.*` so `python scripts/...` works without `PYTHONPATH`; one test file mirrors both patterns. **Bucket B (circular/conditional): 0**. **Bucket C (accidental late imports): 6 findings across 3 files** — `app/eval/confabulation_query_gen.py` had imports below a function body, `tests/test_tier1_handler.py` had Slice 41/#27 patch-block imports at line 330, `tests/test_tier2_formatter.py` had a Slice 27/#5 patch-block import at line 292. **Implementation note:** The bootstrap's authorized form was per-line `# noqa: E402  # <reason>`, but with `line-length=100` the long reason text triggered I001 multi-line wrapping (+139/-60 churn across 9 files) AND mis-positioned the noqa onto inner-paren lines so 24 findings remained unsuppressed. Switched to `[tool.ruff.lint.per-file-ignores]` after a halt-and-report — same explicit-reasoning discipline (one comment per file in `pyproject.toml`), no per-line noqa, no wrap churn. Bucket C imports moved to top alphabetized so I001 stays clean. Final diff: 4 files, +29/-13. Ruleset is now `["F", "I", "W", "E402"]`. **Pytest unchanged at 964** (959 non-integration + 5 integration deselected). E501 (line-length) remains intentionally off; re-enabling needs a project-wide line-length budget conversation.

- **`decf6c5`..`dee30b7`** — **Repo-root tidy: relocate local SQLite + drop stray verify DB (Slice 48)** — Two dev-only files lived at the repo root: `events.db` (3.5 MB local SQLite) and `zzz_fresh_verify.db` (86 KB stray). Both were already gitignored and not deployed (Railway uses `DATABASE_URL`/Postgres). Slice moves the SQLite default in `app/db/database.py` from `Path(__file__).resolve().parents[2] / "events.db"` to `./data/events.db` (with `_DATA_DIR.mkdir(exist_ok=True)` at import time so fresh checkouts work without a manual mkdir), adds `data/` to `.gitignore`, and locally moves the file plus deletes the stray. Pure local-dev hygiene; production-equivalent path (Postgres via `DATABASE_URL`) is unaffected. **Filed and resolved Backlog #28 in the same slice** per the #26/#27 hygiene-ship pattern. Verification: `init_db` round-trip on the new path returned the same event count (88) as the pre-move snapshot — confirms the move worked, not that data is intact in any production sense (local dev data only). Pytest 959 passed (non-integration; +5 integration deselected = 964 total). Net diff: 2 files changed, 6 insertions, 1 deletion.

- **`812aaab`..`0fddd5e`** — **Phase D follow-up: ruff I001 (isort) ruleset extension (Slice 47)** — Auto-fix-only ruleset extension, no judgment calls. Slice 39 (Path 1) had narrowed the ruff `select` to `["F", "W"]` to land CI green without churning on cosmetic findings; this slice extends to `["F", "I", "W"]` and applies the deterministic `ruff --fix` output across the tree (31 files touched, +94/-36, pure import-block reordering — verified by spot-check of `app/main.py` and `app/db/database.py`). Step 0 found 34 I001 findings (down from Slice 39's 39, well within the ±10 tolerance — accounted for by intervening doc-and-code slices that landed clean imports). CI will now fail on any future PR that touches imports without sorting. **Pytest unchanged at 964** (959 non-integration + 5 integration deselected). **E402 (49 findings) deferred to Slice 49** — some findings are intentional `ensure_dotenv_loaded()` bootstrap-ordering imports that require `# noqa: E402` annotation rather than mechanical reorder, so per-file triage is needed.

- **`e63b03e`..`d8c01e8`** — **Phase C component-docs growth: tier1_templates + tier2_schema + normalizer (Slice 46 Tier 3 batch)** — Three small component docs in one slice covering audit Tier 3 priority modules that were referenced-but-not-defined in earlier higher-priority docs. (1) `docs/components/tier1_templates.md` (~75 lines) covers the regex `INTENT_PATTERNS` + per-intent `TEMPLATES` engine and the `render()` function, with the persona-brief voice constraints and the `None`-for-missing-slot fall-through convention. (2) `docs/components/tier2_schema.md` (~70 lines) covers the `Tier2Filters` Pydantic schema, the one-temporal-plan-at-a-time structural validator, and the prompt-schema coupling discipline (`prompts/tier2_parser.txt` and the schema must move together). (3) `docs/components/normalizer.md` (~65 lines) covers the four-step pre-classification text normalizer (lowercase + edge-strip → contraction expansion → whitespace collapse → preserve internal hyphens/apostrophes), pure and idempotent. Indexed in `project_index.md`. Pytest unchanged at 964. **`app/chat/` coverage 11/17 → 14/17**, plus 9 docs outside `app/chat/` — closes the audit's referenced-but-not-defined cross-refs.

- **`424499a`..`5183bb1`** — **Provider lane Phase 1: admin direct-create UI (Slice 45)** — First admin write surface added since Slice 22 (which removed `POST /events`). Implements **Source #1** from the Slice 29 options doc (manual admin entry, lowest-effort lane). Two new routes in `app/admin/router.py` mirroring the existing `/admin/programs/new` pattern: `GET /admin/providers/new` renders the form (cookie-gated via `_guard`); `POST /admin/providers` validates required fields (provider_name + category), creates a `Provider` row directly with `source="admin"`, `verified=True` (operator IS the verifier), `is_active=True`, `draft=False`, `pending_review=False`, atomic `db.add + commit` with rollback on exception, and redirects to `/admin` on success or returns the form with a 400 + friendly error on validation failure. New `_provider_form_html` and `_parse_provider_form` helpers use the existing `_programs_tab_shell` wrapper, `_escape` helper, and `inp()` style for visual consistency. Dashboard toolbar gains a **"+ Create provider"** button next to "+ Create program". Six new tests in `tests/test_admin_provider_create.py` cover both routes' cookie-gates, the form-renders-when-authenticated path, the happy-path POST + DB write, and the missing-name / missing-category 400 paths. **Threat model verified** against `docs/components/admin_auth.md`: cookie-gate present on both routes, all user input HTML-escaped, no CSRF (single-admin model, matches existing program-create pattern), no rate-limit (cookie auth is the gate), DB write atomicity preserved. **Pytest 958 → 964** (+6). Ruff clean. **Backlog #9 (Tier 1 hit rate, DEFERRED) precondition partially unblocked** — operators can now seed providers without waiting for an automated lane; full re-opening still requires actual provider rows + representative query mix + measurement window. Phase C provider-ingestion-lane sub-bullet under #18 advances to its first implementation step.

- **`3d1d874`..`024e46f`** — **Phase C component-docs growth: rate_limit.md + dedupe.md (Slice 44 core batch)** — Two `app/core/` component docs in one slice. (1) `docs/components/rate_limit.md` (~22-line module → ~75-line doc) covers the shared slowapi `Limiter` (per-IP keying via `get_remote_address`, env-toggleable for tests via `RATE_LIMIT_DISABLED`), enumerates callers (`app/main.py`, `programs/router.py`, `contribute.py`, `chat.py`), and documents known limitations (in-memory backend, multi-worker scaling, `X-Forwarded-For` trust assumption). (2) `docs/components/dedupe.md` (~74-line module → ~80-line doc) covers the embedding+date+location three-gate event duplicate detection: 0.85 cosine threshold (empirically chosen), ±1 day date window, location text similarity, full-table-scan O(n) characteristic, and a clear AND-gate convention (loosening any single signal would cascade false positives). Pytest unchanged at 958. **Indexed in `project_index.md`** — adjacent-and-core component-doc coverage now totals **9 docs outside `app/chat/`** (river_scene, approval_service, mention_scanner, llm_messages, event_quality, admin_auth, admin_router, rate_limit, dedupe).

- **`ae40445`..`b3eb8ee`** — **Phase C component-docs growth: event_quality.md (Slice 43)** — New `docs/components/event_quality.md` (~75 lines) covering `app/core/event_quality.py` — the post-Slice-21 minimal request-validation pretty-printer (sole live caller is `app/main.py:518`'s `RequestValidationError` handler). Documents the three exported names (`CHAT_CONCIERGE_QUERY_VALIDATION_MESSAGE` constant, `friendly_errors` function, `_errors_touch_concierge_query_field` helper), the four-step linear scan with early returns (concierge-query branch → inner-`ValueError` branch → `"Value error, "`-prefix branch → fallback), the loc-shape duality (accepts both `('query',)` and `('body', 'query')`), conventions (hard-coded canonical message, generic fallback, no logging, exported underscore-prefixed helper), and known limitations. Captures the Slice 21 pruning history (265 → 38 lines) so future readers don't re-derive whether to prune further. Pytest unchanged at 958. Indexed in `project_index.md`.

- **`4ec2787`..`b48b76f`** — **Phase C component-docs growth: mention_scanner.md (Slice 42)** — New `docs/components/mention_scanner.md` (~117 lines) covering `app/contrib/mention_scanner.py` — the Path 3 catalog-creation feedback loop (Tier 3 mentions → admin review → approved Provider rows). Documents the public surface (`MentionCandidate` dataclass; pure `scan_tier3_response` function; best-effort `scan_and_save_mentions` background task; `STOP_PHRASES` frozenset), the five-step scan logic (URL strip → title-case regex → per-phrase filter w/ length-6 minimum + stop-list + within-response dedupe → context snippet → candidate build), conventions (best-effort persistence — never raises across boundary; pure-vs-side-effect split for testability; capped 300-char `mentioned_name` matching DB constraint), and known limitations (permissive heuristic; no semantic filtering; within-response dedupe only). With this doc the **contrib trio is complete** — `river_scene` (Slice 36) + `approval_service` (Slice 37) + `mention_scanner` (this slice) cover Paths 1–3 of `end_to_end_creation.md`. Indexed in `project_index.md`. Pytest unchanged at 958.

- **`7dd714c`..`5411869`** — **#27 close: Tier 1 OPEN_NOW timezone fix (Slice 41)** — Replaced `_utcnow()` (UTC wall clock) with `now_lake_havasu()` (Lake Havasu local wall clock) in `app/chat/tier1_handler.py` at the two call sites: `_next_event`'s `today` computation and `OPEN_NOW`'s `now` value passed to `_open_now_from_hours`. `now_lake_havasu` was already used by `tier3_handler` / `unified_router` / `tier2_parser` for the same temporal-grounding purpose; no new dependency. Removed the `_utcnow()` helper and the `from datetime import UTC` (no other callers in the file). Two new wiring tests added (`test_open_now_uses_lake_havasu_local_time`, `test_next_event_uses_lake_havasu_today`); 3 existing OPEN_NOW tests in `test_tier1_handler.py` and 4 OPEN_NOW parametrize cases in `test_ask_mode.py` migrated from patching `_utcnow` with tz-aware UTC datetimes to patching `now_lake_havasu` with `ZoneInfo("America/Phoenix")` datetimes. **Pytest 956 → 958** (+2). Ruff clean. **OPEN backlog drops to 2 (#2, #18).**

- **`2d1c6e1`..`2de8b15`** — **Phase C component-docs growth: admin_auth.md + admin_router.md (Slice 40)** — Two admin-subsystem docs in one slice. (1) `docs/components/admin_auth.md` (~110 lines) covers `app/admin/auth.py` cookie-gate logic — public surface (`COOKIE_NAME`, `MAX_AGE_SECONDS`, `sign_admin_cookie`, `verify_admin_cookie`, `admin_password_ok`), the env-at-call-time pattern (Railway-correctness), `itsdangerous` serializer with hard-coded salt for domain separation, and an explicit **threat model** distinguishing in-scope (cookie tamper / expiry / replay / brute-force) from out-of-scope (revocation / multi-admin / audit-log / constant-time-compare). Security-sensitive — flagged for review against the threat model on future edits. (2) `docs/components/admin_router.md` (~120 lines) covers the much-larger `app/admin/router.py` (~1794 lines) as an overview: route inventory pointing at `http_api.md` for the authoritative list, five-section file structure, conventions (per-route `_guard`, hand-rendered HTML XSS risk, `Form(...)` deps, no Jinja, no CSRF), known limitations (single-file size, live analytics queries with no caching). Two new rows in `project_index.md`. Pytest unchanged at 956. **Adjacent-subsystem coverage now includes `app/admin/`.**

- **`519fd37`..`0be0489`** — **Phase D close: CI lint + tests on PR (Slice 39)** — First-time CI infrastructure ship. Five-commit slice: (1) `pyproject.toml` ruff config + `dev-requirements.txt` pinning ruff==0.15.12, narrowed to `select = ["F", "W"]` per Casey's Path 1 decision after Step 0 found 1468 cosmetic E501 findings (auto-fix + E/I triage queued for follow-up); (2) ruff fix-pass: 22 auto-fixed (18 F401 unused-import, 4 F541 f-string-without-placeholder, 1 F811 redefined-while-unused) + 4 manual F841 fixes (3× `_ = _prov(...)` rename in tests; 1 dead `stamp =` removal in `scripts/run_voice_spotcheck.py`); 18 files touched, -9 net lines; (3) `.github/workflows/ci.yml` two-job workflow (Lint runs `ruff check`, Test runs `pytest -q -m "not integration"` under placeholder API keys + sqlite); (3-fix) initial CI run failed on the integration-marked OpenAI hint-extractor tests (they hit the real API by design per their docstring); workflow updated to exclude them via marker; (4) Backlog #18 Phase D tick. **Both jobs verified green on `2f59d4f`** — lint 10s, test 71s, total ~1m20 (well under 5min warning threshold). Pytest local `-m "not integration"` reports 951 passed / 5 deselected; full local 956. With Phase D ticked, **Backlog #18 is now Phase A ongoing + Phase B closed (Slice 6) + Phase C closed at 6/6 + Phase D closed (this slice)**; the Phase C "Component docs growth" sub-bullet stays open as ongoing.

- **`dbfd029`..`1540d67`** — **Phase C component-docs growth: llm_messages.md (Slice 38)** — New `docs/components/llm_messages.md` (~120 lines) covering `app/core/llm_messages.py`, the H2 consolidation point and sole Anthropic Messages API caller. Documents the public surface (`call_anthropic_messages` entry, the frozen `Usage` dataclass with `billable_input` property and `from_sdk_usage` classmethod, `AnthropicResult`, `coerce_llm_text_to_json_object`, `load_prompt`, `DEFAULT_MODEL` constant), the six-step internal flow (API key → package check → model resolve → system block construction with ephemeral cache_control → `messages.create` call → result extraction), and — critically — the **three mock-seam invariants** the test suite depends on (package-level `import anthropic`; fixed `Anthropic(api_key=..., timeout=...)` constructor signature; fixed `messages.create` kwargs `model/max_tokens/temperature/system/messages`). Conventions, configuration, and known limitations (no streaming/batching/tool-use; rigid construction signature; broad exception catch; hardcoded ephemeral cache_control). Closes the cross-reference loop with `tier2_parser.md`/`tier3_handler.md`/`llm_router.md`. Pytest unchanged at 956. Component-docs coverage adds first row in `app/core/`.

- **`c919b9c`..`0003842`** — **Phase C component-docs growth: approval_service.md (Slice 37)** — New `docs/components/approval_service.md` (~109 lines) covering `app/contrib/approval_service.py` — the **sole catalog-write path** post-cleanup. Documents the five public functions (the three `approve_contribution_as_*` for Provider/Program/Event materialization, `enrichment_suggests_verified` helper, and the `parse_*` form helpers), the six-step shared shape (load+validate the contribution → derive `verified`/`source` fields → build the catalog row → insert+flush → update contribution status → commit+refresh with rollback-on-exception), conventions (`db.commit()` in-function for transactional atomicity; naive UTC for TIMESTAMP WITHOUT TIME ZONE columns; `source` field reflects upstream provenance not human review; `strip-and-None` for optional strings), and known limitations (no upsert, no batch, hours-structuring provider-only, `verified` is a one-shot gate, `reviewed_by` plumbing incomplete, auto-approval bypasses admin review). Together with `river_scene.md` (Slice 36) covers the full ingestion-to-catalog-row pipeline. Pytest unchanged at 956. Component-docs coverage adds first row in `app/contrib/` (catalog-write path).

- **`6910a24`..`a7c366a`** — **Phase C component-docs growth: tier1_handler.md + filed #27 (OPEN_NOW tz bug) (Slice 35)** — New `docs/components/tier1_handler.md` (~110 lines) covering `try_tier1()` (zero-LLM-token deterministic-template handler), the four-step guard-and-dispatch flow (entity gate → sub-intent gate → provider lookup → ten-branch dispatch table), conventions (`None` not exception, `_append_voice` for the `(confirmed)` suffix, strip-and-empty-as-falsy, programs filtered `is_active=True`), and known limitations. Closes the audit's Tier 2 priority list (last of the Tier 2 batch). Surfaced **Backlog #27 OPEN** during the audit: `_utcnow()` returns tz-aware UTC but `_open_now_from_hours` compares against operator-entered Lake Havasu local-time hours strings — off by 7 hours (MST/UTC-7). Blast radius small while providers table is empty (RS-only catalog); becomes user-visible when provider data lands. Indexed in `project_index.md`. Pytest unchanged at 956. **OPEN backlog grows to 3 (#2, #18, #27).** Component-docs coverage 7/17 → 8/17 `app/chat/` modules.

- **`d86c7f6`..`f54591d`** — **Phase C component-docs growth: river_scene.md (Slice 36)** — New `docs/components/river_scene.md` (~149 lines) covering the **sole live ingestion lane** (`app/contrib/river_scene.py` parser/fetcher + `app/contrib/river_scene_pull.py` orchestration). Documents the public surface (`RiverSceneEvent` dataclass; `fetch_sitemap_urls`, `fetch_and_parse_event`, `normalize_to_contribution`; `run_pull` with its 8 per-run counters), the sitemap-traversal and event-detail-parse flows, the orchestrator chain (dedupe-before-fetch → parse → seed-overlap check → contribution insert → optional auto-approval), conventions (polite HTTP, retries-then-raise, None-for-skip, dedupe-by-URL, print-driven), known limitations (WordPress structural drift, URL-only dedupe, past-event cutoff at `today`, no incremental sitemap, auto-approval inverts default), and direct/indirect dependencies. Indexed in `docs/maintainability/project_index.md`. Pytest unchanged at 956. Adjacent-subsystem (`app/contrib/`) coverage now non-empty.

- **`da68612`..`1d4aa31`** — **Phase C component-docs batch: intent_classifier + hint_extractor + llm_router (Slice 32)** — Three docs in one slice per `relay/component_doc_audit.md` Tier 2 batch recommendation. (1) `docs/components/intent_classifier.md` covers `classify()` (frozen `IntentResult` dataclass), the four-step heuristic flow (mode → sub-intent → entity → confidence merge), the sub-intent label space shared with `llm_router`. (2) `docs/components/hint_extractor.md` covers `extract_hints()`, the optional OpenAI gpt-4.1-mini caller (sole OpenAI caller in the codebase; Backlog #17 reference), six-step flow, soft 300/100 token budget. (3) `docs/components/llm_router.md` covers the optional `route()` Anthropic structured router (`USE_LLM_ROUTER` env, OFF in prod), `RouterDecision` schema with the cross-field `tier_recommendation == "2" requires tier2_filters` validator, seven-step flow. Three rows added to `docs/maintainability/project_index.md` Repo doc index. Pytest unchanged at 956 (doc-only). Component-docs coverage advances from 4/17 → 7/17 `app/chat/` modules.

- **`fbe8ae6`..`11708d0`** — **Phase C component-docs growth: tier2_parser.md + project_index catch-up + wiring test (Slice 34)** — Three closely-related changes in one commit. (1) New `docs/components/tier2_parser.md` (~140 lines) covering `parse()` public surface, the nine-step internal chain (API key check → prompt load → runtime date prepend → API call → token capture → JSON coerce → schema validate → return), conventions (None-not-exception, token-on-billable-failure, runtime date prepend), known limitations. (2) `docs/maintainability/project_index.md` gains three rows — catch-up entries for `tier2_handler.md` (Slice 25) and `tier3_handler.md` (Slice 33) plus the new `tier2_parser.md`. (3) `tests/test_tier2_parser.py` gains `test_parse_prepends_date_context_to_system_prompt` (Slice 24's wiring gap). Pytest **955 → 956** (+1 wiring test). No production code change. OPEN backlog unchanged at 2 (#2, #18).

- **`66636a6`..`ae5b9b6`** — **Phase C component-docs growth: tier3_handler.md (Slice 33)** — New `docs/components/tier3_handler.md` (~111 lines) following the structure of `docs/components/unified_router.md` and `tier2_handler.md`. Documents the public surface (`answer_with_tier3` with its 4-tuple return shape; `FALLBACK_MESSAGE`), the five-stage internal pipeline (API key + `anthropic` package guard; `build_context_for_tier3` for catalog grounding; mid-block assembly with classifier line + optional `User context:` bias line + `Now:` line + optional `Local voice:` blurbs from `find_matching_blurbs`; the Anthropic call via `call_anthropic_messages` with `max_tokens=150` and `temperature=0.3`; result handling), the system-prompt loading helper with its intentional inline fallback, the failure-mode table (six rows mapping condition → return value), tunable constants, and cross-references. Tier 1 priority per `relay/component_doc_audit.md` (highest-traffic LLM synthesis path). Backlog #18 Phase C component-docs sub-bullet annotated with this addition; sub-bullet stays open as ongoing growth. Pytest unchanged at 955 (doc-only).

- **`486dc11`..`b3ccf21`** — **#5 close: clickable event_url links in Tier 2 LLM-formatter path (Slice 27)** — Backlog #5's deterministic-renderer scope was already correct (`tier2_catalog_render.py` emits `[name](url)` inline); the LLM-formatter path for mixed/non-event rows was missing links because the system prompt forbade markdown. **Hybrid fix:** (a) prompt EXCEPTION in `prompts/tier2_formatter.txt` permitting `[name](event_url)` markdown for rows with non-empty `event_url`, plus (b) `_inject_event_url_links` deterministic post-processor in `app/chat/tier2_formatter.py` as a safety net — runs after `_format_via_llm` returns and injects any missing links via word-boundary regex with an inside-existing-markdown-link guard, falling back to end-of-text append when the row's name doesn't appear inline. Eight new unit tests cover injection paths and edge cases (empty url, non-event rows, double-link prevention, multi-event, word boundaries, inside-existing-markdown-link guard). **Pytest baseline shifts 947 → 955** (+8). Bundled per the audit amendment: new `docs/components/tier2_formatter.md` component doc and a row in `docs/maintainability/project_index.md`. **OPEN backlog drops to 2 (#2, #18).** Multi-sample production verification required after deploy per WORKING_AGREEMENT.md (LLM-mediated user-visible behavior).

- **`732570a`..`006ef64`** — **Phase D: Engineering gates options doc (Slice 31)** — New `docs/maintainability/engineering_gates_options.md` (~152 lines): forward-looking option space mirroring the Slice 29 provider-ingestion-lane pattern. Five decision axes (CI host, linter, formatter, scope, test gate strictness) with provisional defaults (GitHub Actions / ruff / ruff format / new-code-only / any-failure-blocks); two integration patterns (PR-gate-only vs PR+main); a four-slice forward-looking sequence (Slice 40-43) sketching what an actual Phase D ship would look like. Phase D bullet under Backlog #18 stays unticked — this is a prep doc, not a close. Pytest unchanged at 947 (doc-only).

- **`38ce682`..`3f17c75`** — **#2 telemetry-add: time-bucket fire/no-fire logging (Slice 30a)** — Backlog #2 (broad-window event sampling: chronological vs bucketed) is a Product/UX decision that shouldn't be made without data on how often the bucketing branch actually fires. This slice adds `logging.info()` on both branches in `app/chat/tier2_db_query.py:582-585`: `"tier2_db_query: time-bucket fired (window_days=%s, total_matches=%s)"` when the trigger fires, `"tier2_db_query: time-bucket NOT fired (...)"` when there are more than `MAX_ROWS` matches but the trigger doesn't fire. Both lines share the grep tag `tier2_db_query: time-bucket`. Required `import logging` added (alphabetical with stdlib group). Pytest unchanged at 947 (logging is non-functional from a test perspective; no tests assert on log strings — confirmed via grep). #2 stays OPEN as a status update; Slice 30b (~2026-05-12, after 7 days of production logs) will analyze fires-vs-no-fires distribution and pick the A/B/C/D resolution per `relay/decision_2_time_bucket_sampling.md`.

- **`5fbd658`..`b2f469f`** — **Phase C: Provider ingestion lane options doc (Slice 29)** — New `docs/maintainability/provider_ingestion_lane_options.md` (~143 lines): forward-looking option space for re-introducing provider ingestion on top of the post-2026-04-30 RS-only catalog. Five candidate sources (manual admin entry, public submission, web scraping, third-party API, bulk CSV/sheet) with coverage/effort/reliability profiles and removed-precedent notes; three architectural patterns (mirror RS, divergent provider pipeline, generalized ingestion framework) with effort estimates; four open product questions (cross-referenced to `chat_behavior_followup_plan.md` §7); provisional first-build recommendation (Pattern A + Source 1) explicitly flagged as not a commitment. Closes the Phase C "§5 gap: Provider ingestion lane (forward-looking spec)" sub-bullet under Backlog #18; **Phase C now 5/6 ticked.** Pytest unchanged at 947 (doc-only).

- **`3598621`..`d10f3d4`** — **Phase C: CI query-battery story doc (Slice 28)** — New `docs/maintainability/ci_query_battery.md` (~155 lines) covering manual battery invocation (`python scripts/run_query_battery.py`), expected JSON output shape with the `total`/`matched`/`mismatched` summary fields, success criteria (clean run = 120 matched; flake budget 1/120 on borderline date phrases per Slice 23 verification), where labels live (`SINGLE_SHOT` tuples in `scripts/run_query_battery.py`), label-update discipline (update labels in the same commit as the routing change), and three plausible CI integration patterns (GitHub Actions on push-to-main, Railway scheduled task, pre-deploy verification hook) without committing to one — current state is manual runs only. Closes the Phase C "§5 gap: CI query-battery story" sub-bullet under Backlog #18; Phase C now 4/6 ticked. Prereqs #12 (Slice 16) and #25 (Slice 23) both RESOLVED. Pytest unchanged at 947 (doc-only ship).

- **`8d063ff`..`71f897c`** — **#26 close: importlib EventRead workaround replaced with direct import (Slice 26)** — Slice 22 (#24 close) included an over-strict directive to remove the `EventRead` import from `app/main.py`; the GET `/events` route at line 533 still used `EventRead` as `response_model`, so the Slice 22 executor preserved correctness via `importlib.import_module(...)` + `getattr(..., "Event" + "Read")` to pass a literal grep gate. This slice restores a normal direct import. Edits in `app/main.py`: removed `import importlib` (line 11), added `EventRead` to the `app.schemas.event` import (line 37), removed the `_EventOut = getattr(...)` workaround (former line 41) plus one redundant blank line, updated the GET `/events` `response_model` to `list[EventRead]` (former line 533). Net -3 lines (4 edits, 3 deletions). Pytest unchanged at 947 (import-shape only; no test-surface change). #26 was filed and closed in this slice (was not on the prior OPEN list); OPEN backlog count unchanged at 3 (#2, #5, #18).

- **`7c339e0`** — **Phase C component docs growth: tier2_handler.md (Slice 25)** — New `docs/components/tier2_handler.md` (~103 lines) following the structure of `docs/components/unified_router.md`. Documents the three exported callables (`try_tier2_with_usage`, `try_tier2_with_filters_with_usage`, `answer_with_tier2`), the `TIER2_CONFIDENCE_THRESHOLD = 0.7` constant, the seven-step parser → DB → formatter chain (with verbatim INFO log strings on each fallback gate), conventions (binary fallback shape, no exception wrapping at this level, `or 0` token coercion), current deployment posture, known limitations, and direct/indirect dependencies plus callsites. Function signatures and docstrings quoted verbatim from source; test-file references verified before inclusion. Pytest unchanged at 947. Backlog #18 Phase C component-docs sub-bullet ticked with tier2_handler entry; bullet stays open as ongoing growth.

- **`85cedd0`..`7668423`** — **#3 close: year inference for undated calendar queries (Slice 24)** — `app/chat/tier2_parser.py:parse()` now prepends a date-context paragraph to its loaded system prompt on every call: `"Today's date is YYYY-MM-DD (Lake Havasu City, Arizona; MST/UTC-7, no DST). Use this to resolve year for ambiguous calendar queries..."`. `today_iso` is computed via `now_lake_havasu().strftime("%Y-%m-%d")` from `app.core.timezone` (the same helper `tier3_handler` and `unified_router` already use, so no new dependency). Implemented as a runtime prepend rather than editing the static `prompts/tier2_parser.txt` so the date never goes stale on disk. Pytest unchanged at 947. Production smoke post-merge: query `"events on May 8"` returned `tier_used=2` with events from May 7–9, **2026** and May 8–10, **2026** — year correctly inferred from today's `2026-05-04`. **OPEN backlog drops to 3 items (2, 5, 18).**

- **`b34fea1`..`ead421a`** — **#25 close: battery expected labels rebuilt from production baseline (Slice 23)** — Restored expected/actual matching to `scripts/run_query_battery.py` using captured production `tier_used` values. Approach: ran the battery once against production (~119 queries with valid `tier_used`; 1 returns 422 for the empty string) and used those captured values as the expected labels in SINGLE_SHOT and SEQUENCES tuples. Edits: restored `matches()` helper (removed in Slice 16), updated all 120 tuples with new tier-based labels (TIER2/TIER3/CHAT/GAP_TEMPLATE/PLACEHOLDER/ERROR(422)), restored `expected` and `match` fields in record output, added `matched`/`mismatched` summary fields to `run_all()` return value. Verify run: 119/120 matched, 1 mismatch (#45 "rotary park" — TIER2 baseline → TIER3 verify, expected LLM non-determinism on a borderline date-phrase parse). Pytest unchanged at 947 (script not in test coverage). Phase C CI query-battery sub-bullet under #18 now has functional regression detection; CI infra still queued under Phase D. **OPEN backlog drops to 4 items (2, 3, 5, 18).**

- **`ee6bf75`..`0a63e77`** — **#24 close: POST /events removed; tests refactored to SQLAlchemy fixtures (Slice 22)** — 6-file refactor: `app/main.py` removed handler + `require_admin` local + auth import + `EventRead` import; 3 test files refactored (2 to use SQLAlchemy direct seeding via `Event.from_create(EventCreate(...))`; 2 obsolete tests removed); 2 doc files updated. **Pytest baseline shifted from 949 → 947** (2 removed tests). Phase B follow-up family fully closed (#19 Slice 10, #20 Slice 11, #24 here). #25 still OPEN as Phase C follow-up. OPEN backlog drops to 5 items.
- **`9020b2d`..`44b8229`** — **#7 close: event_quality orphan symbols removed (Slice 21)** — Pruned `app/core/event_quality.py` from 265 lines to ~32 lines by removing 14 symbols (9 from the original #7 list + 5 cascading helpers including `friendly_validation_error` zero-callers wrapper) and 4 unused imports. Preserves the live `friendly_errors` RequestValidationError handler path used by `app/main.py:518`. Pytest count unchanged at 949 (zero tests exercised the deleted code). Historical doc reference in `search-pipeline-for-claude.md` left as-is per the historical-snapshot convention.
- **`35577cd`** — **#9 deferred: Tier 1 hit rate pending provider population (Slice 20)** — Decision-only slice: changed Backlog #9 status from OPEN to DEFERRED. Refined the precondition from time-based ("catalog stabilizes") to data-based: `providers` table is empty in current RS-only catalog posture; Tier 1 templated provider lookups have nothing to hit so re-measurement is meaningless until providers populate (via approved contributions, future ingestion lane, or Tier 3 mention promotions). Mirrors #11/#17 deferral pattern. OPEN list drops to 7; DEFERRED line gains 9.
- **`6ca0639`** — **#11 deferred: slowapi Python 3.14 deprecation pending upstream fix (Slice 19)** — Decision-only slice: changed Backlog #11 status from OPEN to DEFERRED. The original Scope line already said "Track until slowapi releases a fix" — DEFERRED formalizes this per BACKLOG.md conventions. Precondition for re-opening documented: either slowapi ships a Python 3.14-compatible version using `inspect.iscoroutinefunction()`, or a version pin becomes warranted. Until then the 5 warnings per pytest run are benign. Mirrors #17's deferral pattern. OPEN list drops to 8; DEFERRED line gains 11.
- **`dbb16f8`** — **#14 close: --collect-only discipline (decision: not canonical) (Slice 18)** — Decision-only slice grounded in 17 slices of post-H1 evidence: every ship used `pytest -q` alone for verification, including 4 deletion ships (Slices 5, 11, 14, 17), and none had hidden collection-time issues that `--collect-only` would have caught earlier. The H1-era cross-cutting parametrize/skipif references to legacy modules don't exist post-H1. Resolution paragraph documents a heuristic for future case-by-case use. No `POST_SHIP_CHECKLIST` or `WORKING_AGREEMENT` update needed.
- **`ab8df88`..`7a56f7c`** — **#16 close: run_voice_audit.py migrated to llm_messages helper (Slice 17)** — Migrated both `messages.create()` call sites in `_run_voice_audits()` to use `call_anthropic_messages()` from `app/core/llm_messages.py` (the H2 consolidation helper). 6 surgical edits in one file: import add + signature change + 2 call site rewrites + inline-import removal + caller update. Pytest count unchanged at 949. Production runtime verification deferred (script is expensive to run as smoke). With this, `run_voice_audit.py` is the second fully-migrated Anthropic caller; #17 (OpenAI helper extraction) stays DEFERRED until a second OpenAI caller appears.
- **`fd313bb`..`509f254`** — **#12 close: run_query_battery retargeted to /api/chat (Slice 16)** — Script was 404'ing on every call since H1 (2026-04-29) removed `/chat`. Retargeted via raw-passthrough scope: endpoint + payload + response parser updated to use new `/api/chat` and `ConciergeChatResponse` shape; old intent-based expected/actual matching dropped because the new tier-based router output doesn't 1:1 map to pre-H1 intent labels. Single-query production smoke test confirmed runtime behavior (1 chat_log row created in production). Pytest count unchanged at 949 (script not in test coverage). **Backlog #25** filed to track the follow-up: rebuild SINGLE_SHOT expected labels using new tier-based fields so the battery can auto-detect regressions. With #12 closed, Phase C CI query-battery §5 sub-bullet is unblocked (still requires #25 + CI infra to fully address).
- **`b1a0add`..`cd7e986`** — **#21 close: POST /events cookie-gated via option (d) (Slice 15)** — Investigation of `POST /events` (`app/main.py:create_event`) found it allowed unauthenticated clients to create immediately-live, "verified", admin-attributed catalog rows by setting `payload.status`/`source`/`verified` directly — rate limit (5/min) provided only DOS protection, not abuse protection. Disposition (d) chosen: cookie-gate via `Depends(require_admin)` to close the abuse vector, defer full removal + test refactor to **Backlog #24**. Files touched (6): `app/main.py` (import + local `require_admin` + gate), 3 test files (login-first), 2 maintainability docs. Pytest count unchanged at 949. Bonus findings family from Slices 8 and 10 now fully closed (#21, #22, #23 RESOLVED).
- **`72728e2`..`babe738`** — **#22 close: /admin/debug-pw removed via option (c) (Slice 14)** — Removed the unauthenticated `/admin/debug-pw` endpoint and its supporting helper entirely (5-file scope: `app/admin/router.py` import + handler, `app/admin/auth.py` helper, `tests/test_phase6.py` one test, `docs/maintainability/http_api.md` table row, `docs/runbook.md` "Debug (non-secret)" subsection). Endpoint was explicitly "Temporary" per its own docstring; lived past its purpose. Disposition (c) chosen over alternatives (cookie-gate defeats its own purpose; env-var-guard adds maintenance for an unused endpoint). Pytest count dropped from 950 to 949 (one removed test). Bonus findings family from Slices 8 and 10 now 2/3 closed (#21 still OPEN as posture-review question for `POST /events`).
- **`c94afb6`..`954dd38`** — **#23 close: diagnose_search.py cleanup (Slice 13)** — Two surgical edits to `scripts/diagnose_search.py`: line 4 docstring updated to reference `scripts/output/diagnose_output.txt` (matches the actual functional path migrated in Slice 10); line 18 `BASE_URL` updated to `https://havasu-chat-production.up.railway.app` (the old URL `web-production-bbe17` was stale and would have caused connection errors). Minimum-viable fix; env-var wiring deferred. Pytest count unchanged pre/post. Bonus findings family from Slices 8 and 10 now 1/3 closed (#21, #22 still OPEN as posture-review questions).
- **`5412695`** — **Bonus findings filed (Slice 12)** — Three observations from earlier slices filed as trackable Backlog items rather than buried in doc/commit narrative: **#21** (`POST /events` posture review, surfaced in Slice 8 http_api.md), **#22** (`/admin/debug-pw` posture review, surfaced in Slice 8), **#23** (`diagnose_search.py` cleanup — stale `BASE_URL` and docstring, surfaced in Slice 10 #19 closure). All three are LOW severity. Pure backlog bookkeeping; no code or doc change beyond BACKLOG.md additions.
- **`15f7248`..`894dc28`** — **#20 close: legacy tracked-output disposition (Slice 11)** — Removed 5 legacy tracked outputs from `scripts/` (`battery_results.json` 68KB + 4 dated `voice_audit_results_2026-04-*.json` totaling ~340KB; ~410KB freed). All recoverable via `git log -- <path>`. `scripts/README.md` legacy paragraph removed in same commit. Three other narrative/historical references in `havasu-development-plan.md`, `runbook.md`, `known-issues.md` left as-is per project_index convention. Pytest count unchanged pre/post. Phase B follow-up family fully closed (#19 in Slice 10, #20 here).
- **`d429fe7`..`ddb8569`** — **#19 close: tool default-path migration (Slice 10)** — `scripts/run_voice_audit.py:1097` and `scripts/diagnose_search.py:19` updated to write under `scripts/output/` (the gitignored convention established in Slice 4's `scripts/README.md` rewrite). `parent.mkdir(parents=True, exist_ok=True)` added before each `write_text` to handle fresh-clone directory absence; matches the pattern already used by the other scripts/output writers. Three other CLI tools surveyed (`extract_tier3_queries.py`, `run_voice_spotcheck.py`, `confabulation_eval.py`) already used a correct convention; no edits needed. Pytest count unchanged pre/post (behavior-neutral). Bonus finding noted in #19 resolution: `diagnose_search.py:18 BASE_URL` is stale and would fail if the script were run as-is.
- **`c1cd8b0`..`61f73b4`** — **Phase C: End-to-end creation doc (Slice 9)** — New `docs/maintainability/end_to_end_creation.md` (~140 lines) documents the four paths that produce catalog rows: public submission via `/contribute` → admin review → `approval_service.approve_contribution_as_*`; River Scene auto-import (CLI script → contribution-shaped row → same admin review); Tier 3 mention scan via `mention_scanner.scan_and_save_mentions` → `LlmMentionedEntity` queue → admin promotion creates Provider; admin direct create (Programs only via `/admin/programs/new`). Plus Contribution status state machine (pending/approved/rejected/needs_info), per-entity-type fields touched at creation, and explicit non-coverage list. Indexed in project_index Maintainability table; §5 gap bullet for "no end-to-end provider/program creation doc" removed. Backlog #18 Phase C now 3/6 ticked (Railway + HTTP API + end-to-end).
- **`5f14f36`..`f179e84`** — **Phase C: HTTP API sketch (Slice 8)** — New `docs/maintainability/http_api.md` (~140 lines) consolidates all 58 routes: mount layout (7 routers/prefixes), public routes by group (chat, UI/legal, health, events, contribute, programs), admin HTML routes (`/admin/*`, cookie-gated via `verify_admin`), admin JSON API routes (`/admin/api/*`, `Depends(require_admin)`), auth posture summary, rate limits (5 slowapi + 1 custom contribute limiter), schema pointers to `app/schemas/`. Indexed in project_index Maintainability table; §5 gap bullet for "no API reference doc" removed. Backlog #18 Phase C now 2/6 ticked (Railway + HTTP API). Doc notes but doesn't gate on two observations: `POST /events` is a public rate-limited endpoint (Phase 1 leftover?) and `/admin/debug-pw` is an unauthenticated admin debug helper (verify production posture); both queued for future review.

- **`765ee61`..`06b62c7`** — **Phase C kickoff: Railway service layout doc (Slice 7)** — New `docs/maintainability/railway_layout.md` consolidates Hava's Railway deployment surface (process types, build, DB URL resolution, env var matrix, .env semantics, health checks, deploy flow, explicit non-coverage list). Indexed in project_index Maintainability table; §5 gap bullet for Railway service layout removed. Backlog #18 Phase C restructured into sub-bullets (1 ongoing component-docs growth + 5 §5 gaps); Railway sub-bullet ticked. 4 §5 gaps remain (HTTP API sketch, CI query-battery story, provider ingestion lane, end-to-end provider/program creation).

- **`ea4fcfb`..`a4d8449`** — **Phase B `repo root` convention (Slice 6) — Phase B complete** — Repo root reserved for project spine (top-level packages, build/deploy config, tooling config, architecture spine doc); operational clutter (local SQLite DBs, script logs, env overrides, bytecode) gitignored at root or under packages; live-session captures go to `relay/`. Convention documented in `README.md` along with a current-Hava rewrite (replacing the stale 16-line Phase 1 stub that referenced removed `POST /events` endpoints) and a navigation table pointing at STATE/BACKLOG/WORKING_AGREEMENT/HAVA_CONCIERGE_HANDOFF/project_index/persona-brief/CURSOR docs. Misfiled `admin-dashboard-pending.png` (~30KB, no references) removed; recoverable via git log. `project_index.md` §5 gap bullet ("README does not mention relay/") removed since closed by this rewrite. **Backlog #18 Phase B is now complete (4/4 sub-bullets ticked: EOL, scripts, docs, root). Remaining #18 work: Phases A (ongoing), C, D.**

- **`f8da738`..`0fad8ee`** — **Phase B `docs/` archive convention (Slice 5)** — Two misfiled session transcripts (`docs/phase-6-1-2-dry-run-transcript-2026-04-21.txt`, `docs/phase-6-1-3-execution-transcript-2026-04-21.txt`, ~13KB total) removed from the tree (recoverable via git log). Convention formalized in `docs/CURSOR_ORIENTATION.md` (Process conventions bullet) and `docs/maintainability/project_index.md` (post-doc-list paragraph): session transcripts and slice-complete writeups go to git history; live-session captures go to `relay/` (gitignored). Backlog #18 Phase B `docs/` sub-bullet ticked; remaining Phase B work is the root-convention sub-bullet.

- **`28cd5c6`..`97b642d`** — **Phase B `scripts/` convention (Slice 4)** — `scripts/README.md` rewritten from 13 to ~50 lines: directory convention table (committed tools, fixtures, baselines, output, gitignored ephemeral results) plus alphabetical inventory of all 16 tracked CLI tools with one-line purpose, output-path notes, and references to known-issue Backlog items where applicable. Backlog #18 Phase B `scripts/` sub-bullet ticked; tool default-path migration filed as Backlog #19, and the disposition of 5 legacy tracked outputs in `scripts/` (~410KB) filed as Backlog #20. No code change; no file moves in this slice.

- **`857c7fc`..`2627693`** — **Phase A drift sweep (Slice 3)** — `docs/maintainability/project_index.md` picks up the missing `CURSOR_NEW_CHAT_PLAN.md` row and a `.gitattributes` mention in the root tooling files line. Backlog #8 resolved by tightening the `tier_used` comment in `app/chat/unified_router.py:96` to match the precise wording already documented in `docs/components/unified_router.md`'s taxonomy table. Backlog #18 truncation paragraph annotated with a sandbox-unreliability follow-up: bash mount produces NUL-append and mid-content-truncation artifacts that don't reflect Casey's real filesystem; the original incident was never independently verified via PowerShell and may have been spurious.

- **#18 Phase B — EOL normalization (`23b2054`..`dc917f4`)** — Added repo `.gitattributes` (`text=auto eol=lf` default + binary markers). HEAD was already LF; ship primarily fixes Windows-side CRLF drift on checkout via working-tree refresh. Pre/post `pytest -q` matched exactly (verification of behavior-neutrality). Truncation incident on `docs/BACKLOG.md` and `docs/STATE.md` discovered in pre-step survey; HEAD-restore performed before any normalization touched the index; forensic `stat` data and finding logged under Backlog #18.

- **`ed76435`..`67740de`** — **PM organization program kickoff (Slice 1 + Backlog #18 open)** — Added `docs/maintainability/project_manager_organization_brief.md` (zero-context PM onboarding, phases A–D, anti-patterns, success criteria) and `docs/CURSOR_NEW_CHAT_PLAN.md` (Mode A/B playbook, doc read order, paste templates, ship close-out). Indexed in `docs/maintainability/project_index.md`; `docs/CURSOR_ORIENTATION.md` now points to the playbook. Backlog #18 opened to track phased execution; the ~79-file CRLF↔LF working-tree drift surfaced during this session is queued as a Phase B sub-bullet, not bundled into this ship.

- **`364cd5f`** — **`docs/maintainability/findings_app_chat.md`:** H1 “two parallel chat systems” marked historical (`router.py` / **`POST /chat`** removed per H1 ship).

- **Canonical doc sync (`656d54b`)** — Added **`HAVA_CONCIERGE_HANDOFF.md`** content at repo root; rewrote **`docs/STATE.md`** for RS-only posture and current backlog snapshot; fixed **`docs/PROJECT.md`** Tier 1 + catalog lines; refreshed **`START_HERE`**, **`CURSOR_ORIENTATION`**, **`CLAUDE_SESSION_BRIEFING`**, **`persona-brief`** (status + historical sections), **`BACKLOG`** (13/15 resolved, 16 OPEN), **`query-test-battery`**, **`known-issues`**, **`project_index`**, **`findings_app_chat`**, **`havasu-development-plan`** header; small comment updates in **`tier1_templates`**, **`smoke_concurrent_chat`**.

- **Doc hygiene + navigation (`905ce17`..`e83ccf0`)** — Added `docs/maintainability/project_index.md` (repo map, flows, doc index), `docs/maintainability/chat_behavior_followup_plan.md` (deferred chat eval + routing notes), and pruned slice-complete tier/Railway/handoff markdown (substance retained in **git history**). Updated cross-references in orientation docs, `known-issues`, `requirements.txt` comment, RS backfill index/runbook, `h2_consolidation_decision.md`, etc.

- **Non–River-Scene catalog cleanup (`81fe20c` + doc `7cba51e`)** — Script-driven removal of non–River-Scene catalog rows and lanes; production apply per retrospective. Chat and admin paths remain; first-party growth is **River Scene** + **approved contributions**.

- **H2 LLM infrastructure (`h2_consolidation_decision.md` § Status — completed)** — `app/core/llm_messages.py` and migrated Anthropic callers in Tier 2 parser/formatter, `llm_router`, Tier 3. Session-2 kickoff markdown files were later **removed from the tree**; design + commit stack remain in the decision doc and git log.

- **H1 legacy `/chat` removal (`61387e4`..`23a39a5`)** — Sole chat entry is **`POST /api/chat`**. Historical detail in `docs/maintainability/h1_router_decision.md` and git log.

## Queued / open work

See **`docs/BACKLOG.md`**. Snapshot:

- **OPEN** — **2**, **18** (see `docs/BACKLOG.md` for titles).
- **Recently resolved** — **8** (Slice 3, `2627693`); **19** (Slice 10, `d429fe7`); **20** (Slice 11, `15f7248`); **23** (Slice 13, `c94afb6`); **22** (Slice 14, `72728e2`); **21** (Slice 15, `b1a0add`); **12** (Slice 16, `fd313bb`); **16** (Slice 17, `ab8df88`); **14** (Slice 18, `dbb16f8`); **7** (Slice 21, `9020b2d`); **24** (Slice 22, `ee6bf75`); **25** (Slice 23, `b34fea1`); **3** (Slice 24, `85cedd0`); **26** (Slice 26, `8d063ff`); **5** (Slice 27, `486dc11`); **27** (Slice 41, `7dd714c`); historical: **13**, **15** (`656d54b`).
- **DEFERRED** — **9** (Tier 1 hit rate; pending provider population), **11** (slowapi Python 3.14 deprecation; pending upstream fix or version pin), **17** (OpenAI helper extraction until a second caller exists).
- **Confabulation / eval** — operator harness: `docs/confabulation-eval-runbook.md`, code under `app/eval/`. Broader “phase 8.8.6” spec markdown was pruned; recover from git history if needed.

## Working tree

Tracked files should be clean at close-out. Optional untracked artifacts under `relay/` (gitignored) are expected per `relay/README.md`.

## How to update this document

At the end of each session that ships work, update:

- **Repo / deploy pointers** — commit hash, production verification notes, catalog counts if materially changed
- **Recent commits** — prepend; keep ~10–12 lines
- **Recently shipped** — what changed for users or architecture
- **Queued** — align with `BACKLOG.md`

Do not update mid-session; update as part of verified close-out.
