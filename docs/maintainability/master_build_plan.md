# Master Build Plan — Havasu Chat V1

> **Status:** Authoritative execution plan as of 2026-05-14.
> **Audience:** Cowork primary (and all future primaries), Casey as operator, any dispatched agent (Cursor / Claude Code / sub-agent).
> **Supersedes (for execution sequencing):** `docs/STRATEGY_PIVOT_2026-05-12.md` §4 and §5. The pivot doc remains the strategic-direction-of-record; this plan is HOW we execute against it under the 2026-05-14 build-first amendment.
> **Lives alongside:** all 8 architectural design memos in `docs/maintainability/` (see §3 below).
> **How to use:** Read §0 → §3 once for context. Then operate against §4 phase-by-phase. Update §10 (decision log) as decisions land. Update §9 (calendar) as phases ship.

---

## §0 How to use this doc

This is your operating plan for the next 6-9 months. It's not a strategy doc (the pivot doc is) and it's not a design memo (the 8 design memos in `docs/maintainability/` are). It's the sequenced execution sheet that says **what gets built when, by whom, with what dependencies, and what success looks like at each step**.

When in doubt about priorities: this doc wins. When this doc disagrees with a design memo on sequencing, this doc wins. When it disagrees with a design memo on architecture or implementation detail, the design memo wins (and this doc gets amended). When this doc disagrees with the strategy pivot doc on monetization or product framing, the pivot doc wins.

Update cadence: amend after each phase ships (ship-log style — keep old phase content + add new) plus when a strategic question surfaces that resolves in this doc rather than separately.

---

## §1 Strategic context (one paragraph each)

**What we're building.** Havasu Chat is a comprehensive hyperlocal directory and AI chat product for Lake Havasu City. Covers every useful business + place + event + program for every demographic (residents, snowbirds, tourists, vacation-rental guests, families, retirees, hobbyists). Three equal front doors: browse, search, ask Hava. Strategic bet: generic internet (Google/Yelp/Facebook groups) can't compete on hyperlocal context. Bootstrapped, solo-founder.

**Build-first / sell-after sequencing.** No sales until the site is functioning as a comprehensive directory + AI chat. Cold-pitch materials already written (`docs/sponsor_outreach/verified_presence_*.md`) sit on the shelf as default-plan artifacts until monetization is locked. Monetization model kept flexible through build; finalized via cold-pitch ground-truthing at launch.

**The defensible angle.** Hyperlocal context depth. Knowing English Village fills up after 5pm Fri-Sun. Knowing the bridge backs up Sunday afternoons. Knowing snowbird season changes which businesses are open. Knowing emergency plumbers in Havasu operate differently than what Phoenix Yelp suggests. The Generic Internet can't replicate this; the data is structurally inaccessible to them.

---

## §2 What's already shipped (current state, 2026-05-14)

- **Production:** `https://havasu-chat-production.up.railway.app`. Currently at origin `ec6869a` post-Opus-handoff push. Pytest 1476 passed.
- **Schema foundations:** Category model + 12 seeded categories + category_id FKs on Provider/Program + attributes JSON + district String + Provider.slug field + backfill migration. Alembic head `f1a2b3c4d5e6`.
- **Provider profile page:** `/provider/<slug>` shipped end-to-end (route + view-model + queries + full template + 18 tests). Includes hybrid call button, freshness bands at code level (rendering polish lands in Phase 6), Hava's pick badge, claim/upgrade CTAs, /chat?q= prefill working.
- **Rate-limiter:** Option A SourceLimiter live; Google Places routed through it; scripts collapsed to use it. 16 new tests.
- **Rate-limiter §8 decisions:** all 8 locked.
- **Sponsor outreach surface:** Verified Presence pitch + extended objection FAQ + leave-behind + Day-2/Day-7 follow-up emails + referral-ask script.
- **8 architectural design memos** (full catalog in §3).
- **Pivot doc amendments** for build-first sequencing.
- **5 pre-pivot entry-point docs** updated with pivot banners.
- **Dispatch playbook** updated with session-13 lessons (5 new gotchas).
- **Provider.category backfill mapping DRAFT** (sub-agent investigation; awaiting operator review pass).

---

## §3 Foundation inventory — design memos that drive this plan

The phase content below references these. Read each when starting the corresponding phase.

| # | Memo | Purpose |
|---|---|---|
| 1 | `architecture_gaps_for_full_vision_audit.md` | 22 gaps classified by build-phase + monetization-fit |
| 2 | `place_model_design.md` | Place schema (NOTE: superseded for table-shape by unified ENTITY; content carries forward as ENTITY extension fields) |
| 3 | `account_lite_v01_design.md` | Magic-link auth via Resend; 5 tables (User + MagicLinkToken + Session + UserFavorite + Claim) |
| 4 | `background_job_infrastructure_decision.md` | Railway scheduled-jobs + FastAPI BackgroundTasks + optional Outbox table |
| 5 | `layered_scrape_strategy.md` | 5-layer pull: Google → OSM → city/state → specialized → manual |
| 6 | `conditions_panel_and_alerts_design.md` | AirNow + NWS + USGS external API integrations + alert dispatch |
| 7 | `image_storage_design.md` | Cloudflare R2 + Pillow processing + Photo schema |
| 8 | `search_index_decision.md` | Postgres FTS + pg_trgm for V1 with clean Meilisearch upgrade path |
| 9 | `boat_access_mode_design.md` | Directory-wide mode (not just a filter); JSON schema per venue type |
| 10 | `manual_recovery_checklist.md` | Operator field-trip workflow for inventory no API covers |
| 11 | `category_backfill_mapping_DRAFT.md` | 41 legacy category strings → original 12 canonical slugs (now superseded for target slugs by new-taxonomy 12) |
| 11a | `category_backfill_mapping_audit_2026-05-14.md` | Audit of #11 against the locked new-taxonomy 12. Surfaces 5 strings that map CLEANER under new taxonomy + 5 professional-services strings that NULL out (V1.5 deferral). Master-plan-hole flag at §3 led to Phase 3 amendment 2026-05-14 absorbing category-seed rewrite + audited backfill. 2 of 5 §4 lock-now items resolved 2026-05-14; remaining 3 are trivial confirmations. 4 Phase-3 review questions queued for operator. |
| 12 | `phase2_5_rate_limiter_decisions_memo.md` | 8 locked decisions for rate-limiter (already implemented) |
| 13 | `pre_pivot_doc_banner_audit.md` | 13 docs need banners (5 done; 8 remain for Phase 10) |

Plus inputs:
- `outputs/chatgpt_taxonomy_research_synthesis.md` — locked 12-category Tier 1/2/3 taxonomy + sponsor packaging recommendations
- `outputs/opus_design_handoff/README.md` — unified Hava card grammar + 4-level browse + district context paragraphs + Events as ENTITY + freshness band
- `outputs/opus_47_feature_suggestions_response.md` — 7 locked features (conditions panel, heat-aware ranking, seasonal hours, boat-access mode, crowd context, mobile-services, alerts) + 1 deferred (peer recommendations V1.5)
- `outputs/chatgpt_response_eat_and_drink_strategic_review.md` — Eat & Drink-specific UX patterns
- `outputs/chatgpt_response_home_services_category_page_spec.md` — Home Services UX spec with locked decisions

---

## §4 Phase-by-phase build sequence

13 phases total. Phases overlap intentionally where there's no dependency conflict; the rough calendar view in §9 shows this.

### Phase 1 — ENTITY schema foundation (3-4 weeks) — **SHIPPED 2026-05-14**

**Status:** ✅ **COMPLETE.** Phase 1 shipped across four sequential sub-phases (1A schema additive → 1B data backfill → 1C application read pivot → 1D dual-write + entity_id NOT NULL flip + close-out) plus the production-deploy hotfix (`5132162`, Postgres boolean-default coercion). Total commit chain: `ff9832d` → `d475b06` → `5132162` → `e0417c8` → `3f3628e`. Total pytest delta: **1476 → 1518** (+42 net-new). Final alembic head: `f8e9d0c1b2a3` (legacy entity_id NOT NULL flip). Production Postgres confirmed at `b2c3d4e5f6a7` since the `5132162` deploy; the Phase 1C application read pivot + Phase 1D dual-write + NOT NULL flip migration are NOT yet deployed (operator-cadence call — chat-route response shape is pinned unchanged via Cursor's regression suite, so deploy can land at any natural cadence). Phase 2 (account-lite + image storage + search index) is now the next dispatchable lane; Lane 2A and Lane 2B are file-disjoint per dispatch_protocol Rule 3 — can dispatch concurrently. Both need fresh dispatch briefs authored — none queued yet.

**Goal:** Migrate the current Provider + Place (deferred) + Event + Program separate-tables architecture into the unified ENTITY core recommended by ChatGPT's taxonomy research (locked 2026-05-14).

**Why first:** Every subsequent phase depends on this. Scrapers write to ENTITY. Category pages query ENTITY. Chat retrieves from ENTITY. Search indexes ENTITY. Image storage references ENTITY. Doing this before any other work avoids painful production data migrations later.

**Deliverables:**
- New `entities` core table with `entity_type` discriminator (commercial / place / event / program), `slug`, `name`, `description`, `last_verified_at`, `source`, `is_active`, standard timestamps
- New extension tables: `entity_categories` (M:M), `locations`, `hours`, `seasonal_hours`, `contact_points`, `features`, `offerings`, `service_areas`, `schedules`, `source_evidence`, `sponsorship_slots`
- Migration script that moves existing Provider/Program/Event rows into entities + extensions (Provider rows become `entity_type="commercial"`, Program rows become `entity_type="program"`, Event rows become `entity_type="event"`)
- Application-layer updates: app/providers/queries.py, app/providers/view_models.py, app/chat/tier2_db_query.py, app/contrib/places_client.py, app/contrib/enrichment.py, scripts/places_load.py — all queries pivoted to entities table
- Sponsor.entity_type discriminator added (Sponsor.business_id already FK-less per audit; just need to disambiguate Provider vs Place ID)
- Pytest stays green throughout — extensive regression coverage

**Dependencies:** None. Foundational.

**Effort estimate:** L+ (3-4 weeks). Touches more app code than any other lane. Sequential dispatch (not parallelizable — too much overlap on app/db/models.py and app/chat/).

**Success criteria:** All existing pytest passes against the new schema. Provider profile page renders correctly using ENTITY queries. No chat-route regression.

**Open question for Casey:** should the ENTITY migration use Alembic's `op.execute` for the data move, or a separate Python script run post-migration? Recommend Alembic batch operations for atomicity. **RESOLVED 2026-05-14 in `outputs/cursor_brief_phase_1_entity_schema.md` §2 + §6.1:** Alembic batch operations within the migration's `op.execute` blocks; idempotency guards on backfill INSERTs so the migration is safe to re-run.

**Shipped (incremental, each sub-phase committed separately):**

- **Phase 1A — schema additive (2026-05-14, commit `ff9832d`):** entities core + 11 extension tables (entity_categories, locations, hours, seasonal_hours, contact_points, features, offerings, service_areas, schedules, source_evidence, sponsorship_slots) + Sponsor.entity_type discriminator. Migration `a1b2c3d4e5f6` chains off `f1a2b3c4d5e6`. Pytest **1476 → 1497** (+21 net-new in `tests/test_entity_schema.py`). Ruff clean. Cursor session, ~half-day operator-walltime vs the brief's 5-8 day estimate — brief was conservative since Phase 1A is genuinely lighter than 1B/1C/1D (no data touched, no app code). **Four well-reasoned brief deviations transparently flagged by Cursor + accepted by primary:** (a) `passive_deletes=True` on all Entity→extension relationships so SQLite `ON DELETE CASCADE` works without SQLAlchemy's NULL-before-cascade dance; (b) `engine.dispose()` after the cascade-delete test (caught a `PRAGMA foreign_keys=ON` leak that would have silently broken unrelated DELETE tests via pooled connections — strong catch, would have been flakiness in CI later); (c) `SourceEvidence.verified_at` nullable to match legacy `Provider.last_verified_at` for Phase 1B backfill compatibility; (d) `SeasonalHours.hours_overlay` nullable since the table ships empty in Phase 1. All four within the brief's §11 acceptable-deviations guardrails. **Operator-side carry-over before 1B re-dispatch:** drop + recreate the local SQLite dev DB (Cursor's Path A chain-walk surfaced pre-1A drift at the `1a2b3c4d5e6f → 2a3b4c5d6e7f` step with `duplicate column name: slot`; benign for production but compounds confusion if not cleared before 1B). **No production runtime impact** — schema additive, dormant until 1B backfills data and 1C pivots reads.
- **Phase 1B — data backfill (2026-05-14, commit `d475b06`):** migration `b2c3d4e5f6a7` chains off `a1b2c3d4e5f6`. Backfilled all existing Provider/Event/Program rows into `entities` + extension records (locations, hours, contact_points, entity_categories, schedules, source_evidence). Sponsor.entity_type set to `commercial` for legacy rows. Backfill logic factored into `app/db/entity_backfill.py` (579 lines) for unit-testability + idempotent re-run; migration file stays slim at 86 lines (calls into helper). Pytest **1497 → 1503** (+6 net-new in `tests/test_entity_backfill.py`: per-type backfill correctness, Sponsor.entity_type backfill, idempotency, nullable-entity_id pin). Fresh-DB alembic upgrade walks the full chain cleanly. Ruff clean on touched paths. **Two pragmatic deviations flagged by Cursor + accepted:** (a) `entity_id` FK columns on providers/events/programs stay **nullable through Phase 1B AND Phase 1C** instead of flipping to NOT NULL in stage 3 of the migration — the brief over-specified; existing test fixtures create Provider/Event/Program rows directly and would fail integrity if non-null landed before Phase 1D's dual-write helpers populate entity_id at write time; deviation pinned in `test_entity_id_fk_columns_remain_nullable_for_dual_write_gap`. NOT NULL flip moved into Phase 1D deliverables. (b) SQLite time binds normalized via `_sql_time` helper to avoid `datetime.time` bind errors. **Brief amended 2026-05-14** at §3 + §6.1 + §8 to reflect the deferred flip. **Operator note:** Python 3.14 + SQLite datetime adapter deprecation warnings during test run are noise (not blocking). **No production runtime impact** — backfilled data is dormant until 1C pivots application reads.
- **Phase 1C — application read pivot (2026-05-14, commit `e0417c8`):** application-layer reads pivoted from `providers`/`events`/`programs` to `entities` + extensions across the §4 Phase 1 touch list. Pattern A (ENTITY-aware joins on legacy drivers) used across `app/chat/tier2_db_query.py` (the heaviest single file at +176 lines — outerjoin Entity + type guard, location SQL also matches `locations.address`, `_provider_dict` prefers `entity.location.address` when present, category matchers extended for `entity_categories` + `offerings` + Entity name, open-now uses `effective_hours_structured`); Pattern B (legacy + alias via `Provider.entity` relationship) used across `app/providers/queries.py` (+135 — `get_provider_by_slug` eager-loads entity + extensions; new `effective_hours_structured`, display/contact/directions helpers; `is_open_now` reads ENTITY hours when present) and `app/providers/view_models.py` (+12 — consumes the new helpers). Light touches on `scripts/places_load.py` (+20 — upsert lookup keys include `locations.google_place_id` via join) and `app/contrib/places_client.py` (+7 — docstring pointer only, no behavior change). `app/contrib/enrichment.py` no-op verified — contributions store Places results on `Contribution` not `Provider.entity`, so there's no read path to pivot. **No migration; alembic head unchanged at `b2c3d4e5f6a7`** (1C is application-layer only). Pytest **1503 → 1512** (+9 net-new across 3 test files: `tests/test_tier2_db_query_entity_pivot.py` +5 query-shape regressions [entity-name, time-window, location-filter, open-now, plus a program-orphan outerjoin guard] + `tests/test_provider_profile_page.py` +1 [entity-location/hours/contact rendering preference] + `tests/test_chat_route_integration.py` extension `ChatRoutePhase1CEntityPivotTests` +3 methods [tier2 listing shortcut, tier1 phone lookup, tier2 listing coffee — all entity-linked provider variants]). Ruff clean on touched paths after import-order + unused-import sweep. Fresh-DB alembic upgrade walks the full chain through `b2c3d4e5f6a7`. **One substantive deviation flagged by Cursor + accepted:** the brief's §7.1 Pattern A prescribed a strict `select(Entity)` ENTITY-first query shape, but Cursor implemented a hybrid (legacy-driver outerjoin Entity + `entity_id IS NULL` orphan fallback). Correct call given Phase 1B's deferred NOT NULL flip — test fixtures still construct Provider/Event/Program rows directly without `entity_id`, so a pure ENTITY-first SELECT would silently drop them and break the suite. The brief was authored before Phase 1B's NOT NULL deferral landed; Cursor recognized the staleness and chose the safer shape. **Implication for Phase 1D:** once dual-write helpers populate `entity_id` at write time AND every test fixture path goes through the helpers, the NOT NULL flip can land + this hybrid pattern can re-tighten back toward strict ENTITY-first if desired (worth folding into the Phase 1D dispatch brief as an explicit decision point). **Two minor reporting deviations also flagged + accepted:** `_provider_dict` preferring `entity.location.address` is on-spec per Pattern A (Cursor over-cautiously documented as a deviation); `app/contrib/enrichment.py` no-op is documented for completeness. **Operator notes from §11:** profile-test phone numbers must avoid `555-01XX` placeholders (formatter hides them); chat-integration seed fixtures use unique `google_place_id` suffixes to avoid collisions. **No production runtime impact** — application reads pivoted but writes still land in legacy tables (Phase 1D adds dual-write); chat-route response shape unchanged; Provider profile page renders identical HTML; Tier 2 catalog returns identical rows in identical order. Cursor session, single dispatch.
- **Phase 1D — dual-write + entity_id NOT NULL flip + close-out (2026-05-14, commit `3f3628e`):** Phase 1 close-out lane. New shared module `app/db/entity_dual_write.py` factors `create_provider_and_entity` / `create_event_and_entity` / `create_program_and_entity` helpers (legacy row + Entity row + extension records constructed in the same SQLAlchemy session, atomic commit, idempotent via `_get_entity_maybe_pending` + `entity_id already set` skip) plus `sync_provider_entity_from_legacy` for Places/CSV update paths. New `app/db/sponsor_resolve.py` implements §8.2 discriminator-branching sponsor lookup (`commercial → Provider via str(business_id)` lookup; `place → None` no-row code path). Ingest paths routed through helpers: `app/admin/router.py` (provider + program create), `app/contrib/approval_service.py` (provider/event/program approval), `scripts/places_load.py` (insert + sync on update), `scripts/ingest/ingest_enrichment_csv.py` (insert + sync on update), `app/programs/router.py` (JSON POST `/programs` + parent POST `/programs/submit`). New alembic migration `f8e9d0c1b2a3_legacy_entity_id_not_null` chains off `b2c3d4e5f6a7` — `op.batch_alter_table` × 3 with portable `nullable=False` only (Postgres-clean per brief §10 portability rules; no boolean defaults, no raw SQL). `app/db/models.py` updated: `providers.entity_id` / `events.entity_id` / `programs.entity_id` flipped from `Mapped[str | None]` to `Mapped[str]` + corresponding `entity` relationships made non-optional. `app/home/sponsor_store.py` adds optional `linked_provider_id` / `linked_provider_name` when sponsor resolution succeeds. Pytest **1512 → 1518** (+6 net-new in `tests/test_phase1d_dual_write.py`: provider/event/program dual-write + sponsor commercial/place + idempotency). `tests/test_entity_backfill.py` Phase 1B nullable-pin test inverted to `test_entity_id_fk_columns_are_not_null_after_phase_1d_closeout`. Ruff clean. Migration applies cleanly via fresh tests/conftest.py SQLite path through `f8e9d0c1b2a3`. **Three pragmatic deviations flagged by Cursor + accepted:** (a) **`before_flush` Session listener safety net** registered via `register_catalog_dual_write_hooks` in `app/db/database.py::_register_orm_listeners` — catches any raw `db.add(Provider|Event|Program)` paths that bypass the explicit helpers (test fixtures + future stragglers); same precedent as session-13's slug-listener pattern in `register_provider_slug_hooks` (`d967568`); double-call is harmless via the idempotency guards. Circular-import workaround: `_register_orm_listeners` imports `app.db.models` first, then `app.db.entity_dual_write`. (b) **`derive_provider_slug` Entity.slug collision reservation** (`app/db/seed_helpers.py`) — `derive_provider_slug` now reserves slugs used by commercial Entity rows (and pending Entity in session) to avoid `UNIQUE entities.slug` failures when the DB already has Entity rows from Phase 1B backfill or seed; direct consequence of dual-write + global Entity.slug uniqueness. (c) **`Sponsor.business_id` stays Integer**; resolution uses `str(business_id) → Provider.id` lookup (test fixtures use numeric-string IDs like `900042`); brief §8.2 explicitly allowed this — Phase 11 monetization will unify Sponsor.business_id to Entity.id String UUID. Brief §8.1's speculative `app/api/routes/events.py` doesn't exist; Cursor correctly identified that Event creates flow through `approve_contribution_as_event` (River Scene + Parks & Rec); `app/contrib/river_scene.py` is parse-only. **Decision-point resolution:** Cursor chose **Option X** (left the Phase 1C orphan-fallback hybrid pattern unchanged in `app/chat/tier2_db_query.py` + `app/providers/queries.py` + `scripts/places_load.py`) — defensive, no churn, dead-branch cleanup deferred to Phase 13 if ever desired. **Coverage interpretation deviation (acceptable):** brief §8.3 said "extend each ingest-path test"; Cursor instead added 6 helper-level tests in a new `test_phase1d_dual_write.py` file — the `before_flush` safety net guarantees ENTITY creation regardless of which call site invokes it, so testing the helper + safety-net path covers all sites without per-site duplication. **No production runtime impact** until next deploy; once deployed, every new Provider/Event/Program creation populates both legacy and ENTITY in one transaction, and Sponsor lookups disambiguate via entity_type. Cursor session, single dispatch.

---

### Phase 2 — Foundation infrastructure: account-lite + image storage + search index (3-4 weeks, two parallel lanes) — **SHIPPED 2026-05-12**

**Status:** ✅ **COMPLETE.** Phase 2 shipped across two parallel lanes (Lane 2A account-lite v0.1 + Lane 2B image storage + search index), six sub-phase ships total. Lane 2A chain: `6000138` (2A.1 schema) → `714ca52` (2A.2 auth flow) → `5fea2ce` (2A.3 claim flow + favorites + admin role + viewer_is_owner + close-out). Lane 2B chain: `d631c77` (2B.2 FTS infra) → `8338505` (2B.3 search bar UI + /api/search) → `1c57c73` (2B.1 photos schema + R2 + Pillow + upload routes + sweep + Lane 2B close-out). Total pytest delta across Phase 2: **1518 → 1663** (+145 net-new). Final Phase 2 alembic head: `f9e8d7c6b5a4` (2B.1 photos table; chain `92ce4899dc08 account-lite → c8d9e0f1a2b3 FTS → f9e8d7c6b5a4 photos`). Production Postgres still at `b2c3d4e5f6a7` (session-15 hotfix) — `f8e9d0c1b2a3 → 92ce4899dc08 → c8d9e0f1a2b3 → f9e8d7c6b5a4` all ship on the next deploy (four migrations to walk). R2 operator prereq locked 2026-05-12 in session-19; Resend operator prereq locked in session-17. Phase 3 (v1.1 schema pass + districts + alerts + category taxonomy rewrite) is now the next dispatchable lane.

**Goal:** Land the three foundational systems that everything downstream depends on. Two parallel lanes possible because they touch different parts of the codebase.

**Lane 2A — Account-lite v0.1 — SHIPPED 2026-05-12 (commits `6000138` → `714ca52` → `5fea2ce`):**
- Per `account_lite_v01_design.md` Option A (server-side session table)
- 5 new tables: users, magic_link_tokens, sessions, user_favorites (now points to entities.id), claims (now points to entities.id)
- Auth routes: `/login`, `/api/auth/request-link`, `/auth/callback`, `/logout`, `/account`
- Resend integration (operator account creation + API key wiring)
- Session middleware attaching current_user to request state
- Admin role gating on `/admin/*` (currently unprotected past Railway basic auth)
- `viewer_is_owner` flag wired into ProviderProfileVM (pre-built hook per audit)
- Magic-link email template + dev-mode flag for local development
- Effort: M-L (5-7 days dispatch)

**Lane 2B — Image storage + search index — SHIPPED 2026-05-12 (commits `d631c77` → `8338505` → `1c57c73`):**
- Per `image_storage_design.md` (Cloudflare R2)
- Per `search_index_decision.md` (Postgres FTS + pg_trgm)
- Cloudflare R2 bucket + custom CDN domain (operator action)
- `photos` table with polymorphic (entity_id) reference
- Pillow image processing pipeline (thumbnail/medium/hero variants + EXIF strip + WebP conversion + dedup hash)
- Upload route `/api/entities/<id>/photos` with auth + claim ownership + MIME validation + size limit
- Postgres FTS: tsvector columns on `entities` (generated columns combining name/description/extension data with weights), GIN indexes, `pg_trgm` extension + trigram indexes on name
- Search ranking heuristic (verification +30, recency +15, featured +25, etc.)
- Chat tier 2 LIKE → FTS migration in `app/chat/tier2_db_query.py:33+` (preserving the existing `_category_needle_set` synonym expansion at `:469`)
- Search bar UI + endpoint
- Effort: M-L (7-10 days dispatch)

**Dependencies:** ENTITY schema (Phase 1) must be complete before Phase 2 starts. Both lanes can run in parallel within Phase 2.

**Success criteria:** Magic-link login works end-to-end. Photo uploads land in R2 + serve via CDN. Search bar returns ranked results across entities. Chat tier 2 uses FTS instead of LIKE. Pytest stays green.

**Shipped (incremental, each sub-phase committed separately):**

- **Phase 2A.1 — Schema + ORM + Resend scaffold (2026-05-11, commit `6000138`):** Five additive tables (`users`, `magic_link_tokens`, `sessions`, `user_favorites`, `claims`) per `outputs/cursor_brief_phase_2a_account_lite.md` §4. New `app/auth/` package with `email_sender.py` (`send_magic_link` with `AUTH_DEV_MODE` dev-mode fallback, mirrors `RATE_LIMIT_DISABLED` convention). Migration `92ce4899dc08_account_lite_v01` chains off `f8e9d0c1b2a3`; Postgres-portable (`sa.true()` / `sa.func.now()` / no raw SQL); FK `ondelete` `CASCADE` for parent-deletes + `SET NULL` for `claims.verified_by`; CHECK constraints on `users.role` / `claims.status` / `claims.verification_method`; UniqueConstraints on `users.email` + `user_favorites(user_id, entity_id)` + `claims(user_id, entity_id)`. **`user_favorites` + `claims` FK to `entities.id`** (master plan amendment over the design memo's polymorphic shape). Pytest **1518 → 1543** (+25 net-new across `tests/test_account_lite_schema.py` 21 runs + `tests/test_email_sender.py` 4). Alembic head **`f8e9d0c1b2a3` → `92ce4899dc08`**. Ruff clean. Migration upgrade+downgrade+upgrade cycle passes on fresh SQLite. **Three pragmatic deviations flagged by Cursor + accepted:** (a) `Session` ORM class renamed to `AuthSession` (table still `sessions`) to avoid `sqlalchemy.orm.Session` namespace clash — explicit allowance in brief §4.3; (b) `alembic/env.py` import-order reorder (`database` before `models`, with `noqa: I001`) to prevent partial-import deadlock when Alembic loads `Base.metadata` cold; (c) dev-mode test asserts `logger.info` mock instead of `caplog` capture — equivalent coverage. **Dispatch artifacts shipped in companion commit `5bf4c14`** (`outputs/cursor_brief_phase_2a_account_lite.md` + `outputs/cursor_dispatch_prompt_phase_2a.md`). **No production runtime impact** — schema dormant until 2A.2 wires routes. Cursor session, single dispatch.
- **Phase 2A.2 — Auth flow + session middleware + login UI (2026-05-11, commit `714ca52`):** Four magic-link routes shipped (`GET /login`, `POST /api/auth/request-link`, `GET /auth/callback`, `POST /logout`) plus an extra `GET /account` landing route (pragmatic deviation — required for the callback's default `/account` redirect, mirrors `home.html` chrome). New `app/auth/session.py` (136 lines) with `SessionMiddleware` (Starlette `BaseHTTPMiddleware`) that reads `hava_session` cookie + verifies itsdangerous signature + looks up `AuthSession` row + checks `expires_at` + `user.is_active`, attaches `request.state.current_user` / `current_session`, debounced `last_seen_at` update via module-level `_LAST_SEEN_MONO` dict (60s window), cookie-clear-on-response when session invalid. New `app/auth/dependencies.py` (`get_current_user`, `require_user`, `require_admin`). New `app/auth/email_helpers.py` (`normalize_email`, `is_valid_email`, `generate_magic_link_token`, `hash_token`, `hash_request_ip`, `hash_optional_fingerprint`). New `app/auth/routes.py` (229 lines) with rate limits on `/api/auth/request-link` (slowapi 10/hour IP-keyed + DB count ≥5/email/hour silent path → same check-email template), `_safe_next` open-redirect guard (leading `/`, rejects `//`, `://`, `..` after unquote), find-or-create User on callback, `AuthSession` create + cookie sign + redirect to safe `next` path or `/account`. 4 new login templates (`login.html`, `login_check_email.html`, `login_expired.html`, `account.html`). `app/main.py` extended per brief §6.6 (only middleware + router include). `app/auth/email_sender.py` extended with optional `next_path` param. `tests/conftest.py` adds `AUTH_DEV_MODE=1` + `AUTH_MAGIC_LINK_BASE_URL=http://testserver` via `setdefault`. Pytest **1543 → 1563** (+20 net-new in `tests/test_auth_flow.py` covering happy path + 6 design-memo §5.2 edge cases + middleware states + rate-limit silent path + `_safe_next` adversarial inputs + dependency behavior + `send_magic_link` exception path). Alembic head unchanged at `92ce4899dc08` (no migration in 2A.2). Ruff clean. **Six pragmatic deviations flagged by Cursor + accepted:** (a) `GET /account` route + `account.html` template — brief implied via the §6.4 callback redirect target but didn't list explicitly as a deliverable; Cursor filled the gap cleanly; (b) `send_magic_link(..., next_path=...)` optional param via `urllib.parse.urlencode` so emailed links carry safe `next` query param through verify; (c) detached-User-on-request.state via `db.refresh` + `db.expunge` after `last_seen_at` commit so Jinja templates can read `current_user.email` post-session-close — logout uses `db.get(AuthSession, sess.id)` for the detached-row delete; (d) `last_seen_at` debounce mechanism = module-level `_LAST_SEEN_MONO: dict[str, float]` keyed by session id + `time.monotonic()`, tests pin behavior with `time.monotonic` patched to `lambda: 1.0`; (e) `_hourly_cleanup_loop` fold for expired-token/session GC NOT added — would exceed the anchored "only middleware + router" main.py edit; recommended as follow-up in 2A.3 or its own commit; (f) tests import `app.main` before other `app.auth` imports to avoid models ↔ database listener circular-import edge during collection. Plus a bonus 21st test pinning `send_magic_link` exception path → still renders check-email HTML. **No production runtime impact** until next deploy; once deployed, signed-in users can log in via magic link, sessions are durable, but no merchant-facing affordances render yet (claim flow + viewer_is_owner wiring lands in 2A.3). Cursor session, single dispatch.
- **Phase 2A.3 — Claim flow + favorites + admin role parallel-path + viewer_is_owner + Lane 2A close-out (2026-05-12, commit `5fea2ce`):** Final sub-phase of Lane 2A. Claim flow ships `GET/POST /claim/<slug>` (anonymous-redirect to `/login?next=/claim/<slug>`, Entity-slug lookup via Phase 1D's `derive_provider_slug` reservation, find-or-render-status logic) + admin queue (`GET /admin/claims`, `POST /admin/claims/<id>/verify` with claimant `User.role end_user → merchant` auto-promote + `verification_method` + `verified_by` + `verified_at`, `POST /admin/claims/<id>/reject` with `rejection_reason`). Favorites ship `POST /api/favorites/toggle` (entity_is_favoritable validation against `commercial / place / event`; returns `{action, favorite_count}`), `GET /api/favorites` (JSON list), `GET /account/favorites` (HTML), plus heart-icon JS on Provider profile + Tier 2 catalog render helper. Admin role parallel-path on `app/admin/router.py::_guard`: admin-cookie path stays primary, additive role check on `request.state.current_user.role == "admin"`, authenticated non-admin returns 403, anonymous still redirects to `/admin/login`. `viewer_is_owner` wired via new `_viewer_owns_provider(db, *, current_user, provider) -> bool` helper on `app/providers/router.py` (anonymous False, admin role short-circuits True, otherwise Claim row query with `current_user.id` + `provider.entity_id` + `status == "verified"`). New `app/auth/claims.py` (helpers: `entity_is_claimable`, `find_existing_claim`, `create_pending_claim`, `get_entity_by_slug`) + `app/auth/favorites.py` (helpers: `entity_is_favoritable`, `toggle_favorite`, `list_user_favorites`, `favorite_count_for_user`) + `app/static/favorites.js` (heart-icon click handler reading `data-current-user` / `data-favorite-entity-id`; POST + flip + toast). 5 new templates (`claim_form.html`, `claim_submitted.html`, `claim_status.html`, `admin_claims_queue.html`, `account_favorites.html` — flat path matching existing template tree). 3 new test files (`tests/test_claims.py` 8 + `tests/test_favorites.py` 8 + `tests/test_admin_role_parallel.py` 3). 9 modified files (`app/auth/routes.py`, `app/admin/router.py`, `app/providers/router.py`, `app/providers/view_models.py` — `claim_url` uses Entity.slug when present, `app/chat/tier2_catalog_render.py` — new `render_tier2_provider_cards_html` helper not wired into chat formatter so anonymous chat shape preserved, `app/templates/{account,provider_profile}.html`, `tests/test_auth_flow.py` — import order fix, `tests/test_provider_profile_page.py` — 4 viewer_is_owner cases). Pytest **1563 → 1586** (+23 net-new). Alembic head unchanged at `92ce4899dc08` (no migration in 2A.3). Ruff clean. **Six pragmatic deviations flagged + accepted:** (a) admin claim routes in `app/admin/router.py` vs `app/auth/routes.py` (reuses `_guard`, stays next to admin HTML); (b) `_guard` returns 403 for authenticated non-admin instead of password-login redirect loop (clearer; anonymous behavior unchanged); (c) `render_tier2_provider_cards_html` is a helper not wired into the chat Tier-2 formatter (preserves anonymous chat response shape; ready for future catalog surfaces per brief §11 deferring merchant-edit-form UI); (d) claim notes field is UI-only not persisted (V1 simplicity); (e) flat `account_favorites.html` template (matches existing template tree); (f) `tests/test_auth_flow.py` import order — `app.main` imported first to avoid models ↔ database listener circular import at pytest collection. Also: `create_pending_claim` uses `db.get(Entity, id)` + explicit `entity_is_claimable()` check instead of brief §7.1's recommended JOIN-at-insert shape (simpler, atomic enough). **No production runtime impact** until next deploy; once deployed, signed-in users can claim providers + favorite providers + see Saved listings at `/account/favorites`; admin claim queue available at `/admin/claims`. Cursor session, single dispatch.
- **Lane 2A SHIPPED 2026-05-12.** Phase 2A.1 + 2A.2 + 2A.3 commit chain: `6000138` → `714ca52` → `5fea2ce`. Pytest delta across Lane 2A: 1518 → 1586 (+68 net-new). Final Lane 2A alembic head: `92ce4899dc08` (account-lite v0.1 schema, added in 2A.1 and unchanged through 2A.2 + 2A.3 since neither added migrations). Lane 2A's account-lite infrastructure is feature-complete: magic-link login + signed-in user sessions + claim-and-verify flow with admin review queue + favorites + admin role parallel-path + viewer_is_owner plumbing. **Next dispatchable in Phase 2:** Lane 2B (image storage + search index — file-disjoint per Rule 3, parallel-eligible). Lane 2B brief at `outputs/cursor_brief_phase_2b_image_storage_search.md`; 2B.2 dispatch prompt at `outputs/cursor_dispatch_prompt_phase_2b_2.md`; 2B.1 + 2B.3 dispatch prompts pre-positioned at `outputs/cursor_dispatch_prompt_phase_2b_1.md` + `outputs/cursor_dispatch_prompt_phase_2b_3.md`. Phase 2 closes out when Lane 2B's three sub-phases all ship; Phase 3 (v1.1 schema + districts + categories + alerts) becomes the next dispatchable major phase at that point.
- **Phase 2B.2 — Postgres FTS + pg_trgm + tier 2 LIKE→FTS dispatch (2026-05-12, commit `d631c77`):** First Lane 2B sub-phase to ship (2B.1 photos + 2B.3 search bar UI remain). Ships Postgres FTS infrastructure (`entities.search_vector` tsvector generated column with weighted `A=name` / `B=description`, GIN index `ix_entities_search_vector`, `pg_trgm` extension + trigram GIN `ix_entities_name_trgm` for typo-tolerant LIKE), the new `app/search/` package (`fts.py` 140 lines — pure SQLAlchemy expression builders with `to_tsquery` safety via `[a-z0-9]+` token allowlist + rejection of `or` / `and` / `not` operators + `bindparams` to prevent injection + AND across dimensions / OR within synonym needles per brief §6.3; `sqlite_fallback.py` 52 lines — ILIKE chain fallback for dev/test parity, lazy-imports `Entity` for circular-import dodge; `ranking.py` 95 lines — composite rank-float heuristic: featured +25, verification bands +30, recency +15, base FTS rank via `ts_rank_cd` on Postgres), the chat Tier 2 LIKE→FTS dispatch in `app/chat/tier2_db_query.py` (runtime `_is_postgres(session)` check selects `fts.py` on Postgres / `sqlite_fallback.py` on SQLite; preserves `_category_needle_set` synonym expansion semantics across both paths; legacy ILIKE predicates RETAINED alongside the new `entities.search_vector @@ to_tsquery` OR branch so recall is a strict superset on Postgres). New `app/chat/tier2_synonyms.py` (95 lines, extracted from `tier2_db_query.py` to break a `sqlite_fallback ↔ models` circular import; `tier2_db_query.py` re-exports `_category_needle_set` / `_category_synonyms` / `_singularize_category` so existing `tests/test_tier2_db_query.py` callers stay unchanged). New alembic migration `c8d9e0f1a2b3_entities_fts_pgtrgm` chains off `92ce4899dc08`. Dialect-gated upgrade + downgrade with early `bind.dialect.name != "postgresql"` return so fresh-DB SQLite upgrade stays green for tests. Pytest **1586 → 1607** (+21 net-new in 3 new test files: `test_search_fts.py` 92 lines / `test_search_ranking.py` 72 lines / `test_search_parity.py` 62 lines). Alembic head `92ce4899dc08` → `c8d9e0f1a2b3`. Ruff clean. **Six pragmatic deviations flagged + accepted:** (a) synonym helpers extracted to `app/chat/tier2_synonyms.py` (circular-import dodge) with back-compat re-exports from `tier2_db_query.py`; (b) `sqlite_fallback.py` lazy-imports `Entity` inside `build_ilike_entity_select` for the same cycle reason (mirrors Phase 1A's dialect-branch discipline); (c) FTS tests Postgres-only execution path is `skip-unless-postgres` (no TestContainers in this pass; revisit if test infra ever migrates to Postgres-only); (d) Postgres WHERE clause keeps legacy ILIKE predicates AND adds `entities.search_vector @@ to_tsquery` as an extra OR branch (stronger recall; superset vs ILIKE-only on Postgres); (e) `to_tsquery` token safety: `[a-z0-9]+` allowlist + reserved-keyword rejection so injection-like phrases never become tsquery operators; (f) **scope expansion** — two partial JSON indexes on `providers.attributes` (`ix_providers_emergency_service`, `ix_providers_dog_friendly`, filtered to `'true'`) added alongside the brief §6.2-scoped FTS DDL — additive Postgres-only performance indexes for Tier 2 attribute filters, gated to Postgres via the same dialect check so SQLite path unaffected. **Operator note:** Postgres FTS path NOT smoke-tested locally (no Postgres bind in dev environment); operator should verify on Railway/staging Postgres with a Tier 2 query, confirm `EXPLAIN` uses GIN where expected, and run the `downgrade -1` / `upgrade head` cycle there. Flaky early background pytest run showed two `F` before a fresh full run completed all green; treated as transient unless reproduced. **No production runtime impact** until next deploy; once deployed, Tier 2 queries use FTS + composite ranking on Postgres, SQLite test path keeps ILIKE alive permanently for parity (do NOT drop `sqlite_fallback.py` once FTS lands in prod — SQLite test fixtures depend on it). Cursor session, single dispatch.
- **Phase 2B.1 — Photos schema + R2 client + Pillow pipeline + upload routes + sweep + three-tier hero/gallery + Lane 2B close-out (2026-05-12, commit `1c57c73`):** Final Lane 2B sub-phase to ship. 19 files (+2296/-16). New alembic migration `f9e8d7c6b5a4_photos_table` chains off `c8d9e0f1a2b3` (Phase 2B.2 FTS); single `op.create_table("photos")` with FKs to `entities.id` + `users.id` (both ON DELETE CASCADE), CHECK constraints on `status` ('uploading', 'processing', 'live', 'flagged', 'deleted') + `mime_type` ('image/jpeg', 'image/png', 'image/webp'), 5 indexes (entity_id, uploaded_by_user_id, status, image_hash, entity_hash_status composite). `sa.true()` / `sa.false()` / `sa.func.now()` defaults per Postgres portability rule. Reversible: downgrade drops table + indexes. New `app/photos/` package: `r2_client.py` (lazy-singleton `get_r2_client`, `region_name='auto'`, `upload_bytes` with `Cache-Control: public, max-age=31536000, immutable`, `delete_object` best-effort, `build_public_url`); `processor.py` (6-stage pure-function pipeline: `decode_and_validate` → `strip_exif` (MANDATORY; failure → `exif_strip_failed` flag) → `compute_hash` (SHA-256 dedup on post-strip pixel bytes) → `generate_variants` (256×256 thumbnail / 512×512 medium / 1280×720 hero × WebP-q82 + JPEG-q85 via ImageOps.fit center-crop) → `upload_all_variants` → `finalize_photo_row`; plus `process_uploaded_photo(photo_id, content, declared_mime)` BackgroundTask orchestrator with broad try/except → `flagged/decode_failed` defensive shape, idempotent re-run on `status='live'` row); `routes.py` (4 endpoints: `POST /api/entities/{entity_id}/photos` multipart upload with `auth=require_user` + `authz=admin OR verified Claim via find_existing_claim` + `entity_type IN ('commercial', 'place')` + MIME whitelist + 10 MB cap + rate-limit checks, creates Photo row in `status='uploading'` + schedules BackgroundTask, returns 201; `DELETE /api/photos/{id}` soft-delete with admin OR uploader OR verified-claimant authorization; `POST /api/photos/{id}/set-hero` atomic clear-siblings + flip; `POST /api/photos/{id}/reorder` display_order update); `schemas.py` (`PhotoUploadResponse` + `PhotoListItem` Pydantic models); `limits.py` (`check_uploader_daily_cap` 20/day + `check_entity_photo_cap` 50 place / 100 commercial, entity_cap checked BEFORE daily_cap for clearer UX); `sweep.py` (`run_stuck_photo_sweep` flips `status='uploading'` rows older than 24h to `status='flagged'` with `processing_error='decode_failed'`). Photo model on `app/db/models.py` tail-append. `Entity.photos` viewonly relationship uses string `primaryjoin` (`and_(Photo.entity_id == Entity.id, Photo.status == "live")`) without `foreign()` to avoid forward-ref / circular-import cycle at class body time (mirrors Phase 2B.2 `sqlite_fallback.py` lazy-import discipline). `app/main.py`: `photos_router` include after `search_router`; `run_stuck_photo_sweep` folded into `_hourly_cleanup_loop` alongside `run_expired_review_cleanup`. `app/providers/queries.py`: `derive_hero_photo` + `derive_gallery` extended to three-tier (Tier 1 owner Photo `is_hero=True + status='live'` → `hero_url`; Tier 2 EXISTING `attributes.hero_pin_photo_url`; Tier 3 EXISTING `google_photo_refs[0]`); `selectinload(Entity.photos)` on profile load; `/api/search`'s `hero_url` consumer flows through this chain automatically. `requirements.txt`: `Pillow==12.0.0` + `boto3==1.42.17`. `tests/conftest.py`: 5 R2_* env-var `setdefault` stubs (cite AUTH_DEV_MODE pattern from Phase 2A.2 — production R2 vars are LIVE in Railway since session-19 R2 lockdown; conftest stubs prevent tests from hitting real R2). 47 net-new tests across 6 files (`test_photos_schema.py` 8 + `test_photos_processor.py` 10 + `test_photos_r2_client.py` 4 + `test_photos_routes.py` 13 + `test_photos_sweep.py` 2 + `test_provider_queries_hero_photo.py` 4 + 6 bonus broader coverage). Pytest **1616 → 1663** (+47 for 2B.1). Alembic head `c8d9e0f1a2b3` → `f9e8d7c6b5a4`. Ruff clean. Migration upgrade + downgrade-1 + upgrade cycle green on fresh SQLite. **Five pragmatic deviations flagged + accepted:** (a) `Entity.photos` primaryjoin string + no `foreign()` (circular-import dodge for class-body-time eval; same pattern as Phase 2B.2's `sqlite_fallback.py` lazy-import); (b) upload rate-limit order: `entity_cap` BEFORE `daily_cap` so a full entity (100 live, same user) returns `entity_photo_cap_exceeded` instead of being masked by the 20/day rule when many rows are older than 24h (better UX); (c) `set_hero` response typing `dict[str, Any]` so FastAPI accepts `is_hero: bool` (pragmatic FastAPI shape); (d) orchestrator broad try/except flags unexpected failures to `flagged/decode_failed` (brief §10 explicitly invited; defensive shape); (e) R2 failure path: row stays `uploading` on upload exception; sweep handles long stalls (risk #7 mitigation per brief). **Operator notes:** Postgres upgrade+downgrade cycle NOT exercised by Cursor (SQLite cycle verified); operator should smoke on Railway/staging before deploy. Manual smoke not run (no live R2 from Cursor); operator runs curl POST a real JPEG with verified-claimant cookie + verify R2 bucket has 6 variant objects (thumbnail/medium/hero × WebP/JPEG) + Photo row transitions to `status='live'` + `cdn_url / hero_url / thumbnail_url / medium_url` populated. **No production runtime impact** until next deploy; once deployed, verified-claimant merchants can upload owner photos to provider profiles, three-tier hero/gallery surfaces them on provider profile + /api/search results. Cursor session, single dispatch.
- **Lane 2B SHIPPED 2026-05-12.** Phase 2B.2 + 2B.3 + 2B.1 commit chain: `d631c77` → `8338505` → `1c57c73`. Pytest delta across Lane 2B: 1586 → 1663 (+77 net-new). Final Lane 2B alembic head: `f9e8d7c6b5a4` (photos table from 2B.1; chain `92ce4899dc08 → c8d9e0f1a2b3 FTS → f9e8d7c6b5a4 photos`). Lane 2B's image-storage + search infrastructure is feature-complete: Postgres FTS + pg_trgm + chat Tier 2 LIKE→FTS dispatch (2B.2) + public `GET /api/search` + home + provider profile search bar (2B.3) + photos schema + R2 client + Pillow pipeline + four upload/manage/delete routes + stuck-upload sweep + three-tier hero/gallery extension (2B.1). **Phase 2 SHIPPED 2026-05-12.** Combined with Lane 2A close-out at `5fea2ce` (session-18), Phase 2 of the master build plan is COMPLETE. Total Phase 2 commit chain (six substantive ships): `6000138 → 714ca52 → 5fea2ce` (Lane 2A) + `d631c77 → 8338505 → 1c57c73` (Lane 2B). Total Phase 2 pytest delta: 1518 → 1663 (+145 net-new). Final Phase 2 alembic head: `f9e8d7c6b5a4`. **Next dispatchable lane:** Phase 3 (v1.1 schema pass + operator-curated fields + districts table + alerts schema + category taxonomy rewrite — see §4 Phase 3 below). Phase 3 brief pre-positioned at `outputs/cursor_brief_phase_3_v11_schema_pass.md` (~580 lines, two-sub-phase decomposition: 3.1 schema additions, 3.2 category rewrite + audited backfill + district seed + close-out); Phase 3.1 dispatch prompt pre-positioned at `outputs/cursor_dispatch_prompt_phase_3_1.md` (~230 lines, patched with session-19 ship SHAs).
- **Phase 2B.3 — Public `GET /api/search` + home + provider profile search bar + Lane 2B consumer wiring (2026-05-12, commit `8338505`):** New public no-auth endpoint `GET /api/search` (query params `q` required + `category` / `district` / `entity_type` / `limit` (default 20 max 50) / `cursor`); response shape `{results: [...], next_cursor: str | None}`. Postgres path consumes 2B.2's public API (`build_tsquery_string` + `entities_search_vector_match`); SQLite test path mirrors `sqlite_fallback` shape (Entity name + description ILIKE AND category needles via `_category_needle_set`). Ranking via 2B.2's `build_rank_score_expr_for_filters` on Postgres, `_verification_bonus_sql` + featured-case on SQLite. Search bar in `app/templates/home.html` as `<section class="directory-search-wrap">` placed BEFORE existing hero chat composer (existing chrome untouched); compact `directory-search--compact` variant in `app/templates/provider_profile.html` topbar. Vanilla JS at `app/static/js/search.js` via `[data-directory-search]` marker — fetches `/api/search?q=…`, renders top 8 in dropdown, click navigates to `profile_url`. 3 new files (`app/search/routes.py` + `app/static/js/search.js` + `tests/test_search_route.py`) + 4 modified (`app/main.py` +1 import +1 include_router, `app/templates/home.html`, `app/templates/provider_profile.html`, `app/static/styles/home.css`). 9 net-new tests: 8 brief §7.3 scenarios (q=plumber JSON, anonymous 200, missing-q 400, filter combinations, cursor round-trip, hero_url via derive_hero_photo, q=barbershop synonym, home + provider profile HTML smoke) + cursor encode/decode unit. Pytest **1607 → 1616** (+9 net-new). Alembic head unchanged at `c8d9e0f1a2b3` (no migration in 2B.3). Ruff clean. **Seven pragmatic deviations flagged + accepted:** (a) brief symbol name `build_fts_query` → actual 2B.2-shipped names `build_tsquery_string` / `entities_search_vector_match` / `build_entity_select_for_filters` (brief-staleness adaptation; exactly the §0 re-grep discipline we want); (b) UI placement = between topbar and hero on home.html (above the chat-style composer in the hero — brief §10 menu option); (c) JS path `/static/js/search.js` (matches existing `js/chat-new.js` convention, not `app/static/search.js`); (d) pagination cursor = opaque base64 JSON `{"o":<offset>}` (offset shape — brief §10 explicitly allows); (e) additive `profile_url` field on row shape so events route to `/events/{id}` + programs to `/programs/{id}` (events/programs not slug-routable, places fall back to `/home` placeholder); (f) **scope expansion** — additive OR-branch `_provider_synonym_exists_predicate` reuses 2B.2's `_category_needle_set` to add Provider-table EXISTS+ILIKE for commercial recall (FTS only indexes Entity text, so `q=barbershop` wouldn't hit `barber_shop`-tagged providers without this; parallel taxonomy matching in additive OR, **NOT a raw-LIKE FTS bypass** — reuses 2B.2 helpers); (g) template smoke = string containment on rendered HTML (`assert '<input name="q"' in response.text`), not BS4 parse — brief §10 explicitly accepts. **Operator notes:** manual smoke not executed in Cursor session (no browser; operator runs `/home` → type "plumber" → click result → land on `/provider/<slug>`; repeat from profile page when convenient). `alembic upgrade head` on ad-hoc DATABASE_URL hit benign pre-Phase-1A SQLite drift (`duplicate column name: slot` on `sponsors` at `1a2b3c4d5e6f → 2a3b4c5d6e7f` — same mid-chain drift surfaced since session-15; not a 2B.3 issue; cure is drop + recreate local dev DB Windows-side). **No production runtime impact** until next deploy; once deployed, anonymous viewers see the search bar on home + provider profile and can search via FTS-backed `/api/search`. Cursor session, single dispatch.

---

### Phase 3 — v1.1 schema pass: operator-curated fields + districts table + alerts schema + category taxonomy rewrite (1-2 weeks) — **SHIPPED 2026-05-12 · DEPLOYED 2026-05-13**

**Status:** ✅ **COMPLETE + LIVE IN PRODUCTION.** Phase 3 shipped in two sub-phases (3.1 schema additions + 3.2 data pass + close-out). Sub-phase ship chain: `7925a14` (3.1) → `5dbde39` (3.2). Total pytest delta across Phase 3: **1664 → 1702** (+38 net-new across 3.1's 17 + 3.2's 21). Final Phase 3 alembic head: `e1f2a3b4c5d6` (data pass; chain `f9e8d7c6b5a4 photos → d0e1f2a3b4c5 schema → e1f2a3b4c5d6 data`). **Production Postgres deployed 2026-05-13 (session-22)** — Railway auto-deployed when 9 staged env-var changes were applied; the six-migration chain (`b2c3d4e5f6a7 → f8e9d0c1b2a3 → 92ce4899dc08 → c8d9e0f1a2b3 → f9e8d7c6b5a4 → d0e1f2a3b4c5 → e1f2a3b4c5d6`) walked cleanly; `/api/search?q=marina` verified returning ranked results post-deploy. District paragraph UX direction locked as **path (b)** per session-21 operator decision: 10 districts seeded as backend tag (`slug + name + display_order`, `paragraph = NULL`); paragraph landing pages deferred clean to V1.5 where the UX primitive can be re-thought against Havasu's actual scale. **Next dispatchable major phase:** Phase 4 (background-jobs + layered scrape infrastructure).

**Goal:** Add the operator-curated data fields and supporting tables that the Opus features + ChatGPT taxonomy work assumes. Reshape the seeded `categories` table to match the locked new-taxonomy 12 (see "Category taxonomy rewrite" deliverable below — amended 2026-05-14 per `category_backfill_mapping_audit_2026-05-14.md` §3). Single additive migration; minimal app-layer changes.

**Deliverables (all in one migration):**
- `entities.heat_exposure` (enum: indoor / shaded / outdoor / water_adjacent) — for Opus #2
- `entities.crowd_notes` (JSON, operator-curated text per venue) — for Opus #5
- `entities.is_mobile_service` (bool) — for Opus #6
- `entities.boat_access` (JSON, shape varies per venue type) — for Opus #4
- `entities.seasonal_hours` (JSON, summer/winter/shoulder blocks) — for Opus #3
- New `districts` table: `slug`, `name`, `paragraph` (operator-written rich text), `display_order`, `created_at`, `updated_at` — for Opus design district context
- `entities.district_id` (FK to districts) — replaces the current `district: String(64)` column
- `entities.featured` (bool, the "Hava's pick" flag — moved from Provider.featured to entities)
- New `alert_subscriptions` table: user_id, alert_type (enum: heat_advisory / aqi_alert / lake_hazard / event_traffic), delivery_channel (email / sms — sms-ready, V1.5 wired), paused_until, created_at
- New `alerts_dispatched` table (audit log): subscription_id, alert_type, trigger_data (JSON), dispatched_at, delivery_status, body_snippet
- New `external_conditions_cache` table: source (string PK), fetched_at, data (JSON), ttl_seconds, last_error, error_count — for Opus #1 conditions panel
- `users.preferred_mode` enum (default / boat) — for boat-access mode persistence
- New `peer_recommendations` table (per Opus #7, ships disabled-by-flag for V1.5 pilot)
- **Category taxonomy rewrite (added 2026-05-14):** rename 7 surviving slugs (`eat-and-drink` → `eat-drink`, `home-services` → `home-property-services`, `health` → `health-wellness-care`, `outdoors-and-parks` → `outdoors-parks-trails`, `shopping` → `shopping-essentials`, `auto-and-gas` → `auto-rv-fuel`, `lodging` → `lodging-vacation-rentals`) with matching display-name updates; delete `family` + `community` rows (guarded by pre-flight check for FK references); insert `classes-sports-recreation` + `public-civic-resources` rows; reset `sort_order` per synthesis §1 Tier 1/2/3 ordering. Updates the parallel hard-coded slug list at `app/home/queries.py:27-55` (CATEGORY_LABELS) and validator vocab at `scripts/ingest/validate_enrichment_csv.py`.
- **Backfill of legacy `Provider.category` / `Program.activity_category` strings → `category_id` FKs (added 2026-05-14):** uses the audited mapping in `docs/maintainability/category_backfill_mapping_audit_2026-05-14.md` §2 (NOT the original DRAFT, which targeted the now-superseded original 12). Audited mapping covers ~24 strings that carry forward cleanly + 5 strings that map cleaner under the new taxonomy + 5 professional-services strings that NULL out (V1.5 deferred per §10 decision 2026-05-14) + Phase-3-locked decisions for ambiguous strings (`beauty_personal_care`, `tourism`, K-12/charter schools, recreational-entertainment).

**App-layer changes:** minimal for the data-field additions. The category taxonomy rewrite touches `app/home/queries.py` CATEGORY_LABELS (~25 lines) and `scripts/ingest/validate_enrichment_csv.py` vocab. Admin form extensions land in Phase 5.

**Operator workload (NEW in this phase):** (a) author 8-12 district paragraphs (~1 hour). Suggested districts: English Village, Downtown / Main Street, North End, Lakefront, Mesquite Bay, Highway 95 corridor, Site Six, Pittsburgh Point, Castle Rock area, South side. Each gets a one-paragraph description per Opus design samples ("English Village fills up after 5pm Fri-Sun — parking lots near the bridge get tight by 6..."). (b) lock the four Phase-3 review questions from the audit memo §4 lock-during-Phase-3 list (~30 min): `beauty_personal_care` final home; `tourism` triage rule; K-12 / charter / public schools → Classes-Sports-Rec vs Public-Civic; bowling/arcades/mini golf → Classes-Sports-Rec vs different home. (c) run `SELECT DISTINCT category FROM providers` against production to confirm no new free-text strings have entered via the admin form since the DRAFT audit (~15 min). **Total operator hours bumped from ~1 hr to ~2-3 hr.**

**Dependencies:** Phase 1 (ENTITY schema) must be complete.

**Effort estimate:** M migration (~5-8 days dispatch, bumped from 3-5 to absorb category rewrite + backfill) + ~2-3 hours operator authoring + decision-locks.

**Success criteria:** Migration applies cleanly; districts table populated with operator paragraphs; categories table holds the locked new-taxonomy 12 (with sort_order reflecting Tier 1/2/3); every existing Provider/Program row has `category_id` populated per the audited mapping (or NULL for the 5 professional-services strings deferred to V1.5); pytest stays green.

**Shipped (incremental, each sub-phase committed separately):**

- **Phase 3.1 — v1.1 schema additions (additive migration + ORM + tests; 2026-05-12, commit `7925a14`):** First sub-phase of Phase 3 shipped. New alembic migration `d0e1f2a3b4c5_phase3_schema_pass` chains off `f9e8d7c6b5a4` (Phase 2B.1 photos). Creates 5 new tables: `districts` (id, slug UNIQUE, name, paragraph, display_order, timestamps, index on display_order); `alert_subscriptions` (user_id FK, alert_type, delivery_channel, UNIQUE on triple, CHECK constraints on alert_type + delivery_channel); `alerts_dispatched` (audit log; user_id, subscription_id, alert_type denormalized per brief, trigger_data, dispatched_at, delivery_status CHECK, body_snippet); `external_conditions_cache` (source string PK, fetched_at, data, ttl_seconds, last_error, error_count, fetched_at index — upsert-by-PK pattern); `peer_recommendations` (recommender_user_id FK, entity_id FK UNIQUE per recommender, recommendation_text mapped to DB column `text`, status CHECK, entity_id_status composite index — ships disabled-by-flag for V1.5 pilot). Extends `entities` via `batch_alter_table` with 7 nullable-or-defaulted columns: `heat_exposure` VARCHAR(20) CHECK in (`indoor` / `shaded` / `outdoor` / `water_adjacent`); `crowd_notes` JSON; `is_mobile_service` BOOL NOT NULL default `sa.false()`; `boat_access` JSON; `seasonal_hours` JSON (coexists with Phase 1A `seasonal_hours` extension table per brief §9 intentional-column path — operator-confirmed during session-20 commit; ORM relationship renamed `seasonal_hours → seasonal_hour_rows` so the new JSON column keeps the `seasonal_hours` name); `district_id` VARCHAR(36) FK `districts.id` ON DELETE SET NULL, indexed; `featured` BOOL NOT NULL default `sa.false()`, indexed (non-partial index on both dialects; Postgres-only partial index skipped per brief §9 invitation). Extends `users` via `batch_alter_table` with `preferred_mode` VARCHAR(16) NOT NULL default `'default'` CHECK in (`default` / `boat`) — direct NOT NULL with server_default per brief §4.7 (3.2 backfill step is a no-op). ORM in `app/db/models.py`: 5 new model classes (`District`, `AlertSubscription`, `AlertDispatched`, `ExternalConditionsCache`, `PeerRecommendation`) appended at file tail; Entity extended with 7 Mapped properties + `District` relationship (`foreign_keys=[district_id]`) + `seasonal_hour_rows` relationship rename; User extended with `preferred_mode` Mapped property. `PeerRecommendation.recommendation_text` Python attribute maps to DB column `text` (minor naming choice; documented). 17 net-new tests in `tests/test_phase3_schema_additions.py` covering migration upgrade-downgrade-upgrade cycle on fresh SQLite (reversibility via DATABASE_URL monkeypatch), table shapes, 5 CHECK constraint violation rejections (heat_exposure / alert_type / delivery_status / peer status / preferred_mode), UNIQUE duplicate rejections, FK CASCADE on user delete via raw DELETE + PRAGMA foreign_keys=ON (stable under SQLite connection pooling — `Session.delete` approach was flaky, raw SQL + PRAGMA is the cure), default values, ORM relationships (Entity.district + None when null), index presence via `inspect(engine).get_indexes`. Pytest **1664 collected → 1681 collected** (+17 net-new; 1680 passed + 1 skipped + 30 subtests). Alembic head **`f9e8d7c6b5a4 → d0e1f2a3b4c5`**. Ruff clean. **Five accepted deviations per brief §9:** (a) seasonal_hours JSON column + Phase 1A extension table coexist (brief §4.1 explicit-HALT target — Cursor identified the conflict, applied the brief §9 "intentional column" deviation path with the `seasonal_hour_rows` relationship rename, did not halt; operator confirmed during commit that the resolution matches intent — brief §4.1 listed the column as additive which IS operator intent; documented for future Cursor inheritance); (b) `users.preferred_mode` direct NOT NULL with server_default — brief §4.7 allowed either NOT NULL+default or nullable+backfill; Cursor picked direct, 3.2 backfill is a no-op; (c) `PeerRecommendation.recommendation_text` Python attribute maps to DB column `text` (minor); (d) `alerts_dispatched.alert_type` left denormalized per brief; (e) `entities.featured` non-partial index on both dialects (Postgres-only partial index skipped per brief §9 invitation; simple index acceptable for V1). **alembic/env.py descriptive note** (Cursor §11): env.py uses `get_database_url()` at runtime in 3 sites (line 24 `set_main_option`, line 30 offline mode, line 44 online mode) so the reversibility test can monkeypatch DATABASE_URL — env.py was already in this shape pre-3.1 (confirmed via `git status` showing only `app/db/models.py` modified in the commit); Cursor's §11 note was descriptive of existing state, not a change. **No production runtime impact** until next deploy; new columns and tables start empty, ORM dormant until Phase 3.2 wires writers + Phase 5/6/8 wire readers. HALT at brief §3 boundary per dispatch prompt; Phase 3.2 dispatches in a fresh session after the 5 Bucket C decisions are locked in the 3.2 dispatch prompt + the district paragraph draft is resolved (status flagged session-20 as illustrative not canonical pending UX direction per the operator reality check — Havasu is too small for a 10-district paragraph-landing-page UX). Cursor session, single dispatch.
- **Phase 3.2 — Category taxonomy rewrite + audited Provider/Program backfill + district seed as backend tag + entity backfills + Phase 3 close-out (2026-05-12, commit `5dbde39`):** Final sub-phase of Phase 3. Single data-pass migration `e1f2a3b4c5d6_phase3_data_pass` chains off `d0e1f2a3b4c5`. Renames 7 surviving slugs to new-taxonomy shape (`eat-and-drink → eat-drink`, `home-services → home-property-services`, `health → health-wellness-care`, `outdoors-and-parks → outdoors-parks-trails`, `shopping → shopping-essentials`, `auto-and-gas → auto-rv-fuel`, `lodging → lodging-vacation-rentals`); backs up `family` + `community` UUIDs to ad-hoc `_phase32_category_bak` table; NULLs providers/programs FK references + DELETEs `entity_categories` junction rows pointing at the soon-to-be-deleted rows (junction `category_id` is NOT NULL so brief §9 NULL-or-DELETE either-or collapsed to DELETE-only); deletes `family` + `community`; inserts `classes-sports-recreation` + `public-civic-resources`; resets sort_order 1-12 per synthesis §1 Tier 1/2/3 ordering. Four-pass Provider/Program backfill driven by audit memo §2 (Pass 1 Bucket A + Bucket E renames; Pass 2 Bucket B improved homes — `childcare_education` / `education` / `edu` → `classes-sports-recreation`, `religion_community` → `public-civic-resources`, `fitness_sports` → `health-wellness-care`, `entertainment_attractions` NULL queue per Phase 5 defer; Pass 3 Bucket B professional-services NULL queue — `insurance` / `financial` / `legal` / `real_estate` / `professional_services`; Pass 4 Bucket C three explicit NULL UPDATEs only — `beauty_personal_care` / `tourism` / `barbershop`). Deterministic 10-district `op.bulk_insert` with `paragraph = NULL` per **path (b) operator lock** (district paragraphs deferred to V1.5; draft preserved at `outputs/chatgpt_response_district_paragraphs_v1.md` for V1.5 re-engagement when the UX primitive can be re-thought against Havasu's actual scale — ~57k pop / ~46 sq mi too small for a 10-district paragraph-landing-page UX per session-20 reality check). `entities.district_id` backfill from `locations.district` name match (the `entities.district` String column never existed — Phase 1A semantics caught at dispatch time; brief §5.4's drop-column instruction was moot). `entities.featured` backfill from `Provider.featured` for commercial entities. `users.preferred_mode` SQL is no-op since Phase 3.1 shipped direct-NOT-NULL with `server_default='default'`. Anchored Edits on `app/home/queries.py` (CATEGORY_LABELS replaced with canonical 12 in synthesis §1 Tier order; NEW `LEGACY_PROVIDER_CATEGORY_LABELS` constant preserves free-text `Provider.category` display until Phase 13 — prevents blank labels for operator-queue rows on profile/admin views; `_category_label` dual-lookup checks canonical first then legacy), `app/providers/queries.py` (`category_label_for` legacy fallback), `app/db/models.py` (`District.paragraph` altered to `Mapped[str | None]` per path b), `scripts/ingest/validate_enrichment_csv.py` (`_REJECTED_ENRICHMENT_CATEGORY_SLUGS` frozenset rejects 7 renamed + 2 deleted legacy slugs with clear error; allowlist sourced from `CATEGORY_LABELS.keys()`). 5 test files updated for new-taxonomy slug ripples (`test_phase3_schema_additions.py` head revision string; `test_directory_schema.py` new 12 slugs + helper-based fixture construction; `test_entity_backfill.py` slug rename ripples; `test_home_queries_lane_a.py` labels + widened-set; `test_enrichment_ingestion.py` good-row category). New `tests/test_phase3_data_pass.py` with 22 tests covering migration upgrade-downgrade-upgrade cycle + 12-slug shape + Tier sort_order + Buckets A/B/C/E + districts (NULL paragraph confirms path b) + `locations.district → district_id` backfill + `entities.district` nonexistence assertion + featured backfill + CATEGORY_LABELS + validator vocab + idempotency + entity_categories junction cleanup + `/home` 200 smoke. Pytest **1681 → 1702** (+21 net-new in 3.2; +38 total across Phase 3). Alembic head **`d0e1f2a3b4c5 → e1f2a3b4c5d6`**. Ruff clean. `alembic upgrade head` + `downgrade -1` + `upgrade head` cycle verified on fresh SQLite. **Five accepted deviations per brief §9:** (a) `entities.district` String column nonexistent — Phase 1A semantics catch at dispatch time; source for `district_id` backfill is `locations.district` instead; ORM relationship stays `district → District` (no rename, no drop_column). (b) `entity_categories` junction DELETE not NULL since junction `category_id` is NOT NULL; brief §9 either-or collapsed to DELETE-only. (c) Bucket C items A.4 (K-12/charter/public schools) + A.5 (bowling/arcades/mini golf) absorbed as **documented-only locks** per session-21 mid-session clarification — A.4 subsumed by Bucket B Pass 2 `education` mapping; A.5 subsumed by `entertainment_attractions` NULL deferral with Phase 5 `google_primary_category` triage rule recorded inline at Pass 2; **Pass 4 final shape is three explicit NULL UPDATEs only** (A.1 `beauty_personal_care`, A.2 `tourism`, A.3 `barbershop`). (d) NEW `LEGACY_PROVIDER_CATEGORY_LABELS` constant in `app/home/queries.py` preserves free-text `Provider.category` display until Phase 13 — prevents blank labels for operator-queue rows (Bucket C NULLs + Bucket B prof-services NULLs) on profile/admin views; arguably "beyond what audit memo + synthesis specify" per brief §11 but rationale sound (interim plumbing for the operator-queue period). (e) Test helpers mirror migration UPDATE/ORM semantics since migration does not re-run per test. **Sub-phase chore** at `bd9b00f` lands the dispatch prompt artifact as a historical artifact (post-clarification reframe narrated in this ship-line + `5dbde39` commit body; on-disk prompt preserves pre-clarification framing per session-20 brief §4.1 narration precedent). **No production runtime impact** until next deploy; once deployed, alembic walks `b2c3d4e5f6a7 → f8e9d0c1b2a3 → 92ce4899dc08 → c8d9e0f1a2b3 → f9e8d7c6b5a4 → d0e1f2a3b4c5 → e1f2a3b4c5d6` (six migrations to walk). **Phase 3 SHIPPED 2026-05-12.** Combined with Phase 3.1 at `7925a14`, Phase 3 of the master build plan is COMPLETE. Total Phase 3 commit chain: `7925a14` (3.1) → `5dbde39` (3.2). Total Phase 3 pytest delta: 1664 → 1702 (+38 net-new). Final Phase 3 alembic head: `e1f2a3b4c5d6`. **Next dispatchable lane:** Phase 4 (background-jobs + layered scrape infrastructure — see §4 Phase 4 below). Cursor session, single dispatch.

---

### Phase 4 — Background-jobs + layered scrape infrastructure (2-3 weeks)

**Status:** ✅ **SHIPPED on origin 2026-05-13 (session-23 + session-23-extension).** Phase 4 completed in four sub-phases per brief `outputs/cursor_brief_phase_4_background_jobs_scrape.md` §3: **4.1** background-jobs scaffold + Outbox (`91cd37b` + `f5b3953`) → **4.2** layered-scrape client + Google Places refactor (`86eeaf8`) → **4.3** OSM Overpass client + cross-layer reconciler + `places_load` wiring (`2f87211`) → **4.4** close-out (this batch: operator runbook + scrape log template + `with_retry` on photo processing + chat mention scan + contribution enrichment BackgroundTasks + close-out tests + master plan + STATE refresh). **Production deploy** of the Phase 4 stack (alembic `0a1b2c3d4e5f` outbox migration + runtime behavior) remains **pending operator Railway push** until the next production cutover.

**Goal:** Wire the data-ingestion pipeline so subsequent data-gathering work can run on a schedule rather than operator-triggered.

**Deliverables:**
- Per `background_job_infrastructure_decision.md` (Option A — Railway scheduled jobs + FastAPI BackgroundTasks + optional Outbox)
- One wide Railway scheduled-job service that polls all condition sources on 15-min tick (per conditions+alerts design memo's recommendation over per-source crons)
- Existing `_hourly_cleanup_loop` pattern documented in a new `app/core/background.py` module (filename: `background.py`, NOT `background_tasks.py` — Phase 4.1 ship at `91cd37b` chose the shorter name; `BackgroundTasks` is FastAPI's class, the module is the retry-helper surface)
- BackgroundTasks integration for magic-link email send (Resend)
- BackgroundTasks integration for image processing (Pillow inline; externalize if volume justifies)
- Per `layered_scrape_strategy.md`:
  - Extract shared scrape logic from `places_discovery.py` + `places_enrichment.py` into `app/contrib/google_places_scraper.py` library
  - New `scripts/places_discovery_places.py` (or `--target-type=place` flag on existing script) writing to entities with `entity_type="place"`
  - `app/contrib/google_types_mapping.py` mapping Google types → Category slug + place_type discriminator
  - New `app/contrib/osm_overpass_client.py` for Layer 2 (OpenStreetMap)
  - `scripts/osm_overpass_pull.py` per-category Overpass queries
  - `app/contrib/lhc_open_data_client.py` for Layer 3 (City of Lake Havasu Parks & Rec + AZ ROC) — operator-confirmed sources only
  - `app/contrib/specialized_apis/` modules for Layer 4 (NPI already integrated; USAPickleball + PDGA as priority adds)
  - Shared `app/contrib/ingest_reconciler.py` for cross-layer deduplication (geo proximity + name normalization + stable IDs)
- Operator monitoring: per-run log files + scheduled-job failure alerts via email

**Dependencies:** Phase 1 (ENTITY schema for write target) + Phase 3 (operator-curated fields the scrapers populate where Google returns data).

**Effort estimate:** L (10-15 days dispatch) — most complex single phase. Several parallel-eligible sub-lanes; Casey may want to dispatch the OSM client + city open data clients separately.

**Operator workload:** confirm available Lake Havasu open-data sources (Parks & Rec facility list URL/format, business license database accessibility). ~2-4 hours research.

**Success criteria:** All four automated layers run on schedule. Reconciliation logic correctly merges duplicate entities. Operator can see per-run logs.

**Shipped (incremental, each sub-phase committed separately):**

- **Phase 4.1 — Background-jobs scaffold + Outbox must-not-lose surface (2026-05-13, commits `91cd37b` substantive → `f5b3953` ruff autofix follow-up):** First sub-phase of Phase 4 shipped. 12 files in the feat commit (+1770 / -14) + 3 files in the chore (+7 / -9). New `app/core/background.py` (~640 lines) ships **`with_retry`** (sync) + **`with_retry_async`** retry helpers with bounded retries + exponential backoff + Sentry breadcrumb integration (category=`background-jobs` + level=`warning` per retry + level=`error` on exhaustion / fatal) + structured logger.warning/error with `extra={"event":"retry"|"exhausted"|"fatal", ...}`; injectable `sleep` callable for tests; tasks must be idempotent; helper returns `None` on exhaustion (does NOT re-raise). Outbox surface: 4 kind constants (`magic_link` / `sponsor_notification` / `image_processing` / `other`) + 4 state constants (`pending` / `in_flight` / `delivered` / `failed`) + `OUTBOX_MAX_ATTEMPTS=5`; `register_outbox_handler` / `get_outbox_handler` registry; **`enqueue_outbox(db, kind, payload)`** writes a pending row in the caller's open session via `db.flush()` (no commit — caller's responsibility); **`deliver_outbox_row(row_id, *, session_factory=None)`** claims the row via `pending → in_flight` transition inside a fresh session, invokes the registered handler exactly once (the Outbox `attempts` counter combined with the redrive sweep IS the retry mechanism — `with_retry` is for non-Outbox `BackgroundTasks` consumers), transitions to `delivered` on success or back to `pending` (incrementing `attempts` + storing `last_error[:500]`) on failure; transitions to `failed` at `attempts >= OUTBOX_MAX_ATTEMPTS`. Default magic-link handler does function-scope `from app.auth.email_sender import send_magic_link` so importing this module never pulls the auth stack. **Gotcha-#17 cure preserved:** the module top deliberately does NOT import `app.db.models` — the `Outbox` ORM class is lazy-imported inside `deliver_outbox_row` + `enqueue_outbox` + `_record_outbox_failure` to let the `app.db.__init__ → entity_dual_write → models` load chain complete before this module reaches for it (session-22's `5faa37c` lesson carried forward). New alembic migration `0a1b2c3d4e5f_phase4_outbox` chains off `e1f2a3b4c5d6` (Phase 3.2 data pass — the production-deployed tip). Creates `outbox` table: `id VARCHAR(36) PK`, `kind VARCHAR(32) NOT NULL` + CHECK constraint, `payload JSON NOT NULL`, `state VARCHAR(20) NOT NULL DEFAULT 'pending'` + CHECK constraint, `attempts INTEGER NOT NULL DEFAULT 0`, `last_attempt_at DATETIME NULL`, `last_error VARCHAR(500) NULL`, `created_at + updated_at DATETIME NOT NULL DEFAULT sa.func.now()`, `delivered_at DATETIME NULL`; 2 indexes (`(state, created_at)` for redrive-poll selectivity + `(kind)` for operator inspection); mirrors Phase 2B.1 photos precedent — `sa.JSON` / `sa.String` + CHECK / `sa.func.now()` defaults / no Postgres-only types / no raw `op.execute()` / no `sa.text("1")` / `sa.text("0")` Boolean defaults. `Outbox(Base)` ORM class appended to `app/db/models.py` BEFORE the trailing `_register_provider_slug_listeners()` call with a comment block clarifying the distinction between the provider-slug listener pattern (leaf-module hook, fine where it is) and the gotcha-#17-governed Phase 1D dual-write hook in `app/db/__init__.py` (cross-module hook, must live in package init). New `scripts/outbox_redrive.py` is the operator-runnable redrive sweep: polls `state=pending` rows whose `created_at <= now - idle_seconds` (default 30s avoids racing the hot-path BackgroundTasks invocation per design memo §6.2); `--max-rows` cap (default 100); `--dry-run` prints without delivering; `--verbose` enables INFO-level stderr logging; function-scope imports keep the script lightweight. **Magic-link send refactored** at `app/auth/routes.py:108-160`: removed the direct `from app.auth.email_sender import send_magic_link` import + the inline `try: send_magic_link(...) except: logger.exception(...)` block at end of `request_link`; replaced with `enqueue_outbox(db, kind=OUTBOX_KIND_MAGIC_LINK, payload={"email", "token", "next_path"})` → `db.commit()` → `background_tasks.add_task(deliver_outbox_row, outbox_id)`. **Email body / recipient / Resend API call semantics unchanged** — the handler flows through `_magic_link_outbox_handler` which lazy-imports `send_magic_link`. `BackgroundTasks` injected as a function param. **28 net-new tests in `tests/test_phase4_background.py`:** `with_retry` happy / exhaustion / retry-then-success / backoff escalation / `fatal_on` bypass / no re-raise / Sentry breadcrumb on retry+exhaustion / invalid max_attempts ValueError; `with_retry_async` happy / exhaustion / uses-asyncio-sleep-not-time-sleep verification via injected captured-backoff list; Outbox model importable with expected columns / default state=pending+attempts=0 / state CHECK rejects invalid / kind CHECK rejects invalid / migration upgrade+downgrade-1+upgrade cycle on tmp_path SQLite (uses `monkeypatch.setenv("DATABASE_URL", ...)` because `alembic/env.py:24` clobbers `cfg.set_main_option` — discovered + documented inside the test docstring); `deliver_outbox_row` success / transient / exhaustion-at-attempts=cap / idempotent on delivered / missing-row-returns-False; `outbox_redrive` script — idle-seconds filter (fresh skipped, old picked), max-rows cap, dry-run skip; **magic-link integration** via live FastAPI `TestClient` — POST /api/auth/request-link inserts Outbox row + BackgroundTasks delivers it via mocked send → row reaches `delivered`; mocked send raise → row stays `pending` with `attempts=1` and `last_error` captured; **gotcha-#17 subprocess regression** — `import app.core.background` in a fresh subprocess does NOT pull `app.db.models` into `sys.modules` (mirrors `tests/test_phase1d_dual_write.py::test_scraper_entry_point_import_chain_does_not_cycle` shape from session-22); `enqueue_outbox` flush-no-commit / rejects unknown kind / `register_outbox_handler` rejects unknown kind. **4 existing test files repatched** to move the `_capture_send` monkeypatch target from `app.auth.routes.send_magic_link` to `app.auth.email_sender.send_magic_link` (the canonical definition the new Outbox handler reaches via function-scope import): `tests/test_auth_flow.py` (helper + 2 inline patches), `tests/test_claims.py`, `tests/test_favorites.py`, `tests/test_photos_routes.py`. Texture rule preserved: same captured-token semantics, just the patch target moves to where the actual definition lives. **2 pre-existing Phase 3 tests future-proofed in the same feat commit:** `test_phase3_data_pass.py::test_migration_upgrade_downgrade_upgrade_cycle` + `test_phase3_schema_additions.py::test_migration_upgrade_downgrade_upgrade_cycle` had hardcoded `assert ver == "e1f2a3b4c5d6"` after a full upgrade-head/downgrade-1/upgrade-head cycle; Phase 4.1's new head `0a1b2c3d4e5f` was the first migration to trip them. Replaced with `ScriptDirectory.from_config(cfg).get_current_head()` so future phases don't trip the same wire. Pytest **1703 → 1733** (+28 net-new + 2 pre-existing tests now stay green at their original count — no count delta from those, just assertion shape). Alembic head **`e1f2a3b4c5d6 → 0a1b2c3d4e5f`** on origin (production still at `e1f2a3b4c5d6` until next Railway deploy). Migration upgrade + downgrade-1 + upgrade cycle verified clean on fresh SQLite. **Ruff clean** after the `f5b3953` autofix follow-up (`91cd37b`'s commit body falsely claimed Ruff clean; ruff was not actually run before the claim — 5 issues surfaced when the operator ran it: 4× I001 import-block sort in `app/auth/routes.py` + `scripts/outbox_redrive.py` + `tests/test_phase4_background.py` (module-top + function-scope alembic-import inside the migration cycle test), 1× F401 unused `OUTBOX_STATE_IN_FLIGHT`; `f5b3953` auto-fixed all 5 and walks back the false claim explicitly in its commit body for audit-trail honesty). **Six pragmatic deviations per brief §9:** (a) Sentry breadcrumb shape — brief specifies category=`background-jobs` + level=`warning`/`error`; existing `app/contrib/rate_limiter.py` uses structured-logging-only (no explicit `sentry_sdk.add_breadcrumb`); shipped both (logger.warning extra= + add_breadcrumb) per brief hard requirement, documented divergence in module docstring. (b) `with_retry_async` shipped alongside sync `with_retry` per brief recommendation — no current async consumer but ~50 lines at no real cost, forward-compat. (c) Anchored edits on existing `BackgroundTasks` call sites (`scan_and_save_mentions`, `enrich_contribution`) deferred to Phase 4.4 close-out per brief §4.3 recommendation — both consumers are already silently fault-tolerant; wrapping is a clarity-only improvement that lands more naturally with the image-processing wrapper. (d) Outbox redrive idle threshold = 30s per brief + design memo §6.2; configurable via `--idle-seconds`. (e) Outbox state enum = 4 values as specified (`pending` / `in_flight` / `delivered` / `failed`), no `paused` 5th state — operator-paused redrive can stop the cron service instead. (f) Outbox payload = free-form `dict` as specified, no per-kind TypedDict aliases yet — V1 sufficient, can slot in later without a migration. **Unforeseen deviations (also flagged in §13):** (g) 4 existing test files repatched to track the moved `send_magic_link` monkeypatch target (texture-rule-preserved; necessary because removing the import from `routes.py` removed the symbol from `app.auth.routes`); (h) `tests/test_phase3_data_pass.py` + `tests/test_phase3_schema_additions.py` hardcoded-head assertions migrated to dynamic `ScriptDirectory.get_current_head()` shape (Phase 4.1 was the first migration to trip the existing test brittleness; cure is forward-compatible for all future phases); (i) `alembic/env.py` `set_main_option` clobber on test `cfg.set_main_option` documented inside `test_outbox_migration_upgrade_downgrade_cycle_clean` — the test uses `monkeypatch.setenv("DATABASE_URL", ...)` so `get_database_url()` resolves to the temp path. **Session-23 lesson absorbed durably:** don't claim "Ruff clean" without actually running `ruff check`; bash-mount-staleness on Edit-modified files can persist for the entire session even after `sync` + retries (extends gotcha #4/#7 — cure is trust Windows-side Read + defer canonical pytest to operator's Windows venv); hardcoded alembic-head literals in reversibility tests are a forward-incompatibility trap every new migration trips, future phase reversibility tests should query `get_current_head()` dynamically. **No production runtime impact** until next deploy; once deployed, alembic walks `e1f2a3b4c5d6 → 0a1b2c3d4e5f` (one migration); the new `outbox` table starts empty + the magic-link route auto-creates rows on each request. Operator manual smoke (deferred): magic-link request → email landed via Resend (sandbox doesn't have Resend access; covered by integration tests with mocked Resend). HALT at brief §3 Phase 4.1 boundary per dispatch instructions; Phase 4.2 (layered-scrape client interface + Google Places refactor) dispatches in a fresh Cursor session. Cursor session, two commits (feat + chore-autofix follow-up).

- **Phase 4.2 — Layered-scrape client interface + Google Places refactor (2026-05-13, commit `86eeaf8`):** Second sub-phase of Phase 4 shipped. 6 files changed (+1094 / -460): 4 new + 2 modified. New `app/contrib/ingest_base.py` defines the abstract `BaseIngestClient` ABC with 4 abstract methods (`discover(query) -> list[RawHit]`, `enrich(hit) -> EnrichedHit`, `dedupe_key(hit) -> str`, `to_entity_payload(hit) -> EntityPayload`) + three `@dataclass` carriers (`RawHit` with source/source_stable_id/name/lat/lng/raw; `EnrichedHit` wrapping a raw_hit + enriched dict; `EntityPayload` source-agnostic shape with name/entity_type/lat/lng/address/phone/website/description/category_slug/google_place_id/source/extension_payloads). Module top is pure-Python dataclasses + ABC — NO ORM imports at module top (gotcha-#17 cure carried forward from Phase 1D session-22 + Phase 4.1 session-23). New `app/contrib/google_places_scraper.py` library module: `GooglePlacesClient(BaseIngestClient)` subclass with `source_name = "google_places"`; reuses `GOOGLE_PLACES_LIMITER` for QPS pacing + retry on 429/5xx (rate_limiter.py untouched — its SourceLimiter interface is stable per its own docstring); preserves the resume-safe `load_processed_ids` pattern verbatim from the old script. Methods: `discover(query)` paginated Google Places Text Search; `enrich(hit)` Place Details fetch via place_id; `dedupe_key(hit)` returns `google_place_id`; `to_entity_payload(hit)` maps Google response → source-agnostic `EntityPayload` using `google_types_mapping` for category_slug + place_type. Orchestration methods `run_discovery(category, dry_run)` + `run_enrichment(dry_run)` match the existing script entry-point shapes so script wrappers are thin. New `app/contrib/google_types_mapping.py` operator-maintainable mapping table per strategy memo §3.1 — `_PRIMARY_TYPE_MAP: dict[str, tuple[str, str | None]]` with ~28 entries (restaurant / cafe / bar / plumber / electrician / hvac_contractor / general_contractor / doctor / dentist / hospital / pharmacy / lodging / rv_park / store / supermarket / grocery_or_supermarket / gas_station / car_repair / car_dealer / park / dog_park / marina / beach / veterinary_care / pet_store / school / gym / library / city_hall, etc.); `map_google_types_to_slug_and_place_type(types: list[str]) -> tuple[str | None, str | None]` tries primary type first then fallbacks; returns `(None, None)` if no match (operator-queue surface for review per Phase 5). Anchored edits on `scripts/places_discovery.py` (now thin wrapper with `--category` flag per design memo §6.1 step 1 — Phase 4.4 close-out's Railway-cron services depend on this flag) + `scripts/places_enrichment.py` (thin wrapper, unchanged `--limit` flag). **Texture rule preserved** — same DB rows produced, same rate-limit semantics, same idempotency posture (resume-safe pattern intact); the two scripts are now ~30-line entry points that instantiate `GooglePlacesClient` and call its `run_*` methods. **+16 net-new tests** in `tests/test_phase4_ingest_client_interface.py` covering: `BaseIngestClient` abstract-method enforcement (subclass must implement all 4); `GooglePlacesClient` is a `BaseIngestClient` subclass + correct `source_name` + correct `dedupe_key` shape; `map_google_types_to_slug_and_place_type` for `["restaurant"]` → `("eat-drink", "commercial")`, `["dog_park", "park"]` → `("outdoors-parks-trails", "place")` (primary-type wins), unknown types → `(None, None)`; `GooglePlacesClient.to_entity_payload` produces `EntityPayload` with `source="google_places"` + correct category_slug + correct place_type; `GooglePlacesClient` uses `GOOGLE_PLACES_LIMITER` for rate-limiting; subprocess import-chain regression that `from app.contrib.google_places_scraper import GooglePlacesClient` does NOT trigger gotcha-#17 cycle (mirrors `tests/test_phase4_background.py::test_background_module_does_not_import_models_at_module_top` shape); refactor regression deferred-to-operator (1 skipped — `python -m scripts.places_discovery --category eat-drink --dry-run` should produce same log output as pre-refactor; operator runs on demand). Pytest **1733 → 1749** (+16 net-new; the +1 over the brief §5.5 expected 10-15 ceiling is the subprocess import-chain regression + parametrized abstract-method-enforcement tests). Alembic head unchanged at `0a1b2c3d4e5f` (no migration in 4.2). Ruff clean (operator verified after-the-fact; the feat commit body did NOT falsely claim Ruff clean this time — session-23's lesson absorbed). **No production runtime impact** until next deploy; once deployed, the scripts run identically. **Pragmatic deviations:** all within brief §9 invitations — 4-method abstract interface (as specified), split `run_discovery`/`run_enrichment` (as specified), free-form `EntityPayload.extension_payloads` dict (as specified), ~28-entry `google_types_mapping` initial coverage (as specified), `--category` flag baked in (as recommended), `places_client.py` untouched (as specified). HALT at brief §3 Phase 4.2 boundary per dispatch instructions; Phase 4.3 (OSM Overpass client + cross-layer reconciler) dispatches in a fresh Cursor session against the pre-positioned + SHA-patched prompt at `outputs/cursor_dispatch_prompt_phase_4_3.md` (operator decision-lock: DEFER `entities.sources` JSON-array migration per brief §6.5 — comma-separated string in `entity.source` suffices for V1). Cursor session, single dispatch.

- **Phase 4.3 — OSM Overpass Layer-2 client + cross-layer reconciler + Google load-path wiring (2026-05-13, commit `2f87211`):** Third sub-phase of Phase 4 shipped — **the parallel-eligibility proof** per the master plan §4 Phase 4 effort estimate (shipping a second `BaseIngestClient` subclass + the reconciler that lets two scrape layers coexist proves the framework holds without rework). 7 files changed (+1030 insertions): 5 new + 2 modified. New `app/contrib/osm_overpass_client.py`: `OsmOverpassClient(BaseIngestClient)` with `source_name = "osm"`; `OSM_OVERPASS_LIMITER = SourceLimiter("osm_overpass", qps=0.5)` at module top (reuses the existing `app/contrib/rate_limiter.py::SourceLimiter` interface verbatim — rate_limiter.py untouched per brief §11); `OSM_OVERPASS_ENDPOINT = "https://overpass-api.de/api/interpreter"`; LHC bounding box `(34.43, -114.41, 34.59, -114.30)` per strategy memo §3.2; `build_query(tag, value) -> str` returns Overpass-QL with `{:.2f}` bbox formatting so `-114.30` appears literally in the string. `discover(query)` POSTs via `httpx.Client` + `OSM_OVERPASS_LIMITER.call_with_retry(lambda: client.post(...))` (per brief §9 deviation — `SourceLimiter.request` method doesn't exist; Cursor used the actual `call_with_retry` interface that matches the Google Places path); returns `[]` on transport errors / non-JSON / non-200 (no raise, aligned with brief §6.4 test #11). `enrich(hit)` is no-op (Overpass discovery returns full detail). `dedupe_key(hit)` returns `f"osm_{type}_{id}"` shape. `to_entity_payload(hit)` maps OSM tags `wheelchair=yes/limited/no` → `extension_payloads["ada_accessible"]` + `fee=no/yes` → `extension_payloads["free"]`; entity_type="place"; category_slug="outdoors-parks-trails" (default for single-category proof). New `app/contrib/ingest_reconciler.py`: `reconcile_hit(db, payload) -> ReconcileResult` with 3 match strategies in priority order — (1) `google_place_id` exact match → `action="update"`; (2) geo proximity within `GEO_PROXIMITY_THRESHOLD_M = 50.0` + normalized name match → `action="update"`; geo proximity + name mismatch → `action="ambiguous"`; (3) normalized name only (no geo) → `action="ambiguous"` (operator-review queue surface); no match anywhere → `action="insert"`. `@dataclass ReconcileResult(action, existing_id, merge_fields, reason)`. `SOURCE_PRIORITY` 8-entry table (`operator: 0`, `google_places: 1`, `osm: 2`, `lhc_open_data: 3`, `az_roc: 3`, `npi_registry: 4`, `usapickleball: 4`, `pdga: 4`). `_compute_merge_fields(db, existing_id, payload)` field-merge: operator-typed entity returns empty merge (no overwrite); higher-priority new source overwrites name + description + source via `_combine_sources` comma-separated multi-source string in `entity.source` (entities.sources JSON-array migration DEFERRED per operator decision-lock + brief §6.5 — comma-separated suffices for V1); lower-priority new source fills missing fields only. `haversine_m` + `slugify` are inline pure helpers (no new deps — no `geopy`, no `python-slugify`). `log_ambiguous_reconcile` helper added beyond brief spec for cleaner operator-review queue logging from the `places_load` hook site. New `scripts/osm_overpass_pull.py` thin CLI wrapper with `--tag`, `--value`, `--dry-run` flags. **Anchored edit on `app/contrib/google_places_scraper.py`** added `enrichment_row_to_entity_payload()` mapping helper that converts an enrichment-JSONL row → `EntityPayload` (the dispatch-aligned mapping surface). **Anchored edit on `scripts/places_load.py`** added the reconciler integration: before each `session.add(Provider(...))` call, runs `reconcile_hit()` and handles three action paths — `insert` (the original behavior: `session.add` + Phase 1D dual-write hook auto-promotes to Entity + extensions); `update` (geo merge into existing entity via reconcile-then-sync, then optional `merge_fields` apply on the Entity row); `ambiguous` (skip + `log_ambiguous_reconcile` to surface operator-review queue). **Texture rule preserved** — same DB rows out for the `insert` action (matches pre-4.3 behavior); `update` action runs reconcile then sync then optional merge_fields on Entity; `ambiguous` action logs warning + skips write (operator-review queue surface, admin form is Phase 5 / V1.5 territory). **+35 net-new tests** across two test files. `tests/test_phase4_osm_client.py` covers: `build_query` bbox / Overpass-QL shape; `OsmOverpassClient` is a `BaseIngestClient` subclass + correct `source_name`; mocked Overpass response → 3 `RawHit` objects with names + lat/lng; `dedupe_key` returns `osm_node_NNNNN` shape; `wheelchair=yes/no` → ada_accessible True/False; `fee=no/yes` → free True/False; empty Overpass JSON → empty `RawHit` list; non-200 → empty `RawHit` list; OSM_OVERPASS_LIMITER is a SourceLimiter; subprocess import-chain regression (OSM client + reconciler import without `app.db.models` pre-loaded). `tests/test_phase4_ingest_reconciler.py` covers: `haversine_m` ~0 for identical points + ~111000 for 1-degree-latitude separation; `slugify("English Village") == "english-village"` + Lake Havasu Aquatic Park! handling + unicode + punctuation; `reconcile_hit` empty DB → `insert`; existing entity with same `google_place_id` → `update`; existing entity within 50m + matching name → `update`; existing entity within 50m + mismatched name → `ambiguous`; existing entity > 50m + matching name → `ambiguous` (name only no geo); `_compute_merge_fields` operator-typed entity → no overwrites empty merge; Google source over existing OSM source → name + description + source updated; OSM source over existing Google source (lower priority) → only fill missing description; idempotency (same hit twice → first inserts, second updates with empty merge no field changes); `GEO_PROXIMITY_THRESHOLD_M == 50.0`; `SOURCE_PRIORITY` priority order including pdga; edge cases (missing lat/lng skips strategy 2, missing google_place_id skips strategy 1); reconciler does not bypass Phase 1D dual-write (returns metadata only, no pending ORM writes from reconcile_hit); subprocess import-chain regression; multiple geo+name candidates → `ambiguous`. Pytest **1749 → 1784** (+35 net-new — slightly above brief §6.4 expected 25-32 because Cursor shipped the bonus `log_ambiguous_reconcile` helper + the subprocess import-chain regression test pair). Alembic head unchanged at **`0a1b2c3d4e5f`** (entities.sources JSON-array migration DEFERRED per operator decision-lock + brief §6.5; reconciler uses `_combine_sources` comma-separated string in existing `entity.source` column). Ruff clean. **Three brief §9 deviations all sensible:** (a) **reconciler hook site moved from `app/contrib/google_places_scraper.py` to `scripts/places_load.py`** — Cursor discovered that 4.2's `google_places_scraper.py` has NO `session.add(Provider(...))` (JSONL → DB pipeline runs through `scripts/places_load.py`, which is the actual catalog-write site); Cursor correctly followed the actual write path. The dispatch prompt explicitly invited this in §9. (b) `SourceLimiter.request` doesn't exist in repo — used `call_with_retry(lambda: client.post(...))` (same interface as Google Places path); brief §6.1 pseudocode was wrong. (c) `build_query` bbox formatting uses `{:.2f}` format strings so `-114.30` appears literally (Python's default `str(-114.30)` would produce `-114.3`). **§12 surprises Cursor flagged (all benign):** `OsmOverpassClient.discover` returns `[]` on transport/non-JSON/non-200 (aligned with brief §6.4 test #11); `log_ambiguous_reconcile` helper added beyond brief spec for cleaner operator-review queue logging; `places_load` summary could later print `reconcile_skipped_ambiguous` / `reconcile_merged_geo` counts (already returned by reconciler, not yet printed). HALT at brief §3 Phase 4.3 boundary per dispatch instructions; Phase 4.4 (close-out: operator runbook + Railway-cron-service template + image-processing wrapper + master plan SHIPPED header + STATE.md refresh) dispatches in a fresh Cursor session against the pre-positioned + SHA-patched prompt at `outputs/cursor_dispatch_prompt_phase_4_4.md` (no operator decision-lock; three recommendations baked in — ship the optional `scan_and_save_mentions` + `enrich_contribution` wrappers per brief §4.3 + §7.4, ship the image-processing wrapper per §7.3, defer Railway-cron stand-up to operator). **No production runtime impact** until next deploy. Cursor session, single dispatch.

- **Phase 4.4 — Phase 4 close-out (2026-05-13):** Fourth and final sub-phase of Phase 4. Ships `docs/operations/railway_scheduled_jobs_runbook.md` (Railway scheduled-job operator runbook: when to use, pre-checks, numbered steps, monitoring, cost, rollback, references) + `docs/operations/scrape_logs_template.md` (per-run markdown template for `docs/scrape_logs/<source>_YYYY-MM-DD.md`). Anchored Edits: `app/photos/processor.py::process_uploaded_photo` wraps the Pillow/R2 pipeline body in `app.core.background.with_retry` (bounded retries + Sentry breadcrumbs on exhaustion; same Photo status transitions on success vs persistent failure — texture rule preserved); `app/api/routes/chat.py` schedules `BackgroundTasks.add_task(with_retry, scan_and_save_mentions, ...)`; all `enrich_contribution` enqueue sites use `add_task(with_retry, enrich_contribution, ...)` (`app/api/routes/contribute.py`, `app/api/routes/admin_contributions.py`, `app/api/routes/admin_mentions.py`, `app/admin/mentions_html.py`). New `tests/test_phase4_close_out.py` (+9) — Outbox magic-link wiring grep smoke on `app/auth/routes.py`; `with_retry` grep on processor + chat + enrichment routes; `_hourly_cleanup_loop` + `asyncio.create_task` lifespan regression strings; non-empty runbook + template files; subprocess import-chain (`with_retry` + GooglePlacesClient + OsmOverpassClient + `reconcile_hit` without loading `app.db.models` at import). **No new alembic migration** — head stays **`0a1b2c3d4e5f`**. Pytest **1786 collected → ~1795** (+9 net-new). **Phase 4 of the master build plan is COMPLETE** — Phase 5 (Tier 1 data gathering) is the next dispatchable major lane. Railway service stand-up remains **deferred-to-operator** per the runbook (not gating SHIPPED).

---

### Phase 5 — Tier 1 data gathering (4-8 weeks, parallel with Phase 6) — **restructured into 5.0 + 5.1–5.6 on 2026-05-14**

**Goal:** Populate Tier 1 categories (Home & Property Services + Health & Wellness + Eat & Drink + On the Water + Auto/RV/Fuel + Shopping). These are the resident-critical spine; making them dense first means the site is immediately useful for residents the moment Phase 6 UI ships.

**Structure (2026-05-14 restructure):** Phase 5 decomposes into a shared lead-up sub-phase (5.0) plus six per-category sub-phases (5.1–5.6). Unlike Phase 4's dependency chain, **5.1–5.6 are independent parallel-capable tracks** — the recommended sequence is for operator learning-curve + field-entry batching, not a hard ordering. Each sub-phase gets its own SHIPPED ledger line. **Partial close-out is explicit:** if the 8-week ceiling hits, ship the sub-phases that pass their acceptance gate and defer the rest to V1.5; Phase 6 renders empty-state copy for deferred categories. Per-category playbooks live in `outputs/cursor_brief_phase_5_tier_1_data.md` §3.1–§3.6; operator decisions are locked in that brief's §2 + `outputs/phase5_prereq_checklist.md` §3.5.

#### Phase 5.0 — Lead-up & shared tooling (~1-2 weeks) — gates 5.1–5.6

The only Phase-4-style sub-phase: it has a clean engineering "done."

- **§3 decision-locks** — 11 operator decisions sealed. **SHIPPED `acf5e2b`** (brief §2 + prereq §3.5).
- **Google types-mapping expansion** — ~54 Tier 1 `types[]` entries + beauty skip + RV-park boundary. **SHIPPED `62ab3b7`.**
- **Lead-up content** — `boat_access_rubric.md`, `manual_recovery_checklist.md` back-fill, `heat_exposure_priority_30_list.md` scaffold, Lane B verification briefing. **SHIPPED `b755b03` + `54ca07d` + `4ba29e4`.**
- **Lane B verifications + 3 tooling-touchup scripts** — `scripts/az_roc_verify.py` + `scripts/npi_verify.py` + `scripts/osm_overpass_load.py`, authored against verified endpoint shapes. Dispatch artifact `outputs/cursor_dispatch_prompt_phase5_leadup_tooling.md` **SHIPPED `23a6a1c`**; awaiting Cursor execution.
- **Operator-only actions** — Google Places billing cap (Cloud Console), Phase 4 Railway redeploy to alembic `0a1b2c3d4e5f`, `heat_exposure_priority_30_list.md` operator amendment (fill 22 LHC-venue placeholders).

**5.0 done when:** Lane B's 10 verifications are recorded, the 3 tooling scripts are pytest-green + ruff-clean, the Google Places billing cap is set, prod is redeployed, and the priority-30 list is operator-amended.

#### Phase 5.1 — Eat & Drink (~1-2 weeks) — warm-up category

Target 90-140 entries. Google Places only (no Layer 2/3/4). Pipeline: discovery → enrichment → load → ambiguous-queue triage → Layer 5 manual recovery → operator-curated field entry (`heat_exposure`, `crowd_notes`, `seasonal_hours`). **Acceptance gate:** 60+ entries loaded, all ambiguous hits reviewed, top-20 have long-form `crowd_notes`, every entry has `heat_exposure`, Phase 6 `/category/eat-drink` smoke renders 15+ per default filter.

**SHIPPED `3948add` 2026-05-15** — All 5 gate items met. 287 providers loaded; **255 active** post-cleanup (32 non-eateries deactivated + 1 Lady Lee's duplicate). `heat_exposure` 0 NULL (`indoor` 279, `water_adjacent` 5, `shaded` 2, `outdoor` 1). 48 `crowd_notes` populated under the locked `{"short": str, "long"?: str}` shape (17 long-form for top eateries + 3 grocery anchors per §3.3.h; 31 short-only for the next tier — absence of `long` signals list-blurb vs profile-section). Both PROVISIONAL heat-list rows resolved by web research (El Paraiso → `shaded`; College Street Brewhouse → confirmed `indoor`, not `water_adjacent`). AZ ROC client built (`fa0fddd`, Option A Playwright — task #6 closed; one-time `playwright install chromium` required before any live `az_roc_verify.py` run). 5th gate item met by **Phase 6.2 (`3948add`, Cursor)** — `/category/eat-drink` renders 255 active entries via `Entity.is_active=True` + `Provider.is_active=True + draft=False` filter. Phase-5-lane commit chain: `1bb2572..46ca430`. Close-out index: `outputs/phase5_1_field_entry_close_out.md`.

#### Phase 5.2 — On the Water (~1-2 weeks)

Target 40-90 entries. Google Places + **OSM Layer 2** (`leisure=marina` + `man_made=pier` + `natural=beach`) + LHC Parks & Rec Layer 3. Heaviest operator-curated surface: `boat_access` JSON per `docs/operations/boat_access_rubric.md`. **Acceptance gate:** 25+ entries, every marina has `boat_access` populated, Google+OSM ambiguous hits reviewed, Phase 6 `/category/on-the-water` + boat-mode toggle both render ≥15.

#### Phase 5.3 — Home & Property Services (~2-3 weeks)

Target 120-220 entries (highest-volume category). Google Places + **AZ ROC Layer 3** license cross-reference via `scripts/az_roc_verify.py`. **Acceptance gate:** 80+ entries, AZ ROC coverage on every licensed-trade entry, `is_mobile_service` populated on every entry, Phase 6 `/category/home-property-services` renders ≥15.

#### Phase 5.4 — Health, Wellness & Care (~1-2 weeks)

Target 30-70 entries. Google Places + **NPI Layer 4** cross-reference via `scripts/npi_verify.py`. **Acceptance gate:** 20+ entries, NPI coverage on every practitioner-led practice, mental-health + alternative-medicine Layer 5 coverage, Phase 6 `/category/health-wellness-care` renders ≥15.

#### Phase 5.5 — Auto, RV & Fuel (~1-2 weeks)

Target 50-100 entries. Google Places + Layer 5. **Acceptance gate:** 30+ entries, `is_mobile_service` populated on every entry, RV-specific Layer 5 coverage, Phase 6 `/category/auto-rv-fuel` renders ≥15.

#### Phase 5.6 — Shopping, Grocery & Essentials (~1-2 weeks)

Target 60-120 entries. Google Places + Layer 5 + (conditional) LHC business-license Layer 3 if Lane B §3 surfaced a viable endpoint. **Acceptance gate:** 40+ entries, independent-boutique Layer 5 coverage, Phase 6 `/category/shopping-essentials` renders ≥15.

**Estimated totals at end of Phase 5:**
- Home & Property Services: 120-220 entries
- Health, Wellness & Care: 30-70 entries
- Eat & Drink: 90-140 entries
- On the Water: 40-90 entries
- Auto, RV & Fuel: 50-100 entries
- Shopping, Grocery & Essentials: 60-120 entries
- **Total Tier 1: 390-740 entries**

**Dependencies:** Phase 4 (layered-scrape framework + reconciler + Outbox — SHIPPED). Phase 5.1–5.6 each depend on Phase 5.0. Runs in parallel with Phase 6 UI work; Phase 6 consumes each 5.X category as it ships.

**Effort estimate:** Operator workload — original master estimate 60-100h; bottom-up audit (`outputs/phase5_prereq_checklist.md` §5) re-derives 83-351h; **realistic mid-range budget: 100-150h over 6-8 weeks.** The long pole is operator-curated field entry (390-740 entries × 5-15 min each). Engineering effort minimal — the 3 tooling-touchup scripts in 5.0, plus reactive `GEO_PROXIMITY_THRESHOLD_M` tuning if a scrape surfaces >50 ambiguous hits.

**Success criteria:** Each of 5.1–5.6 meets its per-category acceptance gate (15+ entries per default filter on the corresponding Phase 6 category landing page). Phase 5 SHIPPED when all six pass; partial close-out (ship the passing sub-phases, defer the rest to V1.5) is acceptable at the 8-week ceiling. **Phase 7 (Tier 2 UI + chat integration) is the next major lane after Phase 5 completes** — see Phase 7 below + `outputs/phase7_handoff_note.md`.

---

### Phase 6 — Tier 1 UI build (4-6 weeks, parallel with Phase 5)

**Goal:** User-facing pages for Tier 1 categories. This is when the directory starts to feel real.

**Critical first deliverable: Unified Hava card grammar.** Per Opus design handoff §6.1. Single Jinja partial `app/templates/components/hava_card.html` that renders any ENTITY (commercial / place / event) in any context (category page, search results, group landing, profile reference). Place vs event differentiation via status-line color (green vs lake-blue) + content ("Open until 10pm" vs "Tonight at 6:00pm"), NOT via separate templates or chrome. Same shell everywhere.

**Other deliverables (after card lands):**
- Honest freshness band on cards (colored dot) + on profile pages (colored band at top) per Opus design §6.3
- Category landing pages for Tier 1 (6 pages): each has the locked 3-row chip system (cuisine/sub-trade · district · operational+time), sort dropdown, sponsor slot, organic stream of unified Hava cards, editorial copy footer, map toggle, pagination
- District context paragraphs rendering on every entity profile in that district (single SQL join from entities → districts)
- Boat-access mode toggle in header (URL param + localStorage + user preference); map water-overlay when active; profile-page boat-access top-of-fold region when active
- Map view (Leaflet + OSM) with marker clustering
- Search bar in homepage hero + category page headers
- Time-aware default ranking + heat-aware bias (Opus #2)
- Seasonal hours rendering on profile pages (Opus #3)
- Homepage with hero + Ask Hava box + Today in Havasu conditions strip (data hookup deferred to Phase 8) + 8 themed group tiles for browse
- Themed group landing pages for Tier 1 groups (Eat & Drink, Health & Fitness, On the Water, Home & Auto) — bundle related categories under each
- Provider profile already has Phase A+B; extend with "What's on at this venue" region (Phase 9 hooks; renders empty until Phase 9)
- Mobile-first responsive throughout (bottom-sheet patterns, swipe gallery, sticky action elements)

**Dependencies:** Phase 1 (ENTITY) + Phase 2 (auth + photos + search) + Phase 3 (operator-curated fields + districts). Runs in parallel with Phase 5 data gathering.

**Effort estimate:** L+ (15-25 days dispatch). Multiple parallel sub-lanes possible: unified card (1 lane); category templates (1 lane per ~3 categories); map view (1 lane); homepage (1 lane); themed group landing (1 lane).

**Success criteria:** All 6 Tier 1 category pages render correctly with populated data. Unified Hava card renders identically across all surfaces. District context paragraph appears on every Tier 1 entity's profile.

**Shipped (incremental):**

- **Phase 6.1 — Unified Hava card grammar (2026-05-14, commit `fd16e7a`):** First sub-phase of Phase 6 shipped — the critical first deliverable per Opus design §6.1. New `app/templates/components/hava_card.html` (30-line Jinja partial) renders any ENTITY (commercial / place / event) in any context (category page / search results / group landing / profile reference) with the same shell; place vs event vs commercial differentiation via status-line color + content, NOT separate templates. New `HavaCardViewModel` dataclass appended to `app/providers/view_models.py` (+27 lines; frozen, pure-data, consumed by template). New `build_card_view_model` helper + shared `is_open_status_from_structured_hours` / `is_open_status_for_entity` / `effective_hours_structured_from_entity` / `derive_freshness_band_from_updated_at` / `derive_hero_photo_from_entity` / `_hero_photo_for_card` helpers appended to `app/providers/queries.py` (+377 lines); `is_open_now` refactored to call the shared helper — behavior preserved, `tests/test_provider_profile.py` still 18/18. New `app/static/styles/components/hava_card.css` (202 lines, mobile-first: stacked <768px, two-column ≥768px) imported via `@import` at top of `home.css`. Locks applied: freshness on `entities.updated_at` (green <30d / amber 30-90d / red >90d — Cursor wrote a fresh `derive_freshness_band_from_updated_at` honoring the brief lock rather than switching to `last_verified_at`); 768px breakpoint; subtle uppercase sponsor pill; commercial/event/place status colors per brief; `boat_access_badge` on `IS NOT NULL` semantics; `heat_exposure_pill` omits indoor/null. 17 net-new tests in `tests/test_phase6_hava_card.py` (433 lines) across VM construction, freshness bands, commercial/event/place status, sponsor pill, heat/boat badges, Jinja ×4 render contexts, empty hero/district graceful degradation, CSS + import smoke, hours-helper regression. Pytest **1803 → 1820 collected** (+17 net-new; 1818 passed + 2 skipped + 30 subtests, clean serial `pytest -q` run 9:27). Alembic head unchanged at `0a1b2c3d4e5f` (Phase 6 ships no migrations). Ruff clean. **One brief §3.1-invited deviation:** place/program `profile_url` falls back to `/home` for non-commercial entities without a dedicated permalink, keeping the card a valid full-card link until place routes ship in 6.2/6.3. **Companion `fix(tests)` at `7dac88c`:** `Phase8StabilizationTests.setUp` had a latent FK-ordering bug (bulk `DELETE FROM chat_logs` without first clearing `llm_mentioned_entities.chat_log_id` + nullable `contributions.llm_source_chat_log_id` dependents) — surfaced by Phase 6.1's full-suite verification under overlapping background runs, not caused by 6.1; setUp now clears dependents before the parent delete. **No production runtime impact** until next deploy; the card grammar renders against existing Phase 1+3 schema. HALT at brief §3.1 boundary per dispatch instructions; Phase 6.2 (first category landing page template + Eat & Drink proof) dispatches in a fresh Cursor session against the pre-positioned + SHA-patched dispatch prompt at `outputs/cursor_dispatch_prompt_phase_6_2.md`. Commit chain for the 6.1 close-out batch: `7dac88c` (fix) → `fd16e7a` (feat) → `0ddfc32` (chore: outputs forward-positioning artifacts). Cursor session, single dispatch.

---

### Phase 7 — Tier 2 UI + chat integration (3-4 weeks)

**Goal:** Extend the directory to Tier 2 categories + wire chat to the populated directory.

**Deliverables:**
- Category landing pages for Tier 2 (3 pages): Outdoors/Parks/Trails, Lodging & VR, Pets — same template pattern as Phase 6
- Themed group landing pages for Tier 2 groups (Outdoors, Stay)
- Chat tier 2 / tier 3 wired to query ENTITY table (replaces pre-pivot River Scene events catalog query at `app/chat/tier2_db_query.py:33+`)
- Chat awareness of boat-access mode (when active, queries filter by `boat_access IS NOT NULL`; tier 3 LLM prompt gets "user is in boat mode" preamble)
- Chat awareness of conditions (when heat advisory active, ranking shifts toward indoor venues per Opus #2; when AQI bad, similar)
- HALT 3 close-out work — confabulation guardrails ship; `FEATURE_FLAG_DISCLOSURE_RENDERER` flipped to `true` if validation passes
- Cross-entity chat queries ("where can I take my dog for breakfast?") work — chat returns dog-friendly restaurants AND dog parks interleaved
- Snowbird-return view on homepage (logged-in users active October-April see "what's reopened" panel)

**Operator workload:** run Tier 2 data gathering in parallel (same workflow as Phase 5 but smaller volume — 75-175 entries total across the 3 Tier 2 categories).

**Dependencies:** Phase 1 (ENTITY) + Phase 6 (unified card grammar). Tier 2 data gathering runs alongside.

**Effort estimate:** M-L (10-14 days dispatch).

**Success criteria:** Chat works across all Tier 1 + Tier 2 entities. HALT 3 close-out passes. Cross-category queries return interleaved results.

---

### Phase 8 — Trust layer + conditions panel + alerts (2-3 weeks)

**Goal:** Land the Public & Civic Resources category (the trust/retention layer per ChatGPT taxonomy) + the Opus #1 + #8 conditions infrastructure.

**Deliverables:**
- Public & Civic Resources category page (library, transit, visitor info, utilities, airport, senior resources, payment/licensing links, civic orgs) — entities populated via Layer 3 (city open data) primarily
- Conditions data fetching infrastructure: AirNow (AQI) + NWS (weather + heat advisory + sunset) + USGS (Lake Havasu gauge — operator confirms specific gauge ID) running on Railway scheduled job every 15 min
- `external_conditions_cache` table populated; reads transparently cached
- "Today in Havasu" conditions strip live on homepage with honest staleness indicators ("Updated 12 min ago")
- Alert dispatch evaluation job (every 15 min): reads cache, compares against trigger thresholds per alert_type, queries opted-in subscriptions, dispatches email via Resend BackgroundTasks
- Alert subscription UI on `/account/alerts`
- Alert email templates with the texture-moat venue-context mapping (heat advisory email lists indoor alternatives from user's favorites)
- Per-alert dedup: same alert_type not fired for same user more than once per 6 hours

**Dependencies:** Phase 2 (account-lite for User schema), Phase 3 (alert tables), Phase 4 (background jobs), Phase 6 (favorites system for venue-context mapping). Opus's hidden-dependency warning: Phase 5 + 6 must have tagged enough entities with `heat_exposure` for the alert venue-context mapping to fire (~30 entities minimum).

**Effort estimate:** M (5-8 days dispatch). Plus operator work to register AirNow API key, confirm USGS gauge ID, check City of Lake Havasu emergency-notification feed format.

**Success criteria:** Conditions panel updates every 15 min. Alerts fire correctly for heat advisory / AQI / lake hazard / event traffic. Alert emails include relevant venue-context recommendations.

---

### Phase 9 — Schedule-heavy expansion: Events + Classes/Sports/Recreation (3-4 weeks)

**Goal:** Tier 3 categories. These are deferred per ChatGPT taxonomy recommendation because refresh/expiry/cancellation tooling is real overhead — better to land them when the schedule infrastructure can support them properly.

**Deliverables:**
- Events as ENTITY type fully wired (already in schema from Phase 1; this phase wires the UX)
- RRULE-based recurrence handling (Python `dateutil.rrule` or `recurrent`)
- Event scraper subsystem: scrape Chamber community calendar + Go Lake Havasu events + RiverScene Magazine + city/library calendars
- Freshness anchor for scraped events is scrape timestamp (tighter decay curve: green <7 days / amber 7-21 / red >21)
- Capacity/availability OPTIONAL — only display when venue publishes real data; otherwise omit (NO manufactured scarcity)
- Events category page with date-aware filters (today / this weekend / next month / by date)
- Classes, Sports & Recreation category page — recurring schedules + age bands + drop-in vs registration filters
- "What's on at this venue" region on profile pages (the Phase 6 hook is filled in here)
- Integrated stream on themed group pages (Phase 6 ships static; Phase 9 makes it actually interleave events + places)
- Themed group landing for "Things to Do" group (was deferred from Phase 6)
- Event-card rendering via unified Hava card (status line: "Tonight at 6:00pm" lake-blue) — same card shell as place cards

**Dependencies:** All prior phases. Optional but ideally ships before launch.

**Effort estimate:** L (12-18 days dispatch). Event scraper subsystem is the longest sub-lane.

**Success criteria:** Events appear correctly in category pages, themed groups, profile "what's on" regions, and chat responses. RRULE recurrence handles weekly classes correctly. Schedule freshness band reflects scrape recency.

---

### Phase 10 — Polish + accessibility + pre-launch hardening (2-3 weeks)

**Goal:** Final pass before launch.

**Deliverables:**
- Mobile polish: bottom-sheet patterns finalized, swipe interactions tested across iOS Safari + Android Chrome, sticky elements behave correctly
- Accessibility audit: keyboard nav, screen reader testing, color contrast review, ADA-style audit
- Performance optimization: Cloudflare R2 CDN tuning, static asset serving moved off FastAPI workers (CDN edge), image lazy-loading, cache headers tuned, Postgres query EXPLAIN review
- Apply remaining 8 pivot-notice banners to less-critical pre-pivot docs (per `pre_pivot_doc_banner_audit.md` §2 priority list)
- Sponsor outreach folder banner audit: 6 pre-pivot files reference old $59/$179/$399 tier structure — either delete or banner-stamp as "pre-pivot historical"
- Final QA pass across all 12 categories + chat + map + boat-access mode + alerts
- Sub-trade taxonomy locked (the `attributes.sub_trades` shapes per category that scrapers populate)
- 8 open design questions from Opus handoff resolved + folded into final templates

**Dependencies:** Phase 9 complete. No new feature work in this phase — polish + harden only.

**Effort estimate:** M (5-8 days dispatch).

**Success criteria:** Mobile UX clean, accessibility audit passes, performance hits target (< 1.5s page load on 4G mobile for category pages).

---

### Phase 11 — Monetization decision + wiring (2-3 weeks)

**Goal:** Lock the monetization model and wire it. Casey has kept this flexible through build; this is the lock point.

**Deliverables:**
- Operator decision: which monetization model wins? Default fallback (Verified Presence + Category Visibility + Seasonal Takeover) is in hand. ChatGPT taxonomy recommended changes (intent-cluster pricing $499-$1,250+ for emergency/high-intent, $500-$1,500+ for water/boating peak, $249-$749 for dining/lodging/district, $99-$299 for long-tail; season + district + intent bundles for seasonal takeovers; sponsorship NEVER overrides trust-sensitive ranking in Health/Pets/Civic). Casey decides whether to take these or stick with the default.
- Stripe (or alternative) integration for subscription billing
- Sponsor claim flow + edit UI: merchant logs in via account-lite, claims their entity, verifies (Casey manually confirms via phone for V1 — automated verification is V2), edits their listing
- Per-merchant analytics dashboard: views, calls, directions, Ask Hava handoffs, conversion timeline. Visible value of paid tier.
- Sponsor slot rendering on category pages (already shipped in Phase 6 with no paying sponsors; this phase wires the actual sponsor assignment flow)
- Sponsor disclosure rendering via existing `disclosure_renderer` pipeline — already plumbed
- Test with friendly merchant cohort (3-5 merchants) before broader rollout

**Dependencies:** All UI phases complete (need a working product to demo). Account-lite (Phase 2) live.

**Effort estimate:** M-L (8-12 days dispatch) + operator decision time on monetization model.

**Success criteria:** Friendly cohort can claim, edit, view analytics, and (optionally) pay. End-to-end flow works.

---

### Phase 12 — Launch (1-2 weeks)

**Goal:** Ship to the public.

**Deliverables:**
- Final QA across the full product
- Domain + DNS setup (the actual product URL)
- Soft launch: limited promotion via Havasu News, local Facebook groups, Chamber of Commerce partnership, friend network
- Cold-pitch sales motion kickoff: Casey walks into businesses with the cold-pitch materials (verified_presence_pitch.md + companions) already on the shelf
- Operational dashboards confirmed working (sponsor pipeline, daily user metrics, error tracking)
- Production runbook for incidents
- Ramp to full launch as initial sponsors come online + word spreads

**Dependencies:** All prior phases.

**Effort estimate:** M (4-8 days) + operator field-work for soft launch.

**Success criteria:** Soft launch successful; first paying sponsor (or first 5 free claimed listings if monetization model defers payment). Site stable under load.

---

### Phase 13 — V1.5 features (post-launch, ongoing)

Items intentionally deferred from V1 to post-launch. Build as time + revenue allow:

- Peer recommendations (Opus #7) — 5-10 merchant pilot to test reciprocal-back-scratching risk
- **UGC "describe this district" layer** (paired with peer recommendations because both share UGC moderation surface — design-once). Users submit short descriptions of districts they know; operator moderation gate keeps spam/abuse/wrong-info out; descriptions accumulate as a signal for V2 AI-summarization. V1.5 ships the submission + moderation flow only; canonical district paragraph rendered on profiles remains the operator-authored V1 paragraph until V2. Per 2026-05-14 §10 decision log entry on phased district paragraphs.
- SMS alerts via Twilio (schema is SMS-ready from Phase 3; just code switch + Twilio account)
- Accessibility profile data collection (structured ADA fields per Opus design deferral)
- Provider.category → category_id backfill (the OPEN P2 ticket in BACKLOG)
- Owner-uploaded video (deferred from image storage design)
- Bookings / reservations (if merchant demand justifies)
- Itinerary builder (Phase 3 of original pivot doc)
- Real-time fuel prices / room availability / launch conditions (dynamic data — research API options when justified)
- White-label for other small cities (genuinely V3; ignore unless inbound demand)
- Native review system (still deferred unless review-war dynamics in Havasu prove otherwise)

---

## §5 Dependency graph

```
Phase 1 (ENTITY) ─┬─→ Phase 2A (account-lite) ─┬─→ Phase 3 (v1.1 schema)
                  ├─→ Phase 2B (R2 + search)   ┤
                  └─→ Phase 3 (v1.1 schema) ────┘

Phase 3 ──→ Phase 4 (bg-jobs + scrapers) ──┬─→ Phase 5 (Tier 1 data gathering)
                                            └─→ Phase 6 (Tier 1 UI build)
                                              ↑ parallel ↑

Phase 6 ──→ Phase 7 (Tier 2 + chat)
Phase 6 ──→ Phase 8 (trust + conditions + alerts)

Phase 7, 8 ──→ Phase 9 (Events + Classes/Sports)

Phase 9 ──→ Phase 10 (polish + accessibility)
                ↓
              Phase 11 (monetization)
                ↓
              Phase 12 (launch)
                ↓
              Phase 13 (V1.5+)
```

Parallel-eligible:
- Phase 2A and Phase 2B run alongside each other
- Phase 5 (data gathering) and Phase 6 (UI build) overlap heavily
- Phase 7 and Phase 8 can run alongside each other
- Phase 9 sub-lanes (event scraper + classes/sports + integrated stream) can parallelize internally

---

## §6 Operator workload schedule

Engineering effort runs through dispatched lanes (Cursor / Claude Code / sub-agents). Casey's hands-on operator time is bounded but real. Major operator activities:

| Activity | Phase | Estimated time |
|---|---|---|
| Confirm available LHC open-data sources (Parks & Rec, business licenses) | Phase 4 prep | 2-4 hours |
| Register AirNow API key + confirm USGS Lake Havasu gauge ID + check city emergency-notification feed format | Phase 8 prep | 2-3 hours |
| Author district paragraphs (~8-12 paragraphs) | Phase 3 | ~1 hour |
| Lock 4 Phase-3 audit-memo review questions (`beauty_personal_care`, `tourism`, K-12 schools, recreational-entertainment) + run `SELECT DISTINCT category` against production | Phase 3 | ~1 hour |
| Review + lock Provider.category backfill mapping (audited 2026-05-14; 2 of 5 §4 lock-now questions resolved; remaining lock-now items 4 + 5 are trivial single-mapping confirmations) | Anytime (best at Phase 3 start) | ~30 min |
| Field-trip Layer 5 manual recovery for Tier 1 inventory gaps | Phase 5 | ~30-50 hours over 4-8 weeks |
| Field-trip Layer 5 for Tier 2 + Tier 3 inventory | Phases 7 + 9 | ~20-40 hours additional |
| Admin form data entry: boat_access details, heat_exposure tagging, crowd_notes, seasonal_hours where they differ | Phases 5 + 7 + 9 | ~40-80 hours over the build |
| Operator decision on monetization model | Phase 11 | 1-3 hours active decision time |
| Cold-pitch sales motion at launch | Phase 12 onward | 5-10 hours/week ongoing |

**Total estimated operator hours over the build: ~120-200 hours.** Spread over 7-9 months that's manageable alongside dispatching engineering lanes. Field-trip days are full-day blocks (~8 hours each) so Casey wants to batch them — Saturdays during cooler months especially.

---

## §7 Risk register

| # | Risk | Severity | Mitigation |
|---|---|---|---|
| 1 | Solo-founder burnout — engineering dispatch + operator field-work + sales over 6-9 months is significant load | High | Build-first sequencing means no sales pressure until month 7+. Sub-agent dispatches handle most engineering load. Field-trips batched into full-day blocks. Hire a Havasu-based BDR by month 6 if revenue justifies. |
| 2 | ENTITY schema migration introduces regression risk | High | Extensive pytest coverage required. Phase 1 dispatched as a single sequential lane (not parallel) to keep schema changes coherent. Staging environment validation before production migration. |
| 3 | User adoption — locals may continue using Google/Facebook groups | Medium-high | Hyperlocal context depth (district paragraphs + boat-access mode + conditions panel + crowd context) is the moat. Soft launch lets us measure adoption signals before full marketing push. If adoption clearly fails by month 9, pivot or pause. |
| 4 | Layer 5 manual recovery workload too large | Medium | Layer estimates show ~80-90% automated coverage via Layers 1-4. Manual recovery is the 10-20% gap. If gap turns out larger, defer Tier 3 categories or partner with Chamber for data sharing. |
| 5 | Sponsor packaging recommendations from ChatGPT (intent-cluster pricing) may not match what cold-pitch ground-truthing reveals | Medium | Monetization is flexible by design. Default fallback (Verified Presence $79 etc.) is also fine. Lock pricing after 10-20 cold-pitch reps in Phase 12. |
| 6 | Schedule-heavy expansion (Events + Classes) refresh burden | Medium | ChatGPT recommended deferring to Phase 9 specifically because of this. Schedule freshness band makes staleness visible. Scrape-timestamp anchor minimizes false-fresh signals. |
| 7 | HALT 3 close-out (chat confabulation guardrails) was deprioritized post-pivot and may be larger than expected | Medium | Audit confirmed the close-out scaffolding exists. Phase 7 includes the close-out work. Schedule it as a dispatch lane with explicit halt-and-report acceptance criteria. |
| 8 | Cloudflare R2 + custom CDN domain setup is operator action | Low | Documented in image_storage_design.md §8. ~2 hours operator work in Phase 2. |
| 9 | Production Postgres performance under 1000+ concurrent users | Medium (Phase 10/11) | DB connection pool sizing fix (S effort per audit). Slowapi Redis-backed store (post-Phase 4 once background-job infrastructure has Redis option). CDN for static assets handles most concurrency load. |
| 10 | Chat depends on accurate `heat_exposure` tagging for alert venue-context mapping (Opus hidden dependency) | Medium | Phase 5/6 operator workload prioritizes heat_exposure tagging for top ~30 entities by traffic before Phase 8 alerts ship. |

---

## §8 Open strategic questions

These need operator decision but most can fold into specific phases rather than blocking the master plan. Numbered for reference; recommended phase to resolve:

1. **City-bounded vs cross-border surfaces** (Parker, Havasu Landing, Topock for some intents) — Phase 4 scraper scope. Recommend city-bounded for V1; cross-border in V2 if user demand surfaces.

2. **Google Place ID + Yelp ID joining for dedupe and entity resolution** — Phase 4 scraper architecture. Recommend yes for Google Place IDs (we already store `google_place_id`); skip Yelp IDs unless we add Yelp Fusion API which isn't in current plan.

3. **Dynamic / semi-dynamic data (fuel prices, room availability, launch conditions)** — V1.5 or V2 question. Defer.

4. **Vacation-rental permits joined to lodging records** — Phase 7. Recommend yes if LHC publishes the data; check during Phase 4 source confirmation.

5. **Professional services as separate expansion layer (lawyers, accountants, insurance)** — V1.5 or V2. Currently absorbed into Home & Property + Community-replacing categories. Defer.

6. **Sponsorship strictness rules in Health / Pets / Civic** — Phase 11. Recommend per ChatGPT: NEVER override organic ranking on emergency or care-critical queries; sponsorship increases exposure within clearly disclosed bounds only.

7. **First-party neighborhood / district model (districts as entities vs strings)** — Phase 3. Recommend yes, full `districts` table per Opus design. Eight to twelve districts in Havasu means small table with high leverage.

8. **Themed group cuts (8 right? Real Estate as 9th?)** — Phase 6. Recommend stick with 8; Real Estate folds into Home & Auto for V1; revisit if user behavior shows the grouping is wrong.

9. **Place vs event visual differentiation** — Phase 6. Currently status-line color + content only (subtle). Fallback if user testing shows too subtle: add small "Event" word tag (NOT colored background).

10. **Persistent map vs toggle on desktop category pages** — Phase 6. Recommend default-on with collapse option.

11. **Ask Hava as search vs parallel surfaces** — Phase 6. Recommend keep separate Search input + Ask Hava button initially; collapse into single intelligent input is a V1.5 candidate after user data shows behavior.

12. **Capacity display on schedule entries** — Phase 9. Only honest if venue publishes real availability. Otherwise omit. No manufactured scarcity.

13. **SMS alerts in V1 or V1.5** — Phase 8. Recommend defer to V1.5 per `conditions_panel_and_alerts_design.md` §10. Schema is SMS-ready; just no Twilio integration in V1.

14. **Monetization model lock** — Phase 11. Operator decision based on cold-pitch ground-truthing.

---

## §9 Rough calendar view (6-9 months at solo-founder pace)

| Month | Phase(s) active | Major milestones |
|---|---|---|
| Month 1 | Phase 1 (ENTITY schema) | Schema migrated; app code on ENTITY; pytest stays green |
| Month 2 | Phase 2A + 2B parallel; Phase 3 toward end | Account-lite + R2 + search shipped; v1.1 schema migration applies |
| Month 3 | Phase 4 (scrapers); Phase 5 starts | Background jobs running; Tier 1 Layer 1 scrape complete |
| Month 4 | Phase 5 + 6 parallel | Tier 1 categories populated; unified Hava card grammar shipped |
| Month 5 | Phase 6 + Phase 7 starts | All Tier 1 category pages live; chat wired to ENTITY |
| Month 6 | Phase 7 + 8 parallel | Tier 2 categories live; HALT 3 close-out passes; conditions panel live |
| Month 7 | Phase 9 | Events + Classes/Sports/Recreation live; schedule integration complete |
| Month 8 | Phase 10 + 11 | Polish + accessibility + monetization wired |
| Month 9 | Phase 12 + Phase 13 starts | Launch + early V1.5 features |

This is aggressive but achievable if engineering output stays steady, no major rework hits, and operator field-trip workload doesn't drag. Realistic worst case: 11-12 months if any phase doubles its estimate.

---

## §10 Decision log

Decisions recorded as they're made. Each decision: date, what was decided, by whom, replaces what.

| Date | Decision | Decided by | Supersedes |
|---|---|---|---|
| 2026-05-12 | Pivot from chat-first to directory-first; three front doors (browse + search + ask) | Operator | Original chat-first product framing |
| 2026-05-13 | Pivot §8.1 — 12 categories locked (original list: eat-and-drink, events, family, home-services, etc.) | Operator | — |
| 2026-05-13 | Pivot §8.2 — Place model deferred to Phase 2 | Operator | — |
| 2026-05-13 | Pivot §8.3 — Resend for magic-link auth | Operator | — |
| 2026-05-13 | Pivot §8.4 — Leaflet + OSM tiles for maps | Operator | — |
| 2026-05-13 | Phase 2.5 rate-limiter §8 decisions (8 decisions locked) | Operator + Cowork primary | — |
| 2026-05-14 | Build-first / sell-after sequencing; full vision is everything Lake Havasu | Operator | Pivot §4/§5 parallel build-and-sell sequencing |
| 2026-05-14 | Place model promoted from Phase 2 to Phase 1 of build sequence | Operator | Pivot §8.2 (effective amendment to LOCKED status) |
| 2026-05-14 | Opus 4.7 first round — 7 of 8 features in V1 (conditions panel, heat-aware ranking, seasonal hours, boat-access mode, crowd context, mobile-services, alerts); peer recs deferred to V1.5 pilot | Operator | — |
| 2026-05-14 | Boat-access elevated from filter chip to directory-wide mode | Operator | Original Eat & Drink ChatGPT review framing |
| 2026-05-14 | ChatGPT taxonomy research — 12 categories restructured: deletes Family + Community; adds Classes/Sports/Rec + Public/Civic; Tier 1/2/3 sequencing | Operator | Original 12-category locked list (rename most, replace two) |
| 2026-05-14 | Unified ENTITY schema (single core table + extensions) | Operator | Place model design Option A (separate places table); supersedes the place_model_design.md recommendation |
| 2026-05-14 | Opus 4.7 second round — unified Hava card grammar + 4-level browse hierarchy + 8 themed groups + district context paragraphs + Events as third ENTITY type + honest freshness band | Operator (implicit acceptance via task completion) | — |
| 2026-05-14 | Revenue optimization deferred per build-first sell-after; cold-pitch ground-truthing at launch | Operator | — |
| 2026-05-14 | Category taxonomy rewrite + audited backfill mapping land in Phase 3 v1.1 schema pass (NOT Phase 1B, NOT a standalone Phase 1.5 ticket, NOT deferred to V1.5). Phase 3 effort bumps from ~3-5 days + 1 hr operator to ~5-8 days + 2-3 hr operator. Resolves the master-plan hole flagged by `category_backfill_mapping_audit_2026-05-14.md` §3. | Operator | Phase 3 deliverables list pre-audit (silent on category-seed rewrite) |
| 2026-05-14 | Professional-services strings (`insurance`, `financial`, `legal`, `real_estate`, `professional_services`) NULL category_id during Phase 3 backfill; deferred to V1.5+ per synthesis §7 Q5. NULL operator-queue is the honest disposition; do NOT force into `public-civic-resources` or `home-property-services` for V1. Phase 13 V1.5 lane revisits if cold-pitch demand justifies a dedicated Professional Services category. | Operator | DRAFT mapping's "community catch-all" target (the catch-all no longer exists under new taxonomy) |
| 2026-05-14 | District paragraphs phased across V1 / V1.5 / V2 (hybrid pattern). **V1 (Phase 3):** operator-authored 8-12 paragraphs, ~1 hour. Preserves the launch-day hyperlocal moat per Opus design §2.4. **V1.5 (Phase 13):** UGC "describe this district" layer ships alongside peer recommendations — shared UGC moderation surface, design-once for both. User descriptions accumulate. **V2 (post-V1.5):** AI-summarizes accumulated user descriptions; operator-approval gate before replacing the canonical operator-authored paragraph. Pattern preserves day-1 moat (operator authority + specificity) while scaling over time (UGC voice + AI synthesis) without compromising provenance (operator gate keeps texture rules intact). Texture rule reminder: AI-summarized content always renders with explicit provenance label ("Updated from 12 community descriptions, last reviewed by operator on..."). | Operator | Operator-only-forever framing implicit in Opus design §2.4 (now scoped to V1 only); does NOT modify Phase 3 deliverables (V1 work unchanged) |

---

## §11 Living-document principles

This doc gets updated. Specifically:

- **After each phase ships:** add a "Shipped:" line under the phase header with date + commit SHA + actual effort vs estimate. Don't delete the original phase content.
- **When a new strategic decision lands:** add to §10 decision log. Update affected phases inline if sequence/scope changes.
- **When a design memo gets superseded or amended:** update §3 with the supersession + reference the new doc.
- **When a risk materializes:** add to §7 with date observed and resolution; don't delete the original risk entry.
- **When a phase's effort estimate is clearly wrong (observed during build):** amend in place with a note explaining the delta.

The decision log (§10) is the audit trail. The phase content (§4) is the operating sheet. The risk register (§7) is the alert layer. Together they're the operating doc for the next 6-9 months.

If a future agent reads this doc and finds it badly outdated relative to the actual code state, that's a hygiene failure on whoever shipped the last phase without updating here.
