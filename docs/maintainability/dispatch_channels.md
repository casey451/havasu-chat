# Dispatch channels — operator playbook

This document is the *how-to* companion to `docs/maintainability/dispatch_protocol.md` (which covers the 12 working-agreement rules). Use this doc when picking which channel to route a piece of work to and how to write the prompt. Use the protocol doc when you're about to do something with `git`, edit a shared file, or run a smoke test.

---

## The five channels

You (the Cowork primary) have five effective ways to get work done. The first four are paste-based or operator-mediated; the fifth is direct.

### 1. Cursor — focused single-file edits

**Best for:** bounded code changes with precise scope. Single-file or small multi-file lanes. Schema migrations. Operator scripts. Anything where the diff should be small and reviewable.

**Pattern:** you write a self-contained dispatch prompt → operator (Casey) pastes it into a fresh Cursor chat → Cursor returns a text report → Casey pastes the report back to you → you review the diff and propose the commit recipe.

**Strengths:** anchored-Edit-friendly, executes cleanly when scope is well-defined, good at "do exactly this and report back."

**Weaknesses:** occasionally ships pragmatic deviations from the dispatch and reports them at the end (re-read for "deviation" callouts before integrating). Has been observed bundling unrelated working-tree changes into commits despite explicit constraints (the *absorption pattern* documented in `2026-05-10_absorption_forensics.md`). Solution: instruct Cursor to report ship-log entry text rather than editing `BACKLOG.md` directly; you add the entry after the commit lands.

**Prompt anatomy:**
1. Context (1-2 paragraphs — what's the ticket, what's the production state, what's blocked elsewhere)
2. Scope (numbered steps, files to touch, files to leave alone)
3. Constraints (anchored Edit only, don't run `git add -A`, don't push, don't amend)
4. Report format (files modified, tests added, pytest before/after, SHA, ship-log text)

**Recent example dispatches** (look at the corresponding ship-log entry in `BACKLOG.md` for the executed result):
- `#50` (matcher floor) — single-file edit
- `#56` (UTF-8 wire test) — single new test file
- `#57 + #59` bundle — two related tickets in one prompt

### 2. Claude Code — heavy multi-file lanes

**Best for:** multi-file refactors, audit lanes, comprehensive test-suite generation, architectural investigations. Anything where "investigate first, then ship" is the right shape.

**Pattern:** same paste-based as Cursor.

**Strengths:** handles multi-file scope without losing the thread. Strong at "investigate this and propose a fix" lanes. Comprehensive test coverage. Proactive about halt-and-report when scope is unclear (this is good behavior — encourage it explicitly in your prompts).

**Weaknesses:** can over-investigate when given an unbounded prompt. Brief-baseline-staleness pattern: pytest counts and SHAs in dispatch briefs go stale during execution; CC has flagged this 4+ times. Mitigation: instruct CC to run `git pull && pytest -q --collect-only | tail -1` as brief step 0 to confirm the actual baseline at execution time.

**Prompt anatomy:** same as Cursor, plus:
- Multi-phase scope: investigation → design → implementation → tests → commit
- Halt-and-report etiquette: tell CC explicitly when to stop. Investigation phases should always allow stopping if scope is bigger than expected. The session 2026-05-10 #51 and #56 ships are good models — both halted appropriately.

**Recent example dispatches:**
- `#51` (UTF-8 charset patches) — investigation + halt-and-report (turned out to be smoke-harness encoding artifact, not an app bug)
- `#58` (delegating-entry floor coverage) — investigation + ship
- chat-route integration test — halted with 4 scoping decisions, shipped reduced scope

### 3. ChatGPT — pure prose, no codebase access

**Best for:** anything that doesn't need codebase access. Cold-email drafts, FAQ writing, operator-facing documentation, brainstorming, market research, copy editing.

**Pattern:** you write the prompt → Casey pastes to ChatGPT → ChatGPT returns text → you write to file via the Write tool, polishing markdown structure on the way in.

**Strengths:** fast, no file-access overhead. Good at structured creative work when given clear voice/format anchors.

**Weaknesses:**
- Cannot read codebase, cannot verify product reality. Use `[CASEY: confirm <fact>]` placeholders in prompts for facts ChatGPT can't ground.
- Tends to skip markdown structure (no `#`/`##`/`---`) — always do a polish pass on save.
- Auto-appends tracking parameters to URLs (e.g., `?utm_source=chatgpt.com`) — strip them.
- Drifts toward marketing speak ("amazing," "thrilled," "excited") — explicitly ban these in the prompt.

**Prompt anatomy:**
1. Context (1-2 sentences — what's this for, who reads it)
2. Voice anchor — paste an excerpt of existing prose that matches the target voice; tell ChatGPT to match it exactly
3. Output requirements (sections, length, tone)
4. Constraints (no exclamation points, no marketing speak, sign-off conventions, fact-check placeholders)

**Recent example dispatches:**
- Three new cold-email variants (restaurants, boat repair, auto repair) — voice-matched against existing variants
- Reply handlers (yes/no/questions/follow-up) — operator-facing email templates
- End-user FAQ — first content under `docs/user_facing/`
- Lake Havasu seasonality calendar — operator reference for sprint pacing

### 4. Sub-agents — your Agent tool

**Best for:** parallel verification, code review, voice-battery / adversarial testing, doc audits, recovery investigations, forward-looking forensics.

**Pattern:** you dispatch directly via your `Agent` tool with a self-contained prompt. The agent runs in its own context window and returns a text report. **No operator round-trip needed.**

**Strengths:** parallel work that doesn't block the main conversation. Has full file tools + bash. Good at "go investigate X and report back." Can write files, but should be instructed not to (the operator's commit workflow expects deliberate staging).

**Weaknesses:**
- Burns YOUR context (the agent's report comes into your context). For long sessions this adds up.
- Parallel commits can race the operator's git workflow. Always instruct sub-agents not to run any git operations.
- Multiple sub-agents in parallel can race each other on shared files. Stay in non-overlapping file domains.

**Prompt anatomy:**
1. Context (current production HEAD, pytest baseline, what's already in flight elsewhere)
2. Scope (file domain — explicit allowlist of files the agent may touch)
3. What NOT to touch (every other file, especially files other in-flight lanes are using)
4. Constraints (no git operations, use Read with Windows-side paths preferred, anchored Edit only)
5. Report format (what to return as text)

**Recent example dispatches:**
- Cross-`docs/` reference audit (subsumed `#54`) — read-only forensics
- Post-enrichment smoke catalog draft — forward-looking artifact
- HALT 3 close-out template — forward-looking artifact
- Absorption-pattern forensics + Rule 13 candidate — recommendation was DON'T add Rule 13

### 5. Yourself — direct file tools

**Best for:** small docs edits, `BACKLOG.md` status flips after a ship lands, anchored Edits to known files, writing prose ChatGPT just produced.

**Pattern:** Read first if needed, then Edit/Write directly.

**Strengths:** zero operator burden, immediate feedback.

**Weaknesses:** burns your context if reading large files. The bash mount is unreliable for `git` operations (Rule 7 of dispatch protocol — Linux mount serves stale views; Windows-side Read is authoritative).

---

## Picking a channel

| Work shape | Channel |
|---|---|
| Single-file edit, code | Cursor |
| Multi-file lane / audit / heavy investigation | Claude Code |
| Pure prose, no codebase needed | ChatGPT |
| Parallel verification or read-only forensics | Sub-agent |
| Tiny doc edit / `BACKLOG.md` status flip / append a paragraph | Yourself |

**When in doubt:** start with a sub-agent investigation lane. Read-only investigation reveals the right channel for the actual ship.

---

## Multi-channel parallel dispatch

You can run multiple sub-agents in parallel by sending a single message with multiple `Agent` tool calls — they execute concurrently. Casey-paste channels (Cursor, Claude Code, ChatGPT) run sequentially via Casey's keyboard; he pastes one prompt at a time.

**Realistic parallel topology** (proven on session 2026-05-10):
- 1 Cursor lane in flight + 1 Claude Code lane in flight + 1 ChatGPT prompt awaiting paste + 1-4 sub-agents running concurrently from your message.

**Constraints when running multiple lanes:**
- Each lane in a different file domain (no two lanes touching the same file).
- Each lane instructed not to touch `BACKLOG.md` (you consolidate ship-log entries after commits land).
- Sub-agents instructed not to run git operations.
- Casey's commit boundaries are sequential — he commits one lane at a time as reports come in.

---

## Common gotchas

**1. `BACKLOG.md` absorption.** Agents instructed not to touch `BACKLOG.md` sometimes commit it anyway when their `git add` stages working-tree changes implicitly. Substance is correct (the entries are right), history is messy. Mitigation: instruct agents to *report* ship-log entry text rather than editing `BACKLOG.md`; you add it after their commits land. Forensic memo: `docs/maintainability/2026-05-10_absorption_forensics.md`.

**2. Brief baseline staleness.** Dispatch briefs say "pytest baseline 1391, HEAD `f990488`" but by the time the agent runs, other lanes have committed. Pytest count and HEAD have moved. Mitigation: instruct the agent to confirm baseline at execution time with `git pull && pytest -q --collect-only | tail -1`.

**3. PowerShell encoding.** `Invoke-RestMethod -Body` defaults to ISO-8859-1. Always include `-ContentType "application/json; charset=utf-8"` in PowerShell smoke calls. See dispatch protocol Rule 4 + Rule 5.

**4. Bash mount staleness.** The Linux bash mount serves stale `.git` views — `git log`, `git status`, `wc -l` on `/sessions/.../havasu-chat/...` all unreliable. Use the Read tool with Windows-side paths (`C:\Users\casey\projects\havasu-chat\...`) — those are authoritative. See dispatch protocol Rule 7.

**5. `git commit --amend` while parallel lanes are in flight.** Don't. The amend will rewrite the most recent commit, which may not be yours. Dispatch protocol Rule 12 — added 2026-05-10 after the `#50`/`#51` git wrinkle.

**6. ChatGPT URL tracking parameter.** ChatGPT auto-appends `?utm_source=chatgpt.com` when it inlines URLs. Always strip on save.

**7. Sub-agent context burn.** Each sub-agent report comes back into your context window. After 4-5 sub-agent runs, your context is meaningfully fuller. Plan accordingly — for long sessions, prefer Cursor/CC/ChatGPT (whose contexts don't burn yours) over sub-agents.

**8. PowerShell `$` interpolation hits commit subjects too.** Dispatch protocol Rule 4 scopes the single-quote rule to `Invoke-RestMethod -Body`, but the same interpolation bites `git commit -m "..."`. Session-13's commit `11b248f` lost the `$79` from the intended subject `Verified Presence ($79/mo) cold-pitch scripts` — PowerShell read `$79` as a variable inside the double-quoted `-m` argument and the committed subject reads `(/mo)`. Cure: single-quote any `git commit -m '...'` whose subject contains `$`, `` ` ``, or other PowerShell-interpolable sigils. Cosmetic this time; would be a real semantic problem if a future subject embedded a variable-looking string that the shell silently emptied.

**9. Local ruff must match the `dev-requirements.txt` pin.** Session-13's schema-commit push surfaced 4 pre-existing ruff failures in three unrelated test files because the Cowork primary's local ruff was older than CI's pinned `ruff==0.15.12`. Newer ruff has stricter I001 (isort) behavior; older locals pass clean while CI fails. Cure: `python -m pip install ruff==0.15.12` (or whatever the pin currently reads) before any pre-commit lint check. CI is the source of truth; local must match.

**10. `alembic current` `(mergepoint)` label is a chain-walk diagnostic, not a multi-head alarm.** Session-13's schema-commit pre-push cycle: Casey's local SQLite dev DB reported `1a2b3c4d5e6f (mergepoint)` on `alembic current`, which Cowork primary initially read as a multi-head conflict and held back the push. Chain-walking `down_revision` values via `Grep ^down_revision alembic/versions/` revealed `1a2b3c4d5e6f` was a long-resolved merge living 6 revisions earlier in the linear chain — Casey's local DB was just stale; production was unaffected. Cure: when `alembic current` shows an unexpected revision with a `(mergepoint)` label, walk the chain forward via Grep before raising an alarm. False-alarm rate on this signal is high.

**11. "Done" with no chat output may still have written the file.** Session-13's first attempt at the rate-limiter §8 decisions memo went to Claude Code; Casey reported "CC errored or refused" because no chat-side output appeared. Sub-agent recovery later revealed CC had actually written the memo to `docs/maintainability/phase2_5_rate_limiter_decisions_memo.md` — the sub-agent saw a Claude Code header in the existing file and overwrote it. The fix path was correct (sub-agent recovery worked), but the detection was wrong: Casey assumed refusal from missing chat-side output rather than checking the filesystem. Cure: when any channel reports "done" but the chat-side output is missing or partial, check the target file path directly before concluding the channel failed. Applies to CC, Cursor, and sub-agents equally.

**12. Session sandbox `outputs/` doesn't persist; save artifacts under the workspace path.** Session-12's prior-session artifacts (`cc_prompt_rate_limiter_decisions_memo.md`, `chatgpt_prompt_provider_profile_ux.md`, etc.) lived in the session sandbox path (`local-agent-mode-sessions/.../outputs/`) and were gone by session-13. The handoff implicitly expected those files to be available; reauthoring was required. Cure: anything the next session needs to reference must be saved under the workspace `outputs/` path (`C:\Users\casey\projects\havasu-chat\outputs\`), not the session-scratch path. The workspace `outputs/` folder is not gitignored, so artifacts get committed alongside their associated ship-logs as durable records of the dispatch.

**13. PowerShell command chaining: `;` is universal; `&&` only works in pwsh 7+.** Session-16 surface: Cowork primary suggested `git add ...; git status; git commit ...; git push` blocks using `&&` between commands; Casey's PowerShell rejected the `&&` operator. Windows PowerShell 5.1 (the default on most Windows installs) doesn't support `&&` / `||` as command-chaining operators — those landed in pwsh 7+. The semicolon (`;`) is universally safe across both shells. Tradeoff: `;` doesn't short-circuit on failure (each command runs regardless of the previous one's exit code), so for any chain where you genuinely need stop-on-failure semantics, run the commands as separate lines instead. Cure: use `;` (or newline-separated commands) in any PowerShell chain unless you've confirmed Casey is on pwsh 7+. Same lesson scope as gotchas #3 + #8 — PowerShell's syntax surprises catch agents that default to bash-shaped command chains.

**14. The reflog (`.git/logs/HEAD`) is NOT the commit ancestry — walk parent links instead.** Session-17 boot surface: Cowork primary needed to verify origin/main top-5 matched the boot prompt's expected SHAs (`dcf2f7a → 4bb74bc → 03f7160 → ...`). Bash mount git was broken per Rule 7, so the primary fell back to grepping `.git/logs/HEAD`. None of the expected SHAs matched, AND the reflog tail showed subjects from a completely different work stream (`tier2 category filter`, `feat(home+chat): editorial home + voice/component chat refactor`, `Add stale aquatic event pruner`, `merge redesign/phase-1-hero-and-palette`) — all from abandoned branches the repo's HEAD had visited historically. The primary false-alarmed Casey, claiming "none of the boot prompt SHAs exist in this repo." Wrong: the reflog is the chronological record of every commit HEAD has ever pointed at — including checkouts to other branches, dead experiments, and reset/rebase-orphaned commits — NOT the parent ancestry of the current HEAD. The decisive verification of "is commit X in the current ancestry of main?" is to walk parent links from HEAD via the commit objects. From the bash sandbox: `python3` + `zlib.decompress` on `.git/objects/<sha[:2]>/<sha[2:]>` reads a commit body and exposes its `parent` line; chain that walk and you have the ancestry. Cure: when verifying ancestry from the sandbox, walk parent links from HEAD via commit-object reads, don't grep the reflog. Reflog forensics are valid for "what has HEAD pointed at over time" (investigating force-pushes, accidental resets, branch experiments) — wrong tool for "is SHA X currently reachable from HEAD." Sample walker:

```python
import zlib, os
def read_obj(sha):
    p = f'.git/objects/{sha[:2]}/{sha[2:]}'
    with open(p, 'rb') as f: raw = zlib.decompress(f.read())
    return raw[raw.index(b'\0')+1:].decode('utf-8', errors='replace')
sha = open('.git/refs/heads/main').read().strip()
for _ in range(20):
    body = read_obj(sha)
    parents = [ln.split()[1] for ln in body.splitlines() if ln.startswith('parent ')]
    subject = body[body.index('\n\n')+2:].splitlines()[0]
    print(f'{sha[:10]} :: {subject}')
    if not parents: break
    sha = parents[0]
```

Same lesson scope as Rule 7 — Windows-side reads are authoritative when the Linux mount is unreliable, AND the right verification mechanism matters as much as the right side of the mount.

**15. Bash mount `git` operations leave a `.git/index.lock` that Linux can't unlink — Windows `git` then refuses to commit.** Session-18 close-out surface: Cowork primary ran `git status -s` + `git diff --stat HEAD` from the bash sandbox during spot-check of an incoming Cursor return — output was substantively correct (right modified-files list, right untracked-files list) but accompanied by two warning lines: `error: cache entry has null sha1` and `warning: unable to unlink '/sessions/.../havasu-chat/.git/index.lock': Operation not permitted`. Primary surfaced the warnings to Casey but didn't act on them. Casey then ran `git commit` from PowerShell on the same working tree and hit a hard fatal: `fatal: Unable to create 'C:/Users/casey/projects/havasu-chat/.git/index.lock': File exists. Another git process seems to be running in this repository, e.g. an editor opened by 'git commit'. Please make sure all processes are terminated then try again. If it still fails, a git process may have crashed in this repository earlier: remove the file manually to continue.` Root cause: the bash sandbox is a different filesystem layer with restricted permissions on the workspace mount; Linux `git` correctly acquires the index lock at start of operation but the sandbox permission model blocks the close-time unlink (`Operation not permitted`), leaving a stale `.git/index.lock` that Windows `git` then correctly refuses to step on. Both halves are doing the right thing — the failure is the seam between them. Cure: `Remove-Item .git\index.lock` from PowerShell, sanity-check with `git status`, retry the commit (clean — Windows status will also reveal that the bash-side "extra modified files" Cowork primary saw, like phantom changes in `tests/voice_battery/reports/static_review.md` or a botched-quote-named file, are Linux-mount-only artifacts; Windows is the source of truth). Scope: **bash mount `git` is not safe even for read-only operations in mixed-OS sessions.** This extends gotcha #4 (bash mount staleness) — #4 covered the unreliability of bash-side `git log` / `status` reads; #15 covers the side-effect harm those reads can do to Windows-side operations. From session-18 forward in any mixed-OS session, do NOT run `git ...` from the bash sandbox against the working tree; use the Read + Grep + Glob tools (Windows-authoritative, don't touch git internals) for everything: directory listings, file diffs, status surveys. The only safe bash-side git-adjacent operations against the working tree are pure object reads via `python3` + `zlib.decompress` on `.git/objects/...` (per gotcha #14's parent-walk pattern) — those bypass the index lock entirely. Session-19 extended the scope: even read-only plumbing commands like `git ls-tree` for commit-content inspection should be replaced with the parent-walk pattern. One slip in session-19 left a `.git/index.lock` on the Linux mount that did NOT propagate to Casey's Windows filesystem (Remove-Item returned path-not-found) — harm was theoretical only that time, but the rule from session-20 forward is zero `git ...` from the bash sandbox against the working tree.

**16. Embedded `"..."` inside `-m '...'` bodies on PowerShell break the commit.** Session-19 docs-commit surface: Cowork primary proposed a commit subject that read approximately `docs: Phase 3 brief + 3.1 dispatch prompt artifacts (pre-positioned for "SHIPPED 2026-05-12" Phase 2 close-out)` and wrapped the full message in single quotes — `git commit -m 'docs: Phase 3 brief + 3.1 dispatch prompt artifacts (pre-positioned for "SHIPPED 2026-05-12" Phase 2 close-out)' -- docs/maintainability/master_build_plan.md docs/STATE.md` — expecting PowerShell's single-quote rule to make the entire body a single literal argument the way bash + zsh would. PowerShell didn't. The command failed with `error: pathspec 'Phase' did not match any file(s) known to git` (subsequent words from the body interpreted as pathspecs). Root cause: PowerShell's native-command argument re-tokenizer (the layer that translates PowerShell arguments into Win32 `argv[]` for non-PowerShell executables like `git.exe`) treats embedded `"..."` as quote-state changes EVEN inside an outer single-quoted body. The single quotes correctly suppress `$variable` interpolation and most metacharacter expansion, but the embedded double-quote pair causes the tokenizer to split the argument: what was supposed to be one `-m` argument becomes `docs: Phase 3 brief + 3.1 dispatch prompt artifacts (pre-positioned for `, then `SHIPPED 2026-05-12`, then ` Phase 2 close-out)` — three positional args. Git takes the first as the `-m` body and treats the rest as pathspecs, which then don't match. Consequence in session-19: the docs files stayed in the staging index from the prior `git add`, the next `git add` for outputs picked them up, and the chore commit at `3d89e58` bundled all four files. Subject mislabel; Rule 12 (no amend after push) meant we live with it. Cure: **avoid embedded `"..."` entirely in `-m '...'` bodies on PowerShell.** Use plain text (no quotes needed for emphasis — most subjects work fine with bare phrases). If emphasis is genuinely needed, use Unicode curly quotes (`"..."` U+201C / U+201D) which the re-tokenizer treats as regular characters. Backtick-escaping (`` `" ``) also works but is harder to read. Heredoc-style multi-line commits via stdin (`git commit -F -` with piped input) sidestep the issue entirely for long messages. Same lesson scope as gotchas #3 + #8 + #13 — PowerShell quoting/parsing surprises catch any agent that defaults to bash-shaped command shapes. Bash + zsh do NOT have this problem because they respect single-quote literalness for the entire argument body; this gotcha is PowerShell-specific.

**17. Module-import-time hook registration creates cycles when alternate entry points hit the module before the canonical entry-point.** Session-22 (2026-05-13) surface: parks-rec-scrapes workflow had been failing every 6h since session-20's Phase 3.1 ship with `ImportError: cannot import name 'ContactPoint' from partially initialized module 'app.db.models' (most likely due to a circular import)`. Session-21 handoff §2 diagnosed the failure as ORM-vs-prod-DB schema mismatch (origin's new Phase 3.1 columns missing in production Postgres at `b2c3d4e5f6a7`). That diagnosis was wrong; it never explained the ImportError text, only the column-does-not-exist runtime error that would have come AFTER the import succeeded. Session-22 deployed the production migrations (correct + necessary anyway), then re-enabled the cron — and the workflow failed in the same way, exposing the real bug. Root cause: `app/db/database.py` ran `_register_orm_listeners()` at module-top (Phase 1D ship `3f3628e` lineage). That function used `importlib.import_module('app.db.models')` to force-load models, then `from app.db.entity_dual_write import register_catalog_dual_write_hooks` — which imports `ContactPoint` and other ORM classes from `app.db.models` at its module top. Worked when uvicorn was the entry point (uvicorn imports `app.main` → loads `app.db.models` first → all classes defined → later imports of `app.db.database` find a fully-loaded models cache). Broke when `scripts/parks_rec_load.py` was the entry point: `parks_rec_loader → approval_service → contribution_store → models` started, `models.py` line 28 did `from app.db.database import Base`, database.py module-top ran `_register_orm_listeners()` while models was still mid-load (only at line 28 of 1475), which tried to `from app.db.entity_dual_write import ...` → entity_dual_write tried `from app.db.models import ContactPoint` → ContactPoint wasn't defined yet (line 824) → ImportError. **The bug had been latent since Phase 1D shipped at `3f3628e`** (session-16, ~5 months earlier) — never surfaced because no entry point exercised the broken path until the parks-rec scraper started running on its 6h cadence after the cron landed. **Cure:** move cross-module registration to the package `__init__.py`. `app/db/__init__.py` runs once when any `app.db.*` submodule is first imported, BEFORE any submodule attribute lookup. From there, `from app.db.entity_dual_write import register_catalog_dual_write_hooks` drives the full load order (`__init__.py → entity_dual_write → models → database → all back up the chain → __init__.py calls hook`), and no matter which submodule the entry point reaches first, `__init__.py` serializes the load. Both leaf modules (database.py + entity_dual_write.py) are then free of cycle-creating imports. First-attempt fix moved registration to end-of-file in `models.py` (mirror of the `_register_provider_slug_listeners()` end-of-file pattern); that shifted the cycle to `entity_dual_write → models → entity_dual_write` whenever a test imported entity_dual_write directly. Only the third-party module (`__init__.py`) location is structurally cycle-free for ALL entry points. **Companion lesson — confirm via log/repro before locking a diagnosis.** Session-21's "ORM-vs-prod-DB schema mismatch" diagnosis was made from the workflow status (exit code 1 every 6h) + the known origin-vs-prod schema gap, without ever inspecting the actual error log. Both pieces of context were true; neither was the actual failure cause. Cure: when diagnosing a workflow failure, *always* read the actual stack trace from the GitHub Actions log before locking a root-cause story — even when the "obvious" diagnosis matches the timeline and the known state-of-the-world. The first-failure surface in the trace tells you what's actually broken. Session-22 regression test `tests/test_phase1d_dual_write.py::test_scraper_entry_point_import_chain_does_not_cycle` runs the failing import path in a subprocess (to bypass sys.modules cache masking from earlier tests in the suite) and asserts both that the chain succeeds and that `register_catalog_dual_write_hooks()` actually fired. Fix landed at `5faa37c`.

**18. Alembic revision DAG is global; parallel sessions must coordinate migrations explicitly.** Discovered 2026-05-20 during Phase 6.4 + Phase 7 parallel Cursor dispatch. Symptom: two parallel Cursor sessions both authored alembic migration files chaining off the same head SHA. Their independently-chosen revision IDs (`b8c9d0e1f2a3` from Phase 6.4 for `users.boat_mode_preference`; `c9d0e1f2a3b4` from Phase 7 for `User.last_active_at`) BOTH wanted to be the next revision after `f6a7b8c9d0e1`. Result: a multi-head DAG state when both sessions tried to commit, with neither head able to ship cleanly. The session that ran its full pytest verification first (Phase 6.4) detected the conflict and reverted the OTHER session's migration + ORM-drift to land cleanly. Root cause: file-scope disjointness (the parallel-dispatch pattern — route disjoint file lists per lane; cited throughout Phase 6/7 wrappers) covers Python modules + templates + static assets + test files — every file each session wrote to was disjoint. But alembic's revision space is GLOBAL across the repo: any new migration writes to `alembic/versions/<revision_id>_<slug>.py` AND modifies the parent revision's `down_revision` reference. Two parallel sessions chaining off the same head create two would-be heads, which is a DAG inconsistency that alembic detects only at upgrade-cycle test time or at deploy time. Why file-scope analysis missed it: the file-scope analysis correctly flagged `alembic/versions/` as a directory NEITHER wrapper explicitly added to its expected file list. But BOTH wrappers conditionally said "ship a migration if needed" — and both decided "yes, ship one". The conditional migration shapes weren't explicit in the file-scope disjointness check. Cure pattern (pick one): **(a) Serial dispatch when both contemplate migrations.** If both parallel lanes' wrappers say "ship a migration if needed", default to sequential dispatch instead of parallel. One ships first (`alembic head` advances); the second rebases its migration against the new head before dispatch. **(b) Schema-reuse collapse.** Review whether one lane's migration is actually NECESSARY vs. reusing an existing column. Phase 6.4 ultimately took this path — reused Phase 3.1's `users.preferred_mode` instead of adding `users.boat_mode_preference`. If reuse is possible, collapse the migration in the wrapper before dispatch. The wrapper says: "verify column doesn't exist before authoring migration" — that's the right reuse-check gate. **(c) Pre-coordinate revision IDs in the wrapper.** If both lanes MUST ship migrations + serialization isn't feasible, assign revision IDs explicitly in each wrapper (rather than letting Cursor pick). Wrap the migration shape with explicit `down_revision = "<head>"` + a manually-chosen `revision = "<unique>"`. Future ships rebase manually if heads shift. Cure pattern preference: **(a) serial > (b) reuse > (c) pre-coordinate.** Sequential dispatch is the lowest-cognitive-load fix; schema reuse is the second-best when one of the migrations turns out to be unnecessary; pre-coordination is brittle and should only be used when both migrations are required + serialization is impossible. Companion lesson — multi-head detection at acceptance-gate time: Cursor 6.4's session caught the collision by running `python -m pytest -q` against the full suite, which exercised both heads. Lesson: dispatch wrappers should explicitly instruct Cursor to run `python -m alembic heads` (note: PLURAL) and verify a SINGLE head is returned before declaring `§12` complete. If multiple heads are returned, HALT and report. Future dispatch wrapper amendment (recommended): add to the §"What NOT to do" section of any wrapper that contemplates an alembic migration: don't author a new alembic migration without first checking the parallel-lane wrapper(s) for migration intent. If the parallel lane also contemplates a migration off the same head, HALT and ask the operator to serialize the dispatches OR coordinate revision IDs explicitly. And add to the §"Pre-dispatch checklist": `python -m alembic heads` returns a SINGLE head (the one expected by this dispatch's chain). If multiple heads exist, fix the multi-head state before paste. Real-world reference: Phase 6.4 + Phase 7 collision 2026-05-20. Recovery handled in `outputs/phase_7_recovery_dispatch_note.md`. Phase 6.4 SHIPPED at `96c915d`; Phase 7 needs re-dispatch with unique revision ID if a migration is still needed (the recovery note proposes operator re-decide whether the migration is actually necessary).

**19. Live prereq verification BEFORE wrapper bake — never trust "operator browser-confirm" deferrals.** Discovered 2026-05-19 during Phase 8a prereq verification pass. Symptom: 5 substantive scope-changing findings surfaced from a 30-min live-web-fetch pass against 3 prereq URLs that had been "pre-positioned" in the wrapper at `outputs/cursor_dispatch_prompt_phase_8.md` + checklist at `outputs/phase_8_operator_prereq_checklist.md` 6 days earlier. Findings: USGS site `09427520` historic-only since 2006 (dead surface; wrapper hardcoded against it); USGS site `09427500` reports only `00054`+`00065` not the assumed `00065`+`00010`+`00060` (wrapper assumed 3 parameters; reality is 2-of-3 with one being storage not discharge); Nixle agency `3726` RSS silent since 2021-09-01 (~4 years 8 months of zero posts; wrapper's keyword-matcher would get zero signal); NWS marine forecasts cover Coastal + Great Lakes ONLY (verified at weather.gov/marine/usamz — inland Lake Havasu not in scope; wrapper's `nws_marine_alerts` cache surface would return empty indefinitely); AirNow has NO monitor within 60mi of LHC (nearest is Blythe CA at ~60mi south; O3-only single-station; wrapper assumed multi-parameter local AQI). Root cause: the prereq checklist explicitly said "Operator should browser-confirm" but the confirm step was deferred + the wrapper was authored against the unconfirmed assumptions. Cure: **when a prereq checklist says "operator browser-confirm," run the confirm BEFORE the wrapper is authored — not after.** Pre-positioning a "research" sub-agent (e.g. via Plan agent + general-purpose web-search agent) before the dispatch wrapper bakes the assumptions is cheaper than amending the wrapper post-research. Specific checks for any wrapper that ingests external data: (a) hit the actual data URL with `mcp__workspace__web_fetch` (or equivalent) and inspect the response shape; (b) verify "active" / "live" claims against `lastBuildDate` / freshness metadata in the response; (c) confirm parameter availability against the real schema, not the assumed schema; (d) check coverage radius / zone scope against the geo of the deployment. Real-world reference: 5 P0 findings + 3 wrapper-amendment waves (§6 + §11 + §12) all of which would have been pre-caught with a 30-min live-fetch pass during checklist authoring. See `outputs/phase_8a_prereq_verification_report.md` for the full findings + amendment chain.

**20. N-wave amendment pattern for layered verification findings — preserve discovery chronology in audit trail.** Discovered 2026-05-19 during Phase 8a prereq verification. Symptom: the initial verification pass surfaced 3 findings (USGS sites + Nixle); after operator picked Option A on each + wrapper amendments landed (§6 wave), a follow-up check on the amended wrapper's "NWS marine" framing surfaced a 4th finding (marine zones don't cover inland LHC) — necessitating a §11 NWS-product-type-correction wave; then operator's AirNow key activation surfaced the 5th finding (Blythe ~60mi south; O3 only) — necessitating a §12 AirNow Blythe wave. Each wave was a substantive correction to a recently-landed amendment, but rolling them into one massive commit OR retroactively rewriting §6 to absorb §11+§12 would have obscured the audit trail. Root cause: layered verification work naturally surfaces nested findings — the second finding depends on the first amendment being in place; the third finding depends on the second wave's surface. Treating them as fix-ups to a single section loses the discovery chronology and confuses future agents reading the artifact "why are there 3 different keyword lists in the same file?". Cure: **ship each correction as a new wave (§6 → §11 → §12 → etc.) with its own numbered section, not as a silent fix-up to the prior section.** Preserve discovery chronology as part of the audit trail. The verification report `outputs/phase_8a_prereq_verification_report.md` is the canonical example — §1 (executive summary) + §2-§7 (original findings) + §11 (NWS-product correction) + §12 (AirNow Blythe finding) each stand as durable evidence of how the scope evolved. Same principle applies to design docs, prereq checklists, and any artifact that gets amended after initial authoring. Real-world reference: Phase 8a's 3-wave amendment chain landed cleanly at `dfdb5aa` (§6 + §11) + `1e1288b` (§12) + `5350977` (master plan §4 Phase 8 entry reflecting all 3 waves).

**21. Schema sanity check on operator-action SQL — cross-check `app/db/models.py` BEFORE writing the package.** Discovered 2026-05-19 during Lane L operator action items chip-away. Symptom: the Lane L package at `outputs/lane_l_operator_action_items_chip_away_package.md` + the original walkthrough at `outputs/operator_action_items_walkthrough.md` both used `UPDATE entities SET draft = ...` patterns. Operator executed against local dev SQLite DB + reported: `draft` lives on `providers`, NOT `entities` (verified `Provider.draft` at `app/db/models.py:50`; `Entity` has no `draft` column at line 627 onwards). Plus `entities.crowd_notes` is JSON (`Mapped[dict | list | None]` at line 673), not string — sub-agent-research's string-concatenation `crowd_notes` patterns were invalid. Plus `name` lives on BOTH `entities` and `providers` — single-table renames leave the pair desynced. Net: 3 schema corrections needed after first execution; operator mentally-translated during execution to land the work. Root cause: the package SQL was authored by a sub-agent doing entity research without a schema-existence cross-check; the walkthrough was authored by Cowork primary against historical pre-entity-pivot wording. Both artifacts had high-confidence SQL that was wrong on critical column existence. Cure: **for any operator-action package containing SQL, cross-check ALL column references against `app/db/models.py` BEFORE writing the package.** Quick checklist: (a) does the column exist on the expected table? (`Grep` for the class definition + field name); (b) what's the column type? (matters for JSON vs string vs Boolean); (c) does the same field exist on parent + child tables? (rename pair-pattern catches name mirrors). Best-effort one-grep gates the cross-check: `Grep("column_name\\s*[:=]", "app/db/models.py")` reveals all tables that define the column. Add this step explicitly to the chip-away-package authoring workflow. Real-world reference: Lane L package + walkthrough both corrected at `dfdb5aa` (schema-fix wave); see `outputs/lane_l_operator_action_items_chip_away_package.md §0` for the schema-correction header that documents all 3 findings.

**22. Railway-wide outage triage — check status.railway.com FIRST when /health is 404.** Discovered 2026-05-19 during Lane H attempted execution. Symptom: operator ran the Lane H pre-flight + got `/health` returning HTTP 404 "Application not found" at Railway edge. Initial diagnostic assumption (per the Lane H action package §5 original failure-handling table): service hibernated / custom domain detached / deploy rolled back / project torn down. Reality: Railway-wide outage. Per `status.railway.com`: "Major Outage" → "Google Cloud has blocked our account, making some Railway services unavailable. We have escalated this directly with Google" (23:37 UTC) → "We do not have an ETA at this time" (00:37 UTC). Railway dashboard ALSO inaccessible (control plane affected too). Root cause: the Lane H package's failure-handling table enumerated Railway-side debugging steps without an upstream-outage check at the top. Cure: **when `/health` returns 404 + Railway dashboard inaccessible, check `https://status.railway.com/` FIRST before assuming any Railway-side or service-side issue.** Both can be affected by upstream cloud-provider problems that the operator dashboard cannot reveal. The Lane H package §6 failure-handling table has been amended to put the Railway-wide-outage row at the top of the diagnosis tree. Companion observation: Railway outages from cloud-provider account blocks are rare but high-impact + no operator action can resolve them; the right posture is "wait for upstream recovery + chip Cowork-side work in the meantime." Lane sequence (Lane H + Lane I) cleanly defers until Railway is green. Real-world reference: 2026-05-19 outage spanning multiple hours; project shipped 8 docs commits Cowork-side during the wait window. Lane H action package amended at `7143976` with the Railway-outage row.

**23. Honest spatial attribution UX — treat source-station distance as first-class alongside temporal staleness.** Discovered 2026-05-19 during Phase 8a §12 AirNow Blythe finding. Symptom: the wrapper's "Today in Havasu" conditions strip design (per Opus design memo §6.3) committed to "honest staleness" indicators ("Updated 12 min ago" / "Updated yesterday by operator") — a clean pattern for temporal-data honesty. The §12 verification surfaced that AirNow's nearest LHC monitor is Blythe CA at ~60mi south — single-parameter (O3) — meaning the conditions strip would show "AQI 47 (Good)" but the data is from a station 60 miles from Lake Havasu. The temporal-staleness pattern alone doesn't capture this; user assumes the AQI is LHC-local when it's actually regional. Cure: **when ingesting external data, treat both temporal staleness AND spatial source-station distance as first-class UX concerns from the start.** Add an `attribution_chip` field to view-models alongside the `staleness_label` field. Pattern: "AQI 47 (O3) — from Blythe, CA ~60mi south" subtitle alongside the value. Cache + JSON response include source-station attribution fields (`aqi_source_station_name`, `aqi_source_state_code`, `aqi_source_distance_mi`). Conditions strip renders both. Same principle generalizes to any external data source: if the data point comes from a station/sensor/feed that is N miles from the geo of interest, that distance is part of the honest-data UX. Real-world reference: Phase 8a `outputs/phase_8_architecture_design.md §4.3` (ConditionsTile dataclass with `attribution_chip` field) + `outputs/cursor_dispatch_prompt_phase_8.md` (API response shape with `aqi_source_*` fields) + `outputs/phase_8a_prereq_verification_report.md §12.3` (the wrapper amendment that landed the pattern). Same UX discipline applies to V1.5 surfaces: PurpleAir sensor distance + AZDEQ station distance + future AirNow station distance changes all surface via the same chip pattern.

**24. Railway dashboard "Edit row" panel serializes cleared timestamp fields as `""` (empty string), which Postgres rejects.** Discovered 2026-05-21 during Phase 8a.3 prep — the v3 §2 circuit-breaker reset for `usgs_09427500`. Symptom: Cowork drove the Railway dashboard via Chrome MCP (railway.com → Havasu chat → Postgres → Database → Data → `external_conditions_cache` → click row → Edit row panel) to clear three columns matching the v3 §2 SQL — `last_error = NULL`, `error_count = 0`, `next_attempt_after = NULL`. Triple-click + Delete cleared all three field inputs. The panel's purple "Save" button (which appears only after edits are made) returned a Postgres error banner immediately: `ERROR: invalid input syntax for type timestamp: "" ^` with the caret on `next_attempt_after`. Root cause: the Railway row editor serializes a cleared input as the empty string `""` and POSTs that to Postgres unchanged. Postgres correctly refuses to coerce `""` to `timestamp without time zone`. For nullable `varchar` columns (`last_error`), the empty-string write succeeds and stores `''` (NOT NULL but empty) — semantically different from NULL but functionally equivalent for most code paths that check truthiness. For nullable `timestamp` columns (`next_attempt_after`), the write hard-fails and the whole multi-field Save is rolled back atomically. Cure: **set the timestamp field to a past-date sentinel rather than trying to clear it.** `1970-01-01 00:00:00` is the conventional epoch sentinel; any date before `now()` works. Functionally equivalent to NULL when the consuming code uses a `now < next_attempt_after` comparison (the Phase 8a circuit-breaker shape per `app/conditions/cache.py:79-82`: `if row.next_attempt_after is None: return False; return now < row.next_attempt_after` — past dates make the second clause return False, same observable behavior as NULL). The fetcher's success path at `cache.py:118` resets `next_attempt_after = None` on next successful fetch, so the past-date sentinel is self-cleaning within one successful cycle. Companion observation — the Save button hides until an edit is made: Cowork's pre-Save `find()` query for "Save or Update or Submit button" returned `Found 0 matching elements`; the button only appears in the DOM after at least one field input fires a change event. Don't conclude "the panel auto-saves on close" or "the panel uses Cancel + X = save"; verify by making one trivial edit (e.g., re-type the same value) to surface the Save button before planning a multi-edit batch. Companion observation — auto-save model: Railway's panel does NOT auto-save on field blur. Pressing Tab between fields commits the edit to the panel's LOCAL state but NOT to the server. The Save click is the only server commit. The X button closes the panel WITHOUT saving unsaved edits. Companion observation — Escape closes the entire Postgres service panel (not just dropdowns): when typing SQL into Railway's Data-view query bar, autocomplete suggestions appear in a dropdown; pressing Escape to dismiss the dropdown ALSO closes the parent Postgres modal and navigates back to the project graph. Cure: click outside the dropdown OR press Enter to submit the typed SQL; never Escape. Cure scope: applies to any Cowork-driven Railway dashboard row-edit operation on any nullable timestamp column, OR any SQL-bar interaction in Railway's Data view. Same lesson scope as gotcha #19 (live prereq verification) for dashboard-UI assumptions — verify the Save button surface and the empty-vs-NULL serialization before authoring multi-step click sequences. Real-world reference: Phase 8a.3 prep 2026-05-21. After the first Save failed, the second attempt set `next_attempt_after = 1970-01-01 00:00:00` (last_error left cleared to empty string, error_count = 0) and Saved cleanly. The 23:00:46 cron tick post-reset fetched USGS successfully against the new `latest-continuous` URL; `/api/conditions` flipped `lake_is_stale=false` with `lake_gauge_ft=49.18` / `lake_storage_acft=592,300` at 23:00:51Z — proving the Phase 8a.2 URL fix end-to-end. Phase 8a.3 (`6c1099e`) subsequently added `--force` to `scripts/fetch_external_conditions.py` as a code-side cure (bypass the breaker without any dashboard intervention) — see `docs/STATE.md` "Recently shipped" Phase 8a.3 entry for the full arc.

**25. Bash mount can invent filesystem state — phantom modifications, control-character file names, fake truncations, fake deletions.** Discovered 2026-05-22 across two dramatic firings in a single Cowork session. Symptom: `git status` and `git diff` from the bash sandbox reported ~40 phantom file modifications across files the session had never touched; weird `UU` merge-conflict markers on files named with single control characters (`\002`, `\004`, `\b`, `l\020`, `r`) that don't exist on the Windows filesystem; a fake 85-line truncation of `docs/STATE.md` (Read tool from Windows side showed the file at its real length); fake `D` deletions of all `tests/voice_battery/reports/*.jsonl` files (Windows-side `ls` confirmed they were intact). Both Windows-side `.cmd`-output-writing-to-result-file AND the Read tool (Windows-direct) consistently confirmed the real state was clean. Root cause: extends #4 (stale Edit-modified file reads) and #15 (bash git ops leaving `.git/index.lock` that Windows can't unlink) with a third failure mode — the bash mount's filesystem-state-snapshot layer can INVENT files, modifications, and deletions that don't exist on the underlying Windows filesystem, then serve those inventions to `ls` / `git status` / `git diff` as if they were real. The fabricated inventory is internally consistent within bash's view (the phantom files show up in `ls` AND in `git status` AND have stat() data), so cross-checking within bash will NOT catch the lie. Different from #4 (which is per-file Edit-vs-mount staleness) and #15 (which is bash-side side effects on Windows git operations); #25 is bash actively manufacturing fake filesystem state. Cure: for ANY filesystem or git-state question this session, trust the Read tool (Windows-direct, bypasses the mount entirely) + Windows-side `.cmd`-writing-to-result-file pattern ONLY. Bash mount remains FINE for `curl` + GitHub API + HTTP smokes + Python subprocess work that doesn't touch the workspace mount. **If a bash filesystem read surprises you, run a Windows-side `.cmd` writing to `outputs/_<topic>_*_result.txt` and Read that file** — never accept the bash answer as authoritative when it contradicts Edit/Write/Read tool's view of the same file. Companion lesson — structural mitigations beat memorization: the verify `.cmd` pattern (every operational `.cmd` redirects stdout+stderr to a `result.txt` file) survived both gotcha #25 firings this session without losing data integrity, because the durable evidence channel was Windows-side files, not bash console scrollback. The 5-second extra to add the redirect block to every operational `.cmd` pays for itself the first time the mount lies. Real-world reference: 2026-05-22 session shipping STATE.md commit-chain refresh + Lane A cosmetic cleanup + Lane B doc framing refresh (`786936a` + `8c19490` + `d4777c0` + `1bb8019`); the gotcha fired twice and was diagnosed both times by cross-checking against `.cmd → result.txt → Read` evidence. **v17-v20 cumulative firing arc — N+1 through N+4 (four additional firings since codification):** v17 N+1 (2026-05-23 autonomous overnight) bash `git status` reported ~20 phantom-modified files; Windows-direct .cmd verify showed only the intentionally-edited `dispatch_channels.md`. v18 N+2 (2026-05-23 close-out) extension of the phantom-modified pattern across more files. v19 N+2-sibling (2026-05-23) bash served a TRUNCATED view of `dispatch_channels.md` itself — Read-via-bash returned line ranges missing terminal gotcha content that Windows-direct Read showed intact. v20 N+3 (2026-05-24 boot) bash `git status --short` returned ~25 phantom-modified files + ~25 phantom-deleted `tests/voice_battery/reports/*.jsonl` deletions at fresh-session boot; HEAD SHA itself remained trustworthy. **v20 follow-up boot N+4 (2026-05-24 ~16:30Z) deepest diagnostic to date: bash mount served TRUNCATED FILE CONTENT** for three real source files — `app/api/routes/category_pages.py` ended at 1053 lines mid-expression (`entities = _apply_classes_sports_filters(` open-paren never closed) vs the intact 1150-line file on Windows; same truncation shape for `app/chat/hint_extractor.py` and `app/conditions/api_payload.py`. Decisive checksum proof: bash `sha256sum app/api/routes/category_pages.py` returned `83d9b9...` while `git cat-file blob HEAD:app/api/routes/category_pages.py | sha256sum` returned `c97798...` — the files are clean on the Windows host; the bash mount is serving stale/truncated bytes. `git diff` from bash CORRECTLY reported the diff between the truncated mount-view and the HEAD blob — git tooling is honest about what bash sees; the lie is upstream in the mount layer itself. Bash `wc -l docs/maintainability/dispatch_channels.md` also returned 217 lines while the real file (Windows-direct Read) extended past line 254 — the truncation-of-content sub-pattern spans many files at once, not just the random three first spot-checked. **Unified diagnosis (v20 N+4):** the sub-patterns observed across firings (phantom modifications, control-character phantom file names, fake D-deletions, phantom truncations of one file's content via Read, phantom `git status` lists, truncated FILE CONTENT served via `cat`/`wc`/`sha256sum`/`git diff`) are all surface symptoms of a single root cause — the bash mount's filesystem-state-snapshot layer can serve stale or fabricated content for arbitrary paths, and every tool running in bash (`git` / `ls` / `wc` / `sha256sum` / `cat` / Read-via-bash) sees and reports whatever the mount serves. The mount lies; everything downstream is honest about the lie. **Cure unchanged from codification, but now broader:** trust Windows-direct Read + Edit/Write + checksum-vs-`git cat-file blob HEAD` for ANY working-tree question — including the contents of a file the session has not modified. Bash mount is unreliable for ANY filesystem-state read on the workspace mount, full stop. The bash sandbox remains fine for `curl` / GitHub API / HTTP smokes / Python subprocess work that doesn't touch the workspace mount. Cumulative-arc references: v17 ship arc per `outputs/session_handoff_2026-05-23_v17.md`; v19 + v20 firings per `outputs/session_handoff_2026-05-23_v19.md` + `outputs/session_handoff_2026-05-24_v20.md` §2 Discovery 3 + §6 boot-firing note; v20 N+4 firing captured in real time during the docs-commit ship session that authored this extension.

**26. `cmd.exe` `git add path1 path2 path3` aborts on the first non-existent path and leaves the rest unstaged silently.** Discovered 2026-05-22 during Lane A cosmetic cleanup (`8c19490` first ship). Symptom: the Lane A `.cmd` ran `git mv tests/test_parks_rec_lhcaz_aquatic_disabled.py tests/test_parks_rec_lhcaz_aquatic_wired.py` (rename succeeded) then immediately ran `git add tests/test_parks_rec_lhcaz_aquatic_disabled.py app/contrib/lhcaz_aquatic_pdf.py tests/test_parks_rec_lhcaz_aquatic_wired.py .gitignore` expecting to stage 4 paths. The first path no longer existed (just renamed away by the preceding `git mv`), so `git add` errored on that path and ABORTED the entire `git add` invocation before staging paths 2-4. `git commit` then ran with only the rename in the index. Net: 1 of 4 intended changes shipped at `8c19490`; recovery via the `d4777c0` follow-up commit picked up the 3 missing files (`.gitignore` patterns + docstring NOTE block drop + vim modeline EOF removal). Root cause: `cmd.exe`'s `git add` treats the first non-existent path as a hard error and bails immediately; subsequent paths in the same command are never reached. Bash + zsh behave the same way on `git add` semantically, but the rename-then-add antipattern shows up specifically in `cmd.exe` ship `.cmds` where the `.cmd` author mentally modeled `git add` as additive ("stage all four files") rather than serial ("try path 1, then path 2, then path 3, then path 4"). Cure (pick one): **(a) Stage paths ONE AT A TIME** with explicit error checks between (`git add path1` then check exit code, then `git add path2`, etc.). Verbose but unambiguous about scope. **(b) `git add -A` with a pre-commit `git diff --cached --stat` confirmation gate.** Simplest if EVERY working-tree change is intended for the commit; the pre-commit stat surface catches scope drift before the commit lands. **(c) Rebuild the path list after the rename** — but this is brittle and requires the `.cmd` author to track which paths exist at which point in the script. Cure-pattern preference: **(a) one-at-a-time > (b) `git add -A` + stat gate > (c) rebuild**. The v8 boot prompt's NEW HARD RULE codifies (a) for any multi-file commit; today's 2026-05-22 STATE.md refresh + .gitignore root-level extension + this gotcha-docs ship all use the single-path `git add docs/STATE.md` / `git add .gitignore` / `git add docs/maintainability/dispatch_channels.md` pattern. Companion observation — the bug masquerades as a small successful commit. `8c19490` looked clean at the time (single commit, push success, CI green) because the missed files were docstring + config changes that don't affect runtime; ruff and pytest both passed against the partial state. The CI green is misleading; only post-commit verify against working-tree state catches the missed files. Real-world reference: `8c19490` + `d4777c0` recovery 2026-05-22; v8 §1 root-cause #2 + §4 lesson #3 codified the fix.

**27. Scratchpad `.cmd` accumulation causes stale-commit-message bugs when an old `.cmd` is double-clicked instead of the intended new one.** Discovered 2026-05-22 (`786936a` wrong-message ship). Symptom: `786936a` was supposed to commit STATE.md line-10 refresh content with a commit message describing that refresh. Actual commit message read "docs(state): line 10 refresh -- post-5350977 chain narrative + HEAD bump to 8f8227e" which matches `a62edff`'s message body, NOT the actual `786936a` content. Cause: operator double-clicked an `outputs/_state_md_commit.cmd` left over from a previous session, which had a hardcoded `-m "..."` body for that prior session's commit. The `.cmd` ran successfully (staged + committed + pushed the current working-tree content) but with the WRONG message body attached. The CI run on `786936a` passed because the actual content was fine; only the commit message was misleading. Living with the misleading message was operator-accepted at ship time per Rule 12 (no amend after push) — reverting + re-committing would have been higher-risk than accepting one ugly `git log` line. Root cause analysis: scratchpad `.cmd` files accumulate in `outputs/` AND at workspace root across sessions; they remain double-clickable + can drift out of sync with current intent. The `.cmd` content is opaque from File Explorer's view (just a filename); operator has no signal that the `.cmd`'s hardcoded `-m` body doesn't match current intent. Cure: structural — `d4777c0` added `outputs/_*.cmd` + `outputs/_*_result.txt` + `outputs/_*_result.json` + `outputs/_*_msg.txt` patterns to `.gitignore` (suppresses tracking of new outputs/-scoped scratchpad); `b484b63` extended with bare `_*.cmd` / `_*_result.txt` / `_*_result.json` / `_*_msg.txt` patterns to also cover workspace root (where older scratchpad accumulated before the outputs/-convention was established). After both ships, every new ephemeral `.cmd` written by Cowork is invisible to git regardless of where it lands. Stale `.cmd` files still physically exist on disk (operator cleanup at leisure) but they cannot be accidentally committed to the repo. Companion principle: **structural fixes (gitignore patterns, naming conventions, file-system-enforced separation) > human-discipline fixes (memorizing not to click old files, manual review of `.cmd` contents before clicking).** When the same gotcha repeats across multiple sessions, look for a structural mitigation. The `786936a` wrong-message bug was the third or fourth time a Cowork session had hit some flavor of "old artifact getting picked up by accident" in the recent ship arc; the `.gitignore` pattern is the structural fix that retires the bug class entirely. Future hardening: when a verify `.cmd` writes `outputs/_<topic>_verify_result.txt`, also write the intended commit subject into `outputs/_<topic>_commit_subject.txt` and have the commit `.cmd` read its `-m` body from that file via `git commit -F outputs/_<topic>_commit_subject.txt`; that eliminates the hardcoded-`-m`-in-`.cmd` antipattern entirely. Real-world reference: `786936a` (the canonical "wrong-message-in-action" SHA) + `d4777c0` (outputs/-scoped cure) + `b484b63` (workspace-root extension); v8 §1 root-cause #4 and §3 NEW carries documented the problem; this gotcha entry is the durable write-up.

**28. Cursor IDE buffer auto-flush on focus-loss / post-commit can corrupt just-committed files.** Discovered 2026-05-23 v10-to-v11 boot. Symptom: a Cowork-primary session resumed from v10's close-out (HEAD = `99e9b57`) and immediately found two files in the working tree with corrupted modifications: `scripts/ingest/lhc_civic_scrape.py` had its tail truncated mid-`print(` (closing `)`, `return 0`, and `if __name__ == "__main__":` guard all gone -- file parses as `SyntaxError: '(' was never closed`); `scripts/seed_cat13_civic.py` had a benign trailing-whitespace + missing-final-newline addition. File mtimes (18:04Z / 18:06Z) were both AFTER v10 was authored (~12:00Z) and BEFORE the v10 walkthrough commit landed at 18:51Z. Root cause: Cursor IDE was open in the background from the prior session's Phase 8b dispatch; when the operator switched away from Cursor (or it lost focus, or it was closed and quit), Cursor flushed a stale or partial buffer to disk -- overwriting the just-committed-clean file with a malformed in-memory state. Different from gotcha #15 (bash leaving `.git/index.lock`) and #24 (Railway dashboard saving `""` to Postgres) -- this is the IDE side-effect-polluting a file that was supposed to be at HEAD. Reproduced twice in the same boot session: the FIRST attempt at running a dryrun `.cmd` saw the syntax check pass (file was clean at that instant); the .cmd window closing triggered focus change -> Cursor flushed again -> re-corrupted the file before the operator could re-run. Cure: **kill Cursor before any file-touch `.cmd`.** Add as Step 0 of every operational `.cmd` that does git restore / git checkout / file overwrites: `tasklist /FI "IMAGENAME eq Cursor.exe" /NH >> "%OUT%" 2>&1` followed by `taskkill /IM Cursor.exe /F >> "%OUT%" 2>&1`. Exit code 128 (no process found) is fine -- means Cursor wasn't running, no action needed. The kill-first pattern fully retires this bug class for Cowork-primary work since the v10/v11 hard rule already forbids opening Cursor IDE; the kill-first step enforces that rule structurally rather than via memory. Companion observation -- **verify FILE CONTENT, not just exit-code, after .cmd runs that touch files.** A .cmd that succeeds (exit 0 everywhere) does not guarantee the on-disk state is what you wanted; Cursor flush-after-script can mutate files between the .cmd's verification step and your next Read. After any restore/checkout, immediately Read the file from Windows side AND check `git status --short`. Real-world reference: 2026-05-23 v10-to-v11 boot at `outputs/session_handoff_2026-05-23_v11.md` sec1 root-cause #1 + sec4 pattern win #11 + `outputs/_fix_local_state.cmd` template. **v15 multi-file-scope extension (2026-05-23 Phase 9b dispatch session):** the original v10/v11 firing was ONE corrupted file. v15 hit **10 corrupted files in a single Cursor session** -- not just the dispatch's actively-edited targets but every file Cursor had OPEN FOR REFERENCE across a ~3-hour Phase 9b session. Specifically: `app/conditions/api_payload.py` (-24 lines), `app/conditions/constants.py` (-30), `app/conditions/fetcher.py` (-15), `app/conditions/usgs_water_temp.py` (-32), `app/conditions/view_model.py` (-39), `tests/test_phase_v15_wave3_water_temp.py` (truncated mid-function at line 268; -191 lines), `scripts/ingest/lhc_civic_scrape.py` (re-corrupted -- same file as original v11 discovery), `scripts/seed_cat13_civic.py`, `docs/maintainability/dispatch_channels.md` (this file), `outputs/cursor_dispatch_prompt_phase_9.md`. Total damage: 325 lines deleted across 6 confirmed code files + 4 docs/scripts files. Pattern: Cursor had all these files open during Phase 9b dispatch (some for reference reading -- v15-wave-3 conditions code to mirror patterns, dispatch wrapper for the 9b scope, gotchas doc for cross-referencing). When focus changed or buffer flushed, every open file got a stale buffer flushed back to disk. Critical post-firing diagnosis pattern: after any Cursor session HALT, **before any ship verify**, run `git ls-files -m` (bash-side OK for this read) to get the full inventory of working-tree-modified files; cross-check against the dispatch's expected scope; ANY file outside expected scope is suspect for buffer-flush corruption. Restore each via Windows-side `git checkout HEAD -- <path>` (gotchas #25/#32 forbid bash-side git mutations). Companion cure carried forward: kill Cursor before any file-touch `.cmd` (Step 0 of every operational .cmd) remains the primary defense -- v15 commit verify result confirmed 14 distinct Cursor.exe PIDs were running at kill time, all flushed to disk before the kill landed. Real-world v15 reference: `outputs/_phase9b_recovery.cmd` (the recovery template) + `outputs/_phase9b_recovery_result.txt` (10-file restore evidence) + `outputs/session_handoff_2026-05-23_v15.md` §2.1.

**29. `railway run` from a `.cmd` silently terminates the parent `.cmd` after a successful run.** Discovered 2026-05-23 v11 Stage A.8 dryrun. Symptom: an .cmd authored to chain 4 sequential `railway run python ...` invocations only ran Step 1 -- the result file showed Step 1's clean `[DRY-RUN] insert=6` summary but no `(exit code N)` echo line, no Step 2 header, no Step 2 output. Multiple repeat-runs confirmed: every `railway run` invocation in a multi-step `.cmd` terminates the parent process after success, even though the railway subprocess itself returned cleanly. Root cause: Railway CLI 4.59's `railway run` opens (or detaches to) a TTY/stdin handle that, when the script exits, signals process termination back up to the parent cmd.exe shell. cmd.exe interprets this as "script ended" and stops executing the .cmd, even though more lines remain. Specific to `railway run`; doesn't fire for `railway --version` / `railway whoami` / `railway status` / `git ...` / `python ...` / `curl ...`. Cure: **wrap every `railway run` invocation in a `cmd /c "..."` subshell.** The subshell absorbs the termination signal; the parent .cmd continues with the next step. Pattern: `cmd /c "set PYTHONPATH=%ROOT% && railway run python ""%ROOT%\path\to\script.py"" [args] 1>> ""%OUT%"" 2>&1"`. Note the doubled `""` quotes for cmd.exe escaping inside `cmd /c`. With this wrapper, a 6-step `railway run` pipeline (count -> dryrun x2 -> commit x2 -> count) runs end-to-end via one File-Explorer click. Without it, the operator needs N separate .cmd files (N File-Explorer round trips). Real-world reference: `outputs/_v15_wave1_prod_run.cmd` (5 sequential railway-run invocations under cmd /c wrappers) + `outputs/_phase8b_prod_pop_commit.cmd` (same pattern).

**30. File-Explorer-launched `.cmd` files inherit cwd from the `.cmd`'s parent directory, NOT from `cd /d` inside the script.** Discovered 2026-05-23 v11 Stage A.8 dryrun second attempt. Symptom: a `.cmd` at `outputs\_phase8b_prod_pop_dryrun.cmd` that began with `cd /d "%ROOT%"` (= `C:\Users\casey\projects\havasu-chat`) ran a `railway run python scripts/ingest/lhc_civic_scrape.py` step and errored with `python.exe: can't open file 'C:\Users\casey\projects\havasu-chat\outputs\scripts\ingest\lhc_civic_scrape.py': [Errno 2] No such file or directory` -- python looked for the script under `outputs/scripts/...`, not under repo-root `scripts/...`. The `cd /d` inside the .cmd correctly set cmd.exe's cwd to repo root, but Windows File Explorer launches a .cmd with the process cwd = the .cmd's parent directory, and `railway run`'s subprocess inherits the launcher's cwd (not cmd.exe's currently-cd'd cwd) for relative-path resolution. Root cause: cwd inheritance from the GUI shell launcher bypasses any `cd /d` inside the script's own cmd.exe instance for subprocesses launched via tools like `railway run` that re-derive cwd from process-tree metadata rather than reading `%CD%`. Cure: **use ABSOLUTE paths for all script + helper file references in any File-Explorer-launched `.cmd`.** Pattern: `"%ROOT%\scripts\foo.py"` not `scripts\foo.py`; `"%ROOT%\outputs\_helper.py"` not `outputs\_helper.py`. Same goes for any `.cmd`-driven file argument -- absolute paths sidestep the cwd-inheritance ambiguity entirely. Same scope as gotchas #29 (cmd /c wrappers) and #31 (ASCII-only) -- three different cmd.exe / Windows-shell surface quirks that bite agents who default to Linux-shaped command shapes. Real-world reference: 3 consecutive .cmd-edit cycles in v11 Stage A.8 before the absolute-path fix landed; `outputs/_phase8b_prod_pop_dryrun.cmd` at the post-fix state.

**31. UTF-8 multi-byte characters in `.cmd` source files break cmd.exe parsing under default Windows OEM codepage.** Discovered 2026-05-23 v11 Stage A.8 first dryrun. Symptom: a .cmd file authored with Unicode em-dashes and Unicode arrows in comments was double-clicked from File Explorer -- the .cmd window opened, ran for a fraction of a second, closed, and produced NO result file. Re-running produced the same instant-close + no-output behavior. Root cause: Windows cmd.exe defaults to the OEM codepage (CP437 on US English installs, CP850 on Western European, etc.) for parsing .cmd source. UTF-8 multi-byte sequences (the em-dash is `E2 80 94` in UTF-8) are interpreted as 3 separate bytes in the legacy codepage, which break cmd.exe's command tokenizer at the embed point and cause silent script abort BEFORE the `(...) > file 2>&1` redirect can create the result file. The .cmd appears to "run" (window opens + closes) but did nothing executable. Cure: **pure ASCII in .cmd source files.** Replace Unicode em-dash with `--` or single `-`; replace Unicode arrow with `->`; use straight ASCII quotes only. Detection signal: `file <name>.cmd` from bash returns "DOS batch file, Unicode text, UTF-8 text" vs the desired "DOS batch file, ASCII text". Or `od -c` on the first few lines reveals any `342 200 224` byte sequence (UTF-8 em-dash). When porting .cmd files from agent-authored content (where em-dashes and arrows are common in agent prose), ASCII-sweep before ship. Same lesson scope as gotchas #3 + #8 + #13 + #16 + #30 -- Windows shell parsing surprises that catch agents defaulting to Unicode-friendly prose or Linux-shaped command shapes. Real-world reference: first authoring of `outputs/_phase8b_prod_pop_dryrun.cmd` in v11; one ASCII-rewrite cycle fixed it.

**32. Bash sandbox can CREATE files on the workspace mount but cannot DELETE them -- including stale `.git/index.lock` files it created itself.** Discovered 2026-05-23 v11 git-restore attempt. Symptom: an attempt to `git restore <files>` from the bash sandbox failed with `error: unable to unlink old 'scripts/...': Operation not permitted` AND `warning: unable to unlink '/sessions/.../havasu-chat/.git/index.lock': Operation not permitted`. The .lock file (0 bytes, owned by `wizardly-eloquent-wozniak` = the sandbox user) blocked all subsequent git writes -- both from bash AND from Windows-side cmd.exe (Windows git correctly refused to step on the existing lock). Direct `rm /sessions/.../havasu-chat/.git/index.lock` from bash also failed with "Operation not permitted" even though the file was owned by the bash user. Root cause: extends gotcha #25 (bash can invent fake state) with the actively-harmful flip side -- bash mount permissions allow CREATE on the workspace mount (so the lock-acquire-then-fail path leaves a 0-byte empty lock on disk) but block DELETE (so the same bash process can't clean up its own lock when the write fails). The 0-byte lock then blocks both bash-side AND Windows-side git operations until manually cleaned up. Different from #15 (bash leaving a .lock that Windows can't unlink because of permission asymmetry) -- #32 is the same shape but extends to "bash can't unlink it either, even though bash owns the file". Cure: **never run git operations from the bash sandbox against the workspace.** Even read-only `git status` / `git diff` reads can leave bash-owned stale state. For any FS/git mutation, use Windows-side `.cmd` `del /f /q "%ROOT%\.git\index.lock"` (Windows ignores bash's ownership claims on workspace-mount files -- DELETE works from Windows even when bash can't unlink). Codified as v11 NEW HARD RULE: "Bash mount CANNOT delete files (write-blocked) -- even if file is owned by sandbox user. Use Windows .cmd `del /f /q` for any deletion." Same scope as gotcha #15 (bash git ops leaving stale state for Windows) and #25 (bash inventing state) -- three different failure modes of the same architectural seam (bash sandbox vs Windows host filesystem permissions). Real-world reference: `outputs/_fix_local_state.cmd` (Step 1 = `del /f /q "%ROOT%\.git\index.lock"` before any git operation); v11 Stage A.8 pre-flight.

**33. Claude-in-Chrome can drive the Railway dashboard end-to-end for env-var changes -- relaxes v11 gotcha #24's pre-extension posture.** Discovered 2026-05-23 v12 wave-3 activation. Symptom: v11 close-out (HEAD = `ed6b8ca`) shipped the wave-3 USGS water-temp fetcher behind feature flag `FEATURE_FLAG_WATER_TEMP_GAGE_09426630`; activation required setting that env var to `true` on the Railway `conditions-cron` service. v11 gotcha #24's hard rule said: "hand the save click to Casey explicitly; do NOT drive that via Chrome MCP" -- a posture inherited from pre-extension Phase 8a.3 caution about Railway dashboard form serialization (the `""` -> Postgres timestamp coercion failure documented in #24 itself). v12 wave-3 wiring landed at `2ab4f23`; Casey then explicitly invited Cowork to drive the activation via the Claude-in-Chrome extension ("can you use my browser and do this"). Cowork ran the full sequence end-to-end: `list_connected_browsers` (Browser 1 already paired from a prior session, deviceId persisted) -> `select_browser` -> `tabs_context_mcp{createIfEmpty:true}` -> `navigate(railway.com/dashboard)` -> click project tile (`havasu-chat-production`) -> click service tile (`conditions-cron`) -> click Variables tab -> click "+ New Variable" -> type name (`FEATURE_FLAG_WATER_TEMP_GAGE_09426630`) -> click value field -> type value (`true`) -> click "Add" -> click "Deploy" in apply-changes toolbar. Total interaction: ~90 seconds end-to-end for 1 variable. Deploy went ACTIVE; first cron tick under the new flag fired at 22:46:17Z (manually triggered via "Run now" -- see gotcha #34). Root cause for the original v11 hard rule's caution: pre-extension Cowork sessions relied on `mcp__computer-use__*` pixel-clicks against the rendered browser window, which were brittle around modal dialogs + form-state-management quirks in the Railway UI (gotcha #24 catalogued three companion failure modes on the row-edit panel alone). The Claude-in-Chrome extension is DOM-aware and uses element references rather than pixel coordinates, sidestepping the brittleness category that informed the original posture. Cure: **drive Railway dashboard interactions via Claude-in-Chrome when the operator invites it or when Cowork-primary posture is "do as much as you can autonomously."** The extension pair persists across sessions via deviceId (Browser 1 / `6440e2ba-be0f-4f39-b9a4-69cce3c1ce49` on Casey's Windows install), so future sessions can `list_connected_browsers` -> `select_browser` directly without re-pairing. Operator hand-off for env-var changes remains acceptable when the operator prefers it (a single-click flip is faster as a Casey keypress than as an MCP sequence), but is no longer mandatory. Same scope-relaxation rule applies to other dashboard-only surfaces (logs viewer, deploy controls, project settings) -- DOM-aware browser automation is the right tool for in-dashboard work that doesn't have an equivalent CLI affordance. Operations that DO have CLI affordances (e.g. setting an env var via `railway variables set` if/when the CLI exposes it for the target service) still prefer the CLI path; the Chrome MCP is for UI-only surfaces. Note: gotcha #24's specific cautions about the row-edit panel (empty-string-to-Postgres-timestamp coercion, Save button visibility, Escape closing the modal) all remain valid -- #33 relaxes the channel-choice posture but doesn't invalidate the per-surface findings. Real-world reference: v12 `FEATURE_FLAG_WATER_TEMP_GAGE_09426630=true` activation 2026-05-23; full transcript at `outputs/session_handoff_2026-05-23_v12.md` sec1 + sec4 pattern win #12.

**34. Click "Run now" on the Cron Runs tab to fire the cron immediately after an env-var change instead of waiting for the next scheduled tick.** Discovered 2026-05-23 v12 wave-3 activation. Symptom: after gotcha #33's `FEATURE_FLAG_WATER_TEMP_GAGE_09426630=true` activation on the `conditions-cron` service, Cowork wanted to verify the new flag took effect end-to-end (cron tick -> fetcher dispatch -> cache row -> `/api/conditions` payload emits new fields). The `conditions-cron` service runs on a 15-minute cadence; waiting for the next scheduled tick would have meant up to 14 minutes of activation-verification idle time. Discovery: Railway's service detail panel exposes a "Cron Runs" tab; the recent-executions table has a "Run now" button per row that enqueues an immediate execution of the cron job against the CURRENT ACTIVE deployment (so the new env var is picked up immediately, no redeploy needed). Click the button -> toast confirms enqueue ("Cron execution started -- Your cron execution has been enqueued") -> the Recent Executions row updates from "Running..." to "Successful" on completion. The cache row was written at 22:46:17Z (matching the manual-trigger timestamp), the cron tick produced no HTTP request to USGS (fetcher self-gates on the flag + early-exits with an empty payload when the gage publishes only the `-100000` sentinel), and `/api/conditions` started emitting the 5 new water_temp_* fields with sentinel-graceful null values (after the lag described in gotcha #35). Total time from "click Run now" to "fresh row in DB": ~25 seconds. Total time saved vs waiting for the next scheduled 15-min tick: up to 14 minutes per activation. Root cause: Railway's cron-execution model decouples the schedule (a static `*/N * * * *` expression) from on-demand triggers (a synchronous request that runs the same code against the same deployment). Both paths share the deployment + env, so manual triggers exercise the production state exactly. Note: Railway's Cron Runs tab may continue to show "Next in N minutes" with a STATIC schedule that doesn't update from the manual trigger -- the "Run now" doesn't reset the regular cadence; both will fire at their respective times. Cure: **after any env-var change on a cron-driven service, click "Run now" on the Cron Runs tab to verify activation immediately instead of waiting for the next scheduled tick.** Same pattern applies to verifying any cron-side code change: deploy lands -> click "Run now" -> read result row -> confirm. Cuts ~10-14 min off every cron-cadence verification cycle for the 15-min-cadence `conditions-cron` service; cuts proportionally more off lower-cadence cron services (e.g. the 6h `parks-rec-scrapes` workflow on the GitHub Actions side, though that one uses GitHub's `workflow_dispatch` trigger rather than Railway's "Run now" button). Companion observation -- **don't conclude activation failed if `/api/conditions` doesn't reflect the new state immediately after the cron tick completes** -- see gotcha #35 for the web service's 300s `_local_cache` lag. Real-world reference: v12 wave-3 activation 2026-05-23; full transcript at `outputs/session_handoff_2026-05-23_v12.md` sec1 + sec4 pattern win #13.

**35. Web service's `_local_cache` wraps `read_source` for 300s -- after the cron writes a fresh cache row, `/api/conditions` may continue to serve the pre-tick state for up to 5 minutes.** Discovered 2026-05-23 v12 wave-3 activation-verification cycle. Symptom: after gotcha #34's "Run now" cron trigger at 22:46:17Z (which successfully wrote a fresh cache row to the DB with the wave-3 `water_temp_*` fields populated per the new flag), `/api/conditions` continued to return a payload WITHOUT the new water_temp_* fields for ~2-3 minutes after the cron tick completed. Initial assumption candidates: the cron's write to the cache table hadn't actually happened, OR the api_payload code path's `feature_enabled` gate wasn't evaluating the new flag correctly, OR the deploy hadn't activated cleanly. None of those was true. Polling `/api/conditions` at 30-60s intervals over ~3 minutes eventually surfaced the new fields with the expected sentinel-graceful null values. Root cause: `app/conditions/cache.py` wraps `read_source` in a process-local TTL cache. Code reference: `app/conditions/cache.py:17` `_LOCAL_TTL_SECONDS = 300`; `:19` `_local_cache: dict[...]`; `:38` `def read_source(...)`; `:45` the TTL check (`if (now - cached_at).total_seconds() < _LOCAL_TTL_SECONDS: return ...`). The cache exists to bound the web service's DB read rate on the conditions surface (called per page-load on `/home` + per API call on `/api/conditions`); without it, every request would round-trip to Postgres. Tradeoff: after the cron writes a NEW cache row to the DB, the web service's local cache may continue to serve the OLD value for up to 300s before the next DB read. Critical seam: `cache.py:125` `invalidate_local_cache(source)` IS called inside the same write path that bumps the DB row, but ONLY for the process that performed the write -- the cron service. The web service runs in a different process (different container, different replica pool) and its `_local_cache` dict is NOT shared. The cron's invalidation does not propagate to the web replicas. Web replicas drop their cached entry only when their own TTL expires + the next request triggers a DB re-read. Multiple web replicas behind Railway's load balancer each have their own local cache; cache misses are staggered across replicas based on each replica's last DB-read time. The observable effect from the client: requests round-robin to different replicas + some return new state earlier than others until all replicas have re-read. Cure (none required from a correctness perspective; this is intended behavior, just needs operator awareness): **poll patiently after cron-side writes -- give it up to 5 minutes for the new state to propagate to all web replicas through the local-cache TTL.** Don't restart the web service to force-flush the local cache unless verification is time-critical (a restart works as a hard flush but interrupts in-flight requests). Don't conclude activation failed if `/api/conditions` doesn't reflect the new state immediately after the cron tick -- check again at the 5-minute mark before raising any alarm. If immediate verification IS time-critical (e.g. during a coordinated incident response), trigger a redeploy of the web service from Railway, which spins up fresh replicas with empty local caches. Companion observation -- **the staleness label in `/api/conditions` reflects DB-write time, not local-cache-write time** -- so an `Updated 4 min ago` label can appear in a payload that hasn't yet picked up the latest cron tick (the cache returned a row from a tick 4 min ago, even though the most-recent tick was 30 seconds ago). The staleness label is correct relative to the row it served, not relative to wallclock time. Companion observation -- **this lag pattern affects ANY cron-write-then-web-read flow on the conditions surface**, not just wave-3 water_temp -- the same 300s ceiling applies to aqi/temp/alerts/lake whenever a fresh cron tick lands. The lag is invisible during normal operation because the cron cadence (~15 min) typically exceeds the cache TTL (5 min), but it surfaces immediately during activation-verification cycles where the cron is manually triggered ad-hoc via gotcha #34's "Run now" pattern. Forward-looking note: if activation-verification cycles became frequent enough to motivate an immediate-flush affordance, the cleanest cure would be a cross-process invalidation channel (e.g. a Postgres LISTEN/NOTIFY on cache-write that web replicas subscribe to, or a small `/internal/cache/flush` endpoint that hits each replica via a known internal token) -- but the current 5-min ceiling is acceptable for the operational cadence and not worth the additional surface area today. Real-world reference: v12 wave-3 activation 2026-05-23; the polling pattern caught the new fields at ~22:49Z (~3 min after the 22:46:17Z tick), well within the 300s ceiling. Full transcript at `outputs/session_handoff_2026-05-23_v12.md` sec4 pattern win #14. Same architectural seam scope as gotchas #15 + #25 + #32 (different processes / different filesystems / different cache views of the same underlying state).

**36. Bare `(...)` parens in `echo` lines inside outer `(...) >> file 2>&1` redirect blocks silently abort cmd.exe scripts.** Discovered 2026-05-23 v13 docs-ship-for-gotchas-33-35 first attempt; re-fired 2026-05-23 v15 Phase 9b recovery .cmd first attempt -- N=2 confirmed. Symptom: a `.cmd` authored as `(echo step 1; echo step 1 -- per CI discipline -- with (extra) parens; python -m ruff check .; echo done) >> "%OUT%" 2>&1` opens a cmd.exe window from File Explorer, runs for a fraction of a second, closes, and produces NO result file in outputs/. Re-running produces the same instant-close + no-output shape. Cleaner rewrite removing the bare parens from the echo lines (or escaping them as `^(per CI discipline^)`) runs end-to-end cleanly + produces the expected result file. Root cause: cmd.exe's outer-block `(...) > file 2>&1` redirect target is CREATED ONLY after cmd.exe successfully parses the entire outer block. The parser counts `(` and `)` characters regardless of whether they appear in quoted strings or `echo` argument context. A bare `(` inside an `echo` line is treated as a nested-block-open marker, throwing off the paren balance for the outer block. When cmd.exe reaches the actual closing `)` of the outer block, it sees an extra unmatched-or-mismatched paren count and rejects the entire block at parse-time. Because the redirect target is created in the cmd.exe write path that only fires AFTER successful parse, the failure-to-parse path leaves no redirect target on disk -- the .cmd literally has nowhere to write its stderr that says "parse error", so the failure is silent. Same scope class as gotchas #29 + #30 + #31 (cmd.exe / Windows-shell parsing surprises). Cure: in any cmd.exe `.cmd` that uses an outer `(...) > file 2>&1` redirect block, **remove or escape bare parens in `echo` lines.** Escape patterns: `^(` and `^)` (caret-escape in cmd.exe). Alternative + preferred: use `--` separators in lieu of parens for parenthetical labels (`echo --- ruff -- per CI discipline ---` instead of `echo --- ruff (per CI discipline) ---`). Detection signal: any operational `.cmd` that produces NO result file should be suspected for this gotcha BEFORE assuming the script logic itself is wrong. Companion observation -- **every operational `.cmd` should already include a first-check "did the result file get created?" assertion**, per gotcha #25's structural mitigation. If the result file is absent, suspect outer-block parse failure (this gotcha) FIRST before suspecting any per-line script logic. Real-world references: 2026-05-23 v13 docs ship for gotchas #33-#35 (first firing; rewrote with `--` separators); 2026-05-23 v15 Phase 9b recovery .cmd (second firing; same cure). Same architectural seam scope as gotchas #29 (cmd /c wrappers) + #30 (absolute paths) + #31 (ASCII-only) -- four different cmd.exe / Windows-shell quirks that bite agents who default to Linux-shaped scripting. **v17 N=3 firing -- python-c sub-pattern (2026-05-23 autonomous overnight docs ship):** the v16-staged `outputs/_docs_v15_verify.cmd` had its `echo` lines sanitized correctly but contained two inline `python -c "import re; ... re.findall(r'^\*\*[0-9]+\.', t, re.M) ..."` lines where the parens are inside double-quoted strings passed to `python -c`. Casey-side launch attempt: cmd window opened from File Explorer, ran for a fraction of a second, closed, no result file written. Diagnosed by pattern recognition (per pattern win #16). Root cause confirmation: cmd.exe's outer-block paren counter scans the ENTIRE line text REGARDLESS of being inside quoted strings -- the double-quoted `"..."` around the `python -c` argument does NOT shield the inner parens from the parser. Cure: **move any complex python out of `-c` inline strings into a helper `.py` file.** Pattern: replace `python -c "import re; t=open('foo.md').read(); print(re.findall(r'pat', t))"` with `python "%ROOT%\outputs\_my_helper.py"` where the .py file holds the same logic. cmd.exe doesn't open the .py file, just passes the path to python -- so no paren-counting issue. Validated this session: `outputs/_docs_v15_verify_helper.py` extracts the python logic; `outputs/_docs_v15_verify_v2.cmd` invokes via path; ran end-to-end producing the 60-line result.txt that authorized ship to `bb160f6`. Same root cause as the echo-line firings (N=1 v13, N=2 v15) -- the inner cure (use `--` separators) generalizes to: "use helper .py for any line whose argument contains bare parens, including inline python-c strings." Architectural takeaway: assume cmd.exe's outer-block parser is paren-counting all line text whether quoted or not; any line that would contain bare parens needs structural extraction (helper file) rather than per-line escaping. Real-world v17 reference: ship commit `bb160f6`; this gotcha extension was authored during the autonomous overnight session and shipped as a single-file docs commit.

**37. Pipe + redirect on a single line INSIDE an outer `(...) >> file 2>&1` redirect block gets parsed as literal arguments to the command, not as pipe + redirect operators.** Discovered 2026-05-23 v15 Phase 9b recovery .cmd v2 + ship verify .cmd v1. Symptom: a line written as `python -m pytest --collect-only -q 2^>^&1 ^| tail -3` inside an outer `(...) >> %OUT% 2>&1` block produced this error in the result file: `ERROR: file or directory not found: 2>&1` followed by `no tests collected in 0.00s`. The caret-escaped `2^>^&1`, `^|`, and ` tail -3` got passed to pytest as literal positional arguments rather than being interpreted as pipe + stderr-redirect operators. Root cause: the caret-escape pattern `^>` `^&` `^|` is designed to escape these characters at PARSE TIME so cmd.exe sees them as literal chars in the outer block's command-tokenization step. The intent is "preserve these literal for the inner line's execution," but the inner line's execution then sees them as literal command-line arguments to `python` -- NOT as shell metacharacters to be re-interpreted by cmd.exe. The pipe + redirect operators are NOT re-evaluated at execution time inside the outer block context; they need to be present unescaped in the source IF the outer block isn't itself a redirect. Two-redirect-target conflict: the outer block has `>> %OUT% 2>&1` already wrapping all child stdout/stderr; trying to add a per-line `2>&1 | tail -3` creates ambiguity about which target captures what. Cure: **drop the per-line pipe + redirect entirely; the outer block captures everything.** If you only want a summary line, run the full command and grep/tail the result FILE in a follow-up step (read offset / sed at specific line range / grep for pattern). Alternative cure: write a separate `.cmd` for the just-the-summary case without the outer block wrapper -- then per-line pipe + redirect work as expected. Real-world reference: 2026-05-23 v15 Phase 9b recovery .cmd v2 (line `python -m pytest --collect-only -q 2^>^&1 ^| tail -3` produced `ERROR: file or directory not found: 2>&1`); cure was dropping the per-line escaping and accepting that pytest dumps all 2400+ test paths into the result file (the final `tests collected in X.XXs` summary is at the tail of the dump and can be Read-offset'd if a tighter parse is needed). Same architectural seam scope as gotcha #36 (cmd.exe outer-block parsing quirks) and gotchas #29 + #30 + #31 (cmd.exe / Windows-shell surface surprises). Author note: caret-escaping IS needed for `2^>nul` patterns when used INSIDE the outer block to silence stderr without redirecting it elsewhere -- the rule is "use caret-escape for stderr-silencing redirects, but NEVER use it for pipe + redirect combos that you want re-interpreted as shell operators."

**38. Ctrl+L address-bar launch is the universal-cure pattern for File Explorer .cmd ship loops -- bypasses both portrait-mode double-click misinterpretation and view-refresh staleness in one move.** Discovered 2026-05-23 v17 autonomous overnight session; N=4 within that session (verify_v2 + commit_v2 + v17-36-N3-verify + v17-36-N3-commit, all clean launches), N=8+ across v18-v20 ship arcs since with 100% success rate. Symptom: in portrait-rotated display orientation (recurring v15+ Windows state on Casey's install), File Explorer double-click on a .cmd is interpreted as two single-clicks -- the first selects the row, the second re-selects rather than launching (unreliable per v15 pattern win 19 in handoff history); AND the visible row listing can be stale (newly-written .cmd files don't appear in the listing even after F5 -- the folder's item-count indicator at the bottom of the window DOES increment, suggesting the underlying NTFS USN journal / shell change notification fired, but the visible row listing remained stuck on its pre-write snapshot; sibling observation captured in `outputs/candidate_gotchas_v17_evidence_sweep.md` §2 Candidate A). Either failure mode blocks the docs ship loop on the .cmd-launch step. Cure: **focus the address bar via Ctrl+L, type the full absolute path to the .cmd file (e.g. `C:\Users\casey\projects\havasu-chat\outputs\_docs_v20_ship.cmd`), press Enter.** Windows triggers `ShellExecuteEx` directly with the typed path, which bypasses both the visible row model (so view-refresh staleness is irrelevant) AND the click-event disambiguation layer (so portrait double-click misinterpretation is irrelevant). Works for any file Windows knows how to handle: .cmd / .py / .md / .txt -- anything with a registered handler. Same accelerator that the Run dialog and taskbar-pinned-item resolver use; not a File-Explorer-specific shortcut. Same lesson scope as gotcha #30 (absolute paths in .cmd contents) -- Ctrl+L launch ALSO requires absolute paths in the address bar (relative paths don't resolve correctly from the address-bar context). Pattern win history: v17 introduced and validated the pattern (N=4 same-session); v18 + v19 + v20 sessions used it as the default .cmd launcher with 100% success rate vs the double-click path. v20 boot's gotcha #25 N+4 firing (extension in #25 above) re-validated the cure: when bash mount lies about filesystem state, the Windows-side .cmd verify + Read pattern remains the ground-truth recovery path, AND Ctrl+L is the most reliable way to fire the .cmd. Detection signal for the surface conditions that motivate this cure: double-click on a known-recent .cmd produces no cmd.exe window OR opens a window that immediately closes with no result file. Note: result-file absence has multiple possible causes; suspect gotcha #36 (outer-block paren parse error) FIRST per #36's structural-mitigation rule, then #38 launch failure SECOND. Same architectural seam scope as #15 + #25 + #32 (Windows-mount vs bash-mount surface mismatches) -- #38 is the positive-cure pattern that survives the negative-failure conditions documented in those entries. Real-world references: v17 autonomous overnight ship arc (`bb160f6` + `411defc` -- both shipped via Ctrl+L); v19 + v20 routine ship cadence; this gotcha entry itself shipped via the same pattern.

---

## Working agreements

The 12 hard-won rules live in `docs/maintainability/dispatch_protocol.md`. Read them once, then reference them by number in dispatch prompts when relevant. Today's session added Rule 12 (amend-safety); the `2026-05-10_absorption_forensics.md` memo includes a Rule 13 candidate that was *not* added (verdict: N=1, low severity, cure already implicit in Rule 2).

---

## When to escalate to a fresh chat

You're a Cowork primary running a long session. Eventually context burn matters. Heuristic:
- After 4-5 sub-agent dispatches, your context is meaningfully fuller.
- After 10+ paste-channel round-trips, you've absorbed a lot of state.
- When a fresh handoff doc has just been authored, that's a natural starting point for a new agent — using it validates the handoff actually works.

When you escalate: write a fresh handoff doc capturing today's work (or update the existing one), then tell Casey the new chat should boot from `docs/SESSION_BOOT_PROMPT.md` + the latest `docs/SESSION_HANDOFF_<date>.md`.
