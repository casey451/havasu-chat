You're the new Cowork primary on the havasu-chat project (Lake Havasu City local concierge — currently pivoting from chat-first concierge to structured local directory + chat as one of three front doors). The previous session shipped #64 (confabulation harness v2: emit `category` / `activity_category` in `per_row.csv` + module-top `_DEFAULT_REGRESSION_ANCHORS` constant + `--anchor-set-file` CLI override; pytest 1417 → 1422), aligned `docs/maintainability/halt3_closeout.md` to the v2 harness shape (4 spots), then — and this is the load-bearing event of the session — reviewed an external strategic deep-research report (`uploads/deep-research-report.md`) against the existing chat-first trajectory and locked a directory-first pivot. Authored `docs/STRATEGY_PIVOT_2026-05-12.md` (~210 lines) capturing the call + reframing existing infrastructure as backend trust layer + 90-day shape + three-bucket backlog re-prioritization + 7 open Casey-owned decisions that block the next 2-3 days of work + honest risk callout. 5 commits across the session: `aa3abd4` (#64 ship), `426f992` (#64 close-out), `eb6f2e1` (handoff doc, pre-pivot form), then the pivot-doc commit on top. Wrapped on `main` 2026-05-12 evening.
Boot sequence — read in this order (the order matters because the pivot makes earlier docs partially stale):

1. `docs/STRATEGY_PIVOT_2026-05-12.md` — read end-to-end (~5 min). This is the new authoritative strategic-priority signal. Its §6 (three-bucket backlog re-prioritization) supersedes BACKLOG.md priority signals until further notice. Its §8 (7 open Casey-owned decisions) is what blocks the next 2-3 days of work.
2. `docs/SESSION_HANDOFF_2026-05-12.md` — read for ship details + state confirmation. The §0 boot sequence + §3 ship list + §7 next-actions were rewritten in lockstep with the pivot. Banner at the top redirects you to the pivot doc.
3. `docs/STATE.md` — current as of pre-pivot (HEAD `aa3abd4`, pytest 1422). Production paragraph reflects #64 ship. The "Recently shipped" section's top entry covers #64; the pivot itself isn't represented in STATE yet (intentional — STATE is project-state-of-record, the pivot doc is strategic-direction-of-record; they layer).
4. `docs/maintainability/dispatch_protocol.md` — 12 working-agreement rules. Unchanged.
5. `docs/maintainability/dispatch_channels.md` — channel-pick playbook + 7 common gotchas. Unchanged.
6. Skim `docs/BACKLOG.md` from line 2330 onward only if you want pre-pivot ticket context — the pivot doc §6 tells you what's actually load-bearing under the new shape; BACKLOG is now historical.
7. `git log --oneline -6` to confirm you're on the pivot-doc commit on top of `eb6f2e1` on top of `426f992` on top of `aa3abd4` on top of `4f9a322`. If origin is current, `git status` reads clean.
Critical context from the previous session you must absorb:

* Strategic posture (post-pivot): the headline product is becoming a structured local directory with browse + search + chat as three equal interfaces, not a chat-first concierge. V1 directory category locked: Home Services (plumbers/HVAC/pool/electrical/landscapers — older homeowner demographic, urgent intent, highest revenue-per-merchant ceiling for early sponsors). Bootstrapped / revenue-funded; Casey-as-primary-salesperson; pivot-now (paused enrichment-as-scoped, redirecting operator effort to feed new directory shape).
* Sponsor packaging strategy (per pivot doc §7): chase Verified Presence ($79/mo) and Category Visibility ($349/mo) and Seasonal Takeover ($1,500-$5,000) first. Defer Deals Engine + Intent Capture (Lead-gen) to Phase 2 — those need 6-10 weeks of build before first sale and are wrong shape for bootstrapped time-to-revenue.
* Existing chat-side engineering is reframed, not deprecated. `disclosure_renderer`, `placement_regime`, `confidence_tier` classifier, `confabulation_eval` harness, `verification_method` CHECK migration, smoke catalog infrastructure, `chat_logs` + disclosure telemetry — all become backend trust infrastructure for the directory. Pivot doc §3 enumerates the translation.
* Deprioritized work (per pivot doc §6 middle bucket): HALT 3 close-out (#53), #62 alias-resolution P3, smoke catalog ambiguity resolution, Phase 2.5 Premier inventory open as originally scoped, #39. Don't close these — just lower-band them. Chat surface is bug-fixes-only; not evolving.
* Load-bearing under either vision (KEEP): #65 rate-limiter (now MORE urgent — directory hits Places API more than chat ever did), #18 repo hygiene, enrichment toolchain (paused-as-scoped, redirected for new structured fields).
* Promoted to top-of-queue: taxonomy lock, schema additions (`Category` model + `category_id` FK + `Place` model + `attributes` JSON), Provider profile pages, Home Services category page, account-lite v0.1 (magic-link), sponsor claim flow + edit UI, per-merchant analytics dashboard.
* Feature flags: `FEATURE_FLAG_DISCLOSURE_RENDERER=false` (HOLD; less urgent post-pivot), `FEATURE_FLAG_CONFIDENCE_TIER=true`. Audience-signal persistence: AUTOMATIC.
* Alembic head: `d6e7f8a9b0c1`. Unchanged.
* Production runtime unchanged since `54a56b1` — all 5+ commits since are doc-only, test-only, or eval-tooling-only.
* Linux bash mount serves stale `.git` views and is missing `pytest`. Use Windows-side paths via the Read tool (Rule 7); pytest is operator-driven on Casey's machine. Don't try to verify pytest count from bash; trust STATE/handoff and ask Casey to run pytest locally for verification steps.
* PowerShell `Invoke-RestMethod` always uses single-quoted `-Body` and `-ContentType 'application/json; charset=utf-8'` (Rules 4 + 5).
* Brief baseline staleness mitigation: instruct dispatched agents to run step-0 baseline confirmation (HEAD + pytest collected count) before doing work; halt-and-report on drift.
* Parallel-commit-absorption pattern (gotcha #1 in dispatch_channels.md): instruct dispatched agents to report ship-log entry text rather than editing `BACKLOG.md` directly; you add the entry after their commit lands.
* Forensic notes from yesterday's session worth preserving: (a) schema-adjacent sub-agent investigations should ground column names against `app/db/models.py` directly, not just consuming code paths — yesterday's halt3 sub-agent surface-read a column name (`track_kind`) that didn't exist; CC caught it at step-0; (b) don't trust a single Glob for "does this file exist" — pair with a Read of a likely path or Grep for distinctive content. Both lessons captured in handoff §5.
Tool channels you can route work to (Casey is the operator who pastes between you and them):

* Cursor — focused-file edits, schema migrations, ops scripts. Yesterday shipped #64 cleanly via Cursor on a heavily prescriptive multi-file brief; channel-fit difference flattens when scope is tightly specified.
* Claude Code — heavy multi-file lanes, audits, comprehensive test suites. Same paste-back pattern.
* ChatGPT — non-file research, drafting, brainstorming (cannot read code). Same pattern; you do a markdown polish pass on save.
* Sub-agents via your `Agent` tool — direct dispatch from you for parallel verification, code review, recovery investigations, doc audits. Burns your context but no operator round-trip.
* Yourself via Read/Write/Edit — small docs edits, BACKLOG.md status flips after a ship lands, applying sub-agent recommendations as anchored Edits, authoring strategic docs.
First action: do NOT auto-pick a thread. The pivot doc's §8 lists 7 open Casey-owned decisions that gate the next 2-3 days. Most leveraged opening move is to work through them with Casey via `AskUserQuestion` — at minimum decisions 1-4 (canonical category taxonomy lock, `Place` model scope for V1, account-lite auth provider choice, map provider choice). Once those land, the immediate work is bounded engineering: schema additions in `app/db/models.py` + Alembic migration, plus pivot notices added to `PROJECT.md` and `HAVA_CONCIERGE_HANDOFF.md` (the substantive rewrites of those two docs are deferred — multi-week task, defer past Day 90).
If Casey isn't available for the §8 decisions in the moment, viable starting alternatives are (a) #65 rate-limiter implementation prep — design is shipped, P1 scope is clear enough to start the implementation lane in parallel, Casey-owned questions on QPS / failure-mode / observability can resolve mid-flight; or (b) #18 repo hygiene continuation. Don't start any of the deprioritized chat-only threads (smoke catalog ambiguity resolution, HALT 3 pre-investigation) without Casey explicit go-ahead — the pivot reframed those as lower-leverage.
That's it. Read the pivot doc first, then the handoff, then ask Casey which §8 decisions he wants to lock today.
