You're the new Cowork primary on the havasu-chat project (Lake Havasu City local concierge — pivoted 2026-05-12 from chat-first concierge to structured local directory + chat as one of three front doors; V1 directory category locked: Home Services). The previous session was the **first bounded engineering session under the directory-first pivot** — locked §8 decisions 1-4 (taxonomy=12 as-proposed, Place model=defer to Phase 2, auth=Resend, map=Leaflet+OSM), shipped the directory pivot V1 schema (Cursor lane: new `Category(Base)` model + 12 seeded categories + additive `category_id` FKs on Provider/Program parallel to legacy string columns + `attributes` JSON + `district` String(64) on Provider + `category_ref` relationships + Alembic migration `e7f8a9b0c1d2` chaining off `d6e7f8a9b0c1` + `tests/test_directory_schema.py` with 7 net-new tests; pytest 1422 → 1429), then chased a CI ruff failure surfaced by the `ruff==0.15.12` pin in `dev-requirements.txt` (4 pre-existing F401/I001 issues in three unrelated test files unrelated to the schema commit; auto-fixed via `python -m ruff check --fix`), wrote BACKLOG + STATE ship-log entries for both, and saved a ChatGPT-drafted Verified Presence ($79/mo) cold-pitch script (60-sec / 60-sec call / 3-min sit-down / voicemail / written follow-up + 8 anticipated objections + disqualifiers + pre-call checklist) to `docs/sponsor_outreach/verified_presence_pitch.md`. 5 commits across the session: `a6e9f6c` (decisions + banners), `6f6ef79` (schema), `aea87b8` (lint), `597d9cb` (BACKLOG/STATE), `11b248f` (cold-pitch). Wrapped on `main` 2026-05-13 evening; all pushed to origin.
Boot sequence — read in this order (the order matters because the pivot still makes some pre-pivot docs partially stale):

1. `docs/STRATEGY_PIVOT_2026-05-12.md` — read end-to-end (~5 min) if you haven't seen the pivot before. Still the authoritative strategic-priority signal. **§8 status block (top of §8) is updated 2026-05-13 to reflect decisions 1-4 LOCKED.** Its §6 (three-bucket backlog re-prioritization) still supersedes BACKLOG.md priority signals.
2. `docs/SESSION_HANDOFF_2026-05-13.md` — read for ship details + state confirmation. **THIS IS THE PRIMARY ENTRY POINT FOR YOUR SESSION.** The §0 boot sequence + §3 ship list + §5 lessons + §7 next-actions are calibrated to where things stand right now.
3. `docs/SESSION_HANDOFF_2026-05-12.md` — skim only if you want the prior-session narrative (the session that locked the pivot itself). Banner at top still points at the pivot doc.
4. `docs/STATE.md` — current as of session close. Production paragraph reflects HEAD `11b248f`, pytest 1429, alembic head `e7f8a9b0c1d2`. New "Recently shipped" entry at top covers the schema lane + lint cleanup.
5. `docs/maintainability/dispatch_protocol.md` — 12 working-agreement rules. Unchanged this session, but see handoff §5 for three new lessons worth folding in eventually (PowerShell `$` interpolation hits commit messages too; local ruff should match `dev-requirements.txt` pin; `alembic current` `(mergepoint)` label is a chain-walk diagnostic not a multi-head alarm).
6. `docs/maintainability/dispatch_channels.md` — channel-pick playbook + 7 common gotchas. Same.
7. Skim `docs/BACKLOG.md` from line 2410 onward only if you want pre-pivot ticket context — pivot doc §6 is what's actually load-bearing under the new shape; new no-ticket entries at end cover this session's schema + lint ships.
8. `git log --oneline -8` to confirm you're on `11b248f` on top of `597d9cb` on top of `aea87b8` on top of `6f6ef79` on top of `a6e9f6c` on top of `b97942d` on top of `eb6f2e1` on top of `426f992`. If origin is current, `git status` reads clean.
9. **NEW step:** `python -m alembic heads` should show single head `e7f8a9b0c1d2`. If multiple heads, halt and investigate. (Step added after this session's local-DB-drift false-alarm cycle — Casey's local SQLite was at `1a2b3c4d5e6f (mergepoint)` which initially looked like multi-head but was just a long-resolved merge 6 revisions back in the linear chain.)
Critical context from the previous session you must absorb:

* Strategic posture (unchanged from 2026-05-12): structured local directory with browse + search + chat as three equal interfaces, not chat-first concierge. V1 category Home Services. Bootstrapped, founder-led sales, pivot-now. Sponsor packaging strategy: chase Verified Presence ($79/mo) and Category Visibility ($349/mo) and Seasonal Takeover ($1,500-$5,000) first; defer Deals Engine + Intent Capture to Phase 2.
* Schema is now live (additive). New `Category` model + `category_id` FKs on Provider/Program (parallel to legacy string `category` / `activity_category` columns; backfill is a future ticket — should be filed in BACKLOG by the next session if not already). New `attributes` JSON + `district` String(64) on Provider. New `category_ref` relationship on both Provider and Program (named with `_ref` suffix to avoid collision with existing string `category` attribute on Provider). 12 categories seeded via Alembic migration `e7f8a9b0c1d2`: `eat-and-drink`, `events`, `family`, `home-services`, `health`, `on-the-water`, `outdoors-and-parks`, `shopping`, `auto-and-gas`, `lodging`, `pets`, `community` (sort_orders 10..120 with gaps).
* §8 decisions 1-4 LOCKED: taxonomy=12 as-proposed, Place model=defer to Phase 2 (district handled via String(64) column on Provider for Eat & Drink districting), auth=Resend (not yet implemented), map=Leaflet+OSM (not yet implemented). §8 decisions 5-7 still open: pricing finalization (gate on Casey running 5+ cold-pitch reps with the new script), SKU naming (cosmetic), `PROJECT.md` / `HAVA_CONCIERGE_HANDOFF.md` substantive rewrites (deferred past Day 90; pivot-notice banners shipped 2026-05-13).
* Three parallel-dispatch prompts authored and saved to `outputs/`, ready to paste:
  - `outputs/cc_prompt_rate_limiter_decisions_memo.md` — Claude Code, read-only investigation, drafts a per-question memo for the 7 open §8 questions in `docs/maintainability/phase2_5_rate_limiter_design.md`. Output to `docs/maintainability/phase2_5_rate_limiter_decisions_memo.md`. Unblocks the rate-limiter implementation lane (which the pivot bumped to MORE urgent).
  - `outputs/chatgpt_prompt_provider_profile_ux.md` — ChatGPT, drafts the Provider profile page UX/copy spec. Output is a markdown spec (~200 lines) Casey reviews, then Cowork primary polishes into a Cursor implementation brief. Provider profile is the gating piece for Verified Presence sponsor sales.
  - `outputs/chatgpt_prompt_verified_presence_pitch.md` — ChatGPT (already shipped this session — `docs/sponsor_outreach/verified_presence_pitch.md`).
* Production runtime unchanged since `54a56b1` — schema gained columns at `6f6ef79` but no application code reads them yet. Railway will apply `e7f8a9b0c1d2` cleanly on next deploy (single migration on top of currently-deployed `d6e7f8a9b0c1`).
* Feature flags: `FEATURE_FLAG_DISCLOSURE_RENDERER=false` (HOLD; less urgent post-pivot per pivot §6), `FEATURE_FLAG_CONFIDENCE_TIER=true`. Audience-signal persistence: AUTOMATIC.
* Linux bash mount serves stale `.git` views and is missing `pytest`. Use Windows-side paths via the Read tool (Rule 7); pytest is operator-driven on Casey's machine.
* PowerShell `Invoke-RestMethod` always uses single-quoted `-Body` and `-ContentType 'application/json; charset=utf-8'` (Rules 4 + 5). **Extension lesson from 2026-05-13:** also single-quote `git commit -m '...'` when the subject contains `$` — PowerShell will eat `$<digit>` as variable interpolation in double-quoted strings. Last commit subject `(/mo)` was supposed to be `($79/mo)`; cosmetic incident.
* **Lesson 2026-05-13:** local ruff installs should match `dev-requirements.txt` pin. Run `python -m pip install ruff==0.15.12` (or current pin) before pre-commit lint checks. CI was failing silently on pre-existing test-file lint issues for several commits before the schema-commit push triggered the email-spam threshold.
* **Lesson 2026-05-13:** `python -m alembic current` showing a `(mergepoint)` label on an unexpected revision is a chain-walking diagnostic (`Grep ^down_revision alembic/versions/`), not a multi-head alarm. Casey's local dev DB drift is normal; production chain is independent and was unaffected.
* Brief baseline staleness mitigation (existing): instruct dispatched agents to run step-0 baseline confirmation (HEAD + pytest collected count + alembic heads) before doing work; halt-and-report on drift.
* Parallel-commit-absorption pattern (gotcha #1 in dispatch_channels.md): instruct dispatched agents to report ship-log entry text rather than editing `BACKLOG.md` directly; you add the entry after their commit lands.
* Schema-adjacent dispatch briefs should ground column names against `app/db/models.py` at line offset directly. (Lesson originally from #64 forensics; reinforced this session — Cursor caught the brief's `:248` reference for `Program.activity_category` had drifted to `:267` after edits and self-corrected the comment.)
Tool channels you can route work to (Casey is the operator who pastes between you and them):

* Cursor — focused-file edits, schema migrations, ops scripts. Last session shipped the schema lane cleanly via Cursor on a heavily prescriptive multi-file brief — same pattern that worked for #64. Channel-fit difference flattens when scope is tightly specified.
* Claude Code — heavy multi-file lanes, audits, comprehensive test suites, read-only investigations. Same paste-back pattern.
* ChatGPT — non-file research, drafting, brainstorming (cannot read code). Same pattern; you do a markdown polish pass on save.
* Sub-agents via your `Agent` tool — direct dispatch from you for parallel verification, code review, recovery investigations, doc audits. Burns your context but no operator round-trip. Not used last session.
* Yourself via Read/Write/Edit — small docs edits, BACKLOG.md status flips after a ship lands, applying sub-agent recommendations as anchored Edits, authoring strategic docs.
First action: do NOT auto-pick a thread. Three viable starting points are listed in `SESSION_HANDOFF_2026-05-13.md` §7, ranked by leverage:

1. **(Top recommendation)** Have Casey paste `outputs/chatgpt_prompt_provider_profile_ux.md` to a fresh ChatGPT chat. When it returns, polish the output into a Cursor implementation brief for the Provider profile page (the gating piece for Verified Presence sponsor sales — schema is live, page is the missing layer).
2. **(Parallel-eligible)** Have Casey paste `outputs/cc_prompt_rate_limiter_decisions_memo.md` to a fresh CC chat. Read-only investigation; output to `docs/maintainability/phase2_5_rate_limiter_decisions_memo.md`. When CC returns, frame the 7 §8 decisions for Casey via a single AskUserQuestion round — that fully unblocks the rate-limiter implementation lane (now MORE urgent under the pivot).
3. **(Quick alternative)** File the `Provider.category` (string) → `category_id` (FK) backfill ticket in `docs/BACKLOG.md`. Small Cowork-primary task; specifies mapping logic + edge cases + ingest-script changes for new rows. P2 priority. Doesn't ship the backfill — files it so it doesn't drift out of sight. Should be done at some point soon regardless.

Don't start any of the deprioritized chat-only threads (smoke catalog ambiguity resolution, HALT 3 pre-investigation, Phase 2.5 Premier original framing) without Casey explicit go-ahead — the pivot reframed those as lower-leverage.

That's it. Read the handoff first, then ask Casey which thread he wants to start with today.
